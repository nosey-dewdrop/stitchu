# 1309 — GECE KOŞUSU

**Hedef:** Fotoğraf + prompt → dikilebilir KALIP + satılır GÖRÜNÜM.
Etsy'de satılacak, ilk 20 kişiye açılacak. Demo değil.

---

## 0. BU PLANIN KENDİSİNE KONAN KISIT

`TRIED.md` son satırı: *"Bir daha yapılmayacak tek cümle: yeni plan/faz/kapı/
numaralandırma kurmak. 150 kapı ve 12 düzen kuruldu, satılan nesne 0."*
Tekrar eden hata deseni #2: *"Plan/kural dosyası doğurmak, ürün doğurmamak —
her biri 'önceki bütün düzenler geçersizdir' diye başlıyor, en az 5 kez tam
yeniden numaralandırma."*

**Bu dosya o listeye 13. düzen olarak girmeyecek.** Kurallar:
- Yeni kapı dosyası AÇILMAZ. Tek kapı var: `src/gate/gate.mjs`. Düzeltilir, çoğaltılmaz.
- Yeni faz numaralandırması (A1-A13, F1-F10, G1-G4 gibi) KURULMAZ. Aşağıdaki 6 iş
  **iş**tir, mertebe değil; sırası bağımlılıktan geliyor.
- Bu dosya koşu sonunda **silinir ya da tek sayfalık sonuç olur.** Kalıcı bilgi
  `TRIED.md` / `RESEARCH.md` / `docs/1309-journal.md`'ye yazılır.
- Her iş bir commit. Rapor yazılmaz; çıktı gösterilir.

---

## 1. ÖLÇÜLEN BAŞLANGIÇ (13 Eyl 01:00 — tahmin yok)

Okundu: `GOAL.md`, `CLAUDE.md`, `TRIED.md` (139), `RESEARCH.md` (336),
`docs/1309-journal.md` (489), `docs/1309-dresses-rework.md`,
`src/draw/draw.js` (1009), `src/pattern/pattern.mjs` (79), `src/gate/gate.mjs`,
`src/serve.mjs`, `src/read/prompt.mjs` (450), `law/_frozen/1309-base-topology.json`,
`law/_frozen/1309-base-graph.json`, `engine/src/grafop.cpp` (1403).

`output/01-pink-bow-mini` çıktısı ile `dresses/01-pembe-fiyonk-mini.png` yan yana
konduğunda **gözle ölçülen** farklar:

| # | fotoğrafta | bizde | cinsi |
|---|---|---|---|
| G1 | kalem silüet, etek ucu kalçadan DAR | A-hattı, etek en geniş yer | silüet sınıfı yanlış |
| G2 | keyhole arkası ten | **gri dolu dikdörtgen kutu** | dolgu nesne gibi duruyor |
| G3 | akan saten fiyonk, iki uzun uç | iki daire + 4 düz çizgi | primitif kaba |
| G4 | ince kavisli boyun bandı | bant YOK | tur 75'te çözülmüştü → **regresyon** |
| G5 | kapak kol gövdeden ayrık | omuzda sivri birleşim | işçilik |
| G6 | prenses dikişi etek ucunda biter | etek ucunda dışa taşıyor | işçilik |

### Üç kök, hepsi ölçüldü

**K1 — Kalıp fotoğrafı HİÇ kullanmıyor.**
`grep -oE "okuma\.[a-zA-Z.]+" src/pattern/pattern.mjs` → yalnız `okuma.ilan`,
`okuma.okuyan`, `okuma.on.kontur`. `grep -c "ogeler" src/pattern/pattern.mjs` → **0**.
Okuyucunun fotoğraftan çıkardığı pens / roba / prenses dikişi / pat / fermuar /
büzgü listesinin **tamamı** kalıba girmeden atılıyor. Kalıba giren tek şey: etek
ucu Y, klos çarpanı, iki genişlik oranı. Geri kalanı
`law/_frozen/1309-base-topology.json`'dan sabit geliyor ve o dosyada
`drop{kol}` (kolu siler) + `addPanel{cep_kapagi_on}` (hiçbir fotoğrafta olmayan cep)
+ koşulsuz `closure{zipper}` var.
→ **Hangi fotoğrafı yüklersen yükle kalıp aynı: kolsuz, cepli, fermuarlı.**
İki yerden kopyalanıyor: `src/pattern/pattern.mjs:26` ve `src/serve.mjs:41`.

**K2 — Kapı A-hattını YASA sanıyor, motoru oraya zorluyor.**
`src/gate/gate.mjs` nesne kontrolünde iki madde:
`kalca <= etek*1.02` ve `yan hat belden eteğe monoton`.
Kalem/balon/peplum silüetli hiçbir elbise bu kapıdan geçemez.
**Dün 7/7 geçen çıktının neden A-hattı olduğunun açıklaması bu** — G1'in kökü kapıda.

**K3 — Kapının 7 hedefi tek elbiseye ezberlenmiş.**
`HEDEF` tablosundaki 7 sayı pembe elbisenin fotoğrafından türetilmiş
(`bel/gogus 0.805`, `govde boyu/gogus 1.96`, `kol boyu/govde 0.195`).
Bu kapı ikinci elbiseye koşulamaz. Regresyon testi değil, tek-elbise ezberi.
Defterin kendi itirafı bunu doğruluyor: **beş kez "oran hedefte, flat yanlış"**.

---

## 2. GÖRÜNÜM MOTORU KARARI — DIFFUSION YOK. SAYIYLA.

Sahibi "Flux lazımsa ok" dedi. **Gerekmiyor**, ve gerekçe artık `RESEARCH.md`'de
birincil kaynağa bağlı:

1. **Diffusion çıktısında stroke weight kavramı yoktur.** Raster'da 2pt ile
   0.75pt farkı trace sonrası kaybolur (`RESEARCH.md` A3 ★). Satılır flat'i
   satılır yapan şey tam olarak o hiyerarşi (A5). Yani "piksel üret + trace et"
   yolu aradığımız şeyi **yapısal olarak** veremez.
2. **Pazar ölçüldü, boşluk tam burada:** *"prompt/foto → temiz VEKTÖR technical
   flat pazarda boş; herkes render üretiyor"* (A1). `src/draw/draw.js` zaten
   native SVG doğuruyor (doğrulandı). Wrapper testinin geçtiği yer burası.
3. **Lisans duvarı:** FLUX.1-dev non-commercial; dev tabanlı LoRA'lar satılamaz.
   Tek olgun flat LoRA `flatsketcher` (schnell/apache) — **14 indirme, 0 beğeni,
   demo kırık, eğitim veri sayısı yayınlanmamış** (A2).
4. **Endüstri flat'te renk/gölge İSTEMİYOR.** *"Black and white sketches can be
   easier to understand"* — "chic" = süs değil, **netlik** (A5).

### ⚠ Bu, bugünkü motorun bir kısmının YANLIŞ yönde olduğu anlamına geliyor

Bu koşunun en pahalı bulgusu: son turlarda eklenen **kumaş rengi + dokuma deseni
+ tek yönlü gölge**, endüstri konvansiyonunun TERSİ. `RESEARCH.md` A5:
gereksiz gölge/hareket efekti YOK. Renkli+dokulu+gölgeli çıktı satıcı paftası
değil, klipart.
→ **`src/draw/draw.js`'den gölge + doku çıkarılabilir olmalı** (silinmez,
anahtara bağlanır — renkli önizleme ayrı ürün olabilir, ama SATILAN pafta
siyah-beyaz).

### Çizgi hiyerarşisi — ölçülmüş çelişki, en ucuz kazanç

`RESEARCH.md` A5 + E1, birincil kaynak Points of Measure:

| eleman | endüstri | bizde | durum |
|---|---|---|---|
| dış kontur / iç dikiş oranı | **2.0 – 2.67** | **1.55** | ihlal |
| kesikli ritmi | **1.5 / 1.5 (eşit)** | `5,3` | ihlal |
| outline | 2 pt | — | |
| hems / seams / stitch / movement | 1 / 0.75 / 0.5 dashed / 0.25 pt | — | |

Kanunun kendi `_yasa` satırı "dış kontur iç dikişin en az 2 katı" diyor ve
**sayılar kendi yasasını ihlal ediyor.** Kod değişmeden, `law/1309-silhouette.json`
içindeki 5 sayı ile düzelir.

**Plan B (görünüm hâlâ satılmazsa):** diffusion ürünün kendisi değil, Etsy
listing'inin **kapak görseli** olur (giyilmiş hali). Teknik pafta vektör kalır.
O zaman diffusion kalıp hakkında yalan söyleyemez.

---

## 3. KALIP MOTORU KARARI — OKUMA → OP DERLEYİCİSİ

Motorun op sözlüğü ölçüldü (`engine/src/grafop.cpp` dispatch tablosu, **19 op**):
`addPanel, attach, closure, drop, extend, extendTo, fitLength, flare, gather,
merge, mirror, moveVertex, overlay, reshapeEdge, sew, shorten, split, subdivide,
suppress`.

Okuyucunun ürettiği öğe tipleri (`src/read/prompt.mjs`): `dikis, roba, kesikli,
fermuar, buzgu, pens, dugme, pat, drape, firfir, keyhole, fiyonk, bag,
bebeYaka, yakaBandi`.

**Motor yetiyor. Eksik olan tek şey öğe→op dönüşümü.** Sabit menü değil
(GOAL md.9 uyumlu — tip listesi değil, dönüşüm kuralı):

| okuma öğesi | op | argüman kaynağı |
|---|---|---|
| `pens` | `suppress` | 3 nokta (uç, a, b) |
| `dikis` (dikey) | `split` | noktalar → prenses dikişi |
| `roba` | `split` (enine) | noktalar |
| `fermuar` | `closure{zipper}` | baş/son kesri |
| `dugme` | `closure{button}` + `overlay` | adet, çap |
| `buzgu` / `firfir` | `gather` | adım, derinlik |
| `kol: null` | `drop{kol}` | okumada kol yoksa |
| `kol: {...}` | kol DURUR + `extendTo` | `kol.dis.y` |
| `pat` | `overlay` | genişlik |
| `keyhole` | `reshapeEdge{neck_front}` | açıklık noktaları |

**Silinecek:** `addPanel{cep_kapagi_on}` ve koşulsuz `closure{zipper}` —
fotoğrafta yoksa üretilmez. `law/_frozen/1309-base-topology.json` sabit op
listesi olmaktan çıkar; taban = 5 panel + 8 dikiş iskeleti
(`law/_frozen/1309-base-graph.json`), op'lar okumadan derlenir.
Kopyalama satırı **iki yerden** kaldırılır (`src/pattern/pattern.mjs:26`,
`src/serve.mjs:41`) — biri unutulursa web'den yüklenen foto yine sabit kalıp alır.

**Kademeli yazılır** (YAPILANLAR §1: "11.547 satırlık GarmentCode köprüsü terk"
— büyük köprü yazma hatası tekrarlanmaz). Önce 3 op: `suppress`, `split`,
koşullu `drop`. Üçü yeşil koşmadan dördüncüsü eklenmez. Her op'tan sonra
`grafdogrula` koşulur; patlarsa op derleyiciden çıkar ve nedeni ölçüyle yazılır.

---

## 4. GECE İŞLERİ (6 iş, sırası bağımlılıktan)

Sıra: **ölçüm aleti → görünüm doğruluğu → kalıp bağlanması → genelleme → paket.**
Bozuk kapıyla çalışmak dünün hatası; o yüzden kapı önce.

---

### İŞ 1 — KAPIYI DÜZELT (motoru A-hattına zorlamayı bırak)
**Neden ilk:** K2 + K3. Bugünkü kapı hem yanlış şeyi ölçüyor hem tek elbiseye
ezberli. Bununla açılan her tur yanar.

**Dosya:** `src/gate/gate.mjs` — **yeni dosya açılmaz**, bu dosya düzeltilir.
- 7 sabit `HEDEF` sayısı SİLİNİR. Kapı artık "bu elbise 0.805 bel oranında mı"
  değil, **"çizim kendi okumasına sadık mı"** sorar. Okuma `bel: ['waist*0.92']`
  diyorsa çizimde ölçülen oran %3 bandında olmalı. Bu her elbiseye koşar.
- `kalca <= etek*1.02` ve `yan hat monoton` maddeleri SİLİNİR (A-hattı dayatması).
  Yerine silüetten bağımsız geometri sağlığı: kırık teğet, kendini kesen yol,
  havada biten çizgi.
- Çizgi hiyerarşisi kontrolü EKLENİR: dış/iç oranı 2.0-2.67 bandında mı
  (`RESEARCH.md` E1 — bugün ölçülmüyor, regresyon ancak gözle görülüyor).

**KANIT:** Aynı kapı iki farklı okumada **farklı** sonuç verir (bugün ikincisine
koşulamaz bile). Bilerek bozulmuş kontrol örneği (etek ucu 100mm kaydırılmış)
KIRMIZI basar. Kapı, bugünkü A-hattı çıktısına artık 7/7 VERMEZ.

**Bağımlı/bağımsız:** BAĞIMSIZ = kapı bandı (%3 mü %8 mi). BAĞIMLI = kaç turun
"geçti" sayıldığı. **Bant tur kurtarmak için gevşetilmez** (YAPILANLAR §1
"eşik-düşürme" E2 kuralı zaten var, koşuyor).

---

### İŞ 2 — ÇİZGİ HİYERARŞİSİNİ ENDÜSTRİYE ÇEK + GÖLGE/DOKUYU ANAHTARA BAĞLA
**Neden burada:** `RESEARCH.md` E1: *"flat'i amatörden satılıra taşıyan tek en
ucuz kalem, iş yükü saatler, risk düşük."* Kapı artık bunu ölçebiliyor (İş 1).

**Dosya:** `law/1309-silhouette.json` `cizgi` bloğu (5 sayı — kod değişmez):
dış/iç oranı 1.55 → 2.0-2.67, kesikli ritmi `5,3` → eşit `1.5/1.5`.
`src/draw/draw.js`: gölge + doku bir anahtarın arkasına alınır (silinmez);
satılan pafta varsayılanı **kapalı**.

**KANIT:** Önce/sonra görsel çifti. Kapının yeni hiyerarşi kontrolü yeşil.
Hakem, siyah-beyaz çıktıyı `dresses/_etsy-flat-referans/`'taki 11 gerçek satılan
paftanın yanına koyduğunda ayırt edemiyor.

**Bağımlı/bağımsız:** BAĞIMSIZ = 5 çizgi sayısı. BAĞIMLI = algılanan ağırlık;
defterde ölçülmüş — kalınlık 1.4→2.2 yapılınca gölge hafif kaldı, 0.50→0.72'ye
çıkarıldı. Gölge zaten kapanıyorsa bu bağ kopar, ikisi birlikte ayarlanmaz.

---

### İŞ 3 — G1-G6'yı MOTORDA KES
**Neden burada:** Kapı doğru, hiyerarşi doğru. Şimdi kusurlar.

**Dosya:** `src/draw/draw.js`
- **G2** açıklık dolgusu: dikdörtgen clip yerine **açıklık yolunun kendisi** clip path.
- **G1** silüet sınıfı: kapı dayatması İş 1'de kalktı; `etekYan.x` artık
  `kalca.x`'in altına inebilir → kalem silüet çizilebilir.
- **G3** `fiyonk`/`bag`: iki daire + düz şerit yerine catmull eğrili akan uçlar.
- **G4** `yakaBandi` regresyonu: **önce git log ile neden kaybolduğu bulunur**,
  sonra kesilir. (Tur 75'te çözülmüştü — tekrar çözmek rework'tür.)
- **G5/G6** kol-omuz teğeti, prenses dikişinin etek ucunda bitişi.

**KANIT:** Her kusur için önce/sonra görsel çifti. Taze hakem altısında da
"düzeldi" diyecek. Birinde "komşusu bozuldu" derse iş kapanmaz.

**Bağımlı/bağımsız:** Defterin değişken haritası geçerli. En kırılgan bağ
ölçülmüş: `etekYan.y` tek başına 3 oranı birden oynatıyor (hakem tur 107-110).
Ona dokunulacaksa **önce gövde boyu oturtulur**, diğerleri sonra ölçülür.

**Tıkanma kuralı:** bir kusurda 3. tura gelinirse o kusur `docs/1309-journal.md`'ye
"AÇIK" yazılır ve iş devam eder. 4 kusuru düzelmiş çıktı, 1 kusurda takılıp
hiçbir şey çıkmamasından iyi. Yazılmadan geçilmez.

---

### İŞ 4 — KALIP DERLEYİCİSİ (K1)
**Neden burada:** Görünüm çalışır ve ölçülebilir olduktan sonra; önce yapılırsa
kalıbın mı görünümün mü yanlış olduğu ayırt edilemez.

**Dosya:** `src/pattern/pattern.mjs` içine derleyici (§3 tablosu). Ayrı dosya
açmak yerine mevcut 79 satırlık dosya büyür — dosya doğurma yasak.
Kopyalama satırları iki yerden kalkar.

**KANIT (üçü de zorunlu):**
1. Üç farklı fotoğrafın `ops.json`'ları BİRBİRİNDEN FARKLI
   (`diff` boş çıkmayacak — bugün üçü de aynı).
2. Pembe elbise: fotoğrafta cep yok → kalıpta cep paneli yok; kol var →
   kalıpta kol paneli var (bugün `drop{kol}` siliyor).
3. `grafdogrula` yeşil kalır — derleyici geçerli op üretiyor.

**Bağımlı/bağımsız:** BAĞIMSIZ = derlenen op listesi. BAĞIMLI = dikilebilirlik.
Yeni op dikiş uzunluğu eşitliğini bozabilir → her op'tan sonra `grafdogrula`.

**Bilinen motor sınırı (kodda yazılı, derleyici çözmez):** taban graf kalça
halkasını taşıdığı için etek ucu kalçanın üstüne çıkamıyor; mini elbiselerde
kalıbın eteği kalçaya KIRPILIYOR (`hemKirpma`). Pembe mini elbisenin kalıbı,
flat'inden uzun çıkıyor. Ayrıca ele alınmalı.

---

### İŞ 5 — İKİNCİ ELBİSE
**Neden burada:** Tek elbiseye çalışan motor ürün değil. **Ama 17 elbise
fotoğrafı elde YOK** (bkz. §6 döküm) — bu iş elde ne varsa onunla koşar.

Sıra öğrettiği şeye göre: kolsuz (drop dalı) → uzun/maxi (etek ucu sınırı) →
roba/pili (panel bölme) → düğmeli/patlı (closure). Her biri derleyicinin ayrı
bir dalını zorlar.

Bir elbisede çıkan kök motorda kesilir ve **önceki elbiseler yeniden koşulur.**
Bir elbise düzelirken başkası bozulursa iş kapanmaz — dünün en pahalı dersi
(hakem: *"düzeltilen kalem düzeliyor ama komşusu bozuluyor, REGRESYON TESTİ YOK"*).

**KANIT:** En az 2 elbise, ikisinde de kapı yeşil VE hakem "alırdım".
Yapılamayan ADIYLA yazılır (GOAL §2: sessiz default yok).

---

### İŞ 6 — PAKET (sahibinin dikeceği şey)
**Neden son:** GOAL §4 — fiziksel kanıt, ilk ürünü sahibi diker.
YAPILANLAR §6.13: *"600+ saatte Damla `kalip-A4.pdf`'i basıp ucuz kumaştan bir
kez bile dikmedi — giyilebilirliği ölçen tek kapı."* Bu koşunun asıl borcu bu.

Bir elbise için tam paket: A4 PDF kalıp (dikiş payı, tile, **test karesi 50mm**),
teknik pafta (ön+arka, siyah-beyaz), dikim rehberi (parça listesi, kesim planı,
dikiş payı, iğne/iplik, sıra), kumaş notu.

**KANIT:** PDF yazdırılıp test karesi cetvelle ölçüldüğünde 50mm ±0.5mm.
`output/01-pink-bow-mini/` altında tek klasör.
**A0 ölçek-down %69.4 yasak** (YAPILANLAR §1: "fiziksel hatayı müşteriye atmak").

---

## 5. HAKEM PROTOKOLÜ

**Kim:** her çağrıda taze ajan. Önceki turun context'i verilmez; görsel +
referans + soru. Brief yönlendirmez ("şunu düzelttik, bak" denmez).

**⚠ YAPILANLAR'dan gelen ZORUNLU düzeltme.** Ölçüldü ve battı:
*"A4 'çizim güzelleşsin' turları — 24+ tur, 29 hakem raporu, kapanmadı. Hakem
her tur İNTERNETTEN farklı satıcı seti çekti; skor gürültü oldu, yükselen eğri
YOK: 3/11 → 10/11 → 0/11 → 7/11."* Ve: *"11/11 iki tur üst üste kabul şartı
ulaşılabilir değil, ölçüldü."*

Bu yüzden:
- **Hakem referans seti SABİTTİR:** `dresses/_etsy-flat-referans/` (11 pafta,
  diskte). İnternetten set çekilmez. Ölçüt kayması = gürültü.
- **Skor eşiği kabul şartı DEĞİLDİR.** Kabul: hakemin cümlesi.
- **Çalıştığı kanıtlanmış hakem korunur** (YAPILANLAR §2): *göz hakemi — ilan
  görseli SOLA, bizim çizim SAĞA, 4 bölge, 3 koşu ORTANCASI. İyi çizim 4/4,
  bozuk 1/4. Altın kopyanın kendi eksiğini yakaladı.* Yeniden yazılmaz, kullanılır.
- **Piksel metriği hakem olarak KULLANILMAZ:** ölçüldü, bozuk çizim 0.805/0.777/
  0.043, altın 0.805/0.770/0.050 — biri giysi bile değil ama sayılar aynı.

**Sorular** (her turda, "kapı yeşil mi" ASLA):
1. Bu bitti mi? 2. Satılsa kendi paramla alır mıydım? 3. Ürün olarak güçlü mü?

**"Almazdım" gelince:**
1. Gerekçe maddelere bölünür, her madde ölçüye çevrilir.
2. Kök aranır — **JSON mu, motor kuralı mı, Bezier geometrisi mi** (defterin
   kendi dersi, tur 57-58). Üçü elenmeden parametre oynatılmaz.
3. Kök motorda kesilir. **Sadece JSON sayısı değişen tur SAYILMAZ.**
4. Taze hakeme tekrar sorulur.
5. Aynı madde ikinci kez "almazdım" alırsa teşhis yanlıştı; tekrar denemek
   yerine bir üst işin kabulü yeniden koşulur.

**Savunma yasak.** Hakem hayır derse gerekçe yazılmaz, kök aranır.

---

## 6. RİSKLER

**R1 — İş 4 derleyicisi motoru patlatır (en yüksek).** Motor 19 op destekliyor
ama her op her panelde geçerli değil; bugün bile hedef düşürüyor
(`hedef: hicbir panel <ring> bollugu tasimiyor`).
→ Kademeli yazım (§3). Patlayan op derleyiciden çıkar, nedeni ölçüyle yazılır.

**R2 — Okuyucu yanlış okur, aşağısı boşuna çalışır.** İş 1'in kapısı bunu
yakalayamaz: kapı okumaya **sadakati** ölçüyor, okumanın **doğruluğunu** değil.
Bilinen ve ölçülmüş arıza: *"prompt pensi ÜÇ yerde anlatıyor, yine kaçıyor;
N3 11 denemede aynı duvara çarptı; prompt 24k karakteri geçince sonuç 0/5'e
ÇÖKÜYOR (şu an 24.5k)."*
→ **Prompt'a kural EKLENMEZ** (YAPILANLAR §6.6: mekanizma sorununu kuralla
çözmeye çalışmak, tekrar eden hata). Her elbisede hakeme ayrıca "okuma
fotoğrafı doğru anlatıyor mu" sorulur. Kök okumadaysa çözüm mekanizma:
yapısal çıktı zorlaması (`RESEARCH.md` E3, structured output, risk düşük).

**R3 — Elbiseler birbirini bozar.** İş 1'in kapısı bunun için var; her motor
değişikliğinden sonra tüm işlenmiş elbiseler yeniden koşulur. Geçen elbise
sayısı düşerse değişiklik geri alınır.

**R4 — Bu planın kendisi 13. düzen olur.** En gerçek risk; 12 kez oldu.
→ §0 kuralları. Tıkanınca yeni faz/kapı/dosya açılmaz; açık madde yazılır,
sıradaki işe geçilir.

**R5 — Elle veriyle kapı geçme.** Ölçüldü: son 14 commit'te önbelleğe **3720
satır elle** yazılmış, çiziciye 168 satır kod. `_cop/kapi.sh`'in E1 kuralı
(elle-veri denetimi) bunu yakalıyordu ve **çalıştığı kanıtlanmış** — yeni
kapı yazmak yerine o denetim koşuya bağlanır.

---

## 7. DÖKÜM — sorulmamış ama bu turda görülen

- **`RESEARCH.md` ve `TRIED.md` bu gece yazılmış** (brief "varsa" diyordu;
  ilk okumamda yoktu, sonra belirdi). Repo da İngilizce yola geçmiş
  (`src/read`, `src/draw`, `src/pattern`, `src/gate`, `law/`, `engine/`,
  `output/`, `docs/`). Bu plan yeni yola göre yazıldı.
- **⚠ Faz 5'in girdisi elde YOK.** `dresses/` altında **tek elbise fotoğrafı**
  var (`01-pembe-fiyonk-mini.png`) + `_etsy-flat-referans/` (11 satıcı paftası,
  elbise fotoğrafı değil). `docs/1309-dresses-rework.md` "#2-18 gece gelen
  elbiseler `girdi/1209/`" diyor ama **o klasör yok**. 17 elbise gelmeden
  genelleme kanıtlanamaz.
- **Bugünkü motorun rengi/dokusu/gölgesi endüstri konvansiyonunun TERSİ**
  (`RESEARCH.md` A5: flat'te renk/gölge istenmiyor, siyah-beyaz tercih).
  Son turların "güzelleştirme" işi yanlış yöne emek olmuş olabilir. İş 2'de
  anahtara bağlanıyor, silinmiyor — ama satılan pafta siyah-beyaz.
- **Kanunun kendi yasası ihlal ediliyor:** `_yasa` "dış kontur iç dikişin en az
  2 katı" diyor, sayılar 1.55.
- **YAPILANLAR §1'de "croquis oran sayılarını contract'a yazmak" DENENDİ VE
  OLMADI** diye geçiyor ("blog seviyesi ve çelişkili"). `RESEARCH.md` E2 ise
  9-head→7-8 head çekmeyi öneriyor ve *"bizim croquis36 kaç head, ölçülmedi →
  DOĞRULANMADI"* diyor. **İki dosya burada gerilimde.** Bu gece croquis'e
  DOKUNULMAZ — İş 1-4'ün hepsi kayar (risk: orta, `RESEARCH.md` E2'nin kendi
  uyarısı). Damla kararı gerekir.
- **`RESEARCH.md` E4 — "walk pieces" dikilebilirlik kapısı:** `law/_frozen/
  1309-base-graph.json` `seams[]` zaten `ratio`/`easeMM` taşıyor, veri hazır,
  **kontrol yok**. Düşük risk, 2-3 gün. Bu gecenin işi değil ama İş 4'ten sonra
  en yüksek getirili kalem — Minerva'nın rakiplere yönelttiği eleştirinin
  ("seams that do not walk together") bizde ölçülen karşılığı.
- **`engine/src` 37.573 satır C++ ve `contract/*.json` 19.268 satır elbiseyi
  ÇİZMİYOR**; çizen 472 satırlık tek dosya (YAPILANLAR §3). Kalıbı C++ üretiyor,
  görünümü JS. İş 4 bu iki dünyayı ilk kez fotoğraf üzerinden bağlıyor.
- **DOĞRULANMADI:** `engine/build/grafuygula` ve `grafciz` binary'lerinin
  bugünkü kaynakla derlendiği. İş 4'ten önce `make` koşulmalı.
- **DOĞRULANMADI:** `output/01-pink-bow-mini/` içeriğinin taşıma sonrası
  bozulmadığı; çıktı PNG'leri taşınma sırasında git'te silinmiş görünüyordu.
