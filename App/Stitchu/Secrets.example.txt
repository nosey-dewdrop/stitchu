// TEMPLATE — copy this into a new file `Secrets.swift` (same folder) and fill in
// the real values from your deployed Cloudflare Worker (see backend/DEPLOY.md).
// Secrets.swift is gitignored. The Anthropic key is NEVER here — it lives only
// as a Cloudflare secret on the Worker.
//
// Rename this file's contents into Secrets.swift; do not add this .txt to the
// Xcode target.

import Foundation

enum Secrets {
    // Full URL of the Worker's analyze route.
    static let backendURL = "https://stitchu-api.YOUR-SUBDOMAIN.workers.dev/api/analyze"

    // Shared app token — must match the Worker's APP_TOKEN secret.
    static let appToken = "your-long-random-app-token"
}
