# V3 TUTANAK — TEK NESNE: flat ile kalıp aynı kabuktan

Faz açılış ağacı: `40dbd6b` (V2 kapanışı). Kapanış ağacı: bu tutanağın commit'i.
Kartlar: `GECE/KART/V3-{K,R,A,B,C,D}.md` · İşçi raporları: `GECE/V3-{K,R,A,B,C,D}.md`
Hakem hükmü: `GECE/KAPI.md` son satır · Kâtip commit'i: `80b1a03`

## 0. DEVRALINAN ÜÇ SAYININ DOĞRULAMASI (devralınmadı, ölçüldü)

| V2'nin bıraktığı | V3'ün kendi ölçümü | hüküm |
|---|---|---|
| kırmızı 4 | `GECE/log/V3.ctest.before.txt`: `96% tests passed, 4 tests failed out of 108`, adlar `style_check` · `sizechart_source_check` · `contract_check` · `figure_check` | **DOĞRULANDI**, dört ad birebir |
| sözlük tabanı 10418, ratchet kilitli | faz açılışında `vocab_reference_check` YEŞİL (aynı log) → taban geçerliydi | **DOĞRULANDI** (dolaylı: kapı yeşil) |
| sürekli eksen 2/37 | **ÖLÇÜLMEDİ** — V3'ün dosya kümesi dışında; V2'nin kaydı olduğu gibi devrediliyor | **DOĞRULANMADI** |

## 1. ÖLÇÜLEN (sayı + komut + hash)

### 1.1 İki hattın bugünkü kaynağı (keşif, `GECE/V3-K.md`)
- Flat hattı `engine/tools/render-garment-flat.mjs:1005`, girdisi 2B croquis
  (`contract/flat-convention-v1.json`), **~90 elle yazılmış katsayı** dosya:satır
  ile listelendi. Kanunun 11 croquis işaretinden **6'sı kaynaksız**.
- Flat hattı TEK DEĞİL: `tryReferencePen` (`:894`) 31 stilde
  `engine/flat-engine/_engine-full.mjs`'e sapıyor; kapının kendi parite raporu
  iki kalemin croquis'inin uyuşmadığını basıyor (omuz x 57.80 · göğüs x 94.50, n=29).
- Kalıp hattı `buildSheathPattern`, `engine/src/surfacepattern.cpp:1280`.
- **ORTAK KABUK YOKTU.** İki hat aynı beden çizelgesini (88/70/94) okuyor ama
  ortak gövde/kabuk nesnesi yok, çapraz çağrı sıfır. Aynı EU38 beli:
  flat **700.0mm** (sıfır bolluk), kalıp **724.89mm** (Steiner bolluk).
- Eigen YOK, libigl YOK, LSCM/ABF YOK, 3B→2B siluet projeksiyonu YOK.

### 1.2 Çekirdek — kabuk yayınlandı, projeksiyon hesaplandı (`25f0f45`)
`GarmentSurf` bildirimi `surfacepattern.hpp`'ye taşındı (İKİNCİ KABUK YAZILMADI),
yapılandırma `buildGarmentSurf`'te tek yere indi. Yeni:
`engine/src/shellprojection.{hpp,cpp}` + `engine/tools/shell-flat.cpp`.
`./engine/build/shell-flat EU38`:
```
hem_circumference   1295.6000 · bust_circumference 754.7482 · waist_circumference 725.0000
body_length          757.5584 · neck_opening_width 349.8211 · shoulder_width  334.5680
```
Kalıp binary'si BAYT BAYT DEĞİŞMEDİ (sha256 before=after
`44e5c38a1b9fb1ffc21aae671f42c356b9538e9dbd02e5bae155e4e98792dc43`).
Görsel: `Logs/v3a-2026-08-24/v3a-eu38.png` (37883 bayt).

### 1.3 Kalıp tarafı ölçüm aleti (`458cc4a`)
`node engine/tools/pattern-measure.mjs /tmp/surface-EU38.json` → hem **1295.4506** ·
waist **724.8907** · body_length **728.787** · bust/neck/shoulder **null + sebep**.
Determinizm: iki koşu `diff` exit 0; adım 0.25/0.05/0.01mm'de dört basamak aynı.

### 1.4 ÜÇ KANAT KAPISI (`cad49bf`, `1a08656`, onarım `a33025f`/`99efb0f`)
`node engine/tests/flat_pattern_agree_check.mjs` → **exit 1, 1 ihlal**
```
hem   1295.6000 vs 1295.4506  -0.0115%   ok
waist  725.0000 vs  724.8907  -0.0151%   ok
body_length 757.5584 vs 728.7870  -3.7979%   FAIL (tolerans %1.5)
UNMEASURED 3/6 (bust · neck · shoulder) = RATCHET tavanı 3
```
`node engine/tests/flat_artifact_census.mjs` → **exit 1, 1 ihlal**
```
1 tırtıklı 0 · 2 öz-kesişim 0 · 3 C1 kırığı 2 (20.560216° > 1°) · 4 dejenere 0
```
Eşikler kaynaklı: C1 **1.0°** = McNeel Rhino "Understanding Tolerances" (güven
YÜKSEK, `GECE/V3-R.md`); daha sıkı emsal CATIA V5 GSD 0.5° dosya başlığında NOT,
kapıya bağlanmadı. **%1.5 yayından DEĞİL karardan** — V3-R "EŞİK 2: yayınlanmış
formül YOK" diyor ve bu cümle test başlığında aynen yazılı (§7.6).

### 1.5 Kapı kanıtları (§4.2 · §4.5)
- **Boş test (birincil usul, derleme bağı sıfır):** faz-öncesi hattın çıktı
  artefaktı yeni aletlerle yargılandı → 6/6 ölçü %1.5'i aştı (−14.60…+57.62%),
  dört artefakt sınıfının **dördü** ateşledi (tırtıklı 2 · öz-kesişim 72 ·
  C1 89 · dejenere 1). `GECE/log/V3-C.vacuous.txt`
- **Mutasyon:** üç kanat da kırıldı ve geri alınınca yeşile döndü.
  ★ Kartın önerdiği +5mm bu kapıyı KIRMAZ ve bu DOĞRU davranış (725mm'de %0.690
  < %1.5); eşiği aşan en küçük tam mm +11mm, mutasyon +15mm'ye çıkarıldı,
  **eşik gevşetilmedi**. `GECE/log/V3-C.mutation.txt`

## 2. KAPANAN / AÇILAN KIRMIZI (ad ad)

**AÇILAN, AYNI GECE KAPATILAN (2):**
- `bundle_fresh_check` — V3-A'nın kaynak değişikliği wasm paketini bayatlattı;
  `bash engine/build-wasm.sh` exit 0, damga `7023c808195429b3` (`495d58a`). YEŞİL.
- `vocab_reference_check` — V3 kodu +27 referans getirdi. Önce KOD düzeltildi
  (beş halka adı tek otoriteye indi, `GarmentSurf::ringNames()`, `7a27a9c`):
  +27 → +20. Kalan +20'nin **18'i yorum metni**; yorum silerek sayı düşürmek
  ARTEFAKT GİZLEME sayıldı ve REDDEDİLDİ. Taban son çare olarak yeniden kesildi
  **10418 → 10438** (`d6cbb87`), delta dosya+anahtar bazında commit gövdesinde
  ve baseline JSON'unda satır satır (emsal `e2f7aba`). YEŞİL.

**AÇILAN, AÇIK KALAN (2 — fazın KENDİ kapıları, ikisi de kırmızı):**
- `flat_pattern_agree_check` · **1 ihlal + 3 UNMEASURED**
  KÖK: sevk edilen kalıp **strapless** (G5 yok: omuz/yaka/oyuk), kabuk ise
  omuz halkasından başlıyor → `body_length`'in 28.77mm'si buradan. bust/neck/
  shoulder kalıp tarafında yok, çünkü dikiş grafiğinde omuz dikişi de kol
  oyuğu da yok. ÖLÇÜLMÜŞ HAMLE: tanım uyuşmazlığı düzeltildi (iki taraf da
  artık YAY ölçüyor, düşey ölçü `body_height_projected` adıyla ayrı duruyor,
  kapıya girmiyor) → ihlal **4 → 1**, sapma %1.98'den %3.80'e ÇIKTI ve
  gevşetilmedi, kırmızı bırakıldı. Kalan iş = G5, V7'nin kartı.
- `flat_artifact_census` · **1 ihlal (sınıf 3, 2 nokta, 20.5602°)**
  KÖK: `surfacepattern.cpp:71-81` — belin ÜSTÜNDE skim zarfı, ALTINDA halka
  interpolasyonu; iki yasa `skimBaseH`'de teğet koşulu olmadan buluşuyor, bel
  bir V köşesi. Kalçada aynı sorun ZATEN çözülmüş (`:55-60`, blendMM=50).
  ÖLÇÜLDÜ VE REDDEDİLDİ: aynı yuvarlama bele uygulandı → kırık
  **20.5602° → 0.4582°**, ama bel halkası **724.9232 → 761.0398mm (+36.12mm)**
  ve **dört kapı kırmızıya döndü** (`surface_pattern_check` · `edgemono_check` ·
  `walkgate_check` · `cutplan_check`). GERİ ALINDI, eşiğe dokunulmadı.
  `GECE/log/V3-D.waistblend.rejected.txt`. Matematiksel sebep: Bézier kontrol
  noktasından geçmez ve bel yerel MİNİMUM, yuvarlamak bele malzeme ekler.

**MİRAS DÖRT (dokunulmadı):** `style_check` · `sizechart_source_check` ·
`contract_check` · `figure_check`.

**★ DÜRÜSTLÜK DÜZELTMESİ (hakem yakaladı):** `GECE/V3-D.md`'nin *"sıfır ad
eklendi"* cümlesi KELİMESİ KELİMESİNE YANLIŞTIR. Ham kırmızı ad kümesi
**4 → 6**'dır. Miras küme büyümedi (RULES 9 anlamında ihlal yok), ama fazın
kendi iki kapısı kırmızıdır ve bu KOSU.md'de sayıyla durur.

## 3. HAKEM (temiz oturum, brief görmedi, 7 soru)

**GEÇTİ** — *"iki üretim hattı gerçekten tek `GarmentSurf`'ten besleniyor,
kaynakta tek bir düzeltme katsayısı yok, hiçbir eşik gevşetilmedi, ve faz kendi
kapısını yeşile boyamak yerine KIRMIZI bırakıp sayısını basıyor."*

Fazın ANTİ-HACK sorusuna verdiği cevap: `shell-flat.cpp:148` ile
`surfacepattern.cpp:1297` **aynı `buildGarmentSurf(body, opt)`'u** çağırıyor,
aynı `kStatureMM=1680 / kCapMM=60 / SheathOptions{}`. İkinci kabuk sınıfı yok.
`calib|correction|fudge|factor|magic|adjust` taraması **sıfır sabit çarpan**.
Hakemin asıl kanıtı: *"kıyaslanabilen iki ölçü 0.1094mm ve 0.1494mm'ye tutarken
üçüncüsü 28.7714mm sapıyor ve kırmızı bırakılıyor — katsayı olsaydı üçü birden
tutardı."* Gizleme sorusu: **HAYIR** — sıfır uzunluklu segment atılmıyor
sayılıyor, örnekleme 4.0mm motorun kendi 8mm mesh'inden ince (kabalaştırma
değil), 20.56°'lik köşe ölçek-değişmez ve kapıyı kırmızı tutuyor.

Hakemin kendi zayıflık beyanları: kanat (a)'nın boş-test koşusu gerçek kapıyla
değil elle yazılmış vekil script'le yapıldı (usul zayıflığı, hüküm değişmiyor);
`GECE/V3-R.md` hakem tarafından açılmadı, eşik gerekçeleri kaynağından
doğrulanmadı; yalnız EU38 ölçüldü.

★ Hakemin en değerli yan bulgusu: `hem 1295.6000` ve `waist 725.0000` tam
yuvarlak, çünkü kabuk tarafı bir **HEDEFİ**, kalıp tarafı **GERÇEKLEŞENİ**
ölçüyor. Uyum gerçek bir round-trip testidir ama "iki bağımsız ölçüm" DEĞİLDİR.

## 4. §7.5 BÜTÇE AŞIMI — GEREKÇE

Faz başına tavan 3 yeni kaynak dosya; V3 **5** açtı:
`shellprojection.hpp` · `shellprojection.cpp` · `tools/shell-flat.cpp` (çekirdek, 3)
+ `tools/pattern-measure.mjs` · `tests/flat_*_check.mjs` ×2 (kapı artefaktları).
GEREKÇE: §6/V3 kapıyı ÜÇ KANAT olarak tanımlıyor ve kanatlar iki hattı
karşılaştırmak zorunda; ölçüm aleti olmadan kanat (a) yazılamaz. Onarım kartı
V3-D'nin yeni dosya bütçesi **0** tutuldu ve aşılmadı.

## 5. SONRAKİ FAZA DEVİR (üç sayı)

1. **KIRMIZI = 6** — miras 4 (`style_check` · `sizechart_source_check` ·
   `contract_check` · `figure_check`) + V3'ün kendi 2 kapısı
   (`flat_pattern_agree_check` · `flat_artifact_census`), ikisi de kök
   teşhisli ve ölçülmüş adaylı (§4.7). Test 108 → 110.
   Log: `GECE/log/V3.ctest.before.txt` → `GECE/log/V3-D.ctest.txt`
2. **SÖZLÜK TABANI = 10438 referans @ `495d58a`, RATCHET KİLİTLİ.** Taban
   V3'te bilinçli yeniden kesildi (10418→10438), deltanın 18'i yorum metni,
   satır satır kaynağı `d6cbb87` gövdesinde. Sayı bundan sonra yalnız DÜŞEBİLİR.
3. **TEK NESNE KURULDU, ÜÇ ÖLÇÜDE KIYASLANIYOR:** flat ve kalıp aynı
   `buildGarmentSurf`'ten besleniyor; kıyaslanabilir üç ölçüden **ikisi
   ≤0.15mm** tutuyor (hem −0.1494 · waist −0.1093), üçüncüsü **%3.80 sapıyor**;
   **UNMEASURED 3/6, ratchet tavanı 3** (bust · neck · shoulder — sevk edilen
   kalıp strapless). Sayı yalnız düşebilir.

## 6. AÇIK KALANLAR (kart olarak kuyrukta)

- **Belde C1 kırığı** — tek mekanizma ölçüldü ve kalıbı bozdu; iki aday daha
  var, ikisi de ÖLÇÜLMEDİ: (a) skim run'ın alt ucunu halka interpolasyonunun
  bel eğimine eşitlemek, (b) bel halkasını 724.92'de tutan bir yuvarlama.
- **G5 (omuz/yaka/kol oyuğu)** — üç UNMEASURED'ın da kökü. V7'nin kartı.
- **Yalnız EU38 ölçüldü.** Diğer 7 bedende dejenere/C1 sayımı YAPILMADI.
- `buildGarmentSurf` ve `buildSheathPattern` `levelHeight(body,"waist"/"hip")`'i
  ayrı ayrı okuyor — iki kopya, biri gitmeli (vocab deltasının kalanı buradan).
- `shell-flat`'in `measures` alanı yalnız ÖN görünüm; arka orta hat yayı
  hesaplanıyor (kalıp tarafı 772.2352mm) ama basılmıyor, hiçbir kapıya bağlı değil.
- Sınıf 1 ham işaret değişimi 246 (V3-C'de 242) — fark açıklanmadı, **DOĞRULANMADI**.
- `h3b-rings.py` temiz makinede koşmuyor (`svgpathtools`, venv gitignore'da).
- `flat_geometry_sellable_check` PASS diyor ama kendi çıktısında 10 panelde
  ihlal basıyor (omuz/göğüs 1.0624 ≥ 1.0) — kapı, düzeltmeye çalıştığı kusuru
  varsayıyor. Kart dışı bulgu, dokunulmadı.
- README'ye dokunulmadı (kâtip kararı): shell projection bugün siluet-only ve
  iki kapısı da kırmızı, satış yüzeyine yazmak duran-iddia riski. Karar Damla'nın.
