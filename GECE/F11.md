# F11 — KAPANIŞ (22 Ağu 2026, v3 koşusu)

Şef: F11 oturumu. Dört kart, dört işçi, hepsi PARALEL (kapalı liste, çapraz
bağımlılık yok). Kartlar: `GECE/KART/F11-A-ctest.md` · `F11-B-kayip-is.md` ·
`F11-C-damar.md` · `F11-D-vitrin.md`.

---

## 0. TEK CÜMLE

**Bu koşuda ana dala tek satır kod girmedi.** Gecenin ürettiği bütün motor işi
(`F0 F6 F9 F10`) `gece.sh`'in kendi temizleme mantığı tarafından, git nesnesi
bırakmadan silindi. Kapanış fazı bu yüzden bir başarı raporu değil, bir
**adli ölçüm** raporudur.

---

## 1. KIRMIZI KÜME — KIMILDAMADI (kart F11-A)

Taze koşu, `962407d` ağacında, Release build:

| ölçü | sayı | kanıt |
|---|---|---|
| toplam test | **96** | `GECE/log/F11.ctest.txt` (`ctest -N` → 96; `grep -c add_test engine/CMakeLists.txt` → 96) |
| kırmızı | **7** | aynı dosya: `93% tests passed, 7 tests failed out of 96` |
| süre | 237.61 sn | aynı dosya |
| taban diff | **BOŞ (0 bayt)** | `GECE/log/F11.reddiff.txt` |
| yeni kırmızı ad | **0** | `comm -13` boş |
| kapanan kırmızı | **0** | `comm -23` boş |

Kırmızı adlar (`GECE/log/F11.red.after`), F0 tabanıyla birebir:
`bugra_bridge_check · contract_check · figure_check · h10_gate_check ·
preview_truth_check · sizechart_source_check · style_check`

§0.6 sağlandı (küme büyümedi) — ama **bedelsiz sağlandı**: hiçbir şey
değişmediği için hiçbir şey bozulamazdı. Bunu bir başarı olarak yazmıyorum.

**Not — "98 test" nereden geldi:** F9 tutanağı 98 test diyor. `GECE/kurtarma/
F9.ctest.b-now-F9.txt` bunu doğruluyor: F9'un kapı worktree'sinde 98 test
vardı (96 taban + `docs_truth_check` + `sleeve_armhole_agree_check`). O ağaç
silindi; 98 HEAD'den çıkamaz.

---

## 2. GECENİN İŞİ NEREDE — KÖK SEBEP (kart F11-B, `GECE/F11-B.md`)

### 2.1 Kusur
`GECE/gece.sh:158` **kapıyı**, `:164-165` **commit'i** çalıştırıyor. Yani kapı
kırmızıya düştüğünde `HEAD` her zaman `$ONCE`'a eşittir. `DUR()`'un `:82`
satırındaki `if [ "$(git rev-parse HEAD)" != "$ONCE" ]` bu yüzden **hiçbir
zaman doğru olmaz**; `:83`'teki `git branch -f "gece/$F-reddedildi"` bir kapı
kırmızısında **hiç çalışamaz**. Akış her seferinde `:93-94`'e düşer:

```
git checkout -- .
git clean -fdq -- engine contract web knowledge docs vision-student
```

`git clean -fd` takipsiz dosyayı nesne veritabanına almadan siler → **geri
dönüşü yok.**

> §3.1'in *"reddedilen iş `gece/F#-reddedildi` dalına alınır, ana dal temiz
> kalır"* sözü **TUTULMUYOR.** Harness kendi anayasasını ihlal ediyordu.

İşçi bunu iddia etmedi, **koşturarak kanıtladı**: reponun izole klonunda
mevcut `:82-95` bloğu çalıştırıldı →
`HEAD != ONCE ? HAYIR-ELSE-DALINA-GIRER · sahte test: SİLİNDİ · açılan dal: ''`
(`GECE/log/F11B.kurtarma.txt` blok [34]).

### 2.2 İkinci kusur: birikme
`:154`'teki `return 2` (ŞEF API hatası) `DUR()` **çağırmıyor**. Koşmamış fazın
işi ağaçta kalıyor ve sonraki faza taşınıyor. Bu gece:

| saat | faz | çıkış | sonuç |
|---|---|---|---|
| 15:20, 15:46 | F0 ×2 | `return 2` | iş ağaçta birikti |
| 15:52 | F6 | `return 2` | iş ağaçta birikti |
| 16:26 | F9 (1) | `return 2` | iş ağaçta birikti |
| **17:08:19** | **F9 (2)** | **KAPI KIRMIZI** | **F0+F6+F9'un BİRİKMİŞ işi toptan silindi** |
| **18:09:49** | **F10 (2)** | **KAPI KIRMIZI** | **F10'un işi silindi** |

Birikme tahmin değil, F9'un kendi kapı logundan okunuyor: `GECE/log/F9.kapi.txt`
K2 hem `docs_truth_check` (F9) hem `sleeve_armhole_agree_check` (**F6**) diyor;
K4'ün diff'i `// GECE/F6-B.md` yorumunu gösteriyor.

> **F6 kendi kapısına hiç girmedi. Başka bir fazın kırmızısıyla öldü.**

### 2.3 Kurtarılabilir mi?
**Kaynak kodu için HAYIR — KURTARILAMAZ.** Umut satmıyorum. Kanıt:
- `find .git/objects -type f -newermt "2026-08-22 15:20" ! -path '*/pack/*'` → **0 dosya**
- `git reflog` en yeni girişi 21 Ağu'nun `962407d`'si
- `.git/objects` en yeni loose dosya 20 Ağu 23:09 · `.git/lost-found/` 17 Ağu
- `*.orig` / `*~` yok
- İşçiler hiç `git add` çağırmadı → hiçbir bayt nesne veritabanına girmedi

**Artefaktlar için KISMEN EVET** — `kapi.sh` kendi TMP'sini silmiyor. Şef
bunları uçucu `/private/tmp`'den kalıcı `GECE/kurtarma/`ya aldı (328K):

| kurtarılan | ne |
|---|---|
| `GECE/kurtarma/F10.index.html.orig` (77548 B) | F10'un düzenlediği `web/index.html`, BAYT olarak |
| `GECE/kurtarma/F10.index.html.yedek` (77200 B) | ikinci yedek |
| `GECE/kurtarma/F9.arch-backup.md` | F9'un docs düzenlemesi |
| `GECE/kurtarma/b-now-F9.LastTest.log.gz` | silinen üç kapının TAM ctest çıktısı |
| `GECE/kurtarma/b-now-F10.LastTest.log.gz` | aynısı, F10 |
| `GECE/kurtarma/*.silinen-add_test.txt` | silinen `add_test` satırları |
| `GECE/kurtarma/F9/F10.ctest.*.txt` | kapı öncesi/sonrası ctest |

Ayrıca `/private/tmp/stitchu-gece/b-now-F9/` içinde F6'nın düzeltilmiş
`sleeve.cpp`'siyle 17:04'te derlenmiş **çalışan ikili** duruyor
(`sleeve_armhole_agree_check`, izole scratch'te koşturuldu: `EXIT=0`,
`cells=96 · H1 0.015479mm (limit 0.79375) · H2 0.001297 (limit 0.0015) ·
H3 0.000000 (limit 0.0200)`). **338M olduğu için repoya alınmadı; makine
yeniden başlarsa gider.** Karar Damla'nın (DAMLA-KUYRUK).

### 2.4 Yeniden inşa — faz faz
- **F0** — kayıp yok, kod üretmedi. Tutanakları tam (`F0.md`, `F0-A…D2.md`).
- **F6 — TAM yeniden yazılabilir.** `GECE/F6-C.md:103-120` ÖNCE/SONRA kodunu
  birebir taşıyor; limitler ikiliden okundu. Eksik: testin 96-hücrelik döngü
  gövdesi + `f6-armhole-cap.cpp`. Kök sebep tutanakta **yazılı**:
  `engine/src/sleeve.cpp` `capSpreadFrac` **genişlik** kesri olarak
  uygulanıyor, oysa oyuğa büzülen fazlalık **yay**.
- **F9** — elde `GECE/F9-A.md:22-44` (dosya adı, `add_test` satırı, ilk kırmızı)
  + yeşil çıktının tam metni. Eksik: gömülü perl'in regex'i.
- **F10** — elde `GECE/F10-A.md`'nin 16 satırlık iddia tablosu +
  `web/index.html` bayt olarak. Eksik: `landing_truth_check.sh` parse kodu ve
  `landing-claims.json` şeması.

### 2.5 Düzeltme adayı (§0.4 — kırmızı raporu tek başına iş değildir)
İşçi bir yama **tasarladı, ölçtü, uygulamadı** (`GECE/F11-B.md` sonu). Özü:
`DUR()` içinde temizlemeden ÖNCE emeği mühürle — `git clean`'inkiyle **aynı**
yol listesi üzerinde `git add -A` + anlık `REDDEDILDI(...)` commit'i +
`git branch -f gece/$F-reddedildi HEAD` + `git reset --hard $ONCE`.
`GECE/` bilerek dışarıda kalır.

Kartın diğer ipucu (**commit'i kapıdan önce atmak**) **ölçüldü ve reddedildi**:
`git add -A` GECE/'yi de alır, `reset --hard` `GECE/log/gece.txt`'yi diskten
siler — `gece.sh:84-90`'da yazılı 21 Ağu kazasına geri dönüş olurdu.
Reddedilen hamle de kayda geçer (§0.4).

Seçilen yol bu reponun klonunda sahte `FTEST` fazıyla koşturuldu:
`main temiz · HEAD==ONCE · sahte test ağaçta YOK · sleeve.cpp 0 satır diff ·
GECE/FTEST.md SAĞ · gece.txt SAĞ · gece/FTEST-reddedildi dalında 3 dosya ·
git checkout ile GERİ GELDİ`.

**`gece.sh` DEĞİŞTİRİLMEDİ** (mtime 17:08:19). Mühür sağlam:
`sha256sum -c GECE/kapi.sha` → `kapi.sh: OK`, `mutasyon.sh: OK`.

---

## 3. DAMAR — KIMILDAMADI, AMA ÜÇE AYRILDI (kart F11-C, `GECE/F11-C.md`)

F0-A §3.1 yöntemi aynen tekrarlandı (payda 21 kalem, ANAYASA satır çıpalı):

| hat | F0-A | F11-C | sapma |
|---|---|---|---|
| garment | %95.2 (20.0/21) | **%95.2** | **0** |
| flat | %81.0 (17.0/21) | **%81.0** | **0** |
| surfacepattern | %11.9 (2.5/21) | **%11.9** | **0** |

**Kımıldamadı, çünkü bu koşuda ana dala tek satır kod girmedi.**

### 3.1 İki doğru teke indi (§0.1)
`GECE/F0.md:93` **%0** ile `GECE/F0-A.md:241` **%95.2** çelişmiyordu —
**farklı soruları** ölçüyorlardı. Adlandırıldı:

- **DAMAR-YETENEK (repertuar)** — *"en az BİR yapılandırmada motor bu detayı
  adıyla çizebiliyor mu?"* (VAR-BİR niceleyicisi)
  → garment **%95.2** · flat **%81.0** · surfacepattern **%11.9**
- **DAMAR-SEVKİYAT (varsayılan nesne)** — *"alıcının bugün eline geçen
  VARSAYILAN nesnede bu detay var mı?"* (TEK-BELİRLİ niceleyici)
  → garment **%9.5 (2/21)** · surfacepattern %0.
  Kaynak: `engine/src/measurements.hpp:248-320` — 21 kalemin **19'u opt-in
  `None`**; varsayılanda açık iki kalem `SleeveStyle::None` (kolsuz) ve
  `SkirtStyle::ALine`.
- **DAMAR-ÜYELİK (stil başına)** — flat **9/31 = %29**. Bu 21'lik paydadan
  DEĞİL, F0.md §2'nin 6-primitifli üyelik testinden geliyor. `KOSU.md:48`
  onu %0'ın parantezinde adsız taşıyordu.

**%0 → %9.5 bir gevşetme DEĞİL** (§0.12): payda aynı, kriter **sertleşti**
(varsayılan yapılandırma), sayı yalnızca KOSU.md'nin kendi ilan ettiği hatta
(`garment`, §0.14) taşındı. Eski %0, sevk EDİLMEYEN `surfacepattern`
kütüphanesinde ölçülmüştü.

### 3.2 Yan ölçümler
- 26 adlı damar kapısının **26'sı Passed**
- `engine/vocab.json` 37 eksen · `buildSpec()`'te okunmayan eksen **0**
- Sicil: 9 shipped · 1 flagged (`shoulderSeam`) · **5 absent**
  (`gatheredOverlayLayer sleeve collarFamily skirtFamily zipperPiece`)
- **ADIYLA EKSİK** (§0.3): garment'ta **dantel/fisto** — `vocab.json`'daki 3
  `lace|scallop` hit'inin üçü de başka şey (`collarEdge::scallop` yaka kenarı,
  `laceUpBack::corset` bağcık). flat'te **mini-düğme sırası · lace-up ·
  halter · kutu-pili** (grep 0).
- **HAT VARSAYIMI (§0.14) GEÇERLİ.** Zincir uçtan uca izlendi:
  `draftJSON` (`bindings.cpp:238`) → `buildSpec` → `GarmentDrafter::draft` →
  `web/js/create.js:9` / `studio.js:11` → `print.js` mm-boyutlu 1:1 A4.
  `grep -c surfacepattern engine/wasm/bindings.cpp` → **0**.

### 3.3 KOSU.md sayı hatası düzeltildi
`KOSU.md:41` *"gerçek absent sayısı 4"* → **5**. `zipperPiece` sayılmamış;
`closure.centreBackZip` ve `closure.buttonFront` ikisi de ona bağlı, ikisi de
ifade edilemez.

---

## 4. VİTRİN — ÖNCE == SONRA (kart F11-D, `GECE/F11-D.md`)

```
git log --oneline 962407d..HEAD                     → BOŞ (0 commit)
git diff --stat 962407d..HEAD -- docs web README.md → BOŞ
git status --porcelain -- docs web README.md        → BOŞ
```

**F11 brief'i madde 5 "docs ve landing'in önce/sonra ekran görüntüsü yolları"
istiyor. İki ayrı görüntü YOK, çünkü iki ayrı hâl yok.** Sahte bir "sonra"
üretilmedi (§0.2). Tek hâl var ve o hâl `962407d`.

### 4.1 F9'un "0+0"ı ana dalda GEÇERSİZ
- `engine/tests/docs_truth_check.sh` → **diskte YOK**
- `git ls-files | grep truth_check` → **BOŞ**
- `engine/CMakeLists.txt`'te tek `truth` kaydı `preview_truth_check` (555-560)

`docs/` baytları `962407d` ile aynı olduğu için `GECE/log/F9A.gate.before.txt`
sayısı **bugünün sayısıdır**: **16 duran-iddia + 36 tanıksız = 52 ihlal**,
EXIT=1. "Ölçülemedi" değil — bayt eşitliği yoluyla **ÖLÇÜLDÜ**.

### 4.2 Landing'deki tek YALAN — tam metin, satır numarasıyla
**`web/index.html:212`**
> *"Every drawing on this page is real engine output, drafted end-to-end and
> live. Fixes and misses are published openly; the full audit trail is in the
> patch notes."*

Ölçüm: `engine/tools/gen-landing.js` izole worktree'de koşturuldu, EXIT=0,
7 anahtar üretti. Sayfadaki 9 inline SVG ile karşılaştırıldı —
**7/7 tam eşleşme HAYIR, 7/7 ilk-`path` eşleşmesi HAYIR.** Aynı viewBox,
farklı koordinat:

```
gen-landing proofSheet : <path d="M20.2 15.8C28.8 15.2 35.7 14.3 40.6 14.0C43.5 25.9 ...
web/index.html:237     : <path d="M19.1 18.1C27.3 17.5 33.9 16.5 38.6 16.2C41.8 28.7 ...
```

Dahası: `gen-landing.js` hiçbir dosyaya **yazmıyor** (`writeFile` 0 eşleşme) —
SVG'ler sayfaya **elle yapıştırılmış**. "live" mimari olarak imkânsız.
`web/index.html` §0.15'in korunan 57 yolunda değil, ELLE yazılan bir dosya;
yani bu bir üreteç kusuru değil, **yazılmış bir cümle**.

18 iddianın 18'i sayfada duruyor — F10-A'nın sayımı bugün aynen geçerli:
**DOĞRU 0 · YALAN 1 · KANITSIZ 17**.

### 4.3 site-health bugün temiz
`GECE/log/F11D.site-health.txt`: 127 sayfa, 2597 iç bağlantı, 124 sitemap URL.
Kırık iç link **0** · sitemap 404 **0** · sitemap eksiği **0** · `?v`
gerilemesi **0** · EXIT=0. `GECE/log/F10A.site-health.txt` ile birebir aynı.

---

## 5. SORULMAMIŞ AMA ÖNEMLİ (§5.5 dökümü)

1. **`web/index.html` kendi içinde çelişiyor:** `:8` (meta) ve `:22` (JSON-LD
   ×3) **EU34–48** diyor; `:179`, `:316`, `:317` **EU34–52** diyor. Google'ın
   gördüğü ile insanın gördüğü farklı. Ölçen test yok.
2. **`CAD` yasağı (CLAUDE.md) 8 satırda ihlal:** `web/index.html:7, 8, 12, 20,
   22, 176, 191, 192`. F10-C bunu kaldırmıştı — iş silindi, yedeği
   `GECE/kurtarma/F10.index.html.orig`'de duruyor.
3. **Üç üreteç bugün KOŞMUYOR:** `gen-collections-page.mjs`,
   `gen-vintage-page.mjs`, `gen-taste-collections.mjs` → hepsi EXIT=1, ENOENT
   `web/patterns/svg/meta.json`. Sebep kodda yazılı:
   `gen-sitemap.mjs:15` → *"af49514 deleted that tree"*. Yayındaki koleksiyon
   sayfaları **öksüz üretilmiş dosyalar**.
4. **`gen-sitemap.mjs` 124 satırın hepsini değiştiriyor** — tek fark `lastmod`
   `2026-08-17` → bugün. URL kümesi aynı; site-health bunu ihlal saymıyor.
5. **`web/js/missing.js` motorun gerisinde olabilir.** Alıcıya *"lace-up
   çizili değil / fiyonk parça olarak çizili değil / düğme patı çizili değil"*
   diyor (`missing.js:30-56`), oysa `engine/src/laceupback.cpp tie.cpp
   buttonrow.cpp collar.cpp gather.cpp` var ve beş kapı da yeşil.
   **Bu, alıcıya motorun çizebildiğini "çizemedim" demek olur — dürüstlük
   katmanının TERS yönde yalanı.** Denenen tek şey grep'ti, **koşturarak
   DOĞRULANMADI.**
6. **Üç katman üç etek sözlüğü konuşuyor:** `engine/vocab.json:12`
   `skirtStyle`'da `fullCircle` YOK; `contract/garment-spec-v2.json
   topology.skirtShape`'te VAR (absent `skirtFamily`'ye bağlı); flat'te 6 stil
   "full-circle" etiketli. Ölçüldü, çözülmedi.
7. **F9'un K4 kırmızısı büyük olasılıkla YANLIŞ POZİTİF.** Yakaladığı iki
   satır **YORUM**; `kapi.sh:195`'in grep'i "kapı"/"gate" kelimesi yorumda
   geçtiği için takılmış. K4 kodu yorumdan ayırmıyor. **DOĞRULANMADI** —
   `kapi.sh` mühürlü, açılmadı.
8. **F9/F10 işçileri canlı çalışma ağacını düzenleyip geri almış**
   (`/tmp/f9-clean` ve iki yedek dosya tanık). F11-D işçisi `git worktree`
   kullandı, gerçek ağaca dokunmadı.
9. **DOĞRULANMADI:** canlı sitenin repo baytlarıyla aynılığı (ağ çağrısı yok) ·
   `web/create.html`, `closet.html`, `studio.html` iddiaları hiç sayılmadı ·
   `generated_ratchet_check` bu 3 ölü üreteci yakalıyor mu ·
   `gece/F1-reddedildi` dalının içeriği · `af49514`'ün geri alınabilirliği ·
   `h10_gate_check`'in "shoulder-seam 0 dikiş" kırmızısı hangi hatta duruyor.
10. **DAMAR-SEVKİYAT statik ölçüldü** — `GarmentSpec` üye başlangıç
    değerlerinden okundu, motor koşturulup çizim sayılmadı.

---

## 6. MUTASYON (§2.3)

`GECE/mutasyon.tsv`'nin kendi kuralı: *"F1 dünya taraması ve **F11 rapor kod
yazmaz → mutasyonu yok**"*. F11 bu koşuda `engine/ contract/ web/` altında
sıfır dosya değiştirdi (`git status --porcelain | grep -v '^?? GECE/'` BOŞ),
dolayısıyla bozulacak bir ölçülen nesne yok. **Sahte knob yazılmadı** — tabloya
uydurma bir mutasyon eklemek `mutasyon.tsv`'nin kendi yasağını ihlal ederdi.
Tabloya F11 için gerekçeli bir yorum satırı eklendi.

---

## 7. F11 NE YAPMADI

- Hiçbir kırmızı kapatılmaya çalışılmadı (kapanış fazının işi değil).
- `gece.sh` **düzeltilmedi** — yama tasarlandı ve ölçüldü, ama uygulanmadı;
  harness değişikliği Damla'nın kararı (DAMLA-KUYRUK).
- Silinen kod yeniden yazılmadı. F11 ölçüm fazıdır; yeniden inşa bir sonraki
  koşunun işi ve tarifleri §2.4'te.
- `/private/tmp/stitchu-gece/` (338M) repoya alınmadı; içindeki çalışan ikili
  makine yeniden başlarsa gider.
- Stash'lerin içi açılmadı (`stash@{0}` Damla'nın kararında).

---

## F11 İKİNCİ AÇILIŞ — İŞ YAPILMADI (22 Ağu, 18:40)

`gece.sh` F11'i `once=6208704` ile yeniden açtı. Bu bir faz değil, bir
**yanlış teşhis**: F11'in kendi kapanış commit'i zaten ana dalda ve uzakta.

| iddia | doğrulandı mı | kanıt |
|---|---|---|
| F11 push edildi | EVET | `git rev-parse origin/main` = `62087045c8…` |
| kırmızı küme büyümedi | EVET | `GECE/log/F11.reddiff.txt` **0 bayt** |
| 7 devralınan kırmızı | EVET | `GECE/log/F11.red.after` **7 satır** |
| ctest sayısı | EVET | `GECE/log/F11.ctest.txt`: *93% tests passed, 7 tests failed out of 96*, 237.61 sn |
| kurtarma paketi | EVET | `GECE/kurtarma/` 328K, `6208704` içinde |

Kök sebep F0'ınkiyle **aynı**: `gece.sh`'in `is_error` taraması ajanın TÜM
logunu tarıyor, düşen tek bir `tool_result` fazı "koşmadı" ilan ettiriyor.
F0 şefi bunu 15:52'de ölçüp `gece.sh:139-146` / `:136` / `:178`'i düzeltmişti,
ama düzeltme **bellekteki koşuya işlemiyor** — aynı çalışan süreç eski mantığı
taşıyor. Bu yüzden F6, F9, F10 ve F11 dört kez "koşmadı" damgası yedi.

İkinci açılışta **kart yazılmadı, işçi salınmadı, hiçbir ölçüm tekrarlanmadı.**
Tekrarlanan bir sayı yeni bir sayı değildir (§0.2); §0.6'nın "bedelsiz yeşil"
uyarısı burada ikinci kez geçerli olurdu. Bu koşu **F11 ile bitmiştir.**
