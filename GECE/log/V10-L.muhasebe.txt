V10-L — TABAN MUHASEBESİ (her sayının yanında onu BASAN komut)
işçi ölçtü, hakemin sayısına güvenilmedi. Çelişkide İŞÇİNİN sayısı geçerlidir.

HEAD              : ade83dc  (git rev-parse --short HEAD)
faz öncesi ağaç   : 9b306f9  (git worktree add /tmp/v10-pre 9b306f9)
eski taban commit : c72f650  (engine/tests/landing-truth-baseline.json "olcumCommit")

═══ 1. FAZ ÖNCESİ (9b306f9) ═══════════════════════════════════════════════
komut: node engine/tests/landing_truth_check.mjs --no-baseline --dir=/tmp/v10-pre/web
çıktı: "sayı+birim taşıyan iddia bloğu 1116 · SAĞLAYICISIZ 1074 · taban 0"
       "hit 829 · taban 0"                                      (L2)
       "data-vision=\"1\" blok 0 · işaretli cümle 0 · MUAF 0 · İHLAL 2"   (L3)
       "koşuldu: .../site-health.mjs · exit 0 · FAIL satırı 0 · sayı 0"   (L4)
       "kaçak EU etiketi 8 · üstküme seçim 2 · toplam 10"        (L5)
  sayısal blok      1116     (hakem: 1116 — TUTTU)
  sağlayıcısız      1074
  sağlayıcılı         42     = 1116 − 1074   (hakem: 42 — TUTTU)
  L4                   0     (hakem: 0 — TUTTU)

═══ 2. HEAD (ade83dc) ═════════════════════════════════════════════════════
komut: node engine/tests/landing_truth_check.mjs --no-baseline
çıktı: "sayı+birim taşıyan iddia bloğu 1106 · SAĞLAYICISIZ 937 · taban 0"
       "hit 0" · "blok 1 · işaretli cümle 30 · MUAF 5 · İHLAL 0"
       "exit 0 · FAIL satırı 0 · sayı 0" · "kaçak 4 · üstküme 0 · toplam 4"
  sayısal blok      1106     (hakem: 1106 — TUTTU)
  sağlayıcısız       937
  sağlayıcılı        169     = 1106 − 937   (hakem: 169 — TUTTU)

═══ 3. L1'İN FAZ İÇİNDEKİ DÜŞÜŞÜ — MUHASEBE ══════════════════════════════
  sağlayıcısız düşüş   1074 → 937   =  −137
  bunun kaynağı:
    SİLİNDİ            1116 → 1106  =   10   (siteden ÇIKAN sayısal iddia bloğu)
    JETONLA AKLANDI      42 →  169  = +127   (blok DURUYOR, sadece sağlayıcı adı kazandı)
    10 + 127 = 137 — tam olarak düşüşün kendisi. Yani düşüşün %92.7'si silme DEĞİL.

═══ 4. JETONLAR (git diff 9b306f9..HEAD -- web/) ═════════════════════════
komut: git diff 9b306f9..HEAD -- web/ | grep '^+' | grep -o 'golden_check' | wc -l
  golden_check EKLENEN GEÇİŞ  : 212      (hakem: 224 — TUTMADI, işçininki geçerli)
komut: git diff 9b306f9..HEAD -- web/ | grep '^+' | grep -o 'engine_check' | wc -l
  engine_check EKLENEN GEÇİŞ  :  60      (hakem: 60 — TUTTU)
komut: git diff 9b306f9..HEAD -- web/ | grep -c '^+.*golden_check'   → 115 satır
komut: git diff 9b306f9..HEAD -- web/ | grep -c '^+.*engine_check'   →  59 satır
komut: git diff 9b306f9..HEAD -- web/ | grep '^-' | grep -o 'golden_check' | wc -l → 0
  (faz boyunca bu iki jetondan tek bir tanesi bile SİLİNMEDİ.)

═══ 5. YALNIZ BU İKİ JETONLA AKLANAN BLOK — ÖLÇÜLDÜ ══════════════════════
yöntem: HEAD'in fikstür kopyasında iki jeton NÖTRLENİR, kapı yeniden koşulur;
        sağlayıcısız sayısındaki artış = yalnız o iki jetonla aklanan blok.
komut:
  git worktree add /tmp/v10-fix HEAD
  cd /tmp/v10-fix/web && grep -rl -E 'golden_check|engine_check' . \
    | xargs perl -pi -e 's/golden_check/GXLDEN_CHK/g; s/engine_check/ENGXNE_CHK/g'
  (dokunulan dosya: 58 · kalan geçiş: 0 — grep -rc ile doğrulandı)
  node engine/tests/landing_truth_check.mjs --no-baseline --dir=/tmp/v10-fix/web
çıktı: "sayı+birim taşıyan iddia bloğu 1106 · SAĞLAYICISIZ 1039"
  1039 − 937 = 102 BLOK yalnız golden_check/engine_check substring'iyle aklanıyor.
  (hakem: 81 — TUTMADI, işçininki geçerli.)
  Örnek (kartın verdiği): web/patches/1-5.html:82 — "the benchmark re-classified to
  exactly 14/54" cümlesi aynı blokta `golden_check` yazdığı için sağlayıcılı sayılıyor.
  golden_check 14/54 BASMAZ. komut: grep -n "14/54" web/patches/1-5.html

═══ 6. HAKEMİN SONDASI — YENİDEN KOŞULDU, BİREBİR TUTTU ══════════════════
fikstür: /tmp/v10-fix/web/index.html, </body> önüne tek <p> eklendi.
  (a) "The seam allowance is 12.5 mm on every piece."
      komut: node engine/tests/landing_truth_check.mjs --no-baseline --dir=/tmp/v10-fix/web
      çıktı: "iddia bloğu 1107 · SAĞLAYICISIZ 938"      → taban 937 AŞILDI = KIRMIZI
  (b) aynı cümle + " (site-health)"
      çıktı: "iddia bloğu 1107 · SAĞLAYICISIZ 937"      → YEŞİL
  Uydurma bir sayı, ONU BASMAYAN bir aletin adı yanına yazılınca kapıdan geçiyor.
  L1 eşleşmesi SUBSTRING'dir (engine/tests/landing_truth_check.mjs:53-54).
  fikstür geri alındı: cd /tmp/v10-fix && git checkout -- web

═══ 7. L2 — KAÇ KALIP, HANGİ DİL ══════════════════════════════════════════
komut: node -e "BANNED bloğundaki ['ad', /re/] adlarını say" (aşağıdaki tek satır)
  node -e "const s=require('fs').readFileSync('engine/tests/landing_truth_check.mjs','utf8');
   const b=s.split('const BANNED = [')[1].split('\n];')[0];
   const n=[...b.matchAll(/^\s*\['([^']*)'/gm)].map(m=>m[1]);
   console.log(n.length, n.filter(x=>x.includes('(TR)')).length)"
çıktı: toplam kalıp 34 · TR 14 · EN 20
  Kartın "İngilizce 18" sayısı TUTMADI; kapının kendi yorumu da (satır 268)
  "18 kalıbın 18'i İngilizce" diyor ama V10-G'nin 7 kalıbıyla EN sayısı 20 olmuş.
  Yorum bayat, ölçüm 20. TR kalıplar V10-I'de 12, V10-J'de 2 eklendi.

═══ 8. L4 — ÖLÜ DENETİM ═══════════════════════════════════════════════════
  faz öncesi (9b306f9): 0     · HEAD (ade83dc): 0
  Bu fazda L4 hiçbir borç taşımadı ve hiçbir şey yakalamadı. Beş denetimden biri
  fiilen ÖLÜ: site-health.mjs faz boyunca tek bir gün bile exit≠0 vermedi.

═══ 9. ESKİ TABANDAKİ ÇELİŞKİ (düzeltiliyor) ═════════════════════════════
  eski `_neden0Degil.L3`: "Bugün sitede data-vision=\"1\" taşıyan blok SIFIR"
  aynı dosyanın `L3.isaretliBlok` alanı: 1 · `isaretliCumle`: 30
  Ölçüm (kapı çıktısı, HEAD): blok 1 · işaretli cümle 30 · MUAF 5 · İHLAL 0.
  Doğru olan JSON'un sayı alanı; prose YANLIŞTI, bu kartta düzeltildi.

═══ 10. TABAN KARŞILAŞTIRMASI (yükselen YOK) ═════════════════════════════
        L1    L2   L3   L4   L5
  eski  937    0    0    0    4     (olcumCommit c72f650)
  yeni  937    0    0    0    4     (olcumCommit ade83dc)
  Hiçbir L yükselmedi. L1 hâlâ TAM TAVANDA (937/937) — sıfır pay.
