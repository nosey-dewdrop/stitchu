# V0-0A — MOTOR ENVANTERİ (ölçüm, 2026-08-24)

Onarım YAPILMADI. Kod/test/pin/tolerans/contract dosyalarına tek bayt yazılmadı.
Her sayının yanında onu basan komut var. Sınıflama (gerileme / bayat pin / kaynak
eksiği) YAPILMADI — hakem koyar.

Koşulan build: `engine/build`, `CMAKE_BUILD_TYPE:STRING=Release`
(komut: `grep -m1 CMAKE_BUILD_TYPE engine/build/CMakeCache.txt`). Rebuild gerekmedi.

---

## MADDE 1 — ctest TAM KOŞUSU

Komut:
```
ctest --test-dir engine/build --output-on-failure > GECE/log/V0-0A.ctest.txt 2>&1
```
Log: `GECE/log/V0-0A.ctest.txt` (455 satır)

| ölçü | sayı | basan komut / kaynak satır |
|---|---|---|
| TANIMLI test | **106** | `ctest --test-dir engine/build -N \| tail -2` → `Total Tests: 106` |
| KOŞAN test | **105** | `V0-0A.ctest.txt:453` → `94% tests passed, 6 tests failed out of 105` |
| KOŞMAYAN (Disabled) | **1** | `V0-0A.ctest.txt:456-458` → `The following tests did not run: 96 - h10_gate_check (Disabled)` |
| KIRMIZI | **6** | aynı satır (`6 tests failed`) |
| YEŞİL | **99** | 105 − 6 |
| toplam süre | **284.70 sn** | `V0-0A.ctest.txt:455` → `Total Test time (real) = 284.70 sec` |

Kırmızıların ADLARI (`GECE/log/V0-0A.red-names.txt`, kaynak `V0-0A.ctest.txt:459-466`):

```
golden_check              (#3)
style_check               (#8)
sizechart_source_check    (#15)
recipe_dress_check        (#71)
contract_check            (#85)
figure_check              (#90)
```

⚠ Kart, v6 §1'in **7 kırmızı** hipotezini doğru varsaymamamı söylüyordu.
Bu gece ÖLÇÜLEN sayı **6**'dır. `h10_gate_check` DISABLED olduğu için ne yeşil ne
kırmızı sayılıyor; 7 ile 6 arasındaki farkın bu olup olmadığı **DOĞRULANMADI**
(v6 dosyası açılmadı, kart yasağı).

Koşan/tanımlı liste farkı ayrıca doğrulandı:
```
ctest --test-dir engine/build -N | grep -oE 'Test +#[0-9]+: [a-zA-Z0-9_]+' | sed 's/.*: //' | sort > /tmp/all106.txt
grep -oE '^ *[0-9]+/[0-9]+ Test +#[0-9]+: [a-zA-Z0-9_]+' GECE/log/V0-0A.ctest.txt | sed 's/.*: //' | sort -u > /tmp/ran.txt
comm -23 /tmp/all106.txt /tmp/ran.txt   → BOŞ (106 = 106; ctest disabled testi de listeliyor)
```

---

## MADDE 2 — HER KIRMIZI İÇİN HAM VERİ

Her satırdaki commit: `git log -1 --format='%h %ad %s' --date=short -- <yol>`

### 2.1 `golden_check` (#3, 0.28 sn)

Çıktı bloğu (`V0-0A.ctest.txt:6-27`), ilk satırlar aynen:
```
golden_check FAIL: engine output differs from the REPO PIN (.../engine/golden-reference.csv).
  dump: 23406 lines   pin: 23406 lines
  first differing lines:
313,314c313,314
< EU38|dress/crew/aLine/none.short|piece0:Bodice Front,outline,2,line,167.3252,47.2004
< EU38|dress/crew/aLine/none.short|piece0:Bodice Front,outline,3,curve,244.2000,219.4004,212.2411,66.8624,213.4501,181.5164
---
> EU38|dress/crew/aLine/none.short|piece0:Bodice Front,outline,2,line,164.0800,45.8893
> EU38|dress/crew/aLine/none.short|piece0:Bodice Front,outline,3,curve,244.2000,225.0000,245.0965,81.4390,164.9867,185.5956
320c320
< EU38|dress/crew/aLine/none.short|piece0:Bodice Front,marking,1,line,120.6000,242.2415
```
- **Karşılaştırılan pin:** `engine/golden-reference.csv`
- **Pin'in son commit'i:** `af49514 2026-07-29 delete fake pattern gallery (67 svgs + 22 product pages) and style-pin artifacts: they presented output that fails the buyable-object test and were steering the work`
- **Dosya mtime:** `28 Tem 22:42` (`ls -l engine/golden-reference.csv`)
- Satır sayısı eşit (23406 = 23406) → parça/satır eklenmemiş, **sayılar** oynamış.
- Testi çalıştıran: `engine/CMakeLists.txt:70` → `tests/golden_check.sh`
- Testin kendi verdiği iki çıkış: (1) motoru düzelt, (2) `scripts/repin-golden.sh` +
  `engine/GOLDEN-PIN.md` defter girdisi + Damla onayı.

### 2.2 `style_check` (#8, 0.03 sn)

Çıktı bloğu (`V0-0A.ctest.txt:36-48`):
```
style_check FAIL: pinlenmiş stil 0 — .../engine/STYLE-PIN yok/boş.
  Bu kapı bugüne kadar YEŞİL basıyordu ve hiçbir kalemi tutmuyordu (T17).
  Kalem yasası pinsiz koşamaz; sessiz skip yasak.
  Tek dürüst çıkış: Damla kalemi onaylar -> scripts/repin-style.sh
  -> engine/STYLE-PIN/<style>.svg + engine/STYLE-PIN/STYLE-PIN.md defter girdisi.
```
- **Karşılaştırılan pin dizini:** `engine/STYLE-PIN`
- **Diskteki hâli:** `ls -ld engine/STYLE-PIN` → `No such file or directory` (**pin YOK**)
- **O yolu en son değiştiren commit:** `af49514 2026-07-29 delete fake pattern gallery
  (67 svgs + 22 product pages) and style-pin artifacts: ...` (silen commit)
- Testi çalıştıran: `engine/CMakeLists.txt:115` → `tests/style_check.mjs`
- Testin kendi ilanı: onay ÖLÇÜMDEN değil KARARDAN gelir; boru hattından beslenen onay reddedilir.

### 2.3 `sizechart_source_check` (#15, 0.06 sn)

Çıktı bloğu (`V0-0A.ctest.txt:...`), aynen:
```
euSizeChart: 7 columns x 10 sizes = 70 numbers on a buyer's body
  sourced (verified against a publication): 3 -> bustCM, waistCM, hipCM
  UNSOURCED (verified absence, declared):   4 -> shoulderCM, backLengthCM, armLengthCM, neckCM
FAIL: column 'shoulderCM'   is UNSOURCED (status NONE) — 10 published values with no publication behind them
FAIL: column 'backLengthCM' is UNSOURCED (status NONE) — 10 published values with no publication behind them
FAIL: column 'armLengthCM'  is UNSOURCED (status NONE) — 10 published values with no publication behind them
FAIL: column 'neckCM'       is UNSOURCED (status NONE) — 10 published values with no publication behind them
mutation probes: 7/7 moved the verdict
sizechart source gate FAILED (4)
This gate is DELIBERATELY RED as of TUR 18B. It closes when the unsourced
columns are matched to a publication or replaced from a named source by Damla
(DAMLA-KUYRUK K10). Do NOT close it by editing the chart or softening the question.
```
- **Karşılaştırılan veri dosyası:** `contract/tables.json` (`euSizeChart` + `_sources`)
- **Son commit:** `d869f45 2026-08-17 the size chart names its source for the first time:
  born in a test harness, and 3 of 7 columns match burda's published table exactly`
- **mtime:** `17 Ağu 18:46`
- Testi çalıştıran: `engine/CMakeLists.txt:154` → `tests/sizechart_source_check.mjs`
- 7 mutasyon sondasının 7'si de hükmü oynattı → kapı ölü değil, tepki veriyor.

### 2.4 `recipe_dress_check` (#71, 0.01 sn)

Blok **135 satır**, **115 PASS / 10 FAIL**, K0 const parite **27/27 PASS**.
Basan komut (python ile blok kesildi, `[PASS]`/`[FAIL]` sayıldı):
```
python3 - <<'EOF'
import re
L=open('GECE/log/V0-0A.ctest.txt').read().split('\n')
s=[(i,re.match(r'\s+Start\s+\d+: (\S+)',l).group(1)) for i,l in enumerate(L) if re.match(r'\s+Start\s+\d+: \S+',l)]
for n,(i,nm) in enumerate(s):
    if nm=='recipe_dress_check':
        b=L[i:s[n+1][0]]
        print(len(b), sum('[PASS]' in x for x in b), sum('[FAIL]' in x for x in b))
EOF
→ 135 115 10
```

FAIL satırları aynen:
```
[FAIL] EU38 piece 'Top Front': meta ok, grainline DIFFERS, geometry (outline+markings+notches+cutline) DIFFERS <= 1e-6 mm
[FAIL] EU38 piece 'Top Back':  meta ok, grainline DIFFERS, geometry ... DIFFERS <= 1e-6 mm
[FAIL] EU38 piece 'Bias binding (neckline + armholes)': meta DIFFERS, grainline DIFFERS, geometry ... DIFFERS <= 1e-6 mm
[FAIL] pear piece 'Top Front'  : ... grainline DIFFERS, geometry DIFFERS
[FAIL] pear piece 'Top Back'   : ... grainline DIFFERS, geometry DIFFERS
[FAIL] pear piece 'Bias binding (neckline + armholes)': meta DIFFERS, grainline DIFFERS, geometry DIFFERS
[FAIL] bigNeckSmallShoulder piece 'Top Front' : ... grainline DIFFERS, geometry DIFFERS
[FAIL] bigNeckSmallShoulder piece 'Top Back'  : ... grainline DIFFERS, geometry DIFFERS
[FAIL] bigNeckSmallShoulder piece 'Bias binding (neckline + armholes)': meta DIFFERS, grainline DIFFERS, geometry DIFFERS
[FAIL] bigNeckSmallShoulder fabric 1.8500 == motor 1.9500
recipe_dress_check: FAILURES
```
HAM VERİ, sınıflanmadan:
- 3 gövdenin (EU38 / pear / bigNeckSmallShoulder) **aynı 3 parçası** düşüyor:
  `Top Front`, `Top Back`, `Bias binding`. 4. parça `Spaghetti Strap` **3/3 PASS**.
- `Spaghetti Strap` PASS, diğer üçü FAIL → fark parça-seçici.
- `seam allowance latched to constants.gen` 12/12 PASS.
- Kumaş: EU38 ve pear `1.9500 == 1.9500` PASS; bigNeck `1.8500 == 1.9500` FAIL.
- (a) iki gövde iki farklı kalıp, (c) dikiş eşitleme + validator: **hepsi PASS**
  (`validator clean (0 issues)` 9/9, `side seams |d| <= 3.0` 9/9).
- Err yolları (v1.1 mührü) 12/12 PASS.
- **Karşılaştırılan reçete dosyası:** `recipes/shift-dress-square-spaghetti.json`
  (`engine/CMakeLists.txt:418-419` argüman olarak veriyor)
  → son commit `edc3f2c 2026-07-28 damar-ici shift dress recipe (vol2 #8): top kernel
     formulas from bodice/garment blocks, dress check 126/126, pinned byte gate 51
     outline lines, a4 png proof`
- İkinci kopya `web/recipes/shift-dress-square-spaghetti.json`
  → `46eb99f 2026-07-28 studio ships the in-vein shift dress as default recipe: ...`
  (bu test onu okumuyor; ikinci kopya olduğu için yazıldı)
- İkinci sabit kaynak: `engine/constants.yaml` → `be2bbb0 2026-07-19 k4 constants table
  single source, 16 embedded assumptions relocated to constants.yaml with generated
  header, values unchanged golden byte identical` (mtime `24 Tem 20:27`)

### 2.5 `contract_check` (#85, 0.21 sn)

Blok 21 satır. **15 satırın 14'ü `ok:`, 1 `FAIL:`** → `contract validation FAILED (1)`.

Tek FAIL aynen:
```
FAIL: DECLARED DECISION (not a breach) — 'patterns_real/' has 41 TRACKED file(s) in git:
  patterns_real/Buttoned Corset Bustier - FIXED/1 - What will i do.jpg, ... +35 more
  ILAN EDILMIS KARAR (Damla, 17.08): patterns_real BILEREK takipli ve repo BILEREK public.
  ... Kapi KALDIRILMADI: sayilan sey hala 'takipli telifli dosya' (bugun 49) ve hedefi
  hala 0, yani kapi KIRMIZI kaliyor ve bedeli gorunur tutuyor.
```
- **Sayı doğrulandı:** `git ls-files patterns_real | wc -l` → **41**
- ⚠ Aynı çıktının içindeki ilan metni "bugun 49" diyor; testin BUGÜN saydığı **41**.
  İkisi aynı çıktıda yan yana duruyor. Ham veri budur, uzlaştırma yapılmadı.
- Diğer telif sınırları temiz: `dataset/` 0, `new_flats/` 0, `benchmark-58/` 0, `engine/dist/` 0.
- Testi çalıştıran: `engine/CMakeLists.txt:586` → `tools/validate-contract.mjs`
- **Karşılaştırılan sözleşme dosyaları** (hepsi `ok:` verdi): `contract/tables.json`
  (`d869f45 2026-08-17`), `contract/garment-spec.schema.json`, `contract/terms.json`,
  `engine/vocab.json` (`8459a60 2026-07-27 bugra locket top construction: ...`),
  `engine/constants.yaml` (`be2bbb0 2026-07-19`).
- Ayrıca `ok:` ile geçen ama not edilmesi gereken satır: *"58-set manifest absent —
  vocabulary-coverage scan CANNOT run here"* → bu tarama koşmuyor, yeşil sayılıyor.

### 2.6 `figure_check` (#90, 0.67 sn)

Blok 36 satır: **30 stil `ok`, 1 `FAIL`**. FAIL aynen:
```
FAIL dress_bandeau_circle  waist/bust 0.872  FAIL tabansız — figure-bands mandal.taban_v3'te pin yok, hükümsüz
figure-lint: 1 FAILURE(S) — boru/drift üretildi, mandal düştü
```
- **Karşılaştırılan pin dosyası:** `contract/figure-bands.json`, anahtar `mandal.taban_v3`
- **Pin sayısı ölçüldü:** `python3 -c "...len(d['mandal']['taban_v3'])"` → **16 pin**;
  `'dress_bandeau_circle' in taban_v3` → **False**
- **Son commit:** `396d42e 2026-08-23 KIRMIZI: preview-truth got its anchor back —
  eleven blind styles now judged, nine reds became eight` (mtime `23 Ağu 05:14`)
- Dosyanın kendi `mandal._taban_v3_pinsiz_kalan` alanı bu kırmızıyı **BİLEREK** açıklıyor:
  *"dress_bandeau_circle — DEVRALINACAK KARDEŞ YOK, pin YAZILMADI, kırmızı BİLEREK
  bırakıldı (DAMLA-KUYRUK'a düşüldü). Ölçüm: 0.872. ... Kendi ölçtüğü 0.872'yi pinlemek
  gate'in kendi yasağı (regen-vs-regen)."*
- Testi çalıştıran: `engine/CMakeLists.txt:631` → `tools/figure-lint.mjs`

### 2.7 KOŞMAYAN: `h10_gate_check` (#96, DISABLED)

- `engine/CMakeLists.txt:687-690`:
  `add_executable(h10_gate_check tests/h10_gate_check_LEGACY.cpp)` +
  `set_tests_properties(h10_gate_check PROPERTIES DISABLED TRUE)`
- CMakeLists'in kendi gerekçesi (`:685-686`): *"sevk edilen giysiyi değil, hattın
  dışında duran bir hattı yargılıyordu. Sevk edilen hattın kapısı: garment_armhole_check"*
- `garment_armhole_check` bu gece **YEŞİL** (`V0-0A.ctest.txt` içinde `Passed`).

---

## MADDE 3 — OPERATÖR SİCİLİ

Kaynak: `contract/garment-spec-v2.json`
Komut: `python3 -c "import json; d=json.load(open('contract/garment-spec-v2.json')); print(len(d['operators']))"`

### 3.1 Sayım

| statü | adet | tanım (dosyanın kendi `_statuses` bloğundan) |
|---|---|---|
| **shipped** | **9** | "runs on the DEFAULT path the buyer receives today (surface-pattern with a default-constructed SheathOptions). Binding is a real symbol in engine/src/surfacepattern.hpp." |
| **flagged** | **1** | "the code exists and is measured, but it is switched OFF in the shipped configuration because turning it on breaks a green gate. A spec requiring it is REFUSED." |
| **absent** | **5** | "SİCİLDE YOK. No engine symbol, no derivative, no approximation. A spec requiring it is REFUSED and the refusal names it." |
| TOPLAM | **15** | |

Komut: `Counter(v['status'] for v in d['operators'].values())` → `{'shipped': 9, 'flagged': 1, 'absent': 5}`

### 3.2 Operatör operatör (statü dosyadan OKUNDU, atanmadı)

| # | operatör | statü | bağlandığı sembol (`binds`) | defect alanı |
|---|---|---|---|---|
| 1 | `bodiceSurface` | shipped | `SheathOptions::skimBodice`, `::shoulderTop` | — |
| 2 | `skirtSurface` | shipped | `SheathOptions::hemDropBelowHipMM` | — |
| 3 | `hemSweepCone` | shipped | `SheathOptions::hemSweepMM` | — |
| 4 | `princessCut` | shipped | `::bodiceCutFracs`, `::skirtCutFracs` | — |
| 5 | `waistAnchoredDart` | shipped | `::skirtDartFracs`, `::bodiceApexFrac`, `::skirtApexFrac` | — |
| 6 | `topAnchoredDart` | shipped | `::topDartSplitFrac`, `::topDartApexFrac`, `::maxDartDeg` | **VAR**: "the operator exists and runs; it produces nothing on this garment" (Tur 8'den beri net-negatif omuz bandı hiç pens türetmiyor) |
| 7 | `necklineDraft` | shipped | `::neckWidthCoefCM`, `::frontNeckDropCoefCM`, `::backNeckDropMM` | **VAR**: "ONE top boundary sampled over the whole circle: the front neck cannot be 5mm narrower than the back" |
| 8 | `armholeNotch` | shipped | `::armhole`, `::shoulderNarrowMM` | **VAR**: "the armhole is not a 2D hole but a thin lens in phi in [0, 19.9 deg] — EU38 33.55cm against Buğra Locket-38 43.30cm, **22% short**. Shipped and defective, NOT absent." |
| 9 | `backOpening` | shipped | `::backOpeningMM` | **VAR**: "an UNSEWN SEAM, not a drafted zip" |
| 10 | `shoulderSeam` | **flagged** | `::shoulderSeam` (default **false**), `::shoulderCrestBandMM`, `::shoulderSeamForwardMM` | proof: "Tur 6: ON -> h10_gate_check 52/63 -> 24/63 FAIL" |
| 11 | `gatheredOverlayLayer` | **absent** | `None` | proof: surfacepattern.cpp/.hpp'de `gather\|pleat\|ruffle` geçen **SIFIR** satır |
| 12 | `sleeve` | **absent** | `None` | proof: `sleeve` geçen 6 satırın 6'sı da `sleeveless`, hepsi yorum. "Kol oyuğu var, kol yok." |
| 13 | `collarFamily` | **absent** | `None` | proof: `collar` geçen **SIFIR** satır |
| 14 | `skirtFamily` | **absent** | `None` | proof: tek etek operatörü `hemSweepCone`; `pleat` geçen **SIFIR** satır |
| 15 | `zipperPiece` | **absent** | `None` | proof: fermuarın kendi payı/parçası çizilmiyor |

⚠ 9 "shipped"in **4'ünün** kendi `defect` alanı var (6, 7, 8, 9). Yani sevk edilen 9
operatörün 4'ü kendi sözleşmesinde kusurlu ilan edilmiş. Bu ayrım sicilin **statü**
sütununda görünmüyor, ayrı alanda duruyor.

### 3.3 Topoloji değerleri hangi operatörü istiyor (dosyadan)

`d['topology']` dökümü. `absent` bir operatör isteyen her değer, sicilin kendi
yasasına göre **REDDEDİLİR**:

| eksen | değer | istediği operatör | sonuç |
|---|---|---|---|
| garment | sheathDress / top / skirt | shipped'lar | ✓ |
| skirtShape | straight, aLine | skirtSurface, hemSweepCone | ✓ |
| skirtShape | **gore, fullCircle, gathered, pleated** | `skirtFamily` (absent) | ✗ RED |
| shoulder | strapless | bodiceSurface | ✓ |
| shoulder | shoulderSeam | `shoulderSeam` (flagged) | ✗ RED (kapalı) |
| sleeve | none | armholeNotch | ✓ |
| sleeve | **setIn, cap** | `sleeve` (absent) | ✗ RED |
| sleeve | **puff** | `sleeve` + `gatheredOverlayLayer` (ikisi de absent) | ✗ RED |
| collar | none | — | ✓ |
| collar | **peterPan, stand, shirt** | `collarFamily` (absent) | ✗ RED |
| closure | none, backOpening | backOpening | ✓ |
| closure | **centreBackZip, buttonFront** | `zipperPiece` (absent) | ✗ RED |
| suppression | seamOnly, dart, topDart | princessCut + dart'lar | ✓ |
| suppression | **gather** | `gatheredOverlayLayer` (absent) | ✗ RED |
| suppression | **pleat** | `skirtFamily` (absent) | ✗ RED |

Sayım: 8 eksende **28 değer**, bunların **14'ü** absent/flagged bir operatöre bağlı = **%50**.

### 3.4 ANAYASA damarı — sicilde İSİM olarak bile bulunmayanlar

ANAYASA.md damar dili (satır çıpalı, `grep -n "" ANAYASA.md | sed -n '42,56p'`):
- `:42-44` detay primitifleri · `:45` yakalar · `:45-46` kollar · `:56` + korpus `:71,:76,:94` etekler

Sicilde kelime araması (komut: `grep -ic "<kelime>" contract/garment-spec-v2.json`):

| ANAYASA damar detayı | sicilde geçen kelime | hit | karşılık |
|---|---|---|---|
| fiyonk (bow/tie) | `bow` **0**, `fiyonk` **0**, `tie` 2 | — | **İSİM YOK** (`tie`'ın 2 hiti operatör adı değil) |
| büzgü/shirring/smocking | `gather` 7 (`gatheredOverlayLayer`), `shirr` **0**, `smock` **0** | var | İSMEN VAR, statü **absent** |
| minik sık düğme sırası | `button` 1 → yalnız `closure.buttonFront` topoloji değeri | — | **OPERATÖR ADI YOK** (zipperPiece'e bağlanmış, o da absent) |
| fırfır/volan | `ruffle` 1 (**sadece bir proof cümlesinin içinde**), `flounce` **0** | — | **İSİM YOK** |
| peplum | `peplum` **0** | — | **İSİM YOK** |
| prenses/panel dikişi | `princessCut` | var | **shipped** |
| lace-up | `laceUp` **0**, `corset` **0** | — | **İSİM YOK** |
| dantel/fisto | `dantel` **0**, `eyelet` **0**, `lace` 1 (alakasız) | — | **İSİM YOK** |
| yaka: derin V | `vNeck` **0** | — | **İSİM YOK** (`necklineDraft` genel, şekilsiz) |
| yaka: kayık | `boat` **0** | — | **İSİM YOK** |
| yaka: kare | `square` **0** | — | **İSİM YOK** |
| yaka: sweetheart | `sweetheart` **0** | — | **İSİM YOK** |
| yaka: bebe/statement devrik | `collarFamily` (peterPan/stand/shirt/notched) | var | İSMEN VAR, statü **absent** |
| yaka: halter | `halter` **0** | — | **İSİM YOK** |
| kol: kolsuz | `sleeve.none` / `armholeNotch` | var | **shipped** (kusurlu: %22 kısa oyuk) |
| kol: askılı (strap) | `strap` 1 → yalnız `shoulder.strapless` | — | **İSİM YOK** (strapless ≠ askılı) |
| kol: kısa puf/balon | `sleeve.puff` topoloji değeri | var | İSMEN VAR, iki operatörü de **absent** |
| kol: kap | `sleeve.cap` topoloji değeri | var | İSMEN VAR, operatörü **absent** |
| kol: uzun puf + büzgülü manşet | `cuff` **0** | — | manşet **İSİM YOK** |
| etek: kloş/A-line | `hemSweepCone` + `skirtShape.aLine` | var | **shipped** |
| etek: kutu-pili/pileli | `pleat` 5 → `skirtShape.pleated`, `suppression.pleat` | var | İSMEN VAR, operatörü **absent** |
| etek: gore/gode | `gore` 4 → `skirtShape.gore` | var | İSMEN VAR, operatörü **absent** |

**SİCİLDE İSİM OLARAK BİLE BULUNMAYANLAR (11 kalem):**
fiyonk · minik düğme sırası (operatör düzeyinde) · fırfır/volan · peplum · lace-up ·
dantel/fisto · derin V · kayık yaka · kare yaka · sweetheart · halter · (+ askılı ve
büzgülü manşet, yarım kalemler)

ANAYASA `:48-50` "NE YOK" satırındaki **görünür fermuar** ise sicilde `zipperPiece`
olarak İSMEN VAR — yani sicil, damarın YASAKLADIĞI bir şeyi adlandırıyor, ama
damarın İSTEDİĞİ 11 şeyi adlandırmıyor. (Gözlem, hüküm değil.)

---

## MADDE 4 — DAMAR YÜZDELERİ

### 4.1 YÖNTEM (F0-A §3.1'den okundu, aynen aktarılıyor — V11 tekrar koşabilsin diye)

Kaynak: `GECE/arsiv/v5-kosusu/F0-A.md:170-205`. **O dosyanın hiçbir SAYISI kullanılmadı.**

> **PAYDA (21 kalem).** ANAYASA.md'nin damar detay dili, satır numarasıyla çıpalanmış.
> `grep -n` ile alındı, dosya bütün olarak okunmadı.
> - ANAYASA.md:42-44 — detay primitifleri: B1 fiyonk · B2 büzgü/shirring/smocking ·
>   B3 minik sık düğme sırası · B4 fırfır/volan · B5 peplum · B6 prenses/panel dikişi ·
>   B7 lace-up · B8 dantel/fisto → 8 kalem ("fırfır/volan/peplum" tek satırdı, fırfır ile
>   peplum AYRI motor operatörleri gerektirdiği için ikiye bölündü — bölme gerekçesi budur,
>   keyfi değil)
> - ANAYASA.md:45 — yaka ailesi (6): B9 derin V · B10 kayık · B11 kare · B12 sweetheart ·
>   B13 bebe/statement devrik yaka · B14 halter
> - ANAYASA.md:45-46 — kol ailesi (4): B15 kolsuz/askılı · B16 kısa puf/balon ·
>   B17 kap kol · B18 uzun puf + büzgülü manşet
> - ANAYASA.md:56 + korpus satırları 71/76/94 — etek ailesi (3): B19 kloş/A-line ·
>   B20 kutu-pili/pileli · B21 gore/godevari
>
> PAYDA = 8 + 6 + 4 + 3 = **21**.
>
> **"ÜRETİLEBİLİR" KRİTERİ — garment hattı.** Üç şart, hepsi sağlanacak:
> - **G1** `engine/vocab.json` `fields` içinde detayı ADIYLA ifade eden bir eksen + değer var.
> - **G2** o alan `engine/wasm/bindings.cpp` `buildSpec()` içinde okunuyor (WASM sınırından
>   geçiyor, ölü kod değil).
> - **G3** ctest'te o değeri çizip geometrisini iddia eden ADLI bir kapı var ve o kapı
>   **bu gece YEŞİL**.
>
> Puan: **1.0** = G1+G2+G3 · **0.5** = G1+G2 var, G3 (adlı kapı) yok · **0.0** = G1 yok.
> PAY = puanların toplamı. YÜZDE = PAY / 21.
>
> ⚠ Bu kriter **İFADE EDİLEBİLİRLİĞİ** ölçer, GÜZELLİĞİ değil. "Çizim ANAYASA damarına
> benziyor mu" sorusu TEK KAPI'dır (Damla'nın gözü) ve bu kartta KOŞULMADI — bu yüzdeye
> o yargı DAHİL DEĞİL.

F0-A ayrıca (`:164-168`) şunu ilan ediyor ve bu gece **yeniden doğrulandı**:
```
grep -c surfacepattern engine/wasm/bindings.cpp   → 0
```
yani WASM sınırı `surfacepattern`'e hiç dokunmuyor; sevk edilen çizim
`garment.hpp` → `GarmentDrafter::draft()` hattından çıkıyor. Bu yüzden yüzde
**iki ayrı hat için ayrı ayrı** verilir.

### 4.2 PAYDANIN ÇAPALARI BU GECE DOĞRULANDI

`grep -n "" ANAYASA.md | sed -n '42,56p'` çıktısından:
- `:42-44` = "Detay dili (frekansla): fiyonk ~14 ..., büzgü/shirring/smocking ~16,
  minik sık düğme sıraları 9 (2'si asimetrik pat), fırfır/volan/peplum ~9,
  prenses/panel dikişi ~9, lace-up 2 (ikonik), dantel/fisto 2-3."
- `:45` = "Yakalar: derin V, kayık, kare/sweetheart, bebe/statement devrik, halter."
- `:45-46` = "Kollar: kolsuz/askılı ezici çoğunluk; kol varsa kısa puf/balon/kap;
  TÜM sette tek uzun kol var, o da puf + büzgülü manşet."
- `:56` = "etek ucu hareketli (kloş/A-line/volan/peplum)" ·
  `:71` = "kutu-pili etek ucu" · `:94` = "godevari kloş"

PAYDA = **21**, F0-A'nın çapalarıyla birebir. ✓

### 4.3 G2 BU GECE ÖLÇÜLDÜ

```
python3 -c "import json,re; src=open('engine/wasm/bindings.cpp').read();
i=src.index('GarmentSpec buildSpec'); j=src.index('return spec;',i); body=src[i:j];
v=json.load(open('engine/vocab.json'))['fields'];
print(len(v), [k for k in v if '\"%s\"'%k not in body])"
```
→ `37 []` — **37 eksenin 37'si** `buildSpec()` içinde okunuyor, okunmayan **0**.
Ayrıca `"skirtLengthMM" in body` → `True`.
**G2 bütün kalemler için sağlanıyor.**

### 4.4 GARMENT HATTI TABLOSU — bu gece ölçülen

G3 sütunundaki her kapı adı `GECE/log/V0-0A.ctest.txt` içinde `Passed` olarak
doğrulandı (kırmızı 6'nın hiçbiri bu listede yok).

| # | damar detayı | G1 eksen/değer (`engine/vocab.json`) | G3 ADLI kapı (bu gece YEŞİL) | puan |
|---|---|---|---|---|
| B1 | fiyonk | `tieClosure` {backWaistBow, frontNeckBow, frontWaistBow, tieBack} | `tie_check` · `fronttie_check` | 1.0 |
| B2 | büzgü/shirring/smocking | `gatherType` {drawstring, shirred, smocked} × `gatherZone` {neckline,bust,waist,sleeve} | `gather_check` | 1.0 |
| B3 | minik sık düğme sırası | `buttonRow` {functional, decorative} + `placketStyle` {standard, asymmetric} | `buttonrow_check` · `placket_check` · `placket_asym_check` | 1.0 |
| B4 | fırfır/volan | `hemFlounce`{gathered} · `backDetail`{ruffle,flounce} · `ruffleHem`/`ruffleTiers` | `hemflounce_check` · `backdetail_check` · `ruffle_check` · `tiered_ruffle_check` | 1.0 |
| B5 | peplum | `peplum` {full, half, pointed} | `peplum_check` | 1.0 |
| B6 | prenses/panel dikişi | `shaping` {princess} | `compose_check` · `cutline_check` | 1.0 |
| B7 | lace-up | `laceUpBack` {corset} | `laceupback_check` | 1.0 |
| B8 | dantel/fisto | **YOK** — ölçüldü: vocab'da `lace\|eyelet\|dantel\|fisto\|broderie` araması yalnız `laceUpBack{none,corset}` döndürüyor, kumaş/trim ekseni yok | — | **0.0** |
| B9 | derin V | `neckline` {vNeck} | `neckline_ext_check` | 1.0 |
| B10 | kayık | `neckline` {boat} | `neckline_ext_check` | 1.0 |
| B11 | kare | `neckline` {square} | `neckline_ext_check` | 1.0 |
| B12 | sweetheart | `neckline` {sweetheart} | `sweetheart_check` | 1.0 |
| B13 | bebe/statement devrik yaka | `collarType` {peterPan, flat, crescent, stand, mock, shirt} + `collarEdge` | `collar_check` · `collar_lies_flat_check` · `collar_bridge_check` | 1.0 |
| B14 | halter | `neckline` {halter} | `halter_check` | 1.0 |
| B15 | kolsuz/askılı | `sleeveStyle`{none} + `ruffledStraps`{ruffled,wide,spaghetti} | `strap_check` | 1.0 |
| B16 | kısa puf/balon | `sleeveStyle`{balloon} + `sleeveCap`{puffed,gathered} + `sleeveLength`{short} | `sleeve_check` | 1.0 |
| B17 | kap kol | `sleeveCap` {cap} | `cap_sleeve_check` | 1.0 |
| B18 | uzun puf + büzgülü manşet | `sleeveLength`{long} + `sleeveCap`{puffed} + `cuffStyle`{button,ribbed} | `cuff_check` | 1.0 |
| B19 | kloş/A-line | `skirtStyle` {aLine, halfCircle} | `hem_check` (`hem_check.cpp:203` `SkirtStyle::ALine`) · `sideseam_adversarial_check` (`:63,:84`) | 1.0 |
| B20 | kutu-pili/pileli | `skirtStyle`{pleated} + `boxPleat`{centerInverted} + `hemShape`{boxPleatHem} | `boxpleat_check` | 1.0 |
| B21 | gore/godevari | `skirtStyle` {gore} | `gore_check` | 1.0 |

**PAY = 20.0 · PAYDA = 21 · GARMENT HATTI = 20/21 = %95.24**

Sıfır çeken tek kalem: **B8 dantel/fisto** (G1 yok — motorun sözlüğünde
dantel/fisto/broderie bir kumaş ya da trim ekseni olarak hiç geçmiyor).

### 4.5 SEVK EDİLEN YÜZEY HATTI (surfacepattern) — sicilden türetildi

⚠ **Bu sütunun yöntemi F0-A §3.1'de TANIMLI DEĞİL.** §3.1 yalnız garment hattının
G1/G2/G3 kriterini yazıyor. Aşağıdaki sütun, MADDE 3'te dosyadan OKUNAN operatör
statülerinden türetildi ve yöntemi burada ADIM ADIM yazıldı ki tekrarlanabilsin:

> **YÜZEY HATTI KRİTERİ (S1–S2), bu kartta tanımlandı:**
> - **S1** `contract/garment-spec-v2.json` `topology` içinde detayı ifade eden bir
>   eksen değeri var.
> - **S2** o değerin `requires` listesindeki operatörlerin **HEPSİ** `status == "shipped"`.
> Puan: 1.0 = S1+S2 · 0.5 = S1 var, requires'ta flagged var ama absent yok ·
> 0.0 = S1 yok VEYA requires'ta bir `absent` var.

| # | damar detayı | topoloji değeri | requires statüleri | puan |
|---|---|---|---|---|
| B1 fiyonk | — | yok | — | 0.0 |
| B2 büzgü | `suppression.gather` | gatheredOverlayLayer **absent** | 0.0 |
| B3 mini-düğme | `closure.buttonFront` | zipperPiece **absent** | 0.0 |
| B4 fırfır | — | yok | — | 0.0 |
| B5 peplum | — | yok | — | 0.0 |
| B6 prenses | `suppression.seamOnly` | princessCut **shipped** | **1.0** |
| B7 lace-up | — | yok | — | 0.0 |
| B8 dantel | — | yok | — | 0.0 |
| B9-B12 yaka şekilleri (4 kalem) | — | `necklineDraft` şekilsiz, vNeck/boat/square/sweetheart topolojide yok | 0.0 ×4 |
| B13 devrik yaka | `collar.peterPan/stand/shirt` | collarFamily **absent** | 0.0 |
| B14 halter | — | yok | — | 0.0 |
| B15 kolsuz/askılı | `sleeve.none` | armholeNotch **shipped** (defect'li) | **1.0** |
| B16 kısa puf/balon | `sleeve.puff` | sleeve + gatheredOverlayLayer **absent** | 0.0 |
| B17 kap kol | `sleeve.cap` | sleeve **absent** | 0.0 |
| B18 uzun puf + manşet | `sleeve.puff`, manşet yok | absent | 0.0 |
| B19 kloş/A-line | `skirtShape.aLine` | skirtSurface + hemSweepCone **shipped** | **1.0** |
| B20 pileli | `skirtShape.pleated` | skirtFamily **absent** | 0.0 |
| B21 gore | `skirtShape.gore` | skirtFamily **absent** | 0.0 |

**PAY = 3.0 · PAYDA = 21 · YÜZEY HATTI = 3/21 = %14.29**

### 4.6 İKİ SAYI YAN YANA

| hat | ne çiziyor | yüzde | payda |
|---|---|---|---|
| **garment** (`bindings.cpp` → `garment.hpp`, alıcının web'de aldığı) | 2B-formül-çizim | **%95.24** (20/21) | 21 |
| **surface** (`surfacepattern.cpp`, "tek yüzey" hattı) | 3B yüzey → düzleştirme | **%14.29** (3/21) | 21 |

Bu iki hat AYNI HAT DEĞİL: `grep -c surfacepattern engine/wasm/bindings.cpp` → **0**.
Sicilin `shipped` tanımı surfacepattern'i "bugün alıcının aldığı yol" sayıyor, ama
WASM sınırı ona hiç dokunmuyor. **Bu çelişki bu kartta çözülmedi, ham veri olarak
bırakıldı** (sınıflama hakemin işi).

---

## ÖLÇÜLEMEYEN / YAPILAMAYAN

- `h10_gate_check`'in **içeriği** koşturulmadı: DISABLED, ve kart ONARIM/DEĞİŞİKLİK
  yasakladığı için `set_tests_properties` satırına dokunulmadı.
- `contract_check` çıktısının içindeki **"bugun 49"** ilanı ile testin bugün saydığı
  **41** arasındaki fark uzlaştırılmadı (dosya değiştirmek gerekirdi).
- `recipe_dress_check`'in "grainline DIFFERS" ve "geometry DIFFERS" farklarının
  **mm cinsinden büyüklüğü** ölçülemedi: test yalnız `DIFFERS <= 1e-6 mm` diyor,
  sapmanın kendisini basmıyor.
- 4.5'teki yüzey hattı yüzdesi **sözleşme dosyasından** türetildi, `surfacepattern.cpp`
  kaynak koduna bakılarak DEĞİL (kart girdi listesinde o dosya yok).
- TEK KAPI (Damla'nın gözü) bu kartta koşulmadı; 4.4/4.5 yüzdeleri güzelliği ölçmez.
