# V9-B2 — TABAN SIFIRA KESİLDİ + MUTASYON YENİDEN KANITLANDI (tutanak)

Koşu: 2026-08-25 · kesim commit'i `4a5f0de64aeb25a67e8cfe63f46175aae95cad5d`
kapı `engine/tests/docs_truth_check.mjs` · taban `engine/tests/docs-truth-baseline.json`
ctest adı `docs_truth_check`.

**`docs/` ve `README.md` KALICI OLARAK DEĞİŞTİRİLMEDİ.** Kanıt:
`GECE/log/V9-B2.mutasyon.txt` §4 → `git status --porcelain -- docs/ README.md` **0 satır**,
`git diff --stat -- docs/ README.md` BOŞ.

---

## 1. TABAN 0'A KESİLDİ (madde 1)

| ölçü | ESKİ taban (V9-B kesimi) | YENİ taban (V9-B2) |
|---|---|---|
| D1 açık borç | **9** (9 kalem satır metniyle kayıtlı) | **0** — `bilinenAcik: []` |
| D2 açık borç | **9** (7 tekil parmak izi) | **0** — `bilinenAcik: []` |
| D3 taban | **52** / 133 sayısal satır | **0** / 139 sayısal satır |

Basan komut:
`node engine/tests/docs_truth_check.mjs --note="…" --baseline`
Doğrulayan komut: `node engine/tests/docs_truth_check.mjs --no-baseline`
→ `HÜKÜM: YEŞİL — HARD-0 kipi · D1 0 · D2 0 · D3 0`, exit **0**.

**Gerekçe (RULES §4.6 — taban dosyasının `_note` alanına da yazıldı, commit
mesajına da):** kâtip kartları (V9-C/D/E) `docs/` + `README.md` onarımını bitirdi;
kayıtlı borcun karşılığı ağaçta kalmadı. Kapanmış borcu taşımak kapıyı GEVŞETİR —
ölü bir parmak izi ileride gerçek bir ihlali sessizce yutabilir.

**Taban yalnız DÜŞEBİLİR kuralı korundu.** Bu kesim bir DÜŞÜŞTÜR (9/9/52 → 0/0/0),
yükseltme değil. Kural kodda değişmedi: `d3.missing.length > d3Base` hâlâ kırmızı,
`bilinenAcik` listesinde OLMAYAN her D1/D2 ihlali hâlâ `FAIL YENI`.

**Yeni:** kapıya `--note="…"` seçeneği eklendi. `--baseline` her kesimde `_note`
alanını yazar; gerekçe verilmezse dosyaya *"GEREKÇE YAZILMADI — RULES §4.6 ihlali"*
basılır, yani gerekçesiz kesim taban dosyasının içinde GÖRÜNÜR kalır.
Kesim tarihi (`olcumTarihi`) ve kesildiği commit (`olcumCommit`) zaten yazılıyordu,
duruyor.

---

## 2. MUTASYON — TABAN 0'DAYKEN YENİDEN KANITLANDI (madde 2)

Tam çıktı (ÖNCE/SONRA, üç mutasyon): `GECE/log/V9-B2.mutasyon.txt`
Hepsi **taban kipinde** koşuldu (`--no-baseline` DEĞİL) — yani kapının kayıtlı
borç muhasebesi devredeyken. Mutasyon hedefi `docs/ARCHITECTURE.md` (dosya sonu).

| # | mutasyon | kapının hükmü | exit |
|---|---|---|---|
| §0 | yok (temiz ağaç) | `YEŞİL — yeni duran-iddia 0, yeni ölü hedef 0, D3 0 <= taban 0` | **0** |
| §1 | **D1** — `The matrix is ALL PASS and the draft is byte-identical.` | `FAIL YENI docs/ARCHITECTURE.md:269 [ALL PASS]` + `[byte-identical]` → `FAIL — D1 yeni 2` | **1** |
| §2 | **D2** — `` `docs/BOYLE-BIR-DOSYA-ASLA-OLMADI.md` `` + `` `engine/src/hayalet-modul.cpp` `` | `FAIL YENI … -> docs/BOYLE-BIR-DOSYA-ASLA-OLMADI.md [TICK]` + `… -> engine/src/hayalet-modul.cpp [TICK]` → `FAIL — D2 yeni 2` | **1** |
| §3 | **D3** — `The worst pair measured 3.75 mm across eight sizes.` | `FAIL docs/ARCHITECTURE.md:269 …` → `FAIL — D3 taban aşıldı (0 -> 1)` | **1** |

Üçünün de ardından `git checkout -- docs/ARCHITECTURE.md` → kapı **exit 0**, YEŞİL.
İhlal her üç halde de dosya:satır + kalıp/hedef adıyla basıldı.

★ Fark: V9-B'de D3 mutasyonu ANLAMLI DEĞİLDİ (taban 52 idi, +1 cümle tabanın
altında kalırdı). Taban 0'a inince D3 kapısı ilk kez GERÇEKTEN sert oldu ve
tek cümlelik mutasyonla kırıldı — bu koşunun kanıtladığı asıl şey budur.

---

## 3. ctest (madde 3)

`ctest --test-dir engine/build -R docs_truth_check --output-on-failure`
→ `GECE/log/V9-B2.ctest.txt`

```
    Start 116: docs_truth_check
1/1 Test #116: docs_truth_check .................   Passed    4.68 sec
100% tests passed, 0 tests failed out of 1
```

---

## 4. BİLİNEN ZAAF — ONARILDI, ÖLÇÜLDÜ

V9-B tutanağı §3 md.2: `docs/H1.0-KAPI.md`'nin ölü
`reports/2026-07-29-endustri-arastirmasi.md` referansı, AYNI SATIRDAKİ
*"Armhole ÇEVRESİ Aldrich'te yok"* yüzünden D2'den düşüyordu — o "yok" başka bir
şey içindi. (Satır bu koşuda **162 değil 189**; kâtip onarımı dosyayı kaydırmış.)

**ÖNCE ölçüldü** (geçici enstrümanlı kopya, koşu sonunda silindi): bütün ağaçta
yalın-`yok` düşürücüsünün TOPLAM 2 düşüşü var —
`docs/SATIS-SARTNAMESI.md:348` (yok hedefin SAĞINDA, meşru) ve
`docs/H1.0-KAPI.md:189` (yok hedefin SOLUNDA, yanlış düşüş).

**İki değişiklik yapıldı (kapı GEVŞETİLMEDİ):**
1. `DECLARED_MISSING_LOOSE` artık satırın tamamında değil, **hedefin SAĞINDAKİ**
   metinde aranıyor. Gerekçe: Türkçe yokluk ilanı hedefi ÖNCE söyler, sonra
   "yok" der. Bu SIKILAŞTIRMADIR.
2. Yerine dar bir sınıf kondu: `GIT_HISTORY_CITE` — bir yolun **hemen sağında**
   `(git \`0e67777\`)` biçiminde commit hash'i varsa doküman o yolun DİSKTE değil
   GEÇMİŞTE olduğunu AÇIKÇA ilan etmiştir; bu, D2'nin zaten tanıdığı "dürüst
   yokluk kaydı" sınıfının kelime yerine hash'le yazılmış hâlidir.

**Ölçüm (önce → sonra, `--no-baseline`):**
- yalnız md.1 uygulandığında: düşen yanlış-pozitif **28 → 27**, GERÇEK ölü **0 → 1**
  (`docs/H1.0-KAPI.md:189`), exit **1**. Yani sıkılaştırma gerçekten ateşliyor.
- md.1 + md.2 birlikte: düşen **28**, GERÇEK ölü **0**, exit **0**.
  Yoklanan hedef 568, VAR 540 — ikisi de değişmedi.

**Net:** yanlış "yok" kanalı KAPANDI; H1.0:189 artık kaza eseri değil, adı konmuş
ve dar bir sınıfla düşüyor. `GIT_HISTORY_CITE`'ın bugünkü ağaçtaki tek adayı bu
satırdır (ölçüldü).

**Ama DÜRÜSTÇE: bu bir kısmi onarımdır.** Hedef hâlâ diskte yok ve kapı hâlâ
kırmızı düşmüyor. Fark, düşme SEBEBİNİN artık doğru olması. Bunun bedeli: bir
doküman yazarı ölü bir yolun yanına herhangi bir 7-40 hane hex yazarak D2'den
kaçabilir. Hash'in gerçekten o dosyayı içerip içermediği **DOĞRULANMADI** ve kapı
bunu doğrulamıyor (`git cat-file` çağrısı eklenmedi — kapı offline çalışmak
zorunda ve kayıp obje kapıyı kendi arızasıyla kırardı).

---

## 5. YAPILAMAYAN / YARGILANMAYAN

- `docs/H1.0-KAPI.md:189`'un ölü hedefi ONARILMADI (docs içeriğine dokunmak bu
  kartta YASAK). Kalıcı çözüm ya dosyayı geri getirmek ya cümleyi yeniden yazmak —
  ikisi de kâtip kartının işi.
- `GIT_HISTORY_CITE`'ın hash'i doğrulanmıyor (yukarıda).
- V9-B §3'ün diğer zaafları (basename çözücü, `engine/`-göreli çözüm, D2 parmak
  izinin satır numarası taşımaması, kapsamın `docs/`+`README.md` ile sınırlı
  olması, Cog sınıfı pozitif tarafın YOKLUĞU) **AYNEN DURUYOR** — bu kart onlara
  dokunmadı.
- D3'ün 0 olması "docs'taki her sayı doğru" DEMEK DEĞİLDİR. D3 yalnız satırın
  yanında bir alet ADI arar; sayının o aletten çıktığını doğrulamaz.
- `GECE/arsiv/`, `KOSU.md`, diğer kartlar AÇILMADI.

---

## 6. KART DIŞI FARK EDİLEN

1. **Kapının kendi sayıları V9-B'den beri kaydı:** taranan prose dosya 18 (aynı),
   ama yoklanan D2 hedefi **502 → 568**, VAR **477 → 540**, D1 ham hit **16 → 14**,
   D3 sayısal satır **133 → 139**. Yani kâtip onarımı borcu kapatırken doküman
   HACMİNİ de büyüttü (daha çok yol referansı, daha çok sayısal satır) — ve hepsi
   sağlayıcılı. Bu, D3'ün "cümle sayısına eşik koyma" uyarısının (V9-A §1) canlı
   kanıtı: 139 sayısal satır var, sağlayıcısız 0.
2. **V9-B tutanağının verdiği satır numaraları BAYAT.** `H1.0-KAPI.md:162` bugün
   başka bir metin; ölü referans `:189`'da. Tutanaklara satır no yazmak kırılgan;
   D2'nin parmak izinin satır no taşımaması (V9-B zaaf md.6) bu yüzden doğru karar.
3. **Taban dosyası artık ihlal METNİ taşımıyor** (listeler boş). V9-B'nin kart
   dışı md.4'te uyardığı "onarım parmak izini bozar, taban elle yeniden kesilmeli"
   sorunu bu kesimle kapandı; bir daha borç yazılırsa aynı kırılganlık geri gelir.
