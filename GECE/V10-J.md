# V10-J — TÜRKÇE YALAN + BOZUK CÜMLE + KIRMIZI ÜRETEÇ

Etiket SIRALI 1/2. Kart: `GECE/KART/V10-J-tr-supurge.md`.
Her sayı, onu basan komutla birlikte yazılıdır. "baktım" yok.

---

## 1) KAPI — iki kalıp EKLENDİ (silme/gevşetme YOK)

`engine/tests/landing_truth_check.mjs`, `BANNED` listesinin SONUNA:

| kalıp adı | regex | neden |
|---|---|---|
| `ölçülerine göre (TR)` | `/ölçüler(?:in|iniz)e göre/gi` | mevcut `ölçüleriniz[a-zçğıöşü]* göre` 2. çoğul iyelik ister, 3. tekil `ölçülerine göre`yi KAÇIRIYOR |
| `kişiye özel (TR)` | `/kişiye özel/gi` | bugün 0 hit; ileriye dönük mandal (hakem adıyla istedi) |

Mevcut hiçbir kalıp silinmedi/gevşetilmedi, muafiyet gömülmedi,
`engine/tests/landing-truth-baseline.json` OKUNDU, YAZILMADI (dosya `git status`'ta yok).

Eski kalıbın kaçırdığı ÖLÇÜLDÜ:

```
$ node -e 'const s="Bunu ölçülerine göre çiz";
  console.log("old:", /ölçüleriniz[a-zçğıöşü]* göre/gi.test(s));
  console.log("new:", /ölçüler(?:in|iniz)e göre/gi.test(s));'
old: false
new: true
```

---

## 2) SÜPÜRGE — 16 koleksiyon sayfası

**ÖNCE → SONRA**

```
$ grep -ro "ölçülerine göre" web/ | wc -l
      16          (önce)          →           0   (sonra)

$ grep -roc "ölçülerine göre" web/ | grep -vc ":0"
      16 dosya    (önce)          →           0   (sonra)
```

Değişen tek satır, 16 dosyanın hepsinde aynı:
`data-tr="Bunu ölçülerine göre çiz, ücretsiz."` → `data-tr="Bunu sabit bir bedende çiz, ücretsiz."`
Bu, `engine/tools/gen-collection-pattern.mjs:371`'in ZATEN taşıdığı metindir; 16 sayfa artık üreteçle birebir:

```
$ grep -rc 'data-tr="Bunu sabit bir bedende çiz, ücretsiz\."' web/collections/*.html | grep -c ":1"
16
```

### ÜRETEÇ KOŞTURULAMADI — SEBEP (kart §2'nin izin verdiği hâl)

```
$ V=85 node engine/tools/gen-collection-pattern.mjs --out=/tmp/colout
Error: ENOENT: no such file or directory, open
  '/Users/damummyphus/damla_projects_2026/stitchu/web/patterns/vintage6070/meta.json'
  at gen-collection-pattern.mjs:49:25

$ ls web/patterns/vintage6070/
ls: web/patterns/vintage6070/: No such file or directory
```

Veri dizini `af49514`'te silinmiş — bu, `generated_ratchet_check.sh`'in başlığında
2026-08-17'de zaten beyan edilmiş dört ölü üreteçten biri. Bu yüzden 16 sayfa
ELLE düzeltildi ve manifesto `--accept` ile AYNI commit'te güncellendi.

### RATCHET — kırmızıdan yeşile

```
$ bash engine/tests/generated_ratchet_check.sh ; echo EXIT=$?
  bytes moved: 16 · declared-but-missing: 0 · undeclared: 0
  HUKUM: FAIL (16)
  EXIT=1                                          (ÖNCE)

$ bash engine/tests/generated_ratchet_check.sh --accept
manifest written: contract/generated-paths.sha256 (57 paths)

$ bash engine/tests/generated_ratchet_check.sh ; echo EXIT=$?
  bytes moved: 0 · declared-but-missing: 0 · undeclared: 0
  HUKUM: YESIL
  EXIT=0                                          (SONRA)
```

Manifestoda oynayan satır sayısı 17 = 16 koleksiyon + `web/styles/open-back.html` (§3):

```
$ git diff --stat contract/generated-paths.sha256
 contract/generated-paths.sha256 | 34 +++++++++++++++++-----------------
 1 file changed, 17 insertions(+), 17 deletions(-)
```

---

## 3) BOZUK İNGİLİZCE — 12 hit, hepsi onarıldı

```
$ grep -ro "unchanged under golden_check to" web/ | wc -l
      12          (önce)          →           0   (sonra)
```

Dosya:satır dökümü (satır numaraları onarım SONRASI dosyalardan; sayım
`grep -roc` çıktısıyla birebir):

| # | dosya:satır | konum | eski → yeni |
|---|---|---|---|
| 1-2 | `web/patches.html:352` | `data-en` + görünür metin | `…is now unchanged under golden_check to the live web build` → `…now matches the live web build` |
| 3-4 | `web/patches.html:617` | `data-en` + görünür metin | `The header markup is still unchanged under golden_check to the previous patch` → `The header markup still matches the previous patch` |
| 5-6 | `web/patches/2-10.html:86` | `data-en` + görünür metin | (3-4 ile aynı cümle) |
| 7-8 | `web/patches/3-20.html:84` | `data-en` + görünür metin | (1-2 ile aynı cümle) |
| 9 | `web/styles/open-back.html:26` | JSON-LD `acceptedAnswer.text` | `facing stitch line unchanged under golden_check to the opening` → `facing stitch line matches the opening` |
| 10 | `web/styles/open-back.html:98` | `p.fact` | `The marked stitch line on the facing is unchanged under golden_check to the opening drawn on the back: truing 0.00 mm, tested.` → `…matches the opening drawn on the back: truing 0.00 mm in tests/backopen_check.` |
| 11 | `web/styles/open-back.html:116` | `p.faq-a` | 9 ile aynı |
| 12 | `web/styles/open-back.html:126` | tablo `td.v` | 9 ile aynı |

**YENİ SAĞLAYICI JETONU EKLENMEDİ.** `golden_check` her 12 yerde SİLİNDİ (mekanik
değiştirmenin soktuğu jetondu), hiçbir yere eklenmedi. Tek istisna ve GEREKÇESİ
aşağıda (§3a).

`open-back.html` ÜRETİLMİŞ bir sayfadır (`web/styles/*.html`, ratchet kapsamında).
Kart §3 usulü aynen uygulandı: kaynak `engine/tools/gen-style-pages.mjs` (satır 445 ve
456) düzeltildi, sayfa ÜRETEÇTEN yeniden basıldı, manifesto `--accept` ile güncellendi.
Üretecin bugün canlı 24 sayfayı BAYT BAYT ürettiği önce ölçüldü (fikstür dizinine
üretip `cmp` ile; 24/24 fark yok), böylece yeniden üretim başka sayfaya bulaşmadı:

```
$ node engine/tools/gen-style-pages.mjs        # düzeltmeden ÖNCE, --out=/tmp/stylout
$ for f in /tmp/stylout/*.html; do cmp -s "$f" "web/styles/$(basename $f)" || echo DIFF; done
(çıktı boş — 24 dosya, 0 fark)

$ node engine/tools/gen-style-pages.mjs        # düzeltmeden SONRA, canlı dizine
$ git status --porcelain web/styles/
 M web/styles/open-back.html                   (tek dosya)
```

`web/patches.html` ve `web/patches/*.html` için üreteç YOK
(`grep -rln "web/patches" engine/tools/*.mjs` yalnız `gen-sitemap.mjs`'i veriyor,
o da sitemap üreticisi) ve bu yollar ratchet kapsamında değil → elle düzeltildi.

Her onarılan cümlenin `data-tr`'si okundu, İngilizcesiyle ÇELİŞMİYOR:
`…artık canlı web derlemesiyle aynı` ↔ `now matches the live web build`;
`Üst menü işaretlemesi hâlâ bir önceki yamayla aynı` ↔ `still matches the previous patch`.
`open-back.html`'in dört hiti `data-tr` taşımıyor (düz metin/JSON-LD).

### 3a) BULUNAN YAN KUSUR: jeton silinince L1 KIRMIZIYA DÜŞTÜ

`golden_check`, `open-back.html:98`'deki cümlenin TEK sağlayıcı jetonuydu.
Silince kapı ölçtü:

```
$ node engine/tests/landing_truth_check.mjs --dir=<fikstür>/web
  SAĞLAYICISIZ 938 · taban 937
  HÜKÜM: FAIL — L1 938/937 AŞTI
```

İzole edildi: aynı fikstürde yalnız `open-back.html` HEAD hâline döndürülünce
937/937 YEŞİL → +1'in kaynağı bu tek blok. Diğer 8 hit (patches) L1'e hiç dokunmadı.

Çözüm jeton aklamak DEĞİL, GERÇEK sağlayıcıyı adıyla yazmak oldu:
`truing 0.00 mm, tested.` → `truing 0.00 mm in tests/backopen_check.`
`backopen_check` uydurma değil, kayıtlı bir testtir
(`engine/CMakeLists.txt:246 add_test(NAME backopen_check …)`), ve zaten AYNI sayfanın
`tests` satırında adı geçiyor. RULES §6 ("sayıyı basan aleti adıyla söyle") bunu
zaten istiyordu; eski `tested` kelimesi sağlayıcısızdı.

---

## 4) MUTASYON — yalnız iki YENİ kalıp, `web/` DIŞINDA

Çıktı: `GECE/log/V10-J.mutasyon.txt`

```
MUT  [ölçülerine göre (TR)]  exit=1  sayilan=1  HÜKÜM: FAIL — L1 937/937 · L2 1/0 AŞTI · …
GERI [ölçülerine göre (TR)]  exit=0             HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · …
MUT  [kişiye özel (TR)]      exit=1  sayilan=1  HÜKÜM: FAIL — L1 937/937 · L2 1/0 AŞTI · …
GERI [kişiye özel (TR)]      exit=0             HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · …
```

Fikstür: `git worktree add --detach /tmp/v10j-fix HEAD` + `rsync -a --delete web/ /tmp/v10j-fix/web/`,
kapı `--dir=/tmp/v10j-fix/web` ile koşturuldu. `web/` altına TEK BAYT mutasyon yazılmadı.
Fikstür `git worktree remove --force` ile kaldırıldı; iş bitiminde
`git status --porcelain` yalnız bu kartın ÇIKTI dosyalarını gösteriyor.

---

## KAPANIŞ DOĞRULAMASI (dördü de geçti)

```
$ node engine/tests/landing_truth_check.mjs          → EXIT 0
  HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4
  (tam çıktı: GECE/log/V10-J.kapi.txt · --baseline KOŞTURULMADI)

$ bash engine/tests/generated_ratchet_check.sh       → EXIT 0
  57 declared paths, 57 on disk · bytes moved: 0 · HUKUM: YESIL

$ node engine/tools/site-health.mjs                  → EXIT 0
  OK  site-health: no dead links, sitemap matches the site, one version.

$ grep -rc "ölçülerine göre\|kişiye özel\|unchanged under golden_check to" web/ | grep -vc ":0"
  0                                                  (sıfır olmayan dosya YOK)
```

---

## YAPILAMAYAN / AÇIK KALDI

1. **`gen-collection-pattern.mjs` koşturulamadı** — `web/patterns/vintage6070/meta.json`
   `af49514`'te silinmiş (ENOENT, yukarıda çıktısı var). 16 sayfa elle düzeltildi.
   Bu, ratchet'in kendi başlığında beyan edilmiş dört ölü üreteçten biridir; K16 o
   veriyi geri getirene kadar bu sayfalar üreteçten DOĞRULANAMAZ.
2. **`?v` elle değişmedi, DEPLOY YOK** — kart yasağı. `web/` diskte değişti, canlıya
   çıkmadı. Site canlı olarak hâlâ eski baytları servis ediyor (DOĞRULANMADI: canlı
   adres bu kartta hiç çekilmedi).
3. **`--baseline` koşturulmadı** — kart §KAPANIŞ 1 açıkça yasakladı, V10-L'in işi.
   Taban `L1 937` olarak DURUYOR; §3a'daki düzeltme onu 937'de tuttu, düşürmedi.

---

## KART DIŞI FARK EDİLEN (DOKUNULMADI)

1. **`gen-style-pages.mjs`'te `unchanged under golden_check` 3 hit DAHA var** —
   satır 391, 412, 434: `…existing outlines unchanged under golden_check`,
   `…outlines unchanged under golden_check`, `…unchanged under golden_check base`.
   Bunlar "…to" taşımadığı için kartın 12 hitine girmiyor ve gramer olarak bozuk
   değil, ama **aynı mekanik jeton değiştirmesinin ürünü**: `unchanged under
   golden_check base` İngilizce olarak yine tuhaf. Kart bunları saymadı, DOKUNMADIM.
   Basan komut: `grep -c "unchanged under golden_check" engine/tools/gen-style-pages.mjs` → 5.
2. **L5'te 4 kayıtlı borç ayakta**: `web/js/contract.gen.js` 28/29/104/113 satırlarında
   `EU50` ve `EU52` geçiyor; motorun kümesi EU34-EU48. Taban 4, kapıyı kırmıyor ama
   sitede motorda OLMAYAN iki beden adı yazılı. Bu kartın konusu değildi.
3. **L1 borcu 937 blok** — en yoğunu `patches.html` 88, `collection-60s70s.html` 48,
   `js/guide-tr.js` 42. Yani sitede 937 ayrı sayı+birim iddiası hiçbir alet adı
   taşımıyor. Kart yalnız 12 cümleyi istedi.
4. **`generated_ratchet_check.sh` bir yeniden-üretilebilirlik kanıtı DEĞİL** — kendi
   başlığında yazıyor: üreteçleri koşturmuyor, baytların üretilebilir olduğunu iddia
   etmiyor. Bugün `--accept` ile yeşile dönen 17 satır, "bu baytlar üreteçten çıkar"
   demek DEĞİLDİR; 16'sı elle düzeltilmiş sayfadır (§2), 1'i üreteçten çıkmıştır (§3).
5. **`git status`'ta takipsiz duran kalemler** (bu kart dokunmadı):
   `GECE/KART/V10-L-taban-muhasebe.md`, `patterns_real/BUGRA-DEFTER.md`,
   `patterns_real/geometry/`, `patterns_real/tools/bugra-geometry-2026-07-23.json`.
   `patterns_real/` CLAUDE.md'de "ASLA push edilmez" yazan telifli dizindir ve
   takipsiz duruyor — commit'e ALINMADI.
