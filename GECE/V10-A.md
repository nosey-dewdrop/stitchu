# V10-A — WEB ENVANTERİ · her iddia BUGÜNKÜ koda karşı ölçüldü

Koşu: 2026-08-25 · ağaç `main` @ `9b306f9` · **`web/` altına tek bayt yazılmadı**
(kanıt: `git status --porcelain web/` → boş, aşağıda §10).

Kapsam kartla sınırlı: `web/**`. `docs/` ve `README.md` bu kartın kapsamı DEĞİL
(V0-0C ikisini de sayıyordu; bu tur saymadı — §10'da "ÖLÇÜLMEDİ").

**V0-0C (24 Ağu) DEVRALINMADI.** Her kalem bugün yeniden koşuldu. V0-0C'de bayat
çıkan her satır aşağıda **BAYAT** diye ADIYLA yazılı, düzeltilmiş sayısıyla.

---

## 1. MTM ↔ SABİT BEDEN YALANI

### 1.1 Sitenin söylediği (bugün sayıldı)

Basan komut (her kalıp için):
`grep -ril "<kalıp>" web/ | wc -l` (dosya) · `grep -roF "<kalıp>" web/ | wc -l` (hit)

| kalıp | dosya | hit | V0-0C (24 Ağu) | fark |
|---|---|---|---|---|
| `your own measurements` | **44** | **104** | 44 / 104 | aynı |
| `to your measurements` | **22** | **77** | sayılmamıştı | YENİ ölçüm |
| `your exact measurements` | **16** | **48** | 16 / 48 | aynı |
| `no fixed sizes` | **23** | **46** | 23 / 46 | aynı |
| `made-to-measure` | **2** | **4** | sayılmamıştı | YENİ |
| `made to measure` | **1** | **2** | sayılmamıştı | YENİ |
| `measurement-fitted` | **2** | **3** | sayılmamıştı | YENİ |
| `custom fit` | **0** | **0** | sayılmamıştı | siteda YOK |

**MTM dilinin toplam hit'i: 284.**

### 1.2 Motorun gerçeği (bugün basıldı)

```
python3 -c "import json; d=json.load(open('contract/layers/shape-ratios.json')); print(d['sizes'])"
→ ['EU34','EU36','EU38','EU40','EU42','EU44','EU46','EU48']     # n = 8
```

⚠ **KART DÜZELTMESİ:** kart `engine/pattern-bridge/shape-ratios.json` diyor.
O yol **DİSKTE YOK** (`ls` → No such file). Gerçek yol
`contract/layers/shape-ratios.json` (`find . -name shape-ratios.json`). Ölçüm o
dosyadan alındı.

### 1.3 Yan yana

| | sayı | kaynak |
|---|---|---|
| motorun yayınlanmış beden kümesi | **8** (EU34–EU48) | `contract/layers/shape-ratios.json` `sizes` |
| sitenin MTM cümlesi | **284 hit / 44+ dosya** | §1.1 grep'leri |
| sitenin ilan ettiği aralık | **EU34–52 (10 beden)** | `web/index.html:176,179,316,317,336` · `web/studio.html:56` · `web/api.html:161` |

**HÜKÜM: YALAN.** Çürüten kaynak repoda ve motorun kendi kontratında.

---

## 2. DİKİŞ PAYI — ★ V0-0C'NİN HÜKMÜ BUGÜN ÇÜRÜDÜ

⚠ **KART SATIR NUMARASI BAYAT.** Kart `web/shop-shift-dress.html:59` diyor.
`sed -n '59p'` → `.cta-row .sb-btn { text-decoration: none; }` (CSS).
Gerçek satır **`web/shop-shift-dress.html:110`**:

```
15 mm seam allowance included: cut on the outer line, sew on the inner fine line, exactly
```

Basan komut: `grep -rn "15 mm seam allowance" web/`

### Motorun payı — İKİ HAT, İKİ SAYI, İKİSİ DE GERÇEK

```
grep -n "kSeamAllowanceMM" engine/src/constants.gen.hpp
→ engine/src/constants.gen.hpp:13:inline constexpr double kSeamAllowanceMM = 15;

sed -n '29,34p' engine/constants.yaml
→ name: seamAllowanceMM / value: 15 / status: verified
   source: "Aldrich/Armstrong standard garment seam allowance (15 mm)"

grep -n "ALLOW = " engine/pattern-bridge/printpack.py
→ engine/pattern-bridge/printpack.py:59:ALLOW = 1.0   # seam allowance: 10mm (Bugra's bought patterns use it)
```

Siteye hangi hat gidiyor:
```
grep -c "garment.cpp" engine/build-wasm.sh        → 2
grep -oE '[a-zA-Z0-9_/.-]*\.cpp' engine/build-wasm.sh | sort -u | grep -cE "surfacepattern|flatten|bodysurface|curvefit"  → 0
```
Web = WASM = `garment.cpp` hattı = **kSeamAllowanceMM = 15**.
`printpack.py`'ın 10mm'i **Python hattının** sayısı; siteden indirilen kalıba girmiyor.

**HÜKÜM: `web/shop-shift-dress.html:110` DOĞRU.**
★ **V0-0C `Y9`'u ("15mm YALAN, gerçek 10mm") BUGÜN ÇÜRÜTÜLDÜ — o hüküm BAYAT.**
V0-0C 10mm'i `docs/SATIS-SARTNAMESI.md`'nin printpack ölçümünden almış, ama
printpack sevk edilen web hattı değil.

**Kalan gerçek sorun (V0-0C'nin bulamadığı):** repo TEK giysi için İKİ dikiş payı
taşıyor (15 / 10) ve hangisinin alıcının eline geçtiğini basan bir alet YOK.
`web/styles/balloon-sleeve.html:98` üstelik aynı sitede **10 mm** diyor,
`web/js/guide-tr.js:82` **1.5 cm** diyor → site kendi içinde de iki sayı taşıyor.

---

## 3. `web/api.html` — worker'ı canlı gibi mi anlatıyor?

### 3.1 Sayfadaki cümleler, ADIYLA

| satır | cümle (aynen) | zaman |
|---|---|---|
| `web/api.html:7` | "The stitchu drafting engine behind one endpoint: POST /api/draft turns measurements + a garment spec into a true-scale sewing pattern." | şimdiki |
| `:84` | "One endpoint. Real patterns. No AI bill." | şimdiki |
| `:85` | "The same C++ drafting engine stitchu runs in the browser, behind one HTTP endpoint. Send seven measurements and a garment spec; get back a true-scale, **measurement-fitted** sewing pattern…" | şimdiki |
| `:86` | "POST /api/draft · zero marginal cost" | şimdiki |
| `:89` | `curl https://stitchu-api.<account>.workers.dev/api/draft \` | şimdiki |
| `:119` | "**Try it live**" | şimdiki |
| `:120` | "Draft a real pattern against the **live endpoint**, the same call your product would make. No key needed for this demo (rate-limited)." | şimdiki |
| `:138` | "One design, a whole size chart. POST /api/grade drafts a spec across an EU size range…" | şimdiki |
| `:158` | "Beta partners" | şimdiki |
| `:161` | "A solo Etsy / **made-to-measure** shop. One photo → a full size run (XS–XL / **EU34–52**) via /api/grade." | şimdiki |
| `:204` | JS fallback: "// The draft endpoint isn't live yet, it activates on the next deploy." | şartlı/gelecek |
| `:217`, `:237`, `:251` | aynı dürüst-düşüş metinleri | şartlı/gelecek |

### 3.2 Worker BUGÜN ÖLÇÜLDÜ — ★ V0-0C'NİN F4'Ü BAYAT

```
curl -s -o /dev/null -w "%{http_code}" -X POST https://stitchu-api.damummyphus.workers.dev/api/draft \
  -H 'content-type: application/json' -d '{}'
→ 422

curl -s -X POST .../api/draft -H 'content-type: application/json' -d '{}'
→ {"error":"missing_spec","detail":"spec object is required","field":"spec"}

curl -s -X POST .../api/draft -d '{"measurements":{"bust":880,...},"spec":{"garment":"dress"}}'
→ {"error":"measurement_out_of_range","detail":"measurements.bust must be between 60 and 160 cm","field":"measurements.bust"}

curl -s -o /dev/null -w "%{http_code}" -X POST .../api/grade -d '{}'   → 422
curl -s -o /dev/null -w "%{http_code}" .../                            → 401
```

**Uç nokta 404 ya da bağlantı hatası DÖNDÜRMÜYOR; kendi şema doğrulamasını
koşturup alan adıyla hata basıyor.** Bu, `backend/worker.js`'in deploy edilmiş
olduğunun ölçümüdür.

⚠ `ENV.md:24` "never claim the worker is live" diyor. Bu satır **iddia değil,
ölçüm**: yukarıdaki komut ve çıktısı. Deploy'un Damla'nın adımı olduğu
değişmedi; değişen, V0-0C'nin "API planlı" hükmünün bugün DOĞRULANMAMASI.

**HÜKÜM:** `api.html`'in şimdiki-zaman API cümleleri (`:84,85,86,119,120,138`)
**DOĞRU**. `:161`'in "made-to-measure" + "EU34–52"si **YALAN** (§1).
`api.html`'in JS düşüş metinleri gereksiz ama zararsız.

---

## 4. SÜRÜM KAYMASI — KAYMA YOK

```
grep -rhoE '\?v=[0-9a-zA-Z._-]+' web/ | sort | uniq -c
→ 488 ?v=136

grep -rlE '\?v=' web/ | wc -l
→ 137

node -e "import('./engine/tools/site-version.mjs').then(m=>console.log(m.siteVersion()))"
→ 136      (exit 0)
```

| ölçü | sayı |
|---|---|
| farklı `?v` değeri | **1** (`136`) |
| toplam `?v` damgası | **488** |
| damga taşıyan dosya | **137** |
| **kaymış dosya** | **0** |

Not: `site-version.mjs` bir MODÜL, CLI değil — çıplak `node engine/tools/site-version.mjs`
sessizce exit 0 verir, sayı basmaz. Sayıyı basmak için `siteVersion()` çağrılmalı
(yukarıdaki komut). Kartın "bastığı sayı" ifadesi bu yüzden bir import gerektiriyor.

---

## 5. UI ≠ MOTOR — ★ EN SERT BULGU, ZİNCİR SATIR SATIR YÜRÜNDÜ

Zincir:

1. `web/js/create.js:810`
   `const EU_SIZES = ['EU34',…,'EU48','EU50','EU52'];` → kullanıcıya **10 beden** sunuluyor.
2. `web/js/create.js:826-827` varsayılan aralık `EU36`→`EU44`; `:858` sıralama düzeltmesi.
3. `web/js/create.js:866` `grade(spec, from, to)` → `web/js/engine.js:71`
   `engine.gradeJSON(engineSpec(spec), {from,to})`.
4. `engine/wasm/bindings.cpp:376` `gradeJSON` → `:387` `const auto& chart = euSizeChart();`
5. `euSizeChart()` = `engine/src/sizechart.hpp`, satır tablosu `engine/src/contract.gen.hpp:30-31`:
   ```
   X("EU50", 116, 98, 122, 41, 43, 61, 40)
   X("EU52", 122, 104, 128, 42, 43.5, 61.5, 41)
   ```
   → motor EU50/EU52 için **gövde ölçüsü taşıyor**.
6. `engine/src/sizechart.hpp:149-156` şekil oranlarını **yalnız etiket eşleşirse** yazıyor:
   ```
   for (SizeChartEntry& e : c)
       for (const contract::BackArcRow& r : contract::kBackArcFraction)
           if (e.label == r.label) { e.body.bustBackFrac = r.bust; … e.body.shoulderWidthCM = r.shoulderWidthCM; }
   ```
7. `engine/src/shaperatios.gen.hpp:22` tablo **EU34–EU48 ile bitiyor**; aynı dosyanın
   `:21` yorumu: "Sizes absent here (EU50/EU52) have no published ratio and keep 0".
8. `engine/src/measurements.hpp:27`
   `double bustBackFrac = 0, waistBackFrac = 0, hipBackFrac = 0;`
   `engine/src/measurements.hpp:47` `double shoulderWidthCM = 0, shoulderInclDeg = 0;`

**→ EU50 ve EU52 motora `bustBackFrac = 0`, `shoulderWidthCM = 0`,
`shoulderInclDeg = 0` ile giriyor.** Yani UI'da seçilebilen iki beden, motorun
kendi kontratında ön/arka payı ve omuz genişliği OLMAYAN bedenler.

**Seçilen beden ile GÖSTERİLEN beden ayrışıyor mu? EVET, ve satırı:**

- `web/js/create.js:866`
  `const sizes = (graded.sizes || []).filter((s) => !s.draft.issues.length);`
  → motor bir bedene issue basarsa o beden **sessizce düşürülüyor**.
- `web/js/create.js:872-875` düşen sayı yalnız bir mesaj metnine yazılıyor
  (`create.grade.done.some {n, dropped}`), **hangi beden düştüğü söylenmiyor**.
- `web/js/print.js:230` `if (sizeLabel) page.appendChild(el('div','print-size-stamp', sizeLabel));`
  → basılan sayfaya **kullanıcının seçtiği etiket** damgalanıyor, motorun
  gerçekten neyi çizdiği değil.
- `web/js/print.js:316` yorumu zaten `EU34<...<EU52` varsayıyor.

**HÜKÜM: YALAN listesine girer.** Site 10 beden vaat ediyor, kontrat 8 yayınlıyor,
kalan 2'si sıfırlanmış şekil verisiyle çiziliyor ve düşerse kullanıcı hangi bedenin
düştüğünü görmüyor.

★ **KART DIŞI, RULES 1 İHLALİ (dokunulmadı):** `engine/wasm/bindings.cpp:387-392`
```
size_t lo = 0, hi = chart.size() ? chart.size() - 1 : 0;
for (…) { if (chart[i].label == fromLabel) lo = i; if (chart[i].label == toLabel) hi = i; }
```
Bilinmeyen bir beden etiketi **`Result::Err` vermiyor**; `lo`/`hi` başlangıç
değerinde kalıyor, yani istek sessizce **tüm tabloya** genişliyor. Kendi
yorumu bunu itiraf ediyor: `:386` "default to the whole chart if a label is
unknown." RULES invariant 1: "Unknown or unsupported enum/spec value → Result::Err
… NEVER silently drop or coerce."

---

## 6. ÖLÜ LİNK

```
node engine/tools/site-health.mjs   →  exit 0
checked: 127 pages, 2597 internal refs, 124 sitemap urls, 124 indexable pages
OK  site-health: no dead links, sitemap matches the site, one version.
```

Tam çıktı: `GECE/log/V10-A.site-health.txt`

| ölçü | sayı |
|---|---|
| ölü iç link | **0** |
| sitemap 404 / eksik | **0** |
| `?v` gerilemesi | **0** |
| taranan sayfa | **127** |
| taranan iç referans | **2597** |

V0-0C'nin aynı sayıları (127 / 2597 / 124 / 124) bugün **birebir tekrarlandı** — bayat değil.

---

## 7. İDDİA TABLOSU

Makine okunur hâli: **`GECE/log/V10-A.iddia.tsv`** (tab ayraçlı, başlık satırlı).
Üreten betik `/tmp/v10a_tsv.py` (gövdesi §7.2), her satır `grep -rnoEi <kalıp> web/`
çıktısından geliyor.

| ölçü | sayı |
|---|---|
| taşıyıcı iddia KALIBI | **20** (2'si web/'de 0 hit → düştü) |
| toplam iddia ÖRNEĞİ (dosya:satır) | **985** |
| — DOĞRU | **54** |
| — YALAN | **298** |
| — KANITSIZ | **633** |

### 7.1 Kalıp kalıp

| id | iddia kalıbı | hit | sınıf | basan alet (ADIYLA) | zaman |
|---|---|---|---|---|---|
| C16 | `byte-identical` | 251 | KANITSIZ | `engine/golden-diff.py` + ctest `golden_check` VAR — **bu turda KOŞULMADI** | şimdiki |
| C3 | `your own measurements` | 104 | **YALAN** | çürüten: `contract/layers/shape-ratios.json` sizes=8 | şimdiki |
| C19 | `3 cm calibration square` | 102 | KANITSIZ | **YOK** — web hattında kareyi basan alet yok; `printpack.py` 4cm kullanıyor | şimdiki |
| C6 | `to your measurements` | 77 | **YALAN** | aynı | şimdiki |
| C13 | `37,800` vocab sweep | 75 | KANITSIZ | `engine/tools/vocab-sweep.cpp` VAR — **KOŞULMADI** | şimdiki |
| C11 | `70,200` draft matrisi | 64 | KANITSIZ | ctest `engine_check` VAR — **KOŞULMADI** | şimdiki |
| C22 | `no paywall` / `free during the build` | 49 | KANITSIZ | **YOK** — fiyat vaadi, alet yok | gelecek |
| C4 | `your exact measurements` | 48 | **YALAN** | aynı | şimdiki |
| C5 | `no fixed sizes` | 46 | **YALAN** | aynı | şimdiki |
| C15 | `zero issues` / `zero validator` | 38 | KANITSIZ | **YOK** — RULES §6 yasaklı duran iddia | şimdiki |
| C10 | `0.000000 mm` | 27 | KANITSIZ | `precision-report.js` **2 basamak** basıyor (`:50`, `:121` `toFixed(2)`); ağaçta 6 basamak basan alet YOK | şimdiki |
| C9 | `seam-pair precision` | 23 | **DOĞRU** | `node engine/tools/precision-report.js` → koşuldu, §7.3 | şimdiki |
| C12 | `19,780` web fuzz | 23 | KANITSIZ | `engine/tools/web-fuzz.js` VAR — **KOŞULMADI** | şimdiki |
| C1 | `EU34–52` | 18 | **YALAN** | `shape-ratios.json` sizes=8 | şimdiki |
| C18 | `/api/grade` | 17 | **DOĞRU** | `curl` → HTTP 422, §3.2 | şimdiki |
| C17 | `POST /api/draft` | 9 | **DOĞRU** | `curl` → HTTP 422 + `{"error":"missing_spec"}`, §3.2 | şimdiki |
| C8 | `15 mm seam allowance` | 5 | **DOĞRU** | `engine/constants.yaml:29-32` → `constants.gen.hpp:13` = 15 | şimdiki |
| C20 | `ALL PASS` | 4 | KANITSIZ | **YOK** — RULES §6 yasaklı | şimdiki |
| C7 | `measurement-fitted` | 3 | **YALAN** | aynı | şimdiki |
| C2 | `10 of 10` beden | 2 | **YALAN** | `shape-ratios.json` 8; `shaperatios.gen.hpp:21` EU50/52 = 0 | şimdiki |
| — | `94 %` vision | **0** | — | `web/` içinde hit YOK (`grep -rnoE "94 ?%|94%" web/`) — `docs/` kalemi, bu kartın kapsamı dışı | — |
| — | `77/77` / `84/84` test | **0** | — | `web/` içinde hit YOK — `README.md`/`CLAUDE.md` kalemi, kapsam dışı | — |

### 7.2 Betik yeri
`/tmp/v10a_tsv.py` (kalıcı değil). Her satır tek bir `grep -rnoEi` çağrısından
üretildi; TSV'nin `basan_alet` kolonu bu tablonun aynısını taşıyor.

### 7.3 DOĞRU derken koşulan alet (çıktı aynen)

```
$ node engine/tools/precision-report.js
…
  ok   sleeve cap ease (0-10% window)   cap 554.5 armhole 533.4  ease 4.0%
  ok   bodice side seam front vs back   153.58 vs 153.58  Δ 0.00 mm
  ok   princess seam front center vs side panel  392.05 vs 392.05  Δ 0.00 mm

precision report: worst pair mismatch 0.00 mm | 0 FAILURES
EXIT=0
```

⚠ Alet **`0.00 mm`** basıyor. Sitenin 24 dosyada 27 kez yazdığı **`0.000000 mm`**
bu aletten GELMİYOR. İki basamaklı bir ölçüm altı basamaklı ilan edilemez →
C10 KANITSIZ.
(`web/index.html:321,322`'deki `0.000000 mm²` ayrı bir iddia: shapely marker
örtüşme alanı. Onu basan koşu bu turda üretilmedi.)

---

## 8. DURAN-İDDİA KALIPLARI (`web/**`)

Basan komut: `grep -riloF "<kalıp>" web/ | wc -l` ve `grep -rioF "<kalıp>" web/ | wc -l`

| kalıp | hit | dosya |
|---|---|---|
| `byte-identical` | **251** | 60 |
| `always` | **31** | 16 |
| `zero issues` | **36** | 10 |
| `0.00 mm` | **30** | 8 |
| `0.000000 mm` | **27** | 24 |
| `0.0000 mm` | **8** | 1 |
| `bitti` | **5** | 4 |
| `ALL PASS` | **4** | 2 |
| `perfect` | **4** | 2 |
| `zero validator` | **2** | 1 |
| `never fails` | **0** | 0 |
| `guaranteed` | **0** | 0 |
| **TOPLAM** | **398** | — |

En yoğun dosyalar (`byte-identical` için `grep -rcF … | sort -t: -k2 -rn`):

```
web/patches.html                50
web/atolye.html                 10
web/patches/2-9.html             8
web/styles/open-back.html        4
web/styles/shirring-drawstring.html  3
web/styles/fabric-ties.html      3
web/styles/collars.html          3
web/patches/3-40.html            3
```

V0-0C'nin 344'ü `docs/ + web/ + README.md` toplamıydı ve farklı bir kalıp
listesi kullanıyordu → **doğrudan kıyaslanamaz**; bu tur yalnız `web/` ve
kartın verdiği kalıp listesi. `web/css/shared-header.css` ve
`web/js/*.js` içindeki `byte-identical` hitleri **yorum satırı**, kullanıcıya
görünmüyor — sayıya dahil, işaretli.

---

## 9. MOBİL KIRILIM TABANI

```
find web -name '*.html' | wc -l                       → 127
grep -rl 'name="viewport"' web/ | wc -l               → 127
```
**Viewport meta'sı eksik sayfa: 0.**

Sabit genişlik taraması (`max-width` AYRI tutuldu — o taşma riski değil):

```
grep -rhoE '(^|[^-a-z])(min-width|width) *: *[0-9]{3,4}px' web/ | grep -oE '(min-width|width) *: *[0-9]+px' | sort | uniq -c | sort -rn
grep -rhoE 'max-width *: *[0-9]{3,4}px' web/ | wc -l   → 188   (güvenli)
grep -rl '@media' web/ | wc -l                          → 8
```

320px'te yatay taşma riski taşıyan gerçek sabitler — **toplam 13 bildirim,
hiçbiri 320px'i AŞMIYOR:**

| bildirim | adet | dosya:satır |
|---|---|---|
| `min-width: 300px` | 2 | `web/shop-shift-dress.html:25,33` |
| `min-width: 280px` | 1 | `web/shop-shift-dress.html:47` |
| `min-width: 260px` | 1 | `web/css/studio.css:20` |
| `min-width:220px` | 1 | `web/api.html:48` |
| `min-width:200px` | 1 | `web/benchmark.html:44` |
| `width:420px` / `308px` / `300px` / `280px` / `260px` / `236px` / `180px` / `120px` | 1'er | (tek tek, en büyüğü 420px) |

**En büyük sabit `min-width`: 300px < 320px → 320px'te taşma yok.**
Tek 320 üstü sabit `width:420px` (tek bildirim) — `width` `min-width` değil,
dar ekranda küçülür.
⚠ **DOĞRULANMADI:** gerçek bir 320px tarayıcıda render edilmedi (kart ölçüm
kartı, PNG üretimi kapsamda değil). Sayı statik grep'ten.

**Media query taşıyan dosya yalnız 8 / 127** — mobil düzenin çoğu
`max-width` + akışkan yerleşime bırakılmış. Bu bir kusur DEĞİL, bir ölçüm.

---

## 10. ÖZET SATIRI

> **DOĞRU 54 · YALAN 298 · KANITSIZ 633 · ölü link 0**
> (birim = iddia ÖRNEĞİ, yani `dosya:satır`. Kalıp bazında: DOĞRU 4 · YALAN 7 · KANITSIZ 9.)

Ek sayaçlar:

| | sayı | basan komut |
|---|---|---|
| MTM dili hit | 284 | §1.1 grep'leri |
| motorun beden sayısı | 8 | `python3 -c` json |
| sitenin ilan ettiği beden | 10 | `grep -rn "EU34.\{0,4\}52" web/` |
| `?v` farklı değer | 1 (`136`) | `grep -rhoE '\?v=…' web/ \| sort -u` |
| ölü iç link | 0 | `node engine/tools/site-health.mjs` (exit 0) |
| duran-iddia hit | 398 | §8 |
| viewport eksik sayfa | 0 | `grep -rl 'name="viewport"' web/` |
| 320px üstü sabit `min-width` | 0 | §9 |
| worker HTTP | 422 (deployed) | `curl -X POST …/api/draft` |

**`web/` YAZILMADI:**
```
git status --porcelain web/    → (boş)
```

---

## 11. ÖLÇÜLMEDİ / YAPILAMADI (adıyla)

- **`engine_check` (70,200) KOŞULMADI** — `ctest` bu makinede 128 sn+, kart 60 dk
  ölçüm kartı. `ctest --test-dir engine/build -N` → **Total Tests: 116**
  (V0-0C'nin **106**'sı **BAYAT**, +10). "116 test geçiyor" DENMEDİ; denilen:
  116 test tanımlı.
- **`web-fuzz.js` (19,780) KOŞULMADI.**
- **`vocab-sweep.cpp` (37,800) KOŞULMADI.**
- **`golden-diff.py` KOŞULMADI** → 251 `byte-identical` hit'i doğrulanmadı.
- **shapely marker örtüşme koşusu üretilmedi** → `web/index.html:321,322`
  `0.000000 mm²` doğrulanmadı.
- **320px gerçek render YOK** — §9 statik grep.
- **`web/patches.html` (50 `byte-identical` hit) tek tek okunmadı** — tarihli
  yama günlüğü.
- **`docs/` ve `README.md` bu turda SAYILMADI** — kart kapsamı `web/**`.
  V0-0C'nin `docs/README` ölü hedef sayısı (6) bu turda tazelenmedi → **BAYAT**.
- **V0-0C'nin `web/index.html` satır numaraları BAYAT:** `:209,210,229` dediği
  iddialar bugün `:316,317,336`'da. Satır numarası devralınmadı, yeniden greplendi.
- **`create.js` grade akışı tarayıcıda koşturulmadı** — §5 zinciri statik okuma
  + `bindings.cpp` kaynağı. EU50'nin gerçekten issue basıp basmadığı
  **DOĞRULANMADI**; ölçülen, motora sıfır şekil verisiyle GİRDİĞİ.
