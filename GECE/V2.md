# V2 — MUTFAK: sözlük reformu · ŞEF TUTANAĞI

Koşu: 2026-08-24. Protokol: `GECE-KOSUSU-v6.md` §6/V2.
Ağaç: `a6b473a..d167ef1` (+ hakem `bf8a2e0`, kâtip `4d94f20`).
Kartlar: `GECE/KART/V2-R-arastirma.md` · `V2-A-sokum-hukmu.md` ·
`V2-B-iki-kapi.md` → `V2-B2-kapilari-bitir.md` · `V2-C-sevk-hatti-durustlugu.md` ·
`V2-D-iki-yeni-kirmizi.md`.
İşçi tutanakları: `GECE/V2-R.md` · `V2-A.md` · `V2-B.md` · `V2-C.md` · `V2-D.md`.

## 0. FAZIN DEVRALINMASI (§3.10)

V2 bu oturumda AÇILMADI, DEVRALINDI. Önceki şef V2-B kartının ortasında kesilmiş:
`V2-R` ve `V2-A` commit'li (`479b0da`, `e5c9628`), V2-B'nin dört çıktı dosyası
diskte ama **commit'siz**, `GECE/V2.md` tutanağı YOK. §3.10'un hükmü uygulandı:
tutanak yoksa faz yeniden açılır, yarım iş ÖNCE commit'lenir. Çivileme commit'i
`6fac6cb`. Silinen iş yok.

---

## 1. ÖLÇÜLEN (sayı · komut · hash)

### 1.1 Makine kapısı (§4.1)

```
$ ctest --test-dir engine/build --output-on-failure          # HEAD d167ef1
96% tests passed, 4 tests failed out of 108
Total Test time (real) = 271.21 sec
FAILED: style_check · sizechart_source_check · contract_check · figure_check
```
Log: `GECE/log/V2-SEF.ctest.final.txt`. Faz öncesi: `GECE/log/V2.ctest.before.txt`
(**105 test / 4 kırmızı**, aynı dört ad). **Test sayısı 105 → 108; kırmızı AD
kümesi 4 → 4; AÇILAN YENİ AD: 0.**

### 1.2 Taban bantları (§4.1 — sessizce aşılamaz)

```
$ node engine/tools/wasm-baseline.mjs      # load averages: 2,27 2,82 3,67
worst abs delta    : 1.0000e-4 mm                      (tavan 1e-4)      OK
draftJSON + parse  : median 1.030  p95 1.107  (ms)     (tavan 1.031/1.338) OK
gradeJSON EU34-48  : median 8.198  p95 8.707  (ms)     (tavan 8.800)     OK
soak repeats       : 5000 -> SURVIVED · external delta 0.00 MB
```
⚠ V2-D aynı aleti `load averages: 4,29 5,36 4,89` altında koştu ve İKİ gecikme
bandını da aşık ölçtü (draftJSON 1.234 · gradeJSON 10.457). Kök V2-D §5.2'de
ölçüldü: ölçülen modül baytı `5d649d2` ile **bayt-aynı** (`fe570bae…0887`), yani
yavaşlama koddan değil makine yükünden. Şefin sakin makinedeki koşusu bantların
altında. **4.6 prosedürüne gerek olmadı; bantlar DEĞİŞMEDİ.** Kalan kusur
kuyrukta: `wasm-baseline.mjs` bugün sayı ne olursa olsun exit 0 dönüyor.

### 1.3 §6/V2'nin kapısı — üçü de yeşil

```
$ ctest --test-dir engine/build -R "preset_resolve_check|vocab_source_check|vocab_reference_check"
100% tests passed, 0 tests failed out of 3
```
`preset_resolve_check` 0.03 sn · `vocab_source_check` 0.10 sn ·
`vocab_reference_check` 4.57 sn. Dördüncü yeni kapı: `wasm_spec_honesty_check`
(0.26 sn) — sevk edilen sınırın dürüstlüğünü yargılıyor.

### 1.4 Boş test kapısı (§4.2, birincil usul — üç yeni kapının üçü de)

| kapı | faz-öncesi hüküm | kanıt |
|---|---|---|
| `wasm_spec_honesty_check` | **165 geçti / 145 KIRMIZI** (faz-öncesi DERLENMİŞ wasm baytı `/tmp/v2c/stitchu-engine.PRE.js`) | `GECE/log/V2-C.bostest.txt` |
| `vocab_source_check` | **11 FAIL, exit 1** (elle yazılmış `vision-student/vocab.py`'a karşı, `ffcd512`) | `GECE/log/V2-B.bostest.source.txt` |
| `vocab_reference_check` | **exit 1** iki ayrı sondada (eksik referanslı taban; taban dosyası yokken "a missing law is never a pass") | `GECE/log/V2-B.bostest.ratchet.txt` |

Hakem üçünü de bağımsız koşturdu ve doğruladı (`GECE/KAPI.md`, `bf8a2e0`).

### 1.5 Mutasyon kanıtı (§4.5)

`GECE/log/V2-B.mutasyon.txt` · `V2-C.mutasyon.txt` · `V2-D.mutasyon.txt`.
- `vocab_source_check`: elle `"trousers"` ekle → exit 1; üreteci kirlet → exit 1;
  geri al → exit 0.
- `vocab_reference_check`: üretece tek yorum satırı → `neckline 991->992`,
  `cowl 14->15`, exit 1; geri al → exit 0.
- `wasm_spec_honesty_check`: M1 `v.as<int>()` geri → 135 fail · M2 gövde kapısı
  açık → 6 fail · M3 `ruffleTiers` kırpma geri → 4 fail; geri al → 310/310 yeşil.
  ★ M3 İLK TURDA SAĞ KALDI — kapının kendi kusuru mutasyonla bulundu, kapı
  gevşetilmedi, (a2) bölümü eklendi.
- `bundle_fresh_check`: kaynağa yorum + commit, rebuild yok → üç artefakt
  "STALE BY 1 COMMITS"; geri al → yeşil. ★ M2: aynı yorum + rebuild → derleyici
  çıktısı **bayt-aynı**, damgalı sevk kalemleri DEĞİŞTİ. Damga olmasaydı
  yeniden derleme bile kapıyı açamazdı.

### 1.6 Tolerans/taban değişikliği (§4.6) — bir adet, ilan edilmiş

Ratchet tabanı `b799748` (10416) → `9487091` (10418). Kımıldayan tek anahtar:
eksen adı `sleeveCap` 144 → 146. Deltanın satır satır kaynağı, iki `git grep`
çıktısıyla, `e2f7aba` commit mesajının gövdesinde. **+2'nin tamamı YORUM satırı**
(`bindings.cpp:125` ve `:131`, onarılan sessiz ikame kusurunun ölçülmüş örneği);
kod tarafı net sıfır (`sleeveCapFrom` helper'ı öldü, çağrı yeri 1 → 2 satır).
`git diff b799748..HEAD -- engine/vocab.json` **boş** — sözlük 37 eksen / 132
değer, büyümedi. Kapının sayım yöntemine, kapsamına ve betiğine DOKUNULMADI.

### 1.7 Söküm hükmü (§6/V2 madde a) — `_LEGACY-ADAYI SIFIR`

`GECE/V2-A.md` §1: 0D'nin menü dili saydığı 34 dosyanın **34'ü BAĞLANDI**
(6 saf MENÜ + 12 KARIŞIK + 16 tek-eksenli). 30'u Katman 3'e, 4'ü Katman 1'e
(`CmdType`, `SpringKind`, `recipe.cpp`'nin 5 parser enum'u, `PatchCorner`/`Girth`).
34 dosya 37 eksenin **37'sini** kapsıyor, fazlası ve eksiği yok. Sürgün kartına
düşen kalem: **sıfır** — menü dağınık değildi, tek dosyadan (`engine/vocab.json`)
türüyordu ve 0D'nin bu hükmü ölçümle doğrulandı.

### 1.8 Damar kalemleri sicile isim (§6/V2 yasası)

V0-0A §3.4'ün "sicilde İSİM olarak bile yok" saydığı **13 kalem** tek tek arandı:
**12'si `contract/vocab-resolution-v1.json`'da ZATEN İSİMLE varmış**; ölçüm
V0-0A'nın kullandığı sicilin (`garment-spec-v2.json`, OPERATÖR sicili) çözüm
tablosundan başka bir dosya olduğunu gösterdi. İsimsiz tek kalem: **dantel/fisto**
— `_damar_sicili` bloğuna `absent` + kök sebep + denenen hamle + sonraki aday
(`primitives-v1.json`'a kenar şeridi paneli + panel içi geometri) ile yazıldı.
Blok inert: `preset_resolve_check.cpp:412` yalnız `resolutions`'ı okuyor
(BIJECTION şartı, `V2-A.md` §2.2), yani kapı gevşetilmeden isim verildi.

### 1.9 İki ölçülmüş kusur — İKİSİ DE KAPANDI

**(1) Sessiz ikame, `bindings.cpp:94`.** `intField()` içindeki `v.as<int>()`
kesiri `parseEnumInt`'e ULAŞMADAN kırpıyordu: `sleeveCap=1.5` ile `sleeveCap=1`
bayt aynı sha. Onarım: değer `double` okunuyor, tam sayı olmayan / sonlu olmayan
/ aralık dışı değer string eksenlerin zaten kullandığı sözlerle **ADIYLA**
reddediliyor (`invalid sleeveCap '1.5' (valid: plain, gathered, puffed, …)`).
26 int ekseni + `ruffleTiers` tek `enumIntField()` okuyucusuna bağlandı.
Yokluk hâlâ hata değil — varsayılan kalıyor. **RULES değişmez 1'in sevk edilen
hattaki ihlali kapandı.**

**(2) `collarType` NaN — KÖK, İDDİA EDİLENDEN BAŞKA ÇIKTI.** V0-0D'nin ölçüm
betiği gövdeyi `{bustCM:…}` diye veriyor, sınır `{bust:…}` okuyor: o koşuda
**gövdenin 7 alanı da 0'dı**. Gerçek gövdeyle `collarType=0..6` faz ÖNCESİ bile
temiz (`nan=0, validJSON`). Zincirin üç halkası da onarıldı: `numField` eksik
ölçüyü 0 yapıyordu (aynı sınıf sessiz ikame) → `bodyFrom` artık ADIYLA
reddediyor · `bodice.cpp makeFacing()` `shoulderLen==0` iken `0/0`=NaN üretiyordu
→ `<1e-6` mandalı (aynı fonksiyonun 15 satır yukarısında ZATEN vardı, omuz
vektöründe unutulmuş) · `num()` NaN'ı çıplak `nan` token'ı basıyordu → artık
basmayı reddedip fırlatıyor, sınır onu JSON.parse edilebilir bir `{"error":…}`'e
çeviriyor. **Gizleme değil, gürültülü başarısızlık.**
★ `contract/vocab-resolution-v1.json`'ın `collarType` için yazdığı 6 `resolved`
**yalan değilmiş.** Yalan olan V0-0D §5 madde 1'in hükmüydü; bozuk ölçüm
gövdesinden geliyordu ve bu tutanakla düzeltiliyor.

### 1.10 Görü kelime listesi artık BUILD ÜRÜNÜ (§6/V2 madde c)

`vision-student/vocab.py` üç elle yazılmış sapma taşıyordu, üçü de kapandı:
`neckline` 7 → **9** (`cowl`, `pussyBow` geldi) · `garment` 5 → **3**
(`trousers`, `other` düştü) · `skirtStyle` 5 → **6** (`gore`). Üreteç
`engine/tools/gen-vision-vocab.mjs`, kaynaklar `engine/vocab.json` (otorite,
`V2-R.md` §3.3) + `contract/vocab-resolution-v1.json`. `trousers`/`other`
SİLİNMEDİ (RULES 1) — `_UNRESOLVED` bloğunda gerekçeleriyle duruyorlar ve kapı
o bloğu üreteçten BAĞIMSIZ olarak iki tablodan yeniden türetip denetliyor.
Dışa verilen 8 isim korundu (`grep -rn "from vocab import" vision-student/*.py`
→ 8 satır).

---

## 2. KAPANAN / AÇILAN KIRMIZI (ad ad)

**KAPANAN:** yok — devralınan dördü de yerinde duruyor ve dördü de sınıf (c)
kaynak/karar eksiği; hiçbiri V2'nin işi değildi.

**AÇILAN VE AYNI GECE KAPATILAN (3 ad):**
1. `bundle_fresh_check` — `5d649d2`'nin doğurduğu. Kök: `build-wasm.sh` glue'yu
   yeniden üretti ama çıktı **bayt-aynı** oldu, git değişmeyen dosyaya commit
   yazamaz, kapı "built commit"i `git log -1 -- <dosya>`'dan okuyor. Kapı, glue'nun
   değişmediği HER motor değişikliğinde kırmızı düşmeye mahkûmdu. Çözüm: sevk
   edilen üç artefakta **kaynak sha256 damgası** (`ecdb56421eadd2a2`) — `.js`'e
   yorum satırı, `.wasm`'a custom section (`stitchu.source-stamp`, saf python3,
   yeni bağımlılık yok). Damga commit sha'sı DEĞİL, çünkü normal sıra
   (düzenle→derle→tek commit) kirli ağaçta derler; özet belirlenimli.
2. `vocab_reference_check` — `5d649d2`'nin doğurduğu (+2 sleeveCap). §1.6.
3. `generated_ratchet_check` — `9487091`'in doğurduğu. Kök: `?v` cache damgası
   bump'ı 137 dosyada 488 kalemi elle düzenliyor, 54'ü ÜRETİLMİŞ ve sha256'ları
   manifest'te ilan edilmiş. RULES 9 gereği **GERİ ALINDI** (`f0c1398`), yama
   atılmadı. Kusur açık, kuyrukta.

**PROTOKOL BULGUSU — kayda geçiyor.** Üç kırmızının üçü de **commit sonrası**
doğdu: üçü de `git log`/HEAD okuyan kapılar, yani hükümleri ancak commit var
olduktan sonra değişiyor. İşçilerin commit-öncesi ctest'i bunları yapısal olarak
göremezdi. §4.3 kanıt kapısını şef koşturmasa faz iki yeni kırmızı adla kapanmış
olurdu. Bundan sonraki her faz için: **tam ctest, COMMIT'TEN SONRA.**

---

## 3. FAZIN DÜRÜST HÜKMÜ — mutfak kuruldu mu?

Hayır, tam olarak değil, ve bu saklanmıyor.

V2 **menüyü DÜRÜST yaptı, mutfağa çevirmedi.** Ölçülmüş hâl:
- Sürekli girdi kabul eden eksen sayısı: **hâlâ 2/37** (`skirtLengthMM`,
  `fabricStretchPct`). Kalan 35 eksen kapalı liste.
- Değişen şey: bu 35 eksen ara değere artık **sessizce ikame etmiyor, ADIYLA
  reddediyor**. Yani "iki tarif arasındaki her ara değer geçerli giysidir"
  yasası bugün 2 eksende DOĞRU, 35 eksende AÇIKÇA YANLIŞ — eskiden 26 eksende
  sessizce yanlıştı ve ölçülemiyordu.
- Katman 1/2/3 mimarisi bu fazda İNŞA EDİLMEDİ; `contract/primitives-v1.json`
  ve `contract/vocab-resolution-v1.json` olarak zaten vardı, ve V2 onların
  132/132 bijeksiyonunu, 34/34 dosya bağını, 12/13 damar isimlendirmesini
  ÖLÇTÜ ve kapıyla mühürledi.
- Kapalı enum'a yeni referans eklemek artık **mekanik olarak** engelli (ratchet).
  Sözlük bundan sonra yalnız KÜÇÜLEBİLİR.

"Sınırlı malzemeden sınırsız ürün" siparişi (§2.4) bu fazda **kapanmadı**;
kapanan, o siparişin ölçülebilir hale gelmesi ve sözlüğün büyümesinin
kilitlenmesi. Sürekli eksen açma işi kart olarak kuyruğa girdi.

---

## 4. KAPI HÜKMÜ (§4, altı alt kapı)

| alt kapı | hüküm | kanıt |
|---|---|---|
| 4.1 makine (ctest + wasm paritesi + bantlar) | **GEÇTİ** | `GECE/log/V2-SEF.ctest.final.txt` · `build-wasm.sh` exit 0 · bantlar §1.2 |
| 4.2 boş test | **GEÇTİ** | üç kapının üçü de faz-öncesinde kırmızı, §1.4 |
| 4.3 kanıt | **GEÇTİ** (önce KALDI) | ilk koşu 6 kırmızı buldu → V2-D → 4 kırmızı |
| 4.4 hakem | **GEÇTİ** | `GECE/KAPI.md`, `bf8a2e0` |
| 4.5 mutasyon | **GEÇTİ** | üç log, §1.5; M3 kapının kendi kusurunu buldu |
| 4.6 tolerans | **GEÇTİ** | tek değişiklik, satır satır gerekçeli, `e2f7aba` |
| kâtip | **GEÇTİ** | `4d94f20` — `README.md`, `docs/ARCHITECTURE.md`, `GECE/INDEX.md` |

---

## 5. AÇIK KALAN (4.7 formatında — kök + ölçülmüş aday)

1. **`?v` cache damgası 136'da donmuş.** `web/vendor/stitchu-engine.js` baytı
   `5d649d2`'den beri iki kez değişti; tarayıcı bayat bundle servis edebilir
   (ENV.md GOTCHA). Kök: bump 54 ÜRETİLMİŞ dosyayı elle düzenliyor ve
   `generated_ratchet_check`'i kırar. Aday (kapının kendi mesajı): 54 dosyayı
   üreteçlerinden geçir, manifest AYNI commit'e girsin (`scripts/deploy.sh`).
2. **Damga bir İDDİA, doğrulama değil.** Artefaktı elle düzenleyip damgayı
   bırakmak `bundle_fresh_check`'i yeşil bırakır. Aday: kapının artefaktın
   kendi özetini yeniden hesaplaması — kapı kaynağına dokunmak bu fazda yasaktı.
3. **`wasm-baseline.mjs` hüküm basmıyor** (sayı ne olursa olsun exit 0).
   Aday (V2-D ölçtü): 5 koşunun **en düşük medyanı** alınsın, tavan aşılınca
   exit 1, ctest'e bağlansın — en-düşük-medyan tam olarak dış yükü eleyen
   istatistik.
4. **`NECKLINE_CLASSES` 7→9, `SKIRT_STYLE_CLASSES` 5→6.** İndeksler kaymadı
   (üreteç ilan sırasını koruyor, yeniler sona eklendi) ama 7 çıkışlı eğitilmiş
   bir kafa artık sınıf sayısıyla uyuşmaz. `vision-student/` altında 8 dosya bu
   modülü import ediyor. Yeniden eğitim gerekip gerekmediği KARARA BAĞLANMADI →
   V6'nın kartı.
5. **`vocab_reference_check` başlık yorumu bir yeniden-kesme geride** (10416 /
   `b799748` diyor, dosyadaki taban 10418 / `9487091`). Hakem bulgusu; kozmetik.
6. **Ratchet düz metni de sayıyor** — ilk 67'lik sıçramanın 25'i bir markdown
   dosyasından geldi. Bilerek onarılmadı (kararlı imza şartı).
7. **`wasm_spec_honesty_check` varsayılan olarak `engine/dist/`'i yargılıyor**,
   o dizin gitignore'da — temiz bir klonda dosya yok. Hakem kapıyı commit'li
   `web/vendor/stitchu-engine.js` baytına karşı da koşturdu: **310/0 yeşil**.
8. **`docs/ARCHITECTURE.md:54`** deploy'u `git subtree split → gh-pages` diye
   anlatıyor, `CLAUDE.md` `scripts/deploy.sh` (Vercel) diyor; üstelik yazdığı
   elle bump kırmızı kapı üretiyor. Kâtip dokunmadı: karar işi, kelime işi değil.

---

## 6. SONRAKİ FAZA DEVİR (üç sayı)

1. **KIRMIZI = 4** — `style_check` · `sizechart_source_check` · `contract_check` ·
   `figure_check`. Dördü de MİRAS ve dördü de sınıf (c) kaynak/karar eksiği.
   V2 üç yeni ad doğurdu, üçünü de aynı gece kapattı. **AÇILAN NET YENİ AD: 0.**
   Test sayısı **105 → 108**. Log: `GECE/log/V2.ctest.before.txt` →
   `GECE/log/V2-SEF.ctest.final.txt`.
2. **SÖZLÜK TABANI = 10418 referans @ `9487091`, 37 eksen / 132 değer, RATCHET
   KİLİTLİ.** Bundan sonraki her faz bu tabana karşı ölçülür ve sayı yalnız
   DÜŞEBİLİR; artıran commit `vocab_reference_check`'te kırmızı düşer. Tabanı
   yeniden kesmek bilinçli bir hamledir ve deltanın satır satır kaynağı commit
   mesajına yazılır (emsal: `e2f7aba`).
3. **SEVK EDİLEN SINIR ARTIK DÜRÜST: 27 alan ADIYLA reddediyor, 7 gövde ölçüsü
   ADIYLA reddediyor, sürekli eksen HÂLÂ 2/37.** Üç sevk artefaktı kaynak
   özeti taşıyor (`ecdb56421eadd2a2`). Bantlar: `draftJSON` medyan **1.030 ms**
   (p95 1.107) · `gradeJSON` **8.198 ms** · native↔wasm **1e-4 mm** · soak 5000
   SURVIVED. Alet: `node engine/tools/wasm-baseline.mjs`.
