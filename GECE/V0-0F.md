# V0-0F — ALTI KIRMIZI: KÖK TEŞHİS + ÖLÇÜLMÜŞ ÇÖZÜM ADAYI (2026-08-24)

Onarım YAPILMADI. `engine/` `contract/` `recipes/` `web/` altında tek bayt değişmedi.
Bütün adaylar `git worktree` (`/tmp/stitchu-wt`, `/tmp/stitchu-wt2`) ve `/tmp` araç
kopyalarında ölçüldü; ölçüm bitince worktree'ler silindi.

Ham koşu çıktıları: `GECE/log/V0-0F.aday.txt` (A1–A9 blokları).

Devralınan sayı DOĞRULANDI, devralınmadı: `GECE/log/V0-SEF.ctest.txt:453` →
`94% tests passed, 6 tests failed out of 105`; kırmızı adları `:461-466`.

---

## ★ BU GECENİN TEK BÜYÜK BULGUSU: İKİ KIRMIZI, TEK KÖK

`golden_check` ve `recipe_dress_check` **aynı sayıya** basıyor.

| | golden pin (28 Tem) | bugünkü motor | fark |
|---|---|---|---|
| EU38 `Bodice Front` armhole Y | 219.4004 | **225.0000** | 5.5996 mm |
| EU38 recete `grainline.from.y` | 225.4004 | **231.0000** | 5.5996 mm |
| pear | 227.6004 | 239.6000 | 11.9996 mm |
| bigNeckSmallShoulder | 227.2004 | 252.0000 | 24.7996 mm |

Kök tek satır — `52ae85c` (23 Ağu, *"source the scye depth to aldrich, solve the scye
hollow, gate the shipped line"*), `engine/src/bodice.cpp:905-907`:

```
-    const double torsoArmholeY = backLength * armholeDepthFactor + shoulderDrop;
+    const double napeBelowNeckLine = m.neckMM() * backNeckCutoutFactor;
+    const double torsoArmholeY =
+        m.bustMM() * scyeDepthPerBust + scyeDepthInterceptMM + napeBelowNeckLine;
```

Scye derinliği kaynaksız `backLengthCM` kolonundan Aldrich p.11'in **büst**'e bağlı
formülüne geçti. Ne golden pin ne de reçete yorumlayıcısı bu geçişle taşındı.

**İleri tarama ile ÇİVİLENDİ** (`GECE/log/V0-0F.aday.txt` A2): golden hattına 28 Tem'den
sonra dokunan 13 commit'in her birinde `golden_dump` kuruldu ve HEAD'deki pin'e diff'lendi.
`af49514`'ten `a571407`'ye kadar **9 commit BAYT-ÖZDEŞ**; ilk sapan **52ae85c**.

---

## KIRMIZI × ÜÇ KALEM

### 1. `golden_check` (#3)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | Motor 23 Ağu'da scye derinliğinin kaynağını değiştirdi (`52ae85c`, `engine/src/bodice.cpp:905-907`); `engine/golden-reference.csv` 28 Tem'de (`af49514`) donmuş haliyle duruyor. Pin bayat değil — **motor ilan edilmemiş bir davranış değişikliği yaptı**. |
| **ÖLÇÜLEN SAPMA** (hakemin istediği dağılım) | `./engine/build/golden_dump` + `/tmp/golden-mag.py` → satır sayısı eşit (23406=23406, parça eklenmemiş). **9651 satır farklı = %41.23**. max **62.7764 mm**, medyan **5.6000 mm**, ortalama 16.2275, p90 49.7051.<br>Kova: ≤0.001mm **1375** · 0.001–0.1mm 625 · 0.1–1mm 202 · 1–5mm 2170 · 5–20mm 1508 · 20–60mm **3659** · >60mm 112.<br>Gövde: bigNeck 3370 · pear 3326 · EU38 2955.<br>Parça: `Bodice Back` 2120 (max 47.44) · `Balloon Sleeve` 2×1080 (max 49.71) · `Bodice Front` 1020 (max **62.78**) · `Sleeve` 2×855 · `Top Back` 840 · `Top Front` 615.<br>**Etek parçaları max 0.0001 mm** — yani sapma gövde+kol hattında, etekte yok.<br>175 ayrı spec ailesi etkilenmiş; en ağırı `dress/crew/gathered/balloon.*` (84 satır, 62.7764 mm). |
| **ADAY 1 — REDDEDİLDİ** | Motoru son yeşil commit'e (`a571407`) döndür. Temiz Release build + tam ctest koşuldu (`/tmp/ctest-a571407.txt`): `80% tests passed, 20 tests failed out of 101`. 20'nin **10'u ENV artefaktı** (worktree'de `engine/dist/stitchu-engine.js` ve `ezdxf` yok), 9'u gerçek. **Kapanan 3** (golden_check, recipe_dress_check, figure_check), **yeni açılan 6** (preview_truth_check, h10_gate_check, edgemono_check, walkgate_check, cutplan_check, printpack_sheet_check). RULES §9: kırmızı SET büyüyemez → **RED**. |
| **ADAY 2 — bu işçi ÜRETEMEZ** | Testin kendi ilan ettiği ikinci yol: `scripts/repin-golden.sh` + `engine/GOLDEN-PIN.md` defter girdisi + **Damla onayı**. Pin yenileme kartın §YASAKLAR'ında. Ölçüldü ki karar verilebilsin: re-pin'in taşıyacağı sayı yukarıdaki dağılımın ta kendisidir — 9651 satır, en büyük hareket 62.78 mm. |
| **MALİYET** | Re-pin: bu gece **değil**, Damla kararı; dokunacağı dosya tek: `engine/golden-reference.csv`. Motoru döndürme: V1'e bile sığmaz (6 yeni kırmızı). |

### 2. `recipe_dress_check` (#71)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | Aynı `52ae85c`. Reçete yorumlayıcısı ikinci bir hat: `recipes/shift-dress-square-spaghetti.json:46` hâlâ ESKİ formülü taşıyor — `torsoArmholeY = backLengthMM * armholeDepthFactor + shoulderDrop`. K0 sabit paritesi 27/27 PASS olmasının sebebi bu: `armholeDepthFactor = 0.44` header'da hâlâ duruyor (`engine/src/bodice.hpp:96`, yorumu **"LEGACY"**), sadece artık motor onu kullanmıyor. Kapı sabitleri kilitliyor, **formülü kilitlemiyor**. |
| **ÖLÇÜLEN SAPMA** (hakemin isim isim istediği mm) | `/tmp/recipe-mag.cpp` (testin `:157-195` satırlarının ölçen ikizi, ağaca yazılmadı) → tam döküm `GECE/log/V0-0F.aday.txt` A4.<br>`Top Front` outline: 9 noktanın **2'si** farklı, EU38 max **48.4634 mm** / pear **57.0341** / bigNeck **45.2878**.<br>`Top Back`: 8 noktanın 2'si, max 37.8842 / 47.4355 / 28.5382 mm.<br>`Bias binding`: max **61.2979 / 83.0439 / 55.5707 mm** — biye şeridi EU38'de 1231 mm yerine 1292 mm oluyor (armhole uzadığı için).<br>grainline: 5.5996 / 11.9996 / 24.7996 mm.<br>notch: 0.6965 mm (EU38), 1.7689 mm (bigNeck).<br>`cutLine` **nokta SAYISI** farklı: 43↔48, 41↔44, 47↔53, 50↔54, 42↔47, 41↔43.<br>`Spaghetti Strap` her yerde **0.0000 mm** — 3/3 PASS'in sebebi. |
| **ADAY — ÖLÇÜLDÜ, KISMİ, DIŞARIDA DURUYOR** | `/tmp/cand-recipe.json` (reçetenin kopyası): `consts` += `scyeDepthPerBust 0.10`, `scyeDepthInterceptMM 122.0`; `torsoArmholeY` → `bustMM * scyeDepthPerBust + scyeDepthInterceptMM + neckMM * backNeckCutoutFactor`. Koşu: `./engine/build/recipe_dress_check /tmp/cand-recipe.json`.<br>**ÖNCE PASS 115 / FAIL 10 → SONRA PASS 116 / FAIL 9.**<br>grainline DIFFERS **6 → 0** · kumaş FAIL **1 → 0** (bigNeck 1.8500 → 1.9500 = motor). armhole Y artık iki hatta **birebir** aynı. |
| **ADAYIN KAPATAMADIĞI** | Kalan 9 FAIL'in tamamı `geometry DIFFERS`. Aday sonrası uç noktalar **birebir eşit** (244.2000,225.0000 == 244.2000,225.0000) ama delta 48.4634 mm sürüyor → sapma **kübiğin kontrol noktasında**. Sebep `52ae85c`'nin (a) yarısı: oyuk artık ölçülen yay/kiriş oranına **iteratif çözülüyor**; reçete DSL'inde çözücü yok, statik formülle ifade edilemez. Ayrıca 1.39–3.72 mm'lik omuz ucu farkı `0cb5d23`'ten ("the shoulder finally grades"), ve `cutLine` çözünürlüğü iki hatta ayrı. |
| **MALİYET** | Formül satırı: **bu gece yapılabilirdi** (tek satır + 2 sabit, `recipes/shift-dress-square-spaghetti.json`), kart yasakladığı için yapılmadı; FAIL 10→9. Kalanı **V1**: reçete DSL'ine çözücü/oyuk operatörü, dokunacağı dosyalar `engine/src/recipe.cpp` + reçete şeması. |

### 3. `contract_check` (#85)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | Kırmızı **BİLEREK**. `patterns_real/` git'te takipli, hedef 0. `git ls-files patterns_real \| wc -l` → **41**. Testin kendi metni "bugun 49" diyor; bugün sayılan 41 (V0-0A da bu çelişkiyi kaydetmiş, uzlaştırılmadı). Kapı bir kusur değil, **görünür tutulan bir bedel**. |
| **ADAY — ÖLÇÜLDÜ, ÇALIŞIYOR** | `/tmp/stitchu-wt2`: `git rm -r --cached patterns_real` → `node engine/tools/validate-contract.mjs` → **`contract validation GREEN`, exit=0** (öncesi `FAILED (1)`). Sonra `git reset HEAD -- patterns_real` ile geri alındı. |
| **MALİYET** | **Damla kararı**, bu geceye sığmaz. Untrack tek başına yeterli değil: geçmişte iz kalır (`filter-repo` kazısı, CLAUDE.md gizlilik maddesi). Dokunacağı yer: `.gitignore` + git index + (kararla) git geçmişi. |

### 4. `figure_check` (#90)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | `dress_bandeau_circle` stilinin `contract/figure-bands.json` → `mandal.taban_v3` içinde pini yok (16 pin var, bu stil değil). Son commit `396d42e` (23 Ağu, on bir kör stil yargıya alındı) — kırmızı o commit'te **bilerek** bırakıldı ve dosya bunu `_taban_v3_pinsiz_kalan` alanında yazıyor: *"DEVRALINACAK KARDEŞ YOK"*. Ölçülen değer 0.872. |
| **ADAY — ÖLÇÜLDÜ, REDDEDİLDİ** | `/tmp/stitchu-wt2`: `taban_v3.dress_bandeau_circle = 0.872` eklendi → **`OK figure-lint green`** (öncesi `1 FAILURE(S)`). `git checkout` ile geri alındı. **REDDEDİLDİ**: pinlenen sayı gate'in kendi ölçtüğü sayı = regen-vs-regen; dosyanın kendi yasağı. Ölçüm yeşile döndürüyor ama **kanıt üretmiyor.** |
| **MALİYET** | Dürüst çıkış iki tane, ikisi de bu gece değil: (a) devralınacak kardeş stil bulunup ondan taşınması (**V1**, dokunacağı dosya `contract/figure-bands.json`), (b) Damla'nın 0.872'yi gözle onaylaması (**TEK KAPI**). |

### 5. `sizechart_source_check` (#15)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | Kırmızı **BİLEREK** (TUR 18B). `contract/tables.json` `euSizeChart`: 7 kolonun 3'ü (bust/waist/hip) Burda'ya bağlı, 4'ü (`shoulderCM`, `backLengthCM`, `armLengthCM`, `neckCM`) `status: NONE` — 10 bedende 40 sayı, arkasında yayın yok. Bu **golden'ın köküyle akraba**: `52ae85c`'nin gerekçesi de tam olarak "kaynaksız `backLengthCM` kolonu EU44→46'da duruyordu" idi. Yani motor o kolonu terk etti; çizelge onu hâlâ satıyor. |
| **ADAY — ÖLÇÜLDÜ, REDDEDİLDİ** | `/tmp/stitchu-wt2`: 4 kolona `status: verified` + uydurma url + `published[]` = **çizelgenin kendi sayıları** yazıldı. ÖNCE: sourced 3 / UNSOURCED 4, kolon FAIL 4, probe 7/7, `FAILED (4)`. SONRA: sourced **7** / UNSOURCED **0**, kolon FAIL **0**, probe **6/7**, `FAILED (2)`. **REDDEDİLDİ** — uydurma kaynak testin ve kartın açık yasağı. |
| **★ İSTENMEMİŞ AMA ÖNEMLİ** | Bu sonda kapının kendi zayıflığını ölçtü: gate **yayının var olduğunu doğrulamıyor**. `https://example.invalid/probe` + çizelgenin kendi sayıları, dört kolon hükmünün dördünü birden düşürüyor. Kapıyı ayakta tutan tek şey o noktada kırmızıya dönen META-PROBE (`'source armLengthCM properly (hypothetical) removes its verdict': gate did NOT react`). Yani kapı "kaynak var mı"yı değil, "`_sources` kendi kendisiyle tutarlı mı"yı ölçüyor. |
| **MALİYET** | Gerçek yayın (Aldrich/Burda/ASTM) bulunup 40 sayının eşleşmesi — **V1**, dokunacağı dosya `contract/tables.json`. DAMLA-KUYRUK K10. |

### 6. `style_check` (#8)

| kalem | içerik |
|---|---|
| **KÖK TEŞHİS** | `engine/STYLE-PIN` dizini **diskte yok** (`ls -ld` → No such file or directory). Silen commit `af49514` (29 Tem, sahte galeri + style-pin artefaktları silindi). Kapı T17'ye kadar "PASS (nothing to enforce)" basıyordu; sıfır pin sıfır hüküm olduğu için **yeşilken de kapı değildi**. Bugünkü kırmızı, o sessiz yeşilin dürüst hâli. |
| **ADAY — YOK, ve neden yok ÖLÇÜLDÜ** | Uydurma aday yazılmadı. `engine/tests/style_check.mjs:3-4, 25-27`: pin'i bu test kendi çıktısından **üretemez** (regen-vs-regen); `scripts/repin-style.sh` onayı **terminalden Damla'nın yazmasını** bekliyor — *"onay ÖLÇÜMDEN değil KARARDAN gelir; boru hattından beslenen onay reddedilir."* Bu kapının tek adayı bir **insan kararıdır**; bir işçi onu ölçerek üretemez. |
| **MALİYET** | TEK KAPI (Damla'nın gözü) + `scripts/repin-style.sh`. Dokunacağı yer: `engine/STYLE-PIN/<style>.svg` + `engine/STYLE-PIN/STYLE-PIN.md`. |

---

## ÖZET — ALTI KIRMIZI, ÜÇ SINIF

| kırmızı | sınıf | ölçülmüş aday var mı | bu gece kapanabilir miydi |
|---|---|---|---|
| `golden_check` | **motorun ilan edilmemiş davranış değişikliği** (52ae85c) | var, ikisi de reddedildi | hayır (pin = Damla kararı) |
| `recipe_dress_check` | **aynı kök**, ikinci hat geride kaldı | var, FAIL 10→9 ölçüldü | kısmen (1 satır), kalanı V1 |
| `contract_check` | **ilan edilmiş bedel** | var, GREEN ölçüldü | hayır (Damla kararı) |
| `figure_check` | **kaynak eksiği** (pinlenecek kardeş yok) | var, yeşil ölçüldü, **reddedildi** | hayır (regen-vs-regen) |
| `sizechart_source_check` | **kaynak eksiği**, bilerek kırmızı | var, **reddedildi** + kapının zayıflığı ölçüldü | hayır (yayın gerek) |
| `style_check` | **karar eksiği** | **yok** — ve yokluğu ölçüldü | hayır (insan kararı) |

Altısının **hiçbiri tolerans gevşetmesiyle** kapanmıyor. İkisi tek mühendislik köküne
(`52ae85c`) iniyor, üçü kaynak/karar eksiği, biri ilan edilmiş bedel.

---

## ÖLÇÜLEMEYEN / YAPILAMAYAN

- `52ae85c` sonrası 4 commit (`0cb5d23`, `12694e0`, `06911d9`, `11c199a`) tek tek
  ölçülemedi: aynı build dizininde obje karışması link hatası verdi
  (`SleeveBlock::draft` undefined), temiz rebuild süre tavanına sığmadı. Bilinen:
  52ae85c → 8356 satır, HEAD → 9651 satır, aradaki **1295 satır** bu 4 commit'e ait
  ama **hangisine ne kadar DOĞRULANMADI**.
- `a571407` ctest'inin 20 kırmızısı, `engine/dist/` ve `engine/.venv-dxf` worktree'de
  bulunmadığı için gürültülü. ENV/REAL ayrımı hata metninden yapıldı (A3'te listeli),
  ama ENV sayılan 10 testin temiz ortamda gerçekten yeşil olacağı **DOĞRULANMADI**.
- `style_check`'in pinli hâli hiç koşturulmadı (pin üretilemedi), bu yüzden "pin varsa
  kapı ne diyor" **ÖLÇÜLMEDİ**.
- `recipe_dress_check`'in `cutLine` nokta-sayısı farkının mm karşılığı ölçülmedi:
  diziler farklı uzunlukta, nokta-nokta karşılaştırma tanımsız.
- TEK KAPI (Damla'nın gözü) bu kartta koşulmadı.
