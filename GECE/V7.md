# V7 — KOL (çekirdek fazı) · 24-25 Ağu 2026

Faz öncesi commit: `e4249b7` · Kartlar: `GECE/KART/V7-{R,A,B,C,D,E,F,G,H}.md`
Hakem hükmü: **GEÇTİ** (`GECE/KAPI.md`, commit `067e084`)

---

## ÖLÇÜLEN (sayı + komut + hash)

### Açılış tabanı — ŞEF KENDİ KOŞTU, DEVRALMADI
`ctest --test-dir engine/build --output-on-failure` @ `e4249b7`
→ **113 test · 6 kırmızı** · 274.55 sn · `GECE/log/V7.ctest.opening.txt`
Kırmızı AD kümesi devralınan listeyle **birebir**: `flat_pattern_agree_check ·
flat_artifact_census · style_check · sizechart_source_check · contract_check ·
figure_check`.

### ★ İKİ DEVRALINAN İDDİA ÖLÇÜLDÜ, İKİSİ DE YANLIŞTI

**1. "KOL 0/8 İFADESİZ — flat sekizini de AYNI çiziyor" — DÜŞTÜ.**
`node engine/tests/flat_expresses_spec_check.mjs` → `RATCHET sleeveStyle
UNEXPRESSED **0/0**`, exit 0. "0/8" doğru okunuşu **"8'in 0'ı ifadesiz"**;
kol bugün kapının **en temiz ekseni**. Sekiz değer = 4 kanonik + 4 **beyanlı
eşanlam**, ve dört ayrı geometri ölçüldü (kontur birimi):
`{straight·fitted·set-in}` 2705.08 · `{balloon·bishop·puff}` 2996.36 ·
`{cap}` 2354.75 · `{none}` 1917.76. Dört eşanlam kanonik eşiyle **0.00u**.
İfadesiz olan **yaka (4/4) ve omuz (1/1)** — `collarType`'ın 7 değerinden
4'ü (`shirt·mock·flat·crescent`) `stand` ile 0.00u farkla ÖZDEŞ çiziliyor.
(V7-B, V7-E)

**2. "Sevk edilen hat yüzey motoru / 2B çizici" — ÖLÇÜLDÜ, İKİSİ DE DEĞİL.**
`web/js/engine.js:56` → `web/vendor/stitchu-engine.js` → `engine/wasm/bindings.cpp:339
draftJSON` → `engine/src/garment.cpp:303,:621` → `engine/src/sleeve.cpp
SleeveBlock::draft`. `grep -c surfacepattern engine/build-wasm.sh` = **0**.
Takipli `backend/engine/stitchu-worker.wasm` (1042915 B) ham dizgi sayımı:
`sleeveStyle 5 · Sleeve 16` vs `SheathOptions 0 · surfacepattern 0 ·
shoulderSeam 0`. **Yüzey motorunun ürün tarafında tüketicisi YOK** — yalnız
`h10_gate_check_LEGACY.cpp` ve `engine/tools/surface-pattern.cpp`. (V7-A, V7-F)

### ★ V7'NİN ASIL BULDUĞU: KAPI BİR TAUTOLOJİYMİŞ
`engine/src/bodice.cpp:509` skaler `armholeLength` yazıyor →
`engine/src/sleeve.cpp:55` ona uyuyor → `engine/src/validator.cpp:300` **aynı
skalerle** doğruluyor. Bugüne kadarki "0.00mm oyuk↔kapak uyumu" bir kenar↔kenar
uyumu DEĞİL, **aynı sayının kendisiyle uyumu**ydu. Eşleme ayrıca üç tahmine
dayanıyordu: parça adı alt-dizgisi `"Sleeve"` (`:282`), sabit komut indeksi
`commands[0..2]` (`:289-295`), ve skaler kopya (`:300`). (V7-A)

### KENAR KİMLİĞİ KURULDU (V7-C, `cb6f078`) — V6'nın V7'ye bloke teşhisi
`engine/src/geometry.hpp:40-71` `struct EdgeRole`: `role` + kapsayıcı
`firstCommand..lastCommand` + `start`/`end` uç-nokta çapası.
**Uzunluk alanı bilerek YOK** — kenar adreslenir, sayı kopyalanmaz.
`geometry.cpp:182-231` `edgePathOf()`/`edgeLengthOf()`; aralık taşarsa ya da
çapa tutmazsa **BOŞ döner** (yanlış kenar sunmaz). Adlar ÇİZEN kodun yanında:
`bodice.cpp:518-525,:709-715,:743-748` · `sleeve.cpp:186-208,:126-133`.
wasm'a `engine/wasm/bindings.cpp:283-301` ile çıkıyor.

Adlandırılmış kenar: **ÖNCE 0 → SONRA 5** (kollu üst, 4 rol adı).
`git show HEAD:web/vendor/stitchu-engine.js | grep -c edgeRoles` ÖNCE = **0**.
EU38: `armhole_front 211.3405 · armhole_back 192.9188 · sleeve_cap 420.3840 ·
sleeve_underarm 421.1106` (×2).
**İlk kez kenar↔kenar cap ease: +16.1246mm = %3.9887.**
Yeni kaynak dosya: **0** (5 dosya genişletildi). Golden **cmp sessiz**.

### ★ ROL BAYATLAMASI TEORİK DEĞİL, CANLI ÇIKTI (V7-D)
Tüketici taşınır taşınmaz **beş kapı düştü** — `locket_check · cup_check ·
yoke_check · boxpleat_check · compose_check`, hepsi aynı cümleyle
(`stale command range or endpoint anchor`). Bir bodice konturunu yeniden yazan
**her** pas adı bayatlatıyordu ve **eski skaler tüketici bunu göremezdi**.
Çözüm: `reanchorEdgeRoles()` (`geometry.*`), `garment.cpp`'de tek boğaz noktası,
`locket.cpp` `rebuildFront()` sonrası yeniden adresleme.
**YARIM AD, AD DEĞİLDİR:** cup seam yalnız ÖN bedeni yeniden yazıyor →
`armhole_back` ADLI + `armhole_front` ADSIZ. Var olanı toplamak kapağı
**yarım oyuğa** göre yargılardı; kural koda yazıldı.

### YENİ KAPI: `sleeve_cap_ease_check` (V7-D, `383936e`)
`node engine/tests/sleeve_cap_ease_check.mjs` → **48 (spec × beden) satırı,
0 ihlal**, exit 0. Sevk edilen **wasm** artefaktını yükler, iki yayı ÇİZİLEN
komutlardan yeniden ölçer.
- Yedirilen başlar (32 satır) cap ease EU34→EU48: **+15.0430 … +19.1027mm**
  (%3.9887–4.0549). EU38: oyuk 404.2594 / kapak 420.3840 / **+16.1246mm**.
  Prenses oyuğu **dört** kenar parçasından toplanıyor.
- **48/48 işaret pozitif · 32/32 tavanın altında.**
- Beyan ↔ ölçüm: motor **%4.00** beyan ediyor (`fabricease.hpp kCap[0]`);
  |fark| en çok **0.0549** puan. **Eşik KONMADI** (kaynaksız eşik yasak §7.6),
  fark BASILIYOR.
- Büzgülü başlar: fazlalık **+56.92 … +393.46mm** — tavan uygulanMADI
  (kaynak *cap ease* hakkında; büzgü yedirme değil).

**EŞİĞİN KAYNAĞI (§5.1/§7.6, V7-R):** tavan **≤1½ in = 38.1mm** — Linda Lee,
*Setting in a Sleeve*, slayt 6, verbatim. Bandın **ALT ucu için YAYIN YOK** →
REPORTED, yargılanmadı. **Puf/balon için NİCEL kapı KURULMADI**: V7-R üç
bağımsız kaynakta puf/balon sınıflandırma eşiği BULAMADI, kaynaklar açıkça
"tasarım kararın" diyor. Kaynaksız eşik kapıya giremez.

### §4.2 BOŞ TEST — BİRİNCİL USUL (derleme bağı sıfır)
`GECE/log/V7-D.bostest.txt`: faz öncesi `e4249b7`'nin sevk ettiği wasm'ın
ürettiği JSON artefaktı (240 parça, **0 adlandırılmış kenar**) aynı alete
verildi → **EXIT 1, 184 ihlal**. Bugünkü artefakt → **EXIT 0**.
Motor hiç çağrılmadı, derleme hatası değil.
⚠ Kırmızılığın tamamı `[a]` sınıfından (metadata yok); `[b]`/`[c]` eski motorda
sınanamadı — hakem bunu kayda geçirdi.

### §4.5 MUTASYON — kapı ÜÇ ayrı yönden kırılıyor
`GECE/log/V7-D.mutasyon.txt`: `cap-grow +40mm` → 32 ihlal `[c]` ·
`cap-shrink −40mm` → 32 ihlal `[b]` · `role-stale +5mm` → 88 ihlal `[a]`.
Geri alınca **PASS**. Kapı süs değil.

### ★ HAKEMİN BULDUĞU CANLI BUG — DÜZELTİLDİ (V7-H, `8a8424f`)
Hakem, bu fazın SOKTUĞU bir bug ölçtü: `engine/src/validator.cpp:419` format
dizgisi 6 belirteç isterken 7 argüman geçiliyordu; `%.1f`'e `const char*`
gidiyordu. `fmt` = `vsnprintf` + varargs, format attribute YOK → derleyici
sessizdi. **Kullanıcıya UYDURMA SAYI basan bir mesaj** — üstelik "sayı uydurma"
tezi üzerine kurulmuş bir fazda.
- ÖNCE: `cap seam 391.0 vs DRAWN armhole 0.0 (-60261330 named edge(s)) + 0% ease
  = 4.0 — convergence missed by 391.0 mm`
- SONRA: `cap seam 391.0 vs DRAWN armhole 375.9 (4 named edge(s)) + 4% ease
  = 391.0 — convergence missed by 0.0 mm` (375.9 × 1.04 = 391.0 ✓)
`GECE/log/V7-H.fmt.txt`. Kod yolu izole sürücüyle değil **gerçek kod yolunda**
ateşlendi (guard geçici `if(true)`, sonra geri alındı).
**KORUMA:** `fmt`'ye `__attribute__((format(printf,1,2)))` eklendi
(`validator.cpp:24-31`). Kanıt: bozuk dizgi geri konulunca **4 `-Wformat`
uyarısı** tam argüman konumlarında. Attribute başka HİÇBİR yerde uyarı
doğurmadı → gizli başka format hatası YOK.

### SİCİL↔ARTEFAKT ÇELİŞKİSİ — KARARA BAĞLANDI (V7-F, `b85c2c8`)
**Karar (B):** motorun bastığı kol, sicilin `sleeve` operatörü DEĞİLDİR.
Kanıt: sicildeki 10 shipped/flagged bağın **10'u** `SheathOptions::`'a bağlı;
`grep -c SheathOptions engine/src/sleeve.cpp engine/src/garment.cpp` → **0 / 0**.
`sleeve.status` **absent kaldı** (değiştirilmedi). Çelişki bir yalan değil
**KAPSAM KAYMASI**: sicil `surfacepattern.cpp`'yi anlatıyor, sevkiyat
`garment.cpp`'yi yapıyor. Tam kanıt `GECE/V7-F.md`.
- İfade etkisi ÖNCE/SONRA: **15/68 = %22.1 → %22.1, fark 0.** Kol tek başına
  68 okumanın **35'ini** düşürmeye devam ediyor.
- ⚠ **V0-0B'nin %36.8 ölçüm aleti REPODA YOK** — tek seferlik yazılmış,
  commit'lenmemiş. **Bugün yeniden üretilemiyor.** Uydurma sayı yazılmadı.

### KOL GÖRÜNÜR — PNG ARTEFAKTLARI (V7-E, `4254c92`, RULES md.3)
`GECE/log/V7-E.png/` — 7 PNG, hepsi geçerli PNG başlığı, en küçüğü 141423 B,
0 bayt YOK (`file -b`). Yeni alet yazılmadı; `render-pages.mjs`,
`render-garment-flat.mjs`, `raster.mjs` koşuldu.
- Kalıp (EU38): `pattern-placket-shirt-dress-EU38.png` ·
  `pattern-puff-sleeve-dress-EU38.png` · `pattern-black-buttoned-blouse-EU38.png`
- Flat: `flat-sleeve-straight.png` · `flat-sleeve-balloon.png` ·
  `flat-sleeve-cap.png` · `flat-sleeve-none.png` (kısa kenar 2000px)
Kol paneli sayıyla: `Sleeve` commands 7 / cutLine **52** · `Puff Sleeve`
commands 7 / cutLine **74** · `Sleeve` commands 7 / cutLine **54**; üçünde
`issues: []`. Sevk hattı doğrulandı: `vendor.includes(dist)` = **true**,
fark tam 127 bayt = tek satırlık source-stamp yorumu.
**Hiçbir PNG'ye bakılıp hüküm verilmedi** (RULES md.3).

---

## KAPANAN/AÇILAN KIRMIZI (ad ad)

**AÇILAN: 0.** **KAPANAN: 0** (miras 6'nın hiçbirine dokunulmadı).
Kapanış: `ctest` → **114 test · 6 kırmızı** · 272.74 sn ·
`GECE/log/V7.ctest.final.txt`. Test 113 → **114** (+`sleeve_cap_ease_check`).
Kırmızı AD kümesi açılışla **birebir** — hakem iki logu kendi `diff`ledi.

### ★ BU FAZ RULES md.9'U BİR KEZ İHLAL ETTİ VE AYNI GECE KÖKÜNDEN GERİ ALDI
`vocab_reference_check` gece **7. kırmızı** olarak düştü
(`garment 1186→1189 (+3)`, `sleeveCap 146→147 (+1)`). Şef kökü izole etti:
`grep -cw garment contract/garment-spec-v2.json` → `e4249b7`'de **4**, gece **7**;
`engine/src/validator.cpp` **22→22 (kod tarafı temiz)**. Repo kopyasında YALNIZ
o dosya faz-öncesi hâline döndürülünce kapı `HUKUM: YESIL, delta -6` bastı.
**Kırmızının TAMAMI V7-F'in sicile yazdığı ŞERH DÜZ YAZISIYDI** — kod değil,
sözlük değil. Sözlük bu gece hiçbir eksen/değer KAZANMADI.
**Onarım (V7-G, `75c9103`):** şerh **işaretçiye indirildi**; çıkarılan **10
dizginin 10'u** `GECE/V7-F.md` §5'te kelimesi kelimesine duruyor (`grep -F`
kanıtı raporda). **Taban KESİLMEDİ** (`--baseline` kullanılmadı), SCOPE/EXCL
ellenmedi, hiçbir kapı gevşetilmedi.
Hakem bunu ayrıca yargıladı: **"GİZLEME DEĞİL, YER DEĞİŞTİRME"** — ama metnin
ratchet KAPSAMI DIŞINA taşındığını da kayda geçirdi. Karar `DAMLA-KUYRUK`
**K-V7A** olarak kuyruğa düştü, varsayılan (A).

⚠ **KAPININ BİLİNEN SINIFI:** ratchet `grep -w` ile ham kelime sayıyor;
`contract/` altındaki HERHANGİ bir Türkçe açıklama cümlesinde bir eksen adı
geçince kapı, sözlük hiç büyümemiş olsa da kırmızıya düşer. Betiğin kendi
başlığı bunu "KNOWN NOISE, unfixed on purpose" diye kabul ediyor. Bu gece
o sınıfın **ikinci tekrarıydı**.

---

## AÇIK BORÇ (gizlenmedi — kapının kendisi adıyla basıyor)

**ADSIZ OYUK BORCU.** Oyuk hiç adlandırılmamışsa `validator` eski skaler
`bodice.armholeLength`'e düşüyor. Reddetmek doğru çizilmiş kalıplara sahte
"dikilemez" derdi ve dört kapıyı kırmızıya sokardı. **Düşüş SESSİZ DEĞİL** —
`sleeve_cap_ease_check` her koşuda borcu adıyla sayıyor:
`bardot_off_shoulder` · `yoke_top` · `cupseam_bustier` **ADSIZ** ·
`boxpleat_swing` **ADLI**. Tautoloji sevk edilen varsayılan hatta ÖLDÜ,
bu üç pas'ta yaşıyor.

Diğerleri:
- `sleeve_underarm` **çifti yargılanmıyor** (sadece çözüldüğü şart); "iki kenar
  eşit uzunlukta" kapısı kurulmadı.
- Prensesde oyuk parçalarının **süreklilik/sıra** şartı yok ("üstün sonu altın
  başıdır" kapısı kurulmadı).
- Yaka, yan dikiş, bel, etek ucu hâlâ **ADSIZ** — dört ad verildi, kenar
  kimliğinin geri kalanı açık.
- `engine/tools/recipe-json-dump.cpp` `edgeRoles` **basmıyor** → native JSON
  dökümü bu kapıyı besleyemiyor; kapı wasm paketinden okuyor.
- ★ **[S2] işaret şartı kaynağıyla aynı çizgide DEĞİL** (hakemin bulgusu):
  V7-R'in önerisi "KESİM çizgisinde pozitif"; `PatternPiece::commands` **DİKİŞ**
  çizgisi. Buğra dikiş çizgisinde 8/8 NEGATİF ölçüldü → **bu kapı referans
  kalıbın kendisini kırmızı düşürürdü.** Motor %4 çözdüğü için bugün
  ateşlemiyor. **KARARA BAĞLANMADI.**
- ★ **[S1] tavanı (38.1mm) bugün hiçbir şeyi bağlamıyor**: ölçülen bant
  15.04…19.10mm. Fiilen bağlayan tek şart **rol çözünürlüğü ve işaret**.
- ★ Ölçülen %4.00 ± 0.055 puan, beyan edilen `kCap[0]`'a karşı → çizilen oyuk
  ile skaler bugün pratikte ÖZDEŞ; "iki bağımsız büyüklük" iddiası sayısal
  olarak **henüz boş bir farkı ölçüyor** (hakem kaydı).
- `fmt` sessizce kırpıyor (`validator.cpp:31`, `char buffer[256]`,
  `vsnprintf` dönüşü atılıyor) — ayrı iş.
- `engine/tests/` `vocab_reference_check` KAPSAMI DIŞINDA → yeni kapı dosyaları
  o sayaca hiç girmiyor.
- `contract_check` faz-öncesinde de sonrasında da KIRMIZI; bu faz
  `contract/garment-spec-v2.json`'ı **kapısı kırmızıyken** düzenledi (hakem
  kaydı). Miras, büyümedi, ama sözleşme değişikliği yeşil kapıdan geçmedi.
- `h10_gate_check` **Disabled** (115 kayıtlı, koşan 114) — sebebi
  **DOĞRULANMADI**.
- `render-garment-flat.mjs:381` `hasSleeve` HAM değer okuyor: alan dışı yazım
  (`kimono`/`dolman`/`ZZZNONSENSE`) reddedilmiyor, düz set-in kolla
  **geometri-özdeş** çiziliyor → RULES invariant 1'in istediği Err YOK.
- `render-garment-flat.mjs:648` **`raglan` arka kapısı**: raglan geçerli bir kol
  değeri olmadığı halde kol ekseninden raglan topolojisini tetikliyor.
- `cap` motorda kalıp tarafında YOK (`engine/vocab.json` `sleeveStyle` =
  `["none","straight","balloon"]`); `cap` yalnız `contract/spec-grammar.json`
  ve 29 spec JSON'unda → kalıp render'ı yapılamadı.
- Web UI 8'in **3'ünü** sunuyor (`web/js/create.js:42`); `cap` yalnız
  `sleeveStyle==='straight'` iken görünüyor → **balloon+cap UI'dan seçilemiyor**.
- **GİZLİLİK:** `git ls-files patterns_real/` → **41 dosya takipli** ve
  `.gitignore`'da HİÇ YOK; `dataset/` → 6 dosya takipli. CLAUDE.md ikisini de
  "ASLA push edilmez" diyor. `contract_check`'in metni "49" derken saydığı **41**.

---

## SONRAKİ FAZA DEVİR (ÜÇ SAYI — şef kendi ölçtü, devralmadı)

1. **KIRMIZI = 6 · TEST = 114.** Açılış `GECE/log/V7.ctest.opening.txt`
   (113/6, 274.55 sn, `e4249b7`), kapanış `GECE/log/V7.ctest.final.txt`
   (114/6, 272.74 sn). **AD kümesi birebir**; hakem bağımsız `diff` aldı.
   +1 test = `sleeve_cap_ease_check`.
2. **SÖZLÜK TABANI = 10438 @ `495d58a` · bugün 10432 (−6, tabanın ALTINDA),
   YEŞİL.** `bash engine/tests/vocab_reference_check.sh`. Yalnız DÜŞEBİLİR.
   ⚠ Gece bir kez FAIL'e düştü ve kökünden geri alındı (yukarı bak).
3. **KİLİTLİ BANTLAR + YENİ BANT — kol yedirmesi** (`sleeve_cap_ease_check.mjs`):
   `[S1]` tavan **38.1mm** (Linda Lee slayt 6, YAYINLANMIŞ) · `[S2]` işaret
   **POZİTİF** zorunlu · `[S3]` ölçülen bant **15.0430…19.1027mm** REPORTED
   (alt uç için yayın YOK) · `[S4]` puf/balon nicel kapısı **YOK** (kaynak yok) ·
   ADSIZ oyuk borcu **3 spec** (`bardot_off_shoulder · yoke_top ·
   cupseam_bustier`). Yalnız SIKILAŞABİLİR.
   ★ İFADE RATCHET: **kol 0/0** (ifade EDİLİYOR) · **yaka 4/4** · **omuz 1/1**
   — V7 yaka ve omza DOKUNMADI, V5'ten aynen devrediyor.
   Taban bantlar (§4.1) V6'dan değişmedi: `draftJSON` 1.030 ms ·
   `gradeJSON` 8.198 ms · native↔wasm 1e-4 mm · çağrı yolu main thread.

## ★ FOTO→SPEC İSABETİ: **%20.0 → %20.0** (V11'in 3. sorusu)
V7 görü hattına DOKUNMADI; prompt ve görü modeli değişmedi (§5.3 veto).
v2 ifade edilebilirliği **15/68 = %22.1 → %22.1, fark 0** (V7-F ölçtü).
Kol tek başına 68 okumanın **35'ini** düşürmeye devam ediyor — çünkü V7 kolu
**ÇİZİM ve DOĞRULAMA** tarafında kurdu, **SİCİL** tarafında `sleeve` bilinçli
olarak absent bırakıldı (karar B, kanıtlı).

## PROTOKOL NOTU (V8/V9 şefine)
- Orakçı işletildi ama **V7-D 60 dk tavanını ~2s20dk'ya taştı** (kök engel
  ortada çıktı: rol bayatlaması beş kapıyı düşürdü). İşini COMMIT'ledi, oturum
  kesilmedi. Diğer 7 işçi tavanın altında kaldı.
- **Şef açılış ctest'ini KENDİ koştu** — V6'nın 1. protokol dersi uygulandı.
- **Hakem bu fazın SOKTUĞU canlı bir bug buldu** (V7-H). Hakem kapısı süs değil:
  fazın kendi 8 işçisi ve 5 yeşil kapısı o bugu görmedi.
