# V0 TUTANAĞI — DÜRÜST ENVANTER (24 Ağu 2026)

Faz kuralı: ölçüm var, ONARIM YOK. Ana ağaçta `engine/`, `contract/`,
`recipes/`, `web/` altında tek bayt değişmedi (`git status --porcelain
engine contract recipes web` → boş, hakem tarafından da doğrulandı).

v6 §1'in sayıları HİPOTEZDİ ve bu gece yeniden ölçüldü. §1 ile çelişen her
kalem aşağıda işaretlidir; §1 sessizce ölür, tartışma açılmaz.

Kartlar: `GECE/KART/V0-0{R,A,B,C,D,E,F}-*.md` (6'sı paralel, 0F sıralı).
Kart setleri kapalı listeden dağıtıldı, çıktı dosya kümeleri kesişmiyor.

---

## 1. ÖLÇÜLEN (sayı + komut + hash)

### 1.1 Makine — ctest

İKİ BAĞIMSIZ KOŞU, aynı sonuç:

| koşu | tanımlı | koşan | kırmızı | disabled | süre | log |
|---|---|---|---|---|---|---|
| işçi 0A | 106 | 105 | 6 | 1 | 284.70 s | `GECE/log/V0-0A.ctest.txt` |
| şef (bağımsız) | 106 | 105 | 6 | 1 | 269.29 s | `GECE/log/V0-SEF.ctest.txt` |

Komut: `ctest --test-dir engine/build --output-on-failure`
(build `CMAKE_BUILD_TYPE=Release`, rebuild gerekmedi).

KIRMIZI ADLARI (iki koşuda `diff` ile birebir aynı):
`golden_check` · `style_check` · `sizechart_source_check` ·
`recipe_dress_check` · `contract_check` · `figure_check`

KOŞMAYAN: `h10_gate_check (Disabled)` — `engine/CMakeLists.txt:690`.

★ §1'İN HİPOTEZİ ÇÜRÜDÜ: 7 değil **6**. Fark `bundle_fresh_check` ve o bu
gece YEŞİL (hakem `GECE/log/F-N.ctest.after.txt` ile karşılaştırdı). Miras
kırmızı AD kümesi büyümedi, KÜÇÜLDÜ.

### 1.2 Altı kırmızının sınıfı ve kökü

Sınıfı işçi değil HAKEM koydu (v6 §6/V0 emri):

| kırmızı | sınıf (hakem) | kök teşhis (0F) |
|---|---|---|
| `golden_check` | bilinçli bayat pin | `52ae85c` scye derinliği formülü |
| `recipe_dress_check` | sınıflanamaz → 0F kökü buldu | AYNI `52ae85c` |
| `style_check` | kaynak eksiği | `engine/STYLE-PIN` `af49514`'te silindi |
| `sizechart_source_check` | kaynak eksiği | 7 kolonun 4'ü UNSOURCED |
| `figure_check` | kaynak eksiği | `taban_v3` 16 pin, `dress_bandeau_circle` yok |
| `contract_check` | üç sınıfın hiçbiri | ilan edilmiş karar borcu: `patterns_real` 41 takipli |

★ **İKİ KIRMIZI TEK KÖK.** `golden_check` ve `recipe_dress_check` aynı sayıya
basıyor: EU38 armhole Y **5.5996 mm** (pear 11.9996, bigNeck 24.7996).
Kök `52ae85c` (23 Ağu), `engine/src/bodice.cpp:903-907` — scye derinliği
kaynaksız `backLengthCM`'den Aldrich'in büst formülüne geçti; ne pin ne
reçete taşındı. Bisect: `52ae85c`'nin ÖNCESİNDEKİ 9 commit'te `golden_dump`
çıktısı pinle **BAYT-ÖZDEŞ** (`a571407` doğrulandı, hakem yeniden koştu).

GOLDEN SAPMASININ BÜYÜKLÜĞÜ:
`./engine/build/golden_dump` + satır-pozisyonu eşlemesi →
**9651 / 23406 satır (%41.23)** farklı · max **62.7764 mm** · medyan
**5.6000 mm** · p90 49.7051 · 20–60mm kovası 3659 · >60mm 112.
Etek parçalarında max **0.0001 mm** — sapma gövde/oyuk bölgesinde.
(Kaba `diff | grep -c '^<'` yöntemi **9661** verir; fark yöntem farkıdır,
hakem ikisini de koşturup uzlaştırdı.)

`recipe_dress_check` sapması mm cinsinden İLK KEZ ölçüldü (test yalnız
"DIFFERS" basıyordu): Top Front max **48.4634 / 57.0341 / 45.2878 mm**,
Top Back 37.88 / 47.44 / 28.54, Bias binding **61.30 / 83.04 / 55.57**.

ÖLÇÜLMÜŞ ÇÖZÜM ADAYLARI (§4.7; hiçbiri ana ağaca bırakılmadı):
- `recipe_dress_check`: metinsel reçete düzeltmesi → **PASS 115→116, FAIL 10→9**
  (grainline DIFFERS 6→0, kumaş FAIL 1→0). Kalan FAIL geometri kökünde.
- `contract_check`: `git rm -r --cached patterns_real` → **GREEN, exit 0**.
  Bu bir Damla kararıdır, tek taraflı yapılmaz.
- `figure_check`: gate'in kendi 0.872'sini pinlemek → yeşil, ama **REDDEDİLDİ**
  (regen-vs-regen; kırmızıyı susturur, iyileşme mühürlemez — v6 §3.5/4).
- `sizechart_source_check`: kolon kaynaklama → FAIL 4→2, ama **REDDEDİLDİ**
  (uydurma kaynak; K10 yasağı).
- "Motoru `a571407`'ye döndür": **REDDEDİLDİ, ölçüldü** — 3 kırmızı kapanır,
  **6 yeni kırmızı ad doğar** → RULES §9 gereği geri alınır.
- `style_check`: aday YOK. Yokluğun gerekçesi ölçüldü, mazeret değil: pin'i
  test kendi çıktısından üretemez (dosyanın kendi yasağı) ve
  `scripts/repin-style.sh` terminalden Damla'nın onayını bekliyor.

### 1.3 Görü hattı (0B)

`node engine/tools/foto-spec-olcum.mjs --offline` →
**FOTO 5 · TAM DOĞRU SPEC 1 (%20.0)** · ALAN YARGISI 51, tutan 47 (%92.2) ·
HATA SINIFI: **GÖRME 4 (%100) · KELİME 0 · MOTOR 0** ·
serbest kanal 26 terim, **26'sı sicilde YOK (%100)**.

Alan bazlı skorlar: `node vision/eval.js eval/live-2026-08-22.json` → **47/50 (%94)**
(şef bağımsız doğruladı) · opus-predictions 67/78 (%86) · siglip 105/162 (%65) ·
clip 71/162 (%44). Foto bazında TAM eşleşme çok daha düşük: live 2/5.

★ v2 SÖZLEŞME KAPISI: 68 fotonun **25'i (%36.8)** bugünkü
`contract/garment-spec-v2.json` ile ifade edilebilir. **%50'si (34 foto)
SIRF KOLLU OLDUĞU İÇİN** düşüyor — `sleeve` operatörü `status=absent`.

★ KELİME LİSTESİ **ELLE YAZILMIŞ**: `vision-student/vocab.py:17,21,22,23`,
üreteç yok, yorum satırı "copied verbatim" diyor (şef doğruladı). Zaten
sürüklenmiş: `backend/worker.js:309` 9 neckline değeri, `vocab.py:17` 7 değer
→ aradaki örnekler `label_to_index`'te sessizce eğitimden düşüyor.

Ölçülemeyenler dürüstçe: 19 etiketli fotonun 14'ü (ücretli çağrı gerekir),
`vision-student` koşusu (dataset/labels yok), CLIP/SigLIP yeniden koşu
(ağırlık indirme = §5.3 kalıcı veto).

### 1.4 Vitrin (0C)

`docs/` + `web/` + README: 134 yüzey, **1248 iddia cümlesi**, 782 tekil,
127 dosya. Yargılanan 41 taşıyıcı iddia: **doğru 14 · YALAN 12 · kanıtsız 15**.

★ §1.8'in "18 iddia: doğru 0 · yalan 1 · kanıtsız 17" fotoğrafı ÇÜRÜDÜ —
sayım çok daha büyük ve yalan sayısı 12.

Duran-iddia (RULES §6 ihlali) **344 ham hit**, yanlış pozitif düşünce **292**:
byte-identical 254 · hazır 42 · zero issues 36 · bitti 7 · ALL PASS 1.
RULES'un kendi docs'u ihlal ediyor: `docs/ARCHITECTURE.md:73` tek satırda
hem `ALL PASS` hem `byte-identical`.

Ölü link: web içi **0** (`node engine/tools/site-health.mjs` → exit 0,
127 sayfa / 2597 iç ref / 124 sitemap url — şef bağımsız koştu);
docs/README hedefi **6 ölü**.

EN AĞIR YALAN — **SİTE MTM SATIYOR, MOTOR SABİT BEDEN**: 44 dosyada "your own
measurements", 23'ünde "no fixed sizes"; karşısında `README.md:3,7`
"deterministic FIXED-SIZE (EU34-48)… Not made-to-measure".
Diğerleri: EU34–52 iddiası (`shape-ratios.json:23` "EU50 … ABORTS") ·
dikiş payı 15mm ↔ ölçülen 10mm · "~218 KB bundle" ↔ gerçek **1.15 MB** (5.5×) ·
"77/77" / "8 suites" / "84/84" ↔ gerçek **106 tanımlı test**.

### 1.5 Sözlük dili (0D) — V2'nin girdisi

`engine/vocab.json`: **37 kapalı eksen, 132 enum değeri** (şef doğruladı).
Referans tabanı (V2'nin `vocab_reference_check` sayacı): eksen adı **7524**,
değer referansı ham **29703** / ekseni-tekil **2833**. Komutlar
`GECE/log/V0-0D.enum-refs.txt` içinde.

SEVK EDİLEN TARAF = **MENÜ**. `web/js/create.js` 33 picker, **sürekli girdi
(slider/number) SIFIR** (`grep -nE 'type=.(range|number)|slider'` → 0).
`GarmentSpec`'te 44 alanın **37'si** enum.
En yoğun malzeme dili SEVK EDİLMİYOR: `surfacepattern.cpp` (200 sürekli
parametre), `flatten.cpp`, `bodysurface.cpp`, `garmentshell.cpp`, `drape.cpp`,
`curvefit.cpp`, `nest.cpp`, `volume.cpp` — 8 dosya `build-wasm.sh`'in
listesinde yok. Tarayıcıdaki motor hâlâ 2B-formül hattı.

★★ **SESSİZ İKAME, SEVK EDİLEN HATTA — RULES DEĞİŞMEZ 1'İN İHLALİ.**
`sleeveCap=1.5` ile `sleeveCap=1` çıktısı BAYT BAYT AYNI (`sha=eaba7a7b64ed`),
hata yok, uyarı yok, `issues` artmıyor. `-0.5` → 0'ın çizimi.
Kök: `engine/wasm/bindings.cpp:94` `v.as<int>()`, `specparse.hpp:36`'ya
ulaşmadan kırpıyor. 26 int-enum alanının HEPSİNDE. String eksenler (10 adet)
dürüst hata veriyor.

★ **`collarType` 1..6'nın HEPSİ NaN koordinat üretiyor** → `"x":nan` geçersiz
JSON → tarayıcıda `JSON.parse` fırlıyor. 5 taban spec'te doğrulandı.
Yaka ailesi sevk edilen hatta KIRIK.

★ Menü kayması: motor 7 yaka tanıyor (`crescent` dahil), `web/js/create.js:34`
6 gösteriyor (`grep -c crescent web/js/create.js` → 0).

### 1.6 Sicil ve damar (0A M3/M4)

`contract/garment-spec-v2.json`: **15 operatör = 9 shipped · 1 flagged ·
5 absent** (şef bağımsız doğruladı: `Counter({'shipped':9,'absent':5,'flagged':1})`).
Absent olanlar: `gatheredOverlayLayer` · `sleeve` · `collarFamily` ·
`skirtFamily` · `zipperPiece`.
Topoloji: 8 eksende **28 değerin 14'ü (%50)** absent/flagged operatöre bağlı.
Shipped 9'un **4'ünün** kendi `defect` alanı var (`armholeNotch`: EU38
33.55cm vs Buğra 43.30cm, **%22 kısa**).

★ ANAYASA damarından sicilde **İSİM OLARAK BİLE OLMAYAN 11 kalem**: fiyonk ·
mini-düğme · fırfır/volan · peplum · lace-up · dantel/fisto · derin V ·
kayık · kare · sweetheart · halter. Sicil, damarın YASAKLADIĞI şeyi
(`zipperPiece`) adlandırıyor ama İSTEDİĞİ 11 şeyi adlandırmıyor.

DAMAR YÜZDELERİ (yöntem `GECE/V0-0A.md`'de ADIM ADIM yazılı, V11 aynı
yöntemle yeniden koşabilir): payda **21** ·
**GARMENT hattı 20/21 = %95.24** · **YÜZEY hattı 3/21 = %14.29**.
`grep -c surfacepattern engine/wasm/bindings.cpp` → **0**: iki hat aynı hat değil.

### 1.7 Wasm paritesi ve performans TABANI (0E, v6 §4.1 emri)

`node --expose-gc engine/tools/wasm-baseline.mjs --reps 200 --soak 5000`

- **PARİTE**: 187 spec × 3 beden = 561 blok, 23406 satır. Şekil farkı **0**,
  wasm hatası **0**, bayt bayt aynı 23281, sayısal farklı 125 (%0.53),
  **en kötü sapma 1.0000e-4 mm** = `printf` ↔ `toFixed` yuvarlaması.
  Native ile wasm birbirine eşit; İKİSİ BİRDEN pinden uzak (§1.2).
- **TAZELİK**: `bundle_fresh_check.sh` exit 0 ·
  `cmp engine/dist/stitchu-engine.js web/vendor/stitchu-engine.js` exit 0 ·
  `git rev-list --count b197ccf..HEAD -- engine/src engine/wasm engine/build-wasm.sh` → **0**.
  §1.3'ün "muhtemelen kapandı" hipotezi DOĞRULANDI, artık kanıt.
- **SÜRE TABANI**: `draftJSON` medyan **1.031 ms** (min 0.981 / p95 1.338 /
  max 1.560) · `+JSON.parse` 1.116 ms · `gradeJSON` EU34→48 medyan **8.800 ms**.
  Node ölçümü; tarayıcı ve modül yükleme dahil DEĞİL.
- **HEAP**: wasm-içi yüksek-su işareti **ÖLÇÜLMEDİ** — modül yalnız 4 embind
  fonksiyonu ihraç ediyor, `HEAPU8`/`_malloc` yok, `ALLOW_MEMORY_GROWTH=0`.
  Yerine: **5000 tekrar SURVIVED** (büyüyemeyen 64MB yığında sızıntı olsa
  abort ederdi), 1.198 ms/üretim (yavaşlama yok), `external` ve `arrayBuffers`
  delta **0.00 MB**.
- **ANA İPLİK**: **MAIN THREAD, Worker YOK.**
  `grep -rn "new Worker" web/js/ web/*.html` → **0 eşleşme** (şef doğruladı).
  Yükleme `web/js/engine.js:56-57`; çağrılar `create.js:730`, `create.js:865`,
  `studio.js:140/338/422`.
  → v6 §4.1 hükmü: süre TABANIN ALTINDA (1.03 ms tek üretim, 8.8 ms beden
  serisi) olduğu için **Web Worker refaktörü kapı DEĞİL, kuyruk kartıdır.**

### 1.8 Araştırma (0R) — eşiklerin kaynağı

Kaynaklanan tek eşik: **50 ms long-task** (W3C Long Tasks / MDN) ve anlamı
DAR — bir BLOKLAMA sınırı, bir iş bütçesi değil. "Kalıp 50 ms'de çıksın" bu
kaynaktan ÇIKMAZ. İkinci bant INP p75 (iyi ≤200 ms) ama alan verisiyle
(CrUX) tanımlı, tek makine ölçümüyle kıyaslanamaz.

**"Wasm kalıp üretiminin kabul edilen gecikme bandı" için yayınlanmış kaynak YOK.**
**"Şu kadar çağrıda şu kadar byte = sızıntı" diyen yayın YOK.**
Uydurulmadı. Sızıntı için tek deterministik gözlem `WebAssembly.Memory`
`buffer.byteLength` monotonluğu; iki sınırı yazılı: 64 KiB'den küçük sızıntı
görünmez, ve "büyümedi" ≠ "sızıntı yok".

---

## 2. KAPANAN / AÇILAN KIRMIZI (ad ad)

KAPANAN: `bundle_fresh_check` — v6 §1'in listesindeydi, bu gece ölçüldü ve
YEŞİL; kaynak commit'i `b197ccf`, sevk edilen bayt `web/vendor` ile özdeş.

AÇILAN: **YOK.** Ana ağaçta motor/contract/recipe/web değişikliği sıfır
(hakem `git diff --stat` ile doğruladı). Miras kırmızı AD kümesi büyümedi.

AÇIK KALAN 6: `golden_check` · `style_check` · `sizechart_source_check` ·
`recipe_dress_check` · `contract_check` · `figure_check` (hepsi §1.2'de
kök + aday ile).

Kapı satırları: `GECE/KAPI.md`. 4.7 önce KALDI hükmü aldı, 0F kartıyla
kapatıldı ve YENİDEN yargılanıp GEÇTİ. Eski KALDI satırı silinmedi.

---

## 3. SONRAKİ FAZA DEVİR (üç sayı)

1. **KIRMIZI = 6** (tanımlı 106, koşan 105, disabled 1). İki bağımsız koşuda
   aynı ADLAR. Hipotezdeki 7 değil 6; `bundle_fresh_check` yeşile döndü.
2. **GOLDEN SAPMASI = 9651 satır (%41.23), max 62.7764 mm, TEK KÖK `52ae85c`** —
   `golden_check` ve `recipe_dress_check` bu tek commit'ten türüyor;
   `52ae85c`'nin öncesi pinle bayt-özdeş.
3. **SİCİL = 15 operatör (9 shipped / 1 flagged / 5 absent)**, damarın 11
   kalemi sicilde İSİMSİZ, foto damarının **%50'si sırf `sleeve` absent
   olduğu için** sözleşmede ifade edilemiyor.

## 4. TABAN BANTLARI (v6 §4.1 — sonraki fazlar bunu sessizce aşamaz)

- `draftJSON` tek üretim: medyan **1.031 ms** (p95 1.338)
- `gradeJSON` EU34→48: medyan **8.800 ms**
- 5000 tekrar soak: **SURVIVED**, 1.198 ms/üretim, external delta 0.00 MB
- Native↔wasm parite: en kötü **1.0000e-4 mm** (yuvarlama)
- Çağrı yolu: **main thread** — Worker refaktörü kuyruk kartı, kapı değil

## 5. KART DIŞI, KUYRUĞA/SONRAKİ FAZA NOT (dokunulmadı)

- `bindings.cpp:94` sessiz int ikamesi = RULES değişmez 1'in sevk edilen
  hatta ihlali. V2'nin sözlük reformunun ilk hedefi.
- `collarType` 1..6 NaN → tarayıcıda `JSON.parse` fırlıyor.
- `sizechart_source_check` yayının VAR OLDUĞUNU doğrulamıyor; `_sources`
  kendi kendisiyle tutarlı mı diye bakıyor (`https://example.invalid/probe`
  ile 4 kolon hükmünün 4'ü birden düştü).
- `armholeDepthFactor = 0.44` `engine/src/bodice.hpp:96`'da hâlâ duruyor
  ("LEGACY" yorumlu) ve K0 sabit paritesi 27/27 PASS veriyor — kapı
  SABİTLERİ kilitliyor, FORMÜLÜ kilitlemiyor.
- `contract_check` çıktısı "bugun 49" ilan ederken test bugün **41** sayıyor.
- `recipes/shift-dress-square-spaghetti.json`'un ikinci kopyası `web/recipes/`
  altında, ayrı commit'ten (`46eb99f`); test yalnız kök kopyayı okuyor.
- `contract/garment-spec-v2*.json` üçlüsü (98 `"enum"` bloğu) motor yolunda
  OKUNMUYOR; motorun tek sözlüğü `engine/vocab.json`. Hangisi otorite —
  DOĞRULANMADI, V2 sökmeden önce karara bağlamalı.
- `vision/README.md` bayat (%86 yazıyor, 22 Ağu ölçümü %94).
- İki ayrı terim sicili: `contract/terms.json` vs `dataset/vocab-canonical.json`.
- RULES.md sat.4 "agent context = ENV + RULES ONLY" diyor ama harness
  `CLAUDE.md`'yi her oturuma otomatik enjekte ediyor — kural ağaçta yazılı,
  kodda zorlanmıyor. Karar Damla'nın.
- `GECE/` altında takipli bir `.DS_Store` (6148 bayt).
