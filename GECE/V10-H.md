# V10-H — YENİ KIRMIZI `generated_ratchet_check` KAPANDI (rapor)

Kanıt dosyaları: `GECE/log/V10-H.kapi.txt` (kapının önce/sonra çıktısı + manifest
diff'i), `GECE/log/V10-H.ctest.txt` (4/4), `GECE/log/V10-H.landing.txt`.

Kapı susturulmadı, `-E` ile dışlanmadı, CMake'ten çıkarılmadı, tabanı yükseltilmedi.
`generated_ratchet_check.sh` ve `landing_truth_check.mjs` DEĞİŞTİRİLMEDİ.

---

## 1. KAPININ BAŞLANGIÇ HÜKMÜ

```
$ bash engine/tests/generated_ratchet_check.sh
EXIT=1 · bytes moved: 44 · declared-but-missing: 0 · undeclared: 0
HUKUM: FAIL (44)
```

44 dosyanın dizin dağılımı (aynı komutun `FAIL bytes` satırları sayıldı):

| dizin | dosya | üreteci | bugün koşuyor mu |
|---|---:|---|---|
| `web/styles/` | 24 | `engine/tools/gen-style-pages.mjs` | **EVET** (bu turda onarıldıktan sonra) |
| `web/guide/` | 4 | `engine/tools/gen-guide.mjs` | **EVET** |
| `web/collections/` | 16 | `gen-collection-pattern.mjs` / `gen-collections-page.mjs` / `gen-taste-collections.mjs` | **HAYIR** |

---

## 2. ★ KÖK SEBEP — ASIL ÜRETEÇ HEAD'DE DERLENMİYORDU

Kart "üreteci koştur" diyordu. Koşmadı:

```
$ node engine/tools/gen-style-pages.mjs --out=/tmp/v10h-styles
gen-style-pages.mjs:375
  desc: '... back sash length follows that size's waist, tie-back, ...'
SyntaxError: Unexpected identifier 's'
```

Bu HEAD'in kendi hâli, çalışma ağacı kiri değil:

```
$ git show HEAD:engine/tools/gen-style-pages.mjs > /tmp/v10h-gsp-head.mjs
$ node --check /tmp/v10h-gsp-head.mjs      → aynı SyntaxError
$ git log --oneline -3 -- engine/tools/gen-style-pages.mjs
dcccc2d v10-g: the mtm sentences that slipped past the gate are gone ...
```

**Kırığı `dcccc2d` (V10-G) soktu.** V10-G bir MTM cümlesini düzeltirken
`your waist` → `that size's waist` yazdı ve tek tırnaklı literali kesti.
Yani V10-G, 12 sayfayı ELLE düzeltmek zorunda kalmadı — üretecini KIRDIĞI için
elle düzeltmekten başka yolu kalmamıştı, ve `generated_ratchet_check` tam olarak
bunu gördü. Kırmızı, kapının doğru çalışmasıydı.

**Onarım:** `gen-style-pages.mjs:375`'te apostrof kaçırıldı (`size\'s`). Tek
karakterlik değişiklik, metne dokunulmadı.

```
$ node --check engine/tools/gen-style-pages.mjs   → PARSE OK
```

---

## 3. ÜRETEÇ KOŞULARI — `web/` ÜSTÜNE, GERÇEK

### 3.1 `gen-style-pages.mjs` — 24 dosya, koştu

Önce güvenli dizine, sonra canlıya:

```
$ node engine/tools/gen-style-pages.mjs --out=/tmp/v10h-styles
generated 23 style pages + hub at ?v=136
$ diff -rq web/styles /tmp/v10h-styles | wc -l
0
$ node engine/tools/gen-style-pages.mjs        # bu kez web/styles üstüne
$ git status --porcelain web/styles | wc -l
0
```

**Bulgu:** üretecin çıktısı canlı 24 sayfanın BAYT BAYT aynısı. Yani V10-D/V10-G'nin
el düzenlemesi ile üreteç metni artık aynı yerde duruyor; bu 24 sayfa gerçekten
yeniden üretilebilir. Manifest bayat olduğu için kırmızıydı, sayfalar "üretilemez"
olduğu için değil. Bunu ölçmeden bilmek mümkün değildi — üreteç parse bile
etmiyordu.

### 3.2 `gen-guide.mjs` — 4 dosya, koştu VE YALANI GERİ GETİRDİ

```
$ node engine/tools/gen-guide.mjs
gen-guide: wrote 6 pages to web/guide/
$ git status --porcelain web/guide
 M web/guide/a-line-skirt.html
 M web/guide/babydoll-dress.html
 M web/guide/collared-top.html
 M web/guide/fitted-dress.html
```

Dört dosyanın dördünde de tek satır, aynı satır:

```
-<p class="fact" ...>Preshrink a natural fibre (cotton, linen) before cutting; ...
+<p class="fact" ...>Always preshrink a natural fibre (cotton, linen) before cutting; ...
```

`gen-guide.mjs` V10-F'in düzelttiği beş üretecin arasında **yoktu** (V10-F §4).
İçindeki `Always` duran iddiası ayakta kalmıştı ve ilk koşuda dört sayfaya geri
bastı. **Kartın öngördüğü hâl bu: üreteç yalan basıyorsa üreteç düzeltilir, taban
değil.** `gen-guide.mjs:253`'ten `Always ` silindi (V10-D'nin canlı sayfaya
yazdığı cümlenin aynısı), yeniden koşuldu:

```
$ node engine/tools/gen-guide.mjs
$ git status --porcelain web/guide      → 0 satır
```

### 3.3 `web/collections/` — 16 dosya, ÜRETEÇ KOŞAMIYOR (ilan)

**Bu 16 dosya için el düzenlemesi tek yoldur ve `--accept` onları böyle kabul
ediyor. Gizlenmiyor, adıyla yazılıyor:**

```
web/collections/seventies-scoop-neck-shift-mini.html
web/collections/sixties-babydoll-scoop-mini.html
web/collections/sixties-boat-neck-longsleeve-mini.html
web/collections/sixties-boat-neck-shift-mini.html
web/collections/sixties-crew-neck-jersey-mini.html
web/collections/sixties-crew-neck-tent-mini.html
web/collections/sixties-empire-gathered-babydoll.html
web/collections/sixties-empire-knit-mini.html
web/collections/sixties-fit-flare-knit-dress.html
web/collections/sixties-front-button-pinafore.html
web/collections/sixties-mod-colorblock-mini.html
web/collections/sixties-mondrian-shift-mini.html
web/collections/sixties-pointed-collar-tunic.html
web/collections/sixties-princess-seam-shift.html
web/collections/sixties-side-tie-tweed-shift.html
web/collections/sixties-vneck-front-zip-dress.html
```

Sebep bu turda yeniden ölçüldü, V10-F §6'nın devamı:

```
$ node engine/tools/gen-collection-pattern.mjs
  Error: ENOENT ... web/patterns/vintage6070/meta.json
$ node engine/tools/gen-collections-page.mjs
  Error: ENOENT ... web/patterns/svg/meta.json
$ node engine/tools/gen-taste-collections.mjs
  Error: ENOENT ... web/patterns/svg/meta.json
$ node engine/tools/gen-vintage-page.mjs
  Error: ENOENT ... web/patterns/vintage6070/meta.json
```

Girdi dizini `web/patterns/**` `af49514`'te (29 Tem) silinmiş. Dördü de bugün
koşturulamıyor. **`--accept` gerekçesi:** bu 16 satır bir üreteç koşusunun değil,
V10-D/V10-G'nin el düzenlemesinin bayt kaydıdır; K16 `web/patterns/**` verisini
geri getirene kadar başka yolu yok. Kapının kendi başlığı bu hâli zaten öngörüyor
(satır 25-34: "It is NOT a regenerability proof").

---

## 4. ÜÇ DOĞRULAMA — ÜÇÜ DE `--accept`'TEN ÖNCE

### (a) `landing_truth_check` — L2 hâlâ 0 ★

```
$ node engine/tests/landing_truth_check.mjs
EXIT=0
HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4
```

| katman | ÖNCE (tur başı) | SONRA (üreteçler koştuktan sonra) |
|---|---|---|
| L1 sağlayıcısız sayı | 937 / taban 937 | **937 / taban 937** |
| L2 yasak vaat + duran iddia | 0 / taban 0 | **0 / taban 0** |
| L3 vizyon zamanı | 0 / taban 0 | **0 / taban 0** |
| L4 ölü link | 0 / taban 0 | **0 / taban 0** |
| L5 beden dürüstlüğü | 4 / taban 4 | **4 / taban 4** |

**Beş sayının beşi de kımıldamadı, taban tek hane yükselmedi.** V10-F'in kök
onarımının aradığı kanıt budur: üreteçler `web/` üstüne gerçekten koştu ve yalan
geri gelmedi. `gen-guide.mjs`'in geri getirdiği tek `Always` cümlesi bu ölçümden
ÖNCE üreteçte kapatıldı (§3.2); kapatılmasaydı L2 4 olurdu.

### (b) `site-health` — ölü link 0

```
$ node engine/tools/site-health.mjs
EXIT=0
checked: 127 pages, 2604 internal refs, 124 sitemap urls, 124 indexable pages
OK  site-health: no dead links, sitemap matches the site, one version.
```

### (c) `?v` damgası tek değer

```
$ grep -rhoE '\?v=[^"'"'"' ]+' web/ | sort -u
?v=136
?v=136`);
?v=136`;
```

Üç satırın üçü de aynı değer: **136**. Sondaki `` `); `` / `` `; `` js şablon
literalinin kapanışı, ayrı bir damga değil. `site-version.mjs`'in bastığı değere
dokunulmadı, elle hiçbir damga değiştirilmedi. `gen-style-pages` kendi çıktısını
`?v=136` diye damgaladığını raporladı.

---

## 5. `--accept` VE KAPI

```
$ bash engine/tests/generated_ratchet_check.sh --accept
manifest written: contract/generated-paths.sha256 (57 paths)

$ bash engine/tests/generated_ratchet_check.sh
generated_ratchet_check: 57 declared paths, 57 on disk
  bytes moved: 0 · declared-but-missing: 0 · undeclared: 0
HUKUM: YESIL   (EXIT=0)
```

`git diff contract/generated-paths.sha256` tam olarak 44 satır oynattı; diff
`GECE/log/V10-H.kapi.txt` içinde.

## 6. ctest — 4/4

```
$ ctest --test-dir engine/build -R '^(generated_ratchet_check|landing_truth_check|docs_truth_check|bundle_fresh_check)$' --output-on-failure
1/4  generated_ratchet_check ..........   Passed  0.70 sec
2/4  bundle_fresh_check ...............   Passed  0.17 sec
3/4  docs_truth_check .................   Passed  0.18 sec
4/4  landing_truth_check ..............   Passed  0.89 sec
100% tests passed, 0 tests failed out of 4
```

**YENİ KIRMIZI AD `generated_ratchet_check` KAPANDI.** RULES 9 anlamında kırmızı
ad kümesi büyümedi; kapı gevşetilmedi, susturulmadı, dışlanmadı.

---

## 7. DOKUNULAN DOSYALAR

- `engine/tools/gen-style-pages.mjs` — 1 karakter (`size\'s`), SyntaxError onarımı
- `engine/tools/gen-guide.mjs` — `Always ` silindi (duran iddia, L2 sınıfı)
- `web/guide/*.html` (4) — **üreteçten yeniden üretildi**, HEAD'le bayt aynı
- `web/styles/*.html` (24) — **üreteçten yeniden üretildi**, HEAD'le bayt aynı
- `contract/generated-paths.sha256` — `--accept`, 44 satır oynadı
- `GECE/V10-H.md`, `GECE/log/V10-H.{kapi,ctest,landing}.txt`

`web/collections/*.html` (16) — **hiç yazılmadı**, üretecleri koşamıyor (§3.3).

## 8. YAPILMADI (adıyla)

1. **DEPLOY YAPILMADI.**
2. **`web/collections/` 16 dosya üreteçten geçmedi** — ENOENT, §3.3. Bu 16 satır
   manifeste bir üreteç koşusunun değil el düzenlemesinin kaydı olarak girdi.
3. **Tam ctest koşulmadı** — kartın istediği dört kapı koşuldu (4/4). Kalan ~113
   test bu turda koşulmadı; C++ derlenmedi, `engine/src/`e dokunulmadı.
4. **Tarayıcıda render yapılmadı.** Üretilen HTML gözle görülmedi — ama zaten
   HEAD'le bayt aynı, yani görünüm bu turda değişmedi (RULES 3 kapsamına giren
   yeni bir görsel iddia yok).
5. **`web/patterns/**` geri getirilmedi** (K16 işi, kart dışı).

## 9. KART DIŞI FARK EDİLEN — DOKUNULMADI

1. **★ ÜRETEÇ KIRIKLIĞI KAPISIZ.** `gen-style-pages.mjs` bir gece boyunca
   (dcccc2d → bcaf8c0, 2 commit) HEAD'de parse etmiyordu ve **hiçbir kapı bunu
   görmedi**. `generated_ratchet_check` bayt kaymasını gördü, sebebini değil.
   `node --check engine/tools/*.mjs` seviyesinde bir söz dizimi kapısı YOK.
   Ucuz, tek satırlık ve bu sınıf hatayı doğduğu commit'te yakalar. **Önerildi,
   yapılmadı** (kart kapı eklemeyi kapsamıyor).
2. **`gen-guide.mjs` V10-F'in tarama listesinden düşmüş.** V10-F §4 beş üreteç
   sayıyor; `gen-guide.mjs` (6 canlı sayfanın üreteci) o listede yok. Bu turda
   sadece `Always` kapatıldı. **Aynı dosyada başka duran-iddia/MTM dili taranmadı**
   — L2 bugün 0 olduğu için kapı sessiz, ama tarama YAPILMADI ≠ temiz.
   Aynı şüphe listede olmayan diğer üreteçler için de geçerli:
   `gen-legacy-redirects`, `gen-gore-grid`, `gen-wrap-grid`, `gen-factory-pack`,
   `gen-satis-pdf`, `gen-techpack-pdf`, `gen-vision-vocab`, `gen-taste-pool`,
   `gen-spec-v2`, `gen-spec-v1v2-map` — **hiçbiri bu turda koşulmadı ya da
   grep'lenmedi. DOĞRULANMADI.**
3. **`gen-style-pages.mjs:376` hâlâ MTM dilinde:** *"long enough to actually reach
   around **YOUR** body and knot"*. V10-F §9.2'nin "karara bağlanmadı" listesinin
   aynı sınıfı; `fabric-ties.html`'e canlı basılıyor ve kapının L2 kalıp listesinde
   olmadığı için yakalanmıyor. **Dokunulmadı** (kart metin turu değil).
4. **`web/styles/` 24 sayfanın hepsi üretilebilir çıktı — `web/collections/` 16'sı
   çıkmadı.** İkisi arasındaki fark tek bir silinmiş dizin (`web/patterns/**`,
   `af49514`). O dizin geri gelirse `generated_ratchet_check` başlığının kendi
   yazdığı yükseltme (üreteci koştur + baytı diff'le) 57 yolun 57'sinde mümkün
   olur; bugün 41'inde mümkün. Bu bir K16 kararı, tek taraflı yapılmadı.
5. **`bcaf8c0` (V10 scribe) `docs/` ve `README.md`'ye yazmış**, bu kartın yasak
   listesinde oldukları için okunmadı bile. `docs_truth_check` bu turda yeşil.
6. **Çalışma ağacında kart dışı kir var:** `GECE/log/V10-E.png/index-320.png`
   değişmiş (M), `patterns_real/geometry/`, `patterns_real/BUGRA-DEFTER.md`,
   `patterns_real/tools/bugra-geometry-2026-07-23.json` takipsiz (??), ve
   `GECE/KART/V10-*.md` kartlarının 8'i takipsiz. **Hiçbirine dokunulmadı**,
   commit'e alınmadı (`git add -A` yok; yollar tek tek verildi).

## 10. SÜRE

Tavan 50 dk. İş tavandan önce bitti; kırmızı ad AÇIK KALMADI.
