# V4 — FLAT KONVANSİYONU + ZEVK ÖN-TARAMASI (tutanak)

Faz: V4 · 24 Ağu 2026 · şef tek oturum, 7 işçi (R · K · C paralel → A → B →
D → E düzeltme turu) + 2 hakem çağrısı + 1 kâtip.
Açılış ağacı `b09c151` (V3'ün kapanışı) · kapanış ağacı `4110ca0`.
Kartlar: `GECE/KART/V4-{R,K,C,A,B,D,E}.md`

---

## 0. FAZIN TEK CÜMLESİ

Konvansiyon "hepsi aynı manken" demektir, "hepsi aynı çizim" değil. Bu gece
ölçüldü ki kalem üç farklı kolu **bayt bayt aynı** çiziyordu ve aynılığı
zorlayan kapı bu sessizliği mühürlüyordu. İki kök düzeltildi: kapı artık
düzeltmeye çalıştığı kusuru varsaymıyor, ve kalem farklı kol istendiğinde
farklı kol çiziyor. **Ama bu düzeltmelerin hiçbiri sevk edilen `web/`
yüzeyine BUGÜN ulaşmıyor** — faz bunu kendi en sert bulgusu olarak ölçtü,
gizlemedi (§6).

---

## 1. ÖLÇÜLEN (sayı + komut + hash)

### 1.1 Devralınan üç sayı DOĞRULANDI (devralınmadı)
| devralınan iddia | V4'ün kendi ölçümü | komut |
|---|---|---|
| kırmızı 6 | **6 · isim isim aynı** | `ctest --test-dir engine/build` → `GECE/log/V4.ctest.before.txt` ("6 tests failed out of 110") |
| test 110 | **110** (aynı log) | aynı |
| sözlük tabanı 10438 @ `495d58a` ratchet kilitli | **taban doğru; gece içinde +6 ihlal doğdu ve ONARILDI** | `bash engine/tests/vocab_reference_check.sh` |
| tek nesne, üç ölçüde kıyas | dokunulmadı; `flat_pattern_agree_check` hâlâ kırmızı | `GECE/log/V4.ctest.after.txt` |

### 1.2 Sessiz çökertme — fazın kurucu ölçümü
Şef bağımsız doğruladı (`node -e`, sha256):

    sleeveStyle none   2537 bayt  sha 0b647b4f1df3cfa3
    sleeveStyle set    3495 bayt  sha 70cb9c7881ce0c0a
    sleeveStyle raglan 3495 bayt  sha 70cb9c7881ce0c0a   <-- set ile AYNI
    sleeveStyle puff   3495 bayt  sha 70cb9c7881ce0c0a   <-- set ile AYNI
    collarType 1/2/3   3509 bayt  sha b26b7091834573e7   <-- ÜÇÜ DE AYNI

RULES invariant 1+2 ihlali. `CLAUDE.md`'de emsali yazılı (*sleeveStyle 'puff'
silently dropped, 2026-07-18*) — aynı kusur sınıfı kalem tarafında açıkmış.

### 1.3 K-V3B ÇÖZÜLDÜ — hangi hat yargılanacak (ÖLÇÜLDÜ, kanaat değil)
`GECE/V4-K.md` · problar `GECE/probe/v4k-*.mjs` · ham çıktı `GECE/log/V4-K.*`

| | HAT-1 `shell-flat` (hesaplanan kabuk) | HAT-2 `render-garment-flat.mjs` (çizim kalemi) |
|---|---|---|
| stil parametresi | **0** | ≥22 (8/8 kapı matrisi, 22/22 eksen değeri) |
| teknik çizim öğesi (omuz·oyuk·yaka·kol·pens·iç dikiş·topstitch·kapama·etek) | **0/9** | **9/9** |
| çizgi sınıfı | 1 çift, beyanlı **0/1** | 5 çift, beyanlı **5/5** |
| ölçek beyanı | `data-scale="1"`, `data-unit-mm` YOK, sapma **+9.56mm** | `1:3` + `unit 3`, sapma **0.10mm** |
| ★ ön panel ≠ arka panel | **0.000000000 mm — ARKADA OLAY YOK** | 3.39…211.91 mm |
| tüketici | 6 dosya, web'de **0** | 19 dosya, canlı `web/atolye.html` dahil |

**HÜKÜM: kapı HAT-2'yi yargılar.** Bedeli sayıyla: HAT-2'yi yargılamak beş
maddenin **0'ını**, HAT-1'i yargılamak **2/5'ini** üretilemez bırakır ve
madde (d)'yi 0.000000000mm'lik ayna yüzünden koşulsuz yeşile çevirir.
HAT-1 kapıya **rapor satırı** olarak girdi (3d kanadı): bel **25.0 mm**
(croquis 700.0 vs kabuk 725.0000) · göğüs yarı-genişliği **9.66 mm**
(219.90 vs 229.56) — eşiğe BAĞLANMADI, yalnız düşmesi beklenir.

### 1.4 V4-A — kapı, düzeltmeye çalıştığı kusuru varsaymayı bıraktı (`af6842c`)
`measureCroquis()` omuz ucunu "x'in ilk yerel maksimumu" diye buluyordu; bu
yalnız omuz göğüsten GENİŞSE doğrudur — yani kapı kusuru varsayıyordu ve
ölçülüp doğru bulunmuş bir düzeltmeyi bloke ediyordu (`contract`in kendi
`_NEDEN_DEGISTIRILMEDI` alanı bunu yazıyordu).
- Yeni çıkarım oran-bağımsız ve geometrik: omuz kirişi (sığ+dışa) ile kol
  oyuğu kirişinin (dik+aşağı) buluştuğu KÖŞE. Beyan okunmuyor (anti-hack).
- KANIT (`GECE/log/V4-A.inference.txt`): aynı çıkarım İKİ croquis'te —
  78.0u (omuz dışarıda) → 78.000u · 70.1799u (omuz içeride) → 70.200u,
  ikisinde de sapma 0.00mm. Eski çıkarım 70.1799u siluetinde KOLTUKALTINI
  omuz sanıyordu.
- §4.6 ölçü değişikliği, gerekçesi `af6842c` gövdesinde satır satır:
  `shoulderTipX` 78.0u/234.00mm → **70.1799u/210.54mm** ·
  `shoulderTipY` 19.36u/58.08mm → **16.8576u/50.57mm**. Eskiler `_previous`'ta.
  0.9570 kaynağı: satın alınmış Buğra Locket EU38 Back Body 196.13/204.94
  (`GECE/log/F-E.bugra-olcum.txt`).
- Yeni kapı satırı **1c**: `shoulderTipX <= chestX` — GEOMETRİK YASA
  (set-in kollu giyside omuz ucu göğüs hattının dışında olamaz), §7.3'e
  uygun: Buğra yalnız büyüklüğü besleyen girdi, benzerlik eşiği DEĞİL.
- §4.5 mutasyon (`GECE/log/V4-A.mutasyon.txt`): 78.0'a geri al → `16 ihlal`,
  exit 1; geri al → `0 ihlal`, exit 0. İkinci hakem kendi eliyle tekrarladı.

### 1.5 V4-B — ifade kapısı + RULES 9 onarımı (`3340cb9`, `c993491`)
- **RULES 9 İHLALİ DOĞDU VE ONARILDI.** `vocab_reference_check` gece içinde
  7. kırmızı AD olarak açıldı: taban 10438 → 10444 (+6), `garment +4` ·
  `peplum +2`, **6 satırın 6'sı V4-C'nin yeni `flat-board.mjs` dosyasından**.
  Onarım tek yol: referans KALDIRILDI, **taban YENİDEN KESİLMEDİ**
  (`GECE/log/V4-B.ratchet.txt`). V3'ün hükmü korundu: sayıyı düşürmek için
  metin silmek artefakt gizlemedir; burada tersi — artıranı geri al.
- Yeni kapı `engine/tests/flat_expresses_spec_check.mjs` (test 110 → 111).
- 3b: `lineClasses.ratios` (1.4286/1.4/2.0) artık kapıda OKUNUYOR — diskte
  duruyordu ve hiçbir kapı bakmıyordu. Sapma **%0.00**, ISO 128-2:2020
  md.5.2 tavanı %22.22 (kaynak `GECE/V4-R.md` §1, test başlığında).
- ★ Kartın varsayımı ölçülerek düzeltildi: "sicilde absent ise reddet ve
  ÇİZME" denendi → `flat_geometry_sellable_check` S5/S6 kırmızıya düştü
  (o kapı kollu stilin kolu çizmesini şart koşuyor) → kırmızı AD 6→7 olacaktı,
  geri alındı. Red artık **çizmemek değil, ADIYLA damgalamak**
  (`data-engine-gap`).

### 1.6 V4-D — ESKİ|YENİ panosu ve fazın en sert bulgusu (`9d582c7`)
- Pano: `GECE/log/V4-D.pano/board-eski-yeni-{1,2,3}.png`, 15 satır × 2 sütun.
  Sol sütun `c396fb4`'ün DİSKTEN okunan çıktısı — bugünden yeniden
  üretilseydi pano yalan söylerdi; `flat-board.mjs`'e `--eski` bunun için eklendi.
- ★ **10 stil hücresinin 10'u da ESKİ ile BAYT BAYT AYNI** (`cmp` 10/10;
  ikinci hakem bağımsız doğruladı). Sebep ölçüldü: 9 stilin 9'u da SALT-OKUNUR
  **referans kaleme** düşüyor (`_engine-full.mjs renderStyle`,
  `renderGarmentFlatAsync:1120 tryReferencePen`), ve `web/atolye.html` o kalemi
  gömülü taşıyor. **Kanunun bağladığı kalem, panoyu ve canlı sayfayı basan
  kalem DEĞİL.**
- Madde 5 (artefakt kırpmayla gizlenmez) ÖLÇÜLDÜ: 3 pano sayfasında
  `clipPath`/`clip-path`/`overflow:hidden`/`slice` = **0/0/0/0**; 30 gömmenin
  30'u `xMidYMid meet` + `overflow="visible"`. Hiçbir artefakt gizlenmedi.

### 1.7 V4-E — DÜZELTME TURU, hakemin iki gerekçesi (`4110ca0`)
İlk hakem **KALDI** dedi. İki gerekçe, ikisi de ölçülmüştü:
(i) kapının değer alanı ELLE SEÇİLMİŞTİ — fiilen kullanılan `straight` (237
kullanım) ve `balloon` (35) kapıya hiç girmiyordu ve `set` ile **0.00u**
farkla özdeşti; kapı geçtiği şeye göre şekillenmişti.
(ii) `raglan` bir raglan değildi — `set` ile birebir aynı path, eklenen dikiş
çift aynalama yüzünden **4 kez** basılıyordu (`set 10 → raglan 18` artışının
8'i üst üste binen kopya).

Onarım:
- `SLEEVE_VALUES` listesi SİLİNDİ. Alan **beş kaynaktan türetiliyor**
  (163 takipli JSON'daki fiili kullanım + `engine/vocab.json` + üç contract
  dosyası). KANIT: aynı türetme faz-öncesi ağaçta (`c396fb4`) **birebir aynı
  alanı** verdi (kol 8 · yaka 7 · omuz 3) — ikinci hakem kendi worktree'sinde
  tekrarladı. Alan bu gecenin düzeltmesine göre şekillenmiyor.
- **`balloon` (35 kullanım) gerçekten ifade edildi**: 10 eleman/2705.08u
  (=straight) → **54 eleman/2996.36u**. `bishop` aynı hamlede (vocab onu
  balloon eşanlamı ilan ediyor). Yeni sayı uydurulmadı; çizim
  `croquis.sleeveLaw`'un ölçülmüş 0.9327'sinden.
- **Raglan topolojik oldu**: path artık yaka tabanından başlıyor
  (`M 30.0 4.0`; set-in'de `M 70.2 16.9`), ayrı `raglan-seam` path'i sıfır,
  eleman 18 → **10**, kontur 2705.08u → **3186.67u**. `c993491`'in yanlış
  *"instead of"* cümlesi commit gövdesinde AÇIKÇA düzeltildi (sessiz silme yok).
- **İFADE RATCHET** kuruldu: kol **0/8** · yaka **4/7** [shirt·mock·flat·
  crescent] · omuz **1/3** [dropped] UNEXPRESSED. Tavan bugünkü ölçümden,
  yalnız düşebilir. Emsal: V3'ün `UNMEASURED 3/6, ratchet tavanı 3`.
- §4.2 boş test (`GECE/log/V4-E.bostest.txt`): faz-öncesi ağaçta **7 FAIL,
  exit 1**. §4.5 mutasyon: `balloon`'u geri eşitle → `RATCHET 1/0 TAVAN
  ASILDI`, exit 1 → geri al → 0 FAIL. İkinci hakem ratchet'i kendi eliyle de
  kırdı (`peterPan` → `UNEXPRESSED 5/4`, exit 1).

### 1.8 Zevk panosu (V4-C, `c396fb4`) — Damla'nın hükmü için
- ESKİ pano: `GECE/log/V4-C.pano/board-eski-1.png` · `board-eski-2.png`
- ESKİ|YENİ pano: `GECE/log/V4-D.pano/board-eski-yeni-{1,2,3}.png`
- Kol ailesi PNG: `GECE/log/V4-B.kol/sleeve-*.png` · `GECE/log/V4-E.kol/*.png`
- **GÖRSEL İNDİRİLMEDİ** (§7.2 kalıcı veto): 17 referans, dört kova
  (Chanel HC · Bershka/Stradivarius · genz · Etsy), her satır link + özellik
  dili + "ölçülebilir mi" kolonu. chanel.com 403 · Bershka PDP bot duvarı ·
  **Etsy tamamen 403** → D kovası indie markaların kendi sitelerinden;
  Etsy'ye özgü sıfır veri, ERİŞİLEMEDİ diye kayıtlı ("yok" DEĞİL).
- Ölçülen yan sayı: Bershka sitemap 1258 slug'da kesim frekansı
  (mini 395 · wide-leg 170 · oversize 156 · baggy 126 · midi 119) — bizim
  sözlük kapsaması kabaca **6/13**.
- ⚠ Damla dışarı söylemeden önce: "gen-Z baggy'e geçti" cümlesi bu veriyle
  KURULAMAZ; karşı veri de var (Depop'a atfen skinny +131%, **DOĞRULANMADI**).

### 1.9 Araştırma zemini (V4-R, `b99e21d`) — birincil kaynaktan çekildi
- **ISO 128-2:2020 md.5.1**: izinli kalınlık serisi 0,13…2 mm, ortak oran
  1:√2; "extra wide : wide : narrow = **4:2:1**"; sapma ≤ ±0,1d (md.5.2).
  Repo'nun 2.0/1.4/1.0'ı serinin son üç elemanı.
- **ISO 128-2 Tablo 4**: dashed 12d:3d = 4:1. Repo `hidden: "1 3"` ISO tip 07
  dotted'a birebir uygun; `topstitch: "4 3"` — **4d ISO alfabesinde YOK**.
- **ISO 128-3:2022 md.4.12** detay callout: kapalı ince sürekli sınır + tek
  büyük harf + `HARF (n:1)`. SVG'de ne aranacağı dört maddede yazılı.
- ★ **`data-scale="1:3"` ISO 5455:1979 izinli dizisinde YOKTUR.** md.5.1
  NOTU'nun "exceptional cases … intermediate scales" cümlesi tek meşru
  dayanak; contract bunu bir ISO ara-ölçeği olarak BEYAN ETMİYOR.
  (Moda alanının 1:8 / 1:4'ü de dizide yok — 1:3 tek başına değil.)
- **Manken çizelgesi: KAYNAK YOK, TEYİT EDİLDİ.** `referenceBody.openItem`
  doğru. Ek engel: croquis literatürü boy-normalize (baş birimi), bizimki
  genişlik-normalize; köprü için mm cinsinden baş uzunluğu gerekiyor ve
  yayınlanmıyor.
- ERİŞİLEMEDİ (yok DEĞİL): ISO 128-2 Annex D · ASME Y14.2/Y14.3 gövdeleri ·
  Szkutnicka/Abling/Ivanova. "0,8mm dikiş / 0,3mm topstitch" **DOĞRULANMADI**.

---

## 2. KAPANAN / AÇILAN KIRMIZI (ad ad)

**AÇILAN VE AYNI GECE KAPATILAN: `vocab_reference_check`** (§1.5).
Taban kesilmeden kapatıldı; kapanış ölçümü `HUKUM: YESIL`.

**AÇILAN, AÇIK KALAN: YOK.**
`GECE/log/V4.ctest.before.txt` → `GECE/log/V4-E.ctest.after.txt`:
altı ad, ikisinde de birebir aynı (`GECE/log/V4-E.reddiff.txt` BOŞ).
`contract_check · figure_check · flat_artifact_census ·
flat_pattern_agree_check · sizechart_source_check · style_check`
**RULES 9 ihlali YOK.** Test 110 → 111.

**KAPANAN MİRAS KIRMIZI: YOK.** V4 bir konvansiyon fazıydı, kırmızı kapatma
fazı değil. `figure_check` (K-V1B) bu faza kart olarak yazılmıştı ama
kesilmedi — sebebi §5'te, kuyrukta duruyor.

---

## 3. KAPI (§4, alt kapı alt kapı)

| alt kapı | hüküm | kanıt |
|---|---|---|
| 4.1 makine (ctest) | GEÇTİ | 111 test, 6 kırmızı, AD kümesi aynı · `V4-E.ctest.after.txt` |
| 4.1 wasm paritesi | GEÇTİ (boş) | `git diff --stat c396fb4..HEAD -- engine/src engine/build-wasm.sh web/` = **BOŞ**; C++/wasm üretim yoluna dokunulmadı |
| 4.2 boş test | GEÇTİ | şef bağımsız koştu: faz-öncesi `c396fb4` worktree'sinde 3 FAIL; V4-E'den sonra **7 FAIL, exit 1** · `V4-E.bostest.txt` |
| 4.3 kanıt | GEÇTİ | her PNG/log yolu `ls` ile, her sayı komut yeniden koşturularak doğrulandı (şef) |
| 4.4 hakem | **KALDI → GEÇTİ** | iki AYRI temiz oturum; ilk hüküm KALDI (2 ölçülmüş gerekçe) → V4-E → ikinci hüküm GEÇTİ · `GECE/KAPI.md` |
| 4.5 mutasyon | GEÇTİ | üç ayrı kapı kırıldı ve geri döndü: `V4-A.mutasyon.txt` · `V4-B.mutasyon.txt` · `V4-E.mutasyon.txt`; ikinci hakem ikisini kendi eliyle tekrarladı |
| 4.6 tolerans | GEÇTİ | `toleranceMM` 2.0'da SABİT; tek ölçü değişikliği `shoulderTipX/Y`, eski+yeni+gerekçe+kaynak `af6842c` gövdesinde |
| 4.7 kırmızı raporlama | GEÇTİ | altı kırmızının kök teşhisi + ölçülmüş adayı KOSU.md'de devrediyor; V4 kendi bulduğu her eksiği bir geliştirme yoluyla yazdı |
| yazma kapısı | GEÇTİ | bu tutanak + KOSU.md + KAPI.md |
| kâtip kapısı | GEÇTİ | `a3e5145` — docs 5 dosya + README + INDEX; 8 bayat cümle düzeltildi, sessiz silme yok |

---

## 4. FAZIN DÜRÜST SINIRI — gizlenmedi, üç yere yazıldı

**Bu gecenin iki kök düzeltmesi sevk edilen yüzeye ULAŞMIYOR.** Pano
hücreleri ve canlı `web/atolye.html` salt-okunur REFERANS kalemden çıkıyor;
kanunun (`contract/flat-convention-v1.json`) bağladığı ÜRETİM kalemi
(`render-garment-flat.mjs`) canlı hiçbir sayfayı basmıyor. Sevk edilen hiçbir
sayfada `data-scale` / `data-shoulder-x` / `data-engine-gap` yok; referans
kalem 31 stilin **0'ında** ölçek beyan ediyor.
Kayıt yerleri: `GECE/V4-D.md` (ölçüm) · `docs/ARCHITECTURE.md` §12 + Known
limits · `docs/KATMAN-HARITASI.md` md.6 · `docs/SATIS-SARTNAMESI.md` §1.
Hiçbir yerde "flat konvansiyonu bitti" cümlesi kurulmadı.
★ Bu, V5'in değil **bir sonraki flat fazının BİRİNCİ kartıdır**: kanun
kaleme değil, SEVK EDİLEN kaleme bağlanmalı.

## 5. YAPILAMAYAN (sebebiyle, saklanmadan)

1. **Yaka ailesi ayrılmadı** — `collarType` 1/2/3 hâlâ aynı bandı basıyor.
   Sebep: ölçülmüş yaka kanunu YOK, sicilde `collarFamily` absent, sayı
   uydurmak yasak (§7.6). UNEXPRESSED 4/7 olarak SAYIYA bağlandı.
2. **`dropped` omuz ayrılmadı** — ölçülmüş kaynak yok. UNEXPRESSED 1/3.
3. **Detay callout üretilmedi** — bugün **0**. Mekanik tanımı `V4-R.md` §3'te
   hazır (ISO 128-3 md.4.12); üretim bütçeye sığmadı, kuyruk kartı.
4. **`figure_check` (K-V1B) kesilmedi** — V4'ün kartları K-V3B'ye ve sessiz
   çökertmeye gitti; ikisi de daha temeldi. Kuyrukta, varsayılan (C).
5. **Raglanda gövde siluetinin omuz parçası silinmedi** — kolun kâğıt
   dolgusunun altında kalıyor. Kökten çıkarmak `halfOutline`'ı değiştirmek
   demek; o yol bütün stillerin ortak yolu, bu kartta yapılmadı.
6. **Tanınmayan değer hâlâ ÇİZİLİYOR** (`kimono`·`dolman`·`flutter`·`bell`·
   `batwing` ve uydurma bir değer `straight` ile özdeş, 2705.08u) — yalnız
   `:unknown` damgası var = **etiketli çökertme, açık red DEĞİL**. RULES
   invariant 1'in tam karşılığı kurulmadı; sebebi §1.5'te ölçülü
   (`flat_geometry_sellable_check` çizmemeyi kırmızı düşürüyor). AÇIK.
7. **8 bedenin 7'sinde ölçüm yok** — V4 de yalnız EU38 ölçtü (V3 gibi).

## 6. KART DIŞI ÇIKAN, DEVREDEN BULGULAR (dokunulmadı)

- **`vocab_reference_check` düz metin sayıyor**: bir ekseni YORUMDA anmak ile
  ona referans vermek ayırt edilemiyor; bu gece iki kez kapıyı kırdı.
- **İfade ratchet SAYIYI tavanlıyor, ALANI değil** (ikinci hakem buldu):
  bir değeri `vocab.json`+şemadan silmek alanı daraltıp UNEXPRESSED'i
  düşürüyor ve kapı yeşil kalıyor — çizim düzelmeden sayı düşebilir.
  Bu fazda YAPILMADI; gelecek için açık kapı, kapatılmalı.
- **`raglan` sözleşmede yanlış eksende**: dört contract dosyası onu OMUZ
  ekseninde ilan ediyor, kalem kol sanıyordu.
- **`cap` iki sözleşmede çelişiyor** (`spec-grammar` kol değeri sayıyor,
  29 kullanım; `vocab.json`+şema saymıyor). `collarType 81` enum dışı.
  `mandarin`/`notched`/`sailor` gramerde PARK'ta, sayısal karşılığı yok.
- **`flat_artifact_census`'ın C1 ölçüsü örnekleme adımına bağlı**: 4mm adımda
  R≈79mm meşru eğri 2.9° veriyor. Eşik/adım ilişkisi ölçülmeli.
- **`sleeveStyle:'straight'` ile `'set'` hâlâ aynı** — iki başka kapı
  `straight`'i kullanıyor, ayırmak onları etkiler.
- **`peterpan_puff` adı ÜST diyor, çıktı ETEKLİ** (V4-C md.4, V4-D §4.2).
- **Stil kümesi dört ayrı sayı**: pano 9 · konvansiyon kapısı 8 · geometri
  kapısı 5 · referans kalem 31. Hangi stilin hangi kapıya girdiğini söyleyen
  tek liste YOK; bir stil hiçbir kapıya girmeden panoda görünebiliyor.
- **`waistY` · `chestY` · `shoulderSlope 0.32` hâlâ `source: ACIK`** —
  croquis'in yarısı ölçülü, yarısı devralınmış. `shoulderTipY` tamamen o
  kaynaksız eğimden türüyor.
- **`flat_convention_check` yasak-boya süzgeci HAM METİN arıyor**:
  `data-pattern-gap` adlı bir öznitelik `<pattern>` yasağına takıldı.
  Öznitelik yeniden adlandırıldı, süzgece dokunulmadı — yanlış pozitif üretir.
- **`flat-board.mjs` `FARK VAR`/`AYNI` basıyor ama exit koduna bağlı değil** —
  bedava bir regresyon kapısı orada duruyor.

---

## 7. SONRAKİ FAZA DEVİR — ÜÇ SAYI

1. **KIRMIZI = 6 · TEST = 111.** V4 kendi ölçtü, devralmadı: faz öncesi
   110 test/6 kırmızı, faz sonrası 111 test/6 kırmızı, **AD kümesi birebir
   aynı** (`GECE/log/V4-E.reddiff.txt` BOŞ). Gece içinde 7. ad
   (`vocab_reference_check`) doğdu ve **aynı gece taban kesilmeden kapatıldı**.
   Log: `GECE/log/V4.ctest.before.txt` → `GECE/log/V4-E.ctest.after.txt`
2. **SÖZLÜK TABANI = 10438 referans @ `495d58a` · RATCHET KİLİTLİ, YEŞİL.**
   V4 tabanı KESMEDİ. Bugünkü ağaç tabanın ALTINDA (kol 351→347,
   yaka 81→80). Sayı yalnız düşebilir. ⚠ Kapının düz metin sayması yüzünden
   yorum yazmak da sayıyı oynatıyor (§6).
3. **İFADE RATCHET = 5 UNEXPRESSED · TAVAN KİLİTLİ** (kol **0/8** ·
   yaka **4/7** [shirt·mock·flat·crescent] · omuz **1/3** [dropped]).
   Kapı `engine/tests/flat_expresses_spec_check.mjs`; değer alanı ELLE
   YAZILMIYOR, beş kaynaktan türetiliyor ve türetme faz-öncesi ağaçta aynı
   alanı veriyor. Sayı yalnız DÜŞEBİLİR.
   ⚠ İki uyarı devrediyor: (a) ratchet sayıyı tavanlıyor, ALANI değil —
   değeri sözlükten silerek sayı düşürülebilir, bu kapatılmalı; (b) tanınmayan
   değer hâlâ çiziliyor (etiketli çökertme), RULES invariant 1'in açık reddi
   kurulmadı.
   ⚠ V3'ün 3. sayısı (tek nesne, üç ölçüde kıyas) V4'te ÖLÇÜLMEDİ ve
   `flat_pattern_agree_check` kırmızı olarak olduğu gibi devrediyor.
   ⚠ V2'nin 3. sayısı (sürekli eksen 2/37) V3'te de V4'te de ÖLÇÜLMEDİ.
