# V9 — DOCS BÜYÜK TURU (kapanış üçlüsünün birincisi, koşulsuz)

Koşu: 2026-08-25 · açılış `a6689ef` · protokol GECE-KOSUSU-v6.md §6/V9.
Kapsam kararı, açıkça: **`docs/**` + `README.md` + `GECE/INDEX.md`.**
`web/` (127 sayfa) bu fazın DIŞINDADIR — V10'un `landing_truth_check`'ine aittir.
0C'nin 41 taşıyıcı iddiasının 28'i web tarafında, bu faz onlara DOKUNMADI.

İşçiler: **9 kart** — R · A · B · B2 · B3 · C · D · E · F · G.
Her işçi kendi commit'ini attı. Şef kod yazmadı.

---

## 1. ÖLÇÜLEN (sayı + komut + hash)

### 1.1 Makine kapısı (§4.1)

| | açılış | kapanış |
|---|---|---|
| koşan test | **114** | **115** (+1 = `docs_truth_check`) |
| kırmızı | **6** | **6** |
| süre | 274.01 sn | 271.98 sn |
| log | `GECE/log/V9.ctest.opening.txt` (`a6689ef`) | `GECE/log/V9.ctest.final.txt` |

**AD KÜMESİ BİREBİR** — şef `diff` aldı, hakem bağımsız olarak yeniden aldı:
`flat_pattern_agree_check · flat_artifact_census · style_check ·
sizechart_source_check · contract_check · figure_check`. RULES md.9 korundu.
`h10_gate_check` 116. tanımlı test olarak `(Disabled)` duruyor (V9-D bunu ölçtü).

### 1.2 Kapının üç alt denetimi (§6/V9 siparişi)

Basan komut: `node engine/tests/docs_truth_check.mjs [--no-baseline]`

| denetim | faz öncesi | faz sonrası | taban |
|---|---|---|---|
| **D1 duran iddia** (RULES §6) | ham 16 → **gerçek 9** | ham 14 → **0** | **0** |
| **D2 ölü repo yolu** | **9** | **0** | **0** |
| **D3 sağlayıcısız sayı** | **52** / 133 satır | **0** / 140 satır | **0** |

`ctest --test-dir engine/build -R docs_truth_check` → **Passed**.
★ D3 taban 52'yken kapı SERT DEĞİLDİ — tek cümlelik mutasyon eşiğin altında
kalırdı. 0'a kesilince ilk kez gerçekten kırıldı (`GECE/log/V9-B2.mutasyon.txt`).

### 1.3 Sözlük ratchet'i (devralınan bant, V9 dokunmadı)

`bash engine/tests/vocab_reference_check.sh` → taban **10438** @ `495d58a` ·
bugün **10432** (delta −6, tabanın ALTINDA) · **YEŞİL**. 37 eksen + 92 kelime.

---

## 2. İDDİA TABLOSU — ÖNCE / SONRA

Kapsam: `docs/` + `README.md` taşıyıcı iddiaları = 0C'nin bu kapsamdaki **22**
kalemi + V9-A'nın bulduğu **13 yeni** kalem = **35**. (0C'nin 24 Ağu'daki
"doğru 14 · YALAN 12 · kanıtsız 15" sayısı `docs+web+README` kapsamındaydı;
kapsam farklı, devralınmadı.)

| | ÖNCE (25 Ağu, onarım öncesi) | SONRA (25 Ağu, onarım sonrası) |
|---|---|---|
| doğru | **21** | **25** |
| **YALAN** | **3** | **0** |
| kanıtsız | 10 | 10 (hepsi artık ADLI sağlayıcı taşıyor) |
| ölü referans | 1 | **0** |

**Kapanan 3 YALAN, adıyla:**
1. **Y6b — `docs/ARCHITECTURE.md` "EU 34-52 … 70,200 drafts".** Kök: TEST GİRDİ
   ARALIĞI ile SATILAN BEDEN ARALIĞI aynı cümlede karışmış. Ölçüldü:
   `engine_check.cpp:30-44` girdi = EU34…EU52 + 5 kenar gövde = **15**;
   satılan = `contract/layers/shape-ratios.json` **8** (EU34-48). İkisi ayrıldı.
2. **YENİ-1 — `docs/H1.0-KAPI.md` kapıyı canlı gibi anlatıyordu.** Üç gerçeklik
   aynı dosyanın ilk 25 satırındaydı. Ölçüldü: fikstürün gerçek adı
   `h10_gate_check_LEGACY.cpp` · `CMakeLists.txt:748` `DISABLED TRUE` ·
   `engine/build-h10` dizini YOK · `ctest -N` → `(Disabled)`. Yani test
   **kırmızı değil, hiç koşmuyor**. "Bugün KIRMIZI olması doğrudur" cümlesi
   üstü çizilerek gerekçesiyle bırakıldı — SESSİZ SİLME YOK.
3. **YENİ-2 — `docs/SATIS-SARTNAMESI.md` "borcu bitti" ↔ aynı dosya "5/5 EKSİK".**
   Kutucuk sayımı ölçüldü: `grep -c "^- \[x\]"` **15** · `grep -c "^- \[ \]"` **5**.
   Dosyadaki "16/17 madde geçti" hiçbir sayımla tutmuyormuş.

**★ Bir iddia sadece düzeltilmedi, ÇÜRÜTÜLDÜ:** `README.md`'nin Z-spread
"143 mm / 238 mm" çifti. V9-A "bu sayıyı basan alet YOK" demişti; V9-C aleti
BULDU (`engine/tools/drape-preview.cpp`, kapı `drape_check` Test #89) ve
koşturdu: bugün `zSpreadMM` **174.3978** (EU38) / **229.4973** (pear).
Eski çift tutmuyor. Hakem bağımsız olarak yeniden bastı, aynı sayıyı aldı.

**★ Ve V9-A'nın kendi hatası bulundu:** §6B "alet adı olmayan sayısal iddia: 4"
diyordu; `drape_check` `CMakeLists.txt:604`'te kayıtlı, doğrusu **3**.

---

## 3. KAPI — `docs_truth_check` (yeni, mekanik)

`engine/tests/docs_truth_check.mjs` · taban `engine/tests/docs-truth-baseline.json`
· node yerleşikleri, sıfır bağımlılık.

### 3.1 §4.2 BİRİNCİL USUL — boş test DEĞİL

Kapı faz-öncesi (`a6689ef`) ONARIMSIZ docs ağacına karşı **EXIT 1**, satır satır
ihlal listesiyle: D1 **9** · D2 **9** · D3 **52**. Derleme hatası değil.
Log: `GECE/log/V9-B.red-before.txt`.
★ Hakem bunu KENDİ worktree'sinde bağımsız üretti: `a6689ef` → EXIT 1, D1 **9**,
D3 **52** — ilk tabanın kaydettiği sayının birebir aynısı.

### 3.2 §4.5 MUTASYON — kapı kırılıyor

Üç denetimin üçü de ayrı ayrı kırıldı, üçü de geri alınınca yeşile döndü
(`GECE/log/V9-B.mutasyon.txt`, `GECE/log/V9-B2.mutasyon.txt`):
`ALL PASS` + `byte-identical` → D1 exit 1 · olmayan yola backtick → D2 exit 1 ·
sağlayıcısız "3.75 mm" → D3 taban 0→1, exit 1.
Her seferinde `git status --porcelain -- docs/ README.md` **0 satır**.
★ Hakem mutasyon loglarına GÜVENMEDİ, üçünü kendisi de kırdı ve geri aldı.

### 3.3 ZORUNLU İSTİSNA KURALI — neden var

Düz `grep`'li bir kapı, RULES §6 ONARIMINI cezalandırır: onarılmış cümle emekli
edilen sayıyı tırnak içinde taşır ("the standing '0.00 mm' wording **was
replaced**"). V9-A ölçtü: istisna olmadan kapı 13 yanlış pozitifin 6'sına ateş
ederdi. Bu yüzden hit DÜŞER eğer (a) tırnak/kod-span içindeyse, VEYA (b) aynı
satırda `replaced|ölçüldü|GECE/|ctest |node engine/tools/` + bir TARİH varsa.
Fenced kod bloğu taranmaz (V9-R'nin GitLab `scope: raw` dersi).
Kaçış mekanizmasının kendisi uydurma değil: V9-R açılan HER alette
(Vale, markdownlint, woke, lychee, doctest) üç kademeli kaçış buldu;
**kaçışsız kapının yayınlanmış emsali YOK.**

### 3.4 ★ HAKEM KAPININ TAŞINAMAZ OLDUĞUNU BULDU — onarıldı

Hakem `git worktree add /tmp/v9head HEAD` ile TEMİZ checkout'ta kapıyı koştu:
**EXIT 1, D2 YENİ 42.** Sebep: D2 varlık sorusunu git'e değil DİSKE soruyordu;
42 hedef gitignore'lı artefaktti. Yani mühürlenen "D2 0" dokümanın değil **tek
bir çalışma dizininin** özelliğiydi — temiz klonda/CI'da `docs_truth_check`
**YENİ BİR KIRMIZI AD** olurdu = RULES md.9 ihlali.

V9-B3 kökünden onardı (yama değil): varlık sorusu artık `git ls-files` +
`git ls-tree -r HEAD`'e soruluyor; gitignore'lı hedefler AYRI sınıf olarak
sayılıyor ve SAYISI basılıyor, saklanmıyor. Kanıt `GECE/log/V9-B3.kanit.txt`:
çalışma dizini ve temiz worktree **bit bit aynı** hüküm veriyor, ikisi de exit 0.
VAR sayısı **540 → 491**: disk yoklaması kalktı, kapı SIKILAŞTI.
★ Kusurun ikinci yarısı da vardı ve ancak ölçünce çıktı: `dist/` gibi dizin-ignore
kuralları yalnız GERÇEK dizinlere uyuyor, git bunu diskten öğreniyor — ilk
onarımdan sonra bile aynı commit iki farklı hüküm veriyordu (artefakt 24 vs 21).

V9-G son kayıtlı borcu da kapattı: `docs/H1.0-KAPI.md:54` iki kaynağı
(`patterns_real/geometry/geometry-full.json`, `seamgraph.json`) "Birincil kaynak"
diye sunuyordu; ölçüldü — **diskte VAR, izlenen ağaçta YOK, gitignore'da da YOK**
(`2f748db`, 20 Ağu çıkardı). Temiz klonda o kaynak yok. Satır dürüstçe yeniden
yazıldı, iki kaynağın ADI YERİNDE KALDI. D2 kayıtlı borcu **1 → 0**.

---

## 4. KAPANAN / AÇILAN KIRMIZI (ad ad)

**Kapanan miras kırmızı: 0.** V9 hiçbirine dokunmadı (kâtip koda dokunamaz, §7.4).
**Açılan yeni kırmızı ad: 0.**
**Eklenen yeşil kapı: 1 — `docs_truth_check`.**
Miras 6 aynen duruyor: `style_check · sizechart_source_check · contract_check ·
figure_check · flat_pattern_agree_check · flat_artifact_census`.

---

## 5. HAKEM (§4.4) — GEÇTİ, dört kusurla

Hüküm `GECE/KAPI.md` (`5885b80`). Hakem temiz oturumdu, brief görmedi, beş
soruyu da kendi ölçümüyle cevapladı. **GEÇTİ.**

Bildirdiği kusurlar, hiçbiri saklanmadı:
1. **Taşınamazlık** → §3.4'te KÖKÜNDEN onarıldı, iki bağımsız kanıtla.
2. **Kaçış grameri tek jetonluk** — AÇIK. `"ALL PASS"` (tırnak) ·
   `Measured 25 Aug 2026: ALL PASS … GECE/V9-A.md` · `` `ölü/yol.md` does not
   exist `` · `` 37 mm (`GECE/V9-A.md`) `` — dördü de kapıyı yeşil bırakıyor.
   **Sayı ile kanıtın ALAKASI hiç aranmıyor.**
3. **Kapı kendi fazını denetlemiyor** — AÇIK. Kapsam `docs/**` + `README.md`;
   bu fazın 5809 satırlık `GECE/` çıktısı, `CLAUDE.md`, `HEDEF.md`,
   `knowledge/`, `contract/`, `engine/*.md` DIŞARIDA.
4. **D3'ün "sağlayıcı"sı semantik değil** — AÇIK. `` `ctest -R nothing_at_all` ``
   gibi hiçbir şeyi koşturmayan dize de sağlayıcı sayılıyor. Bugünkü 140
   sayısal satırın kaçının sağlayıcısının GERÇEKTEN o sayıyı bastığı ÖLÇÜLMEDİ.

Hakemin ayrıca adlandırdığı üç "kapı yatıştırması" (iddia değişmedi, sadece
düşürücü tetiklendi): `docs/KATMAN-HARITASI.md:47` · `:74` ·
`docs/archive/mocks/babyblue-stil-1.html:113`.

---

## 6. `GECE/INDEX.md` (§6/V9 siparişi)

V9-F son hâline getirdi: **349 tekil yol belirteci · 338'i `test -e` ile
doğrulandı** · 5'i YOK/⚠ damgasıyla · 3'ü yan dalda · 3'ü repo yolu değil.
Koşunun ürettiği her kalıcı dosya sınıfı tabloda: `V0.md…V7.md` + `V9-*.md` ·
`V0-0A…0F/0R` · `KART/` (faz başına sayı) · `log/` (ctest açılış/kapanış,
mutasyon, kapı kanıt logları adıyla) · `KAPI.md` · `KOSU.md` · `kapi.sh` ·
`mutasyon.sh/tsv` · `probe/` · `kurtarma/` · `f0-*` üreteç→çıktı çiftleri ·
`arsiv/` TEK satır (açılmadı, listelenmedi) · `docs/` ağacı.

★ Index'in kendi üst tablosu bir ÖLÜ YOL gösteriyordu (`GECE/F0.md`) — bulundu,
damgalandı. ★ Index hiçbir mekanik kapının denetiminde DEĞİL (hakem kusuru 3).

---

## 7. YAPILMADI — adıyla, saklanmadan (§6 anti-bahane kuralı)

- **V8 (kumaş ekseni derinleştirme + rehber) KOŞMADI.** Numara atlandı.
  `fabric_ease_check` kurulmadı; rehber (kumaş önerisi, esneme testi tarifi,
  kumaşa özel püf noktalar) üretime BAĞLANMADI. Girdisi `GECE/V5-R.md` §C
  yerinde duruyor. V11 raporunda bu satır adıyla durur.
- **238 iddia cümlesinin 214'ü tek tek yargılanmadı** (V9-A, süre tavanı).
  Ağırlığı `docs/ARCHITECTURE.md` (104) ve `docs/H1.0-KAPI.md` (54) taşıyor.
  Mekanik kapı üçünü de (D1/D2/D3) 0'a indirdi, ama bu "her cümle doğru"
  DEMEK DEĞİLDİR — kapı kalıp sayar, anlam yargılamaz.
- **Docs'un iddia ettiği sayılar yeniden ÜRETİLMEDİ:** `70,200` (engine_check
  koşulmadı), `19,555`, `27 of 54` (alet `benchmark-58.mjs` VAR, girdisi
  `benchmark-58/manifest.json` telifli ve repoda YOK), `86%` (foto-spec-olcum
  koşulmadı). Hepsinin ALET ADI bağlandı; sayının o aletten çıktığı
  doğrulanmadı ve doküman bunu açıkça yazıyor.
- **`docs/H1.0-KAPI.md` §3'ün sayıları yeniden ölçülemedi** — fikstür DISABLED.
  §4.4'ün "K5/K6 dizileri yeniden ölçülmeli" uyarısı AÇIK.
- **`h10_gate_check` etkinleştirilmedi** — `engine/`'e dokunmak kâtibe yasak.
- **`web/` (127 sayfa) hiç açılmadı** — V10'un işi. 0C'nin 41 taşıyıcısının
  28'i, "duran-iddia 292 hit"in ezici çoğunluğu (119'u tek başına
  `web/patches.html`'de), ve F1–F8'in hepsi orada duruyor.

---

## 8. AÇIK BIRAKILAN BORÇ (gizlenmedi)

- Hakemin 2/3/4 numaralı kusurları (kaçış grameri · kapı kendi fazını
  denetlemiyor · D3 semantik değil) — üçü de AÇIK, `GECE/V9-B3.md` §5'te kayıtlı.
- **V9-B3'ün açtığı İKİ YENİ KAÇIŞ KANALI, işçi kendi ilan etti:**
  (a) gitignore'lı bir dizinin altına uydurma yol yazmak kapıyı yeşil bırakıyor
  (mutasyonla kanıtlandı); (b) dizinsiz uydurma dosya adı (`hayalet.cpp`) artık
  kırmızı düşmüyor, eskiden düşerdi. İkisi de her koşuda sayı + tam liste olarak
  BASILIYOR ama denetlenmiyor. **Bu bir gevşetmedir ve öyle yazıldı.**
- **"D2 0" ≠ "docs'ta ölü kaynak referansı kalmadı"** (V9-G ölçtü): aynı ölü
  dosyayı anan iki satır daha var, kapı ikisini de görmüyor —
  `docs/G5-OMUZ-PLANI.md:64` (backtick YOK) ve `docs/SATIS-SARTNAMESI.md:299`
  (kapsayıcı maddedeki BAŞKA bir yol için verilmiş "DOSYA YOK" ilanı, o maddedeki
  bu yolu da muaf kılıyor). Üstelik `:299` "patterns_real/ altında duruyor"
  diyor — temiz klonda bu YANLIŞ.
- **`docs/H1.0-KAPI.md` içinde iki uzlaşmayan "ön−arka" dizisi** (V9-D buldu,
  gizlemedi): §0 zemin **−13.50…−1.22mm** (`trace-match.py`) ↔ §2/G5
  **−13.83…−1.50mm** (`18-armscye-front-back.py`). Uçlarda 0.28–0.33mm.
  **UZLAŞTIRILMADI.**
- **`Logs/taban-T10-SONRA/pack-EU38` ile mühür başlığındaki koşu aynı paket
  değil** (V9-E buldu): T10-SONRA `print-info` **4 sayfa**, başlık **5 sayfa**
  diyor. O koşunun paketi kalmamış → **"5 sayfa" diskten doğrulanamıyor.**
- **`engine/dist/stitchu-engine.js` 1 günde 44 052 bayt büyümüş**
  (1 209 765 → 1 253 817). Dosya gitignore'da, büyüme commit'lerden izlenemez.
- **`SATIS-SARTNAMESI.md:31`'de `şartname-check` diye bir ctest adı anılıyor;
  öyle bir test YOK.** DOĞRULANMADI, dokunulmadı.
- **`~/damla_projects_2026/reports/` dizin olarak YOK** ama `ENV.md:10` hâlâ
  raporların oraya yazıldığını söylüyor. Repo içi `stitchu/reports/` kullanılıyor.
- **`patterns_real/` gizlilik çelişkisi derinleşti:** 41 dosya izleniyor,
  **5 dosya ne izleniyor ne gitignore'da** (`BUGRA-DEFTER.md`, `geometry/`,
  `tools/bugra-geometry-2026-07-23.json`). CLAUDE.md ikisini de "ASLA push
  edilmez" diyor. **Damla kararı, tek taraflı yapılmadı.**

---

## 9. SONRAKİ FAZA DEVİR — ÜÇ SAYI (V9 şefi kendi ölçtü)

1. **KIRMIZI = 6 · TEST = 115.** Açılış `GECE/log/V9.ctest.opening.txt`
   (114/6, 274.01 sn, `a6689ef`), kapanış `GECE/log/V9.ctest.final.txt`
   (115/6, 271.98 sn). AD kümesi birebir; hakem bağımsız `diff` aldı.
2. **SÖZLÜK TABANI = 10438 @ `495d58a` · bugün 10432 (−6, tabanın ALTINDA),
   YEŞİL.** `bash engine/tests/vocab_reference_check.sh`. Yalnız DÜŞEBİLİR.
3. **YENİ KİLİTLİ TABAN — docs doğruluğu** (`docs_truth_check`):
   **D1 0 · D2 0 · D3 0 / 140 sayısal satır.** Kapsam `docs/**` + `README.md`
   (7 md + 11 arşiv prose). Taban `engine/tests/docs-truth-baseline.json`,
   yalnız DÜŞEBİLİR. **TAŞINABİLİR:** temiz worktree ile çalışma dizini bit bit
   aynı hükmü veriyor (`GECE/log/V9-B3.kanit.txt`). Bilinen zaafı 4 madde,
   §5 ve §8'de adıyla yazılı.

**V10'un HAZIR GİRDİSİ:** `GECE/V0-0C.md` §1 (web'deki 28 taşıyıcı iddia),
§2 (duran-iddia 292 hit, 119'u `web/patches.html`), §4 (F1–F8 UI≠motor farkı,
en ağırı **site MTM satıyor, motor SABİT BEDEN** — 44 dosyada "your own
measurements"). ★ Bu sayılar **24 Ağu'da web kapsamında ölçüldü ve V9
web'e HİÇ DOKUNMADI**, ama aradaki fazlar dokunmuş olabilir: V10 de
devralmasın, KENDİ SAYIMINI koştursun (V9-A'nın docs'ta yaptığı gibi —
0C'nin docs sayılarının 5'i bayat çıkmıştı).

**★ FOTO→SPEC İSABETİ: %20.0 → %20.0.** V9 görü hattına DOKUNMADI
(prompt/model değişmedi, ücretli çağrı vetosu §5.3). V11'in 3. sorusu.

---

## 10. PROTOKOL DERSİ (V10 ŞEFİNE — kart değil KURAL)

1. **Kapının TABANI, kapının kendisi kadar önemlidir.** `docs_truth_check`
   D3 tabanı 52'yken kurulmuştu ve MUTASYONU YUTUYORDU — tek cümlelik bozma
   eşiğin altında kalıyordu. Kapı ancak taban 0'a kesilince kırılabildi.
   **Borç kaydeden taban, kapıyı süse çevirir.** Onarımı bitir, sonra kes.
2. **"Yeşil" bir DİZİNİN özelliği olabilir, dokümanın değil.** Hakem temiz
   worktree'de koşmasaydı bu kapı CI'da yeni bir kırmızı ad olurdu. Kurduğun
   her kapıyı **temiz checkout'ta da koştur** — `git worktree add` bedava.
3. **Devralınan cümleyi ölçmeden kartına yazma** (V7'nin dersi, V9'da yine
   tuttu): brief "docs 12 YALAN taşıyor" diyordu; ölçüm 5'inin önceki fazlarda
   çoktan kapandığını gösterdi. Ölçüm kartı kesilmeseydi faz **kapanmış işi**
   yeniden yapacaktı.
4. **İşçinin kendi kusurunu ilan etmesi çıktıdır.** V9-B3 açtığı iki kaçış
   kanalını kendi yazdı; V9-G "D2 0 ≠ ölü referans yok" diye kendi kapısının
   sınırını basdı; V9-C, V9-A'nın sayısını düzeltti. Bu fazın en değerli
   satırları rapor değil, **itiraf edilmiş sınırlar**.
5. Orakçı işletildi: 10 işçinin hiçbiri 60 dk tavanını commit'siz aşmadı;
   V9-B tavana yaklaşınca işi ikiye bölündü (B → B2 → B3), her parça ayrı
   commit aldı. Aynı anda en fazla 4 işçi koştu (C·D·E·F), dosya kümeleri
   kesişmedi — hakem çapraz kirlenme bulmadı.
