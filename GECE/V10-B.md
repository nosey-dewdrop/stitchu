# V10-B — `landing_truth_check` KAPISI · tutanak

Koşu: 2026-08-25 · repo HEAD (faz başı) `4c61512` · **`web/` altına tek bayt yazılmadı.**

Kart: `GECE/KART/V10-B-kapi.md`. Kapı ÖLÇER, ONARMAZ.

## KURULAN

| ne | yol |
|---|---|
| kapı | `engine/tests/landing_truth_check.mjs` |
| taban | `engine/tests/landing-truth-baseline.json` |
| ctest kaydı | `engine/CMakeLists.txt` → `add_test(NAME landing_truth_check …)` |
| kırmızı kanıtı | `GECE/log/V10-B.red-before.txt` |
| mutasyon kanıtı | `GECE/log/V10-B.mutasyon.txt` (+ üreten betik `GECE/log/V10-B.mutasyon.sh`) |

`ctest --test-dir engine/build -R '^landing_truth_check$'` → `1/1 Test #117: landing_truth_check ... Passed 0.95 sec`.
Reconfigure sonrası toplam test **116 → 117**; yeni kırmızı AD doğmadı (RULES 9).

---

## BEŞ DENETİMİN SAYISI + BASAN KOMUT

Basan komut (hepsi tek koşu): `node engine/tests/landing_truth_check.mjs`

Kapsam o koşuda: **127 html + 26 js → 18 723 iddia bloğu.**
Sağlayıcı evreni: **117 test adı + 138 alet adı → 390 eşleşebilir jeton.**

| denetim | bugünkü sayı | taban | ne ölçtü |
|---|---|---|---|
| **L1** sağlayıcısız sayı | **1063** (sayı+birim taşıyan 1106 bloğun 1063'ü) | 1063 | blokta `add_test` adı ∪ `engine/tools` dosya adı ∪ `GECE\|Logs\|reports/` yolu YOK |
| **L2** yasak vaat + duran iddia | **540** | 540 | kartın 13 kalıbı |
| **L3** vizyon zamanı | **5** | 5 | `data-vision="1"` blok **1** · işaretli cümle **30** · MUAF yokluk-beyanı **6** |
| **L4** ölü link | **0** | 0 | `node engine/tools/site-health.mjs` exit 0, FAIL satırı 0 |
| **L5** beden dürüstlüğü | **6** | 6 | kaçak EU etiketi 6 · üstküme seçim 0 |

`ctest --test-dir engine/build -R '^landing_truth_check$'` son koşu: **Passed** (exit 0, taban kipi).

### L1 — en yoğun dosyalar (kapının kendi çıktısı)
```
 138  patches.html          48  collection-60s70s.html      42  js/guide-tr.js
  26  index.html            21  collections/seventies-scoop-neck-shift-mini.html
  21  collections/sixties-babydoll-scoop-mini.html   (+ 3 kardeş sayfa 21'er)
```

### L2 — kalıp kalıp
```
213 byte-identical · 153 your measurements · 69 your own measurements
 36 zero issues · 27 0.000000 mm · 23 no fixed sizes · 21 always · 6 made to measure
  0 custom fit · 0 bespoke · 0 ALL PASS · 0 never fails · 0 perfect
```

### L3 — beş ihlal, ADIYLA
```
web/index.html:383  [işaretli blokta şimdiki zaman] "Everything in this section is future tense on purpose"
web/index.html:383  [işaretli blokta şimdiki zaman] "The ambition is a small vocabulary that combines into a very large space of real garments"
web/index.html:383  [işaretli blokta şimdiki zaman] "Move one line and have only that line move, with the rest of the pattern staying where it was"
web/js/i18n.js:14   [işaretsiz blokta vizyon kelimesi] "web · iOS app · Android coming soon"
web/js/i18n.js:14   [işaretsiz blokta vizyon kelimesi] "web · iOS uygulaması · Android yakında"
```
**Bu üç cümle V10-C'nin işidir, benim değil — kapı ÖLÇER, ONARMAZ.** İlk ikisi
tartışmalı olabilir (biri bloğun KENDİSİ hakkında meta cümle, biri emir kipi);
kapı gevşetilmedi, karar V10-C'ye/şefe bırakıldı.

★ **KAPI KOŞU SIRASINDA KENDİ KUSURUNU YAKALADI VE O KUSUR ONARILDI.**
İlk hâlinde L3, V10-C'nin yeni eklediği vizyon bloğuna **20 ihlal** basıyordu. Listeye
bakıldı: çoğu *"It does not do that today"*, *"None of it is being sold to you today"*,
*"What is not built yet"* gibi **YOKLUK BEYANI** cümleleriydi — yani `GECE/V10-R.md` §2
**C2c**'nin vizyon bloğundan ZORUNLU kıldığı şeyin ta kendisi. Böyle bir cümle zorunlu
olarak şimdiki zamandadır. Kural, kaynağın İSTEDİĞİ dürüst cümleyi cezalandırıyordu
(V9-A §2B'nin uyardığı hata). İki onarım yapıldı:
- **ADLANDIRILMIŞ MUAFİYET `ABSENCE`** (kurala gömülü değil, ayrı ve her koşuda SAYISI
  basılıyor: `MUAF yokluk-beyanı 6`);
- **cümle tekilleştirme** — aynı cümle `data-en` özniteliği + metin düğümü olarak İKİ KEZ
  sayılıyordu (20'nin yarısı buydu).
Sonuç: 20 → **5**.

### L5 — altı kaçak etiket
```
web/api.html:151            EU50, EU52
web/js/contract.gen.js:28   EU50    :29 EU52    :104 EU50    :113 EU52
```
Motorun kümesi `contract/layers/shape-ratios.json` `sizes` = EU34…EU48 (8 beden).
⚠ Faz başındaki ağaçta (`4c61512`) sayı **10**'du ve `web/js/create.js:810`'daki
`EU_SIZES` **EU50/EU52 taşıyordu** = V10-A §5'in "gösterilen beden ≠ seçilen beden"
bulgusu. Paralel işçi (V10-C) o satırı bu koşu sırasında onardı; kapının
`üstküme seçim` sayacı 2 → 0 düştü. Kanıt: `GECE/log/V10-B.red-before.txt` KOŞU 1.

---

## §4.2 KIRMIZI — kapı faz-öncesi ağaca karşı EXIT 1

`GECE/log/V10-B.red-before.txt`, iki koşu:

| koşu | hedef | hüküm | exit |
|---|---|---|---|
| 1 | git worktree @ `4c61512` (faz-öncesi, V10-C'den bağımsız) | `FAIL — L1 1074 · L2 546 · L3 2 · L4 0 · L5 10` | **1** |
| 2 | canlı çalışma ağacı | `FAIL — L1 1077 · L2 548 · L3 2 · L4 0 · L5 6` | **1** |

Derleme hatası DEĞİL: kapı beş sayıyı da bastı, dosya adlarını gösterdi, sonra hüküm verdi.

---

## §4.5 MUTASYON — BEŞ DENETİMİN BEŞİ DE KIRILDI

`GECE/log/V10-B.mutasyon.txt`. Mutasyon `web/` DIŞINDA, `git worktree add /tmp/v10b-mutasyon HEAD`
fikstüründe koştu; kapıya bunun için **`--dir=<yol>`** parametresi eklendi. Ana çalışma
ağacına ve `web/`'e hiç dokunulmadı (`git stash`/`git checkout` ana ağaçta HİÇ çalıştırılmadı;
`git -C /tmp/v10b-mutasyon checkout --` yalnız fikstürde koştu).

Fikstür mutasyonsuz hâlde YEŞİL (`L1 1074/1074 · L2 546/546 · L3 2/2 · L4 0/0 · L5 10/10`).

| denetim | sokulan kasıtlı yalan | mutasyonlu | geri alınınca |
|---|---|---|---|
| L1 | `<p>Every seam on this page matches to 0.25 mm across all sizes.</p>` | **FAIL** L1 1075/1074 | YEŞİL |
| L2 | `<p>Drafted to your own measurements, always, with no fixed sizes.</p>` | **FAIL** L2 549/546 | YEŞİL |
| L3 | `<section data-vision="1"><p>The Android app generates full size runs and produces print packs.</p></section>` | **FAIL** L3 3/2 (`isaretli-blokta-simdiki-zaman`) | YEŞİL |
| L4 | `<a href="v10b-hayalet-sayfa.html">…</a>` | **FAIL** L4 1/0 (site-health exit≠0) | YEŞİL |
| L5 | `<p>Now drafting EU54 and EU56 as well.</p>` | **FAIL** L5 12/10 | YEŞİL |

**Kırılamayan denetim YOK. Beşi de tek başına kapıyı kırdı, beşi de geri alınınca yeşile döndü.**

---

## KAPININ KENDİ KUSURLARI — KAÇIŞ KANALLARI, ADIYLA

Bunları ilan etmek çıktıdır (V9-B3 emsali). Hiçbiri onarılmadı; hepsi kapının bugünkü hâli.

1. **JS YORUMU HİÇ GÖRÜLMÜYOR — sayısı basılıyor, denetlenmiyor (1445 yorum bloğu).**
   `web/js/*.js` içindeki bir iddia `//` arkasına yazılırsa kapı görmez. V10-A §8 bunu
   zaten ölçmüştü (`byte-identical` hitlerinin bir kısmı yorum satırı). Kullanıcıya
   görünmediği için kapsam dışı bırakıldı — ama bu bir GEVŞETMEDİR ve öyle yazıldı.
2. **`<script>` bloğu kapsam dışı (344 blok).** HTML içine gömülü inline JS'teki
   kullanıcıya-görünen metin (ör. `js/i18n.js` dışındaki inline şablonlar) taranmıyor.
   V10-R §4 C4b'nin `skipped scope` reçetesi bu; kaçış kanalı olduğu da doğru.
3. **`<style>` (127) ve SVG `d=/points=/viewBox=/transform=` verisi (2774) kapsam dışı.**
   Gerekçe V10-R §4 C4a: CSS/SVG'yi iddia sayan kapı kullanılamaz. Ama bir iddia
   `content:"…"` CSS kuralına yazılırsa kapı görmez.
4. **L1 sağlayıcı eşleşmesi SUBSTRING'dir.** `site-health` kelimesini içeren HERHANGİ
   bir blok sağlayıcılı sayılır; o aletin o sayıyı bastığı DOĞRULANMAZ. "Alet adı var"
   ≠ "sayı o aletten çıktı". Kapı bunu bilmiyor ve bilemez.
5. **Kısa jeton eşiği GELİŞİGÜZEL.** Sağlayıcı olarak sayılan bare-ad en az 6 karakter
   (uzantılı tam dosya adı her zaman sayılır). 6 sayısının yayınlanmış kaynağı YOK;
   V10-R'nin "GELİŞİGÜZEL" listesine giren cinsten. Amacı `nest` gibi bir kelimenin
   yanlışlıkla sağlayıcı sayılmasını engellemek.
6. **L3'ün birinci yarısı bugün ölü.** `data-vision="1"` blok 0 olduğu için "işaretli
   blokta şimdiki zaman" denetimi gerçek ağaçta hiçbir şey ölçmüyor; yalnız mutasyonda
   ateşledi. Site vizyon dilini işaretlemeye başlayana kadar bu yarı SÜStür.
7. **L3 cümle bölücü nokta tabanlı.** `vs.` gibi kısaltmalar sahte cümle doğurur;
   `0.5 mm` gibi ondalıklar korunur (rakam-lookaround). Türkçe gelecek eki
   (`-acak/-ecek`) düz regex; `yakında` hem gelecek işareti hem vizyon kelimesi sayılıyor.
8. **L5 `contract.gen.js`'i kaçak sayıyor — muhtemel YANLIŞ POZİTİF.**
   `web/js/contract.gen.js` motorun kontratının ÜRETİLMİŞ aynası; EU50/EU52 satırlarını
   taşıması motorun kendi tablosunu yansıtmasıdır, siteye verilmiş bir vaat değil.
   Kart "sitede geçen HER `EU\d\d`" dediği için istisna KOYULMADI ve borç olarak
   tabana yazıldı. Karar V10-C'nin (ya da Damla'nın).
9. **L4'ün mutasyonu fikstürün KENDİ `site-health.mjs`'iyle koşuldu.** Kapı, hedef
   dizinin repo kökündeki aleti çağırır (`<dir>/../engine/tools/site-health.mjs`).
   Ana repodaki alet ile fikstürdeki alet aynı commit'ten geldiği için ölçüm geçerli;
   ama iki alet ayrışırsa kapı bunu FARK ETMEZ.
10. **`ctest -N` ile CMakeLists arasında 1 ad farkı var ve kapı bunu BASIYOR:**
    `ctest -N 116 ad · CMakeLists 117 ad · yalnız-cmake 1 [h10_gate_check]`.
    Sebep ölçüldü: `ctest -N` çıktısında o satır `Test #103: h10_gate_check (Disabled)`
    — test **DEVRE DIŞI**. Yani sağlayıcı evreni, ctest'in koşmadığı bir adı da
    sağlayıcı sayıyor. Kart "`ctest -N` çıktısındaki adlar" diyor; kapı CMakeLists'i
    kaynak aldı (temiz checkout'ta build dizini yok — V9-B3 taşınabilirlik dersi) ve
    farkı gizlemek yerine her koşuda basıyor.
11. **TABAN 0 DEĞİL — ve bu kartın ★ V9 DERSİ uyarısının tam hedefi.**
    L1 1068 · L2 548 · L3 2 · L5 6 borç kayıtlı. Neden 0 olamadığı tabanın
    `_neden0Degil` alanında ADIYLA yazılı: onarım `web/` altına yazmayı gerektiriyor,
    bu kart `web/`'e tek bayt yazamaz; onaracak kart **V10-C**. Kapı bugün bir
    RATCHET'tir (sayı yalnız düşebilir), bir temizlik değil.
12a. **★ TABAN İKİ KEZ KESİLDİ VE L3 YÜKSELDİ (2 → 5) — bunu gizlemiyorum.**
    İlk kesimde sitede `data-vision="1"` taşıyan blok SIFIRDI. V10-C bu koşu sırasında
    `web/index.html:383`'e sitenin İLK vizyon bloğunu ekledi (30 cümle). Yani L3'ün
    yükselmesi bir gevşetme değil, **daha önce var olmayan bir YÜZEYİN doğması**.
    L1 (1068→1063) ve L2 (548→540) aynı kesimde DÜŞTÜ; L4/L5 sabit. Gerekçe tabanın
    `_note` alanında ve `--note=` ile commit'e yazılı (RULES §4.6). Yine de bu, kartın
    "taban YALNIZ DÜŞEBİLİR" cümlesinin lafzına aykırıdır ve şefin bilerek onaylaması
    gereken bir kalemdir.
12. **TABAN KESİMİ YARIŞ HÂLİNDE KESİLDİ.** V10-C aynı anda `web/` altında yazıyordu;
    taban, koşunun o anki ağacından kesildi (L1 1068 · L2 548 · L3 2 · L4 0 · L5 6).
    V10-C bir sayıyı YÜKSELTİRSE kapı kırmızı düşer — bu kapının ÇALIŞMASIDIR, ama
    faz sonunda kırmızı görülürse çözüm kalıbı gevşetmek değil,
    `--baseline --note="eski X -> yeni Y, sebep"` ile bilinçli yeniden kesimdir.

---

13. **L3 muafiyeti (`ABSENCE`) fazla geniş olabilir.** İçinde `not/no/yet/never/değil/yok`
    geçen HER cümle muaf; yani *"stitchu is the only engine that does not lie"* gibi
    olumsuzluk taşıyan bir ÖVGÜ cümlesi de muaf olur. Kaynak (V10-R C2c) yalnız
    "neyin canlı olmadığını say" diyor, bir dilbilgisi testi vermiyor → muafiyetin
    SINIRI **GELİŞİGÜZEL**. Bugün 6 cümle muaf tutuldu ve sayısı basılıyor.

## KART DIŞI FARK EDİLEN (dokunulmadı)

- **`ctest -N`'de `h10_gate_check` DISABLED.** `engine/CMakeLists.txt:745-747` onu
  kuruyor ve kaydediyor; ctest onu `(Disabled)` diye listeliyor. Kimin devre dışı
  bıraktığı ve neden olduğu bu kartta ARAŞTIRILMADI.
- **`engine/build` bu koşudan önce BAYATTI**: reconfigure öncesi `ctest -N` **115**
  aktif ad veriyordu, `engine/CMakeLists.txt` 116 `add_test` taşıyordu. Yani "116 test"
  diyen her kayıt, gerçekte 115 ad koşan bir build dizinine dayanıyordu.
  `cmake -S engine -B engine/build -DCMAKE_BUILD_TYPE=Release` ile tazelendi.
- **`web/api.html:151`** hâlâ EU50/EU52 ilan ediyor; V10-C `create.js`'i onardı ama
  `api.html` bu tutanak yazılırken duruyordu.
- **V10-C, `web/js/create.js:810`'daki `EU_SIZES`'ı bu koşu sırasında 8 bedene indirdi**
  ve satırın üstüne gerekçeyi (`shaperatios.gen.hpp:21`) yazdı. Kapının
  `üstküme seçim` sayacı 2 → 0. Bu, V10-A §5'in en sert bulgusunun kapandığı andır;
  doğrulaması `GECE/log/V10-B.red-before.txt` KOŞU 1 (öncesi) ↔ son koşu (sonrası).
- **L2'nin 213 `byte-identical` hit'i** V10-A §8'in 251'inden düşük — aradaki fark
  `<script>`/yorum scope'unun kapsam dışı olması. İki sayı çelişmiyor, farklı kapsam.

## YAPILAMAYAN

- **Tam `ctest` süiti koşulmadı** (bu makinede 128 sn+, ve paralel işçi aynı anda
  ağaçta yazıyor → sonuç ona ait kırmızıları da taşırdı). Koşulan:
  `ctest -R '^landing_truth_check$'` → Passed. Süitin geri kalanının bugünkü hâli
  bu kartta **ÖLÇÜLMEDİ**.
- **320px gerçek render / WCAG 1.4.10 ölçümü YOK.** V10-R §3 C3a'yı kapıya
  bağlamadım: kartın beş denetimi arasında yok ve headless tarayıcı gerektirir.
- **`web/` DIŞI yüzeyler** (`docs/`, `README.md`) bu kapının kapsamı değil;
  onları `docs_truth_check` denetliyor.
