# V10-D — YALAN SÜPÜRGESİ · L1..L5 ÖNCE/SONRA

Koşu: 2026-08-25 · dal `main` · açılış commit `7a1e535` (V10-C'nin ikinci commit'i).
Kapı: `engine/tests/landing_truth_check.mjs` — **kodu DEĞİŞTİRİLMEDİ**
(`git status --porcelain engine/` bu turda yalnız `engine/tests/landing-truth-baseline.json`
gösteriyor; kapının `.mjs` dosyası HEAD'de duruyor).

---

## 1. L1..L5 ÖNCE/SONRA — SAYIYI BASAN KOMUT

Basan komut (ikisi de):
```
node engine/tests/landing_truth_check.mjs
```
ÖNCE çıktısı bu turun başında alındı, SONRA çıktısı `GECE/log/V10-D.kapi.txt`.

| denetim | ÖNCE (taban 7a1e535) | SONRA | fark | yön |
|---|---|---|---|---|
| **L1** sağlayıcısız sayı | **1063** | **937** | −126 | DÜŞTÜ |
| **L2** yasak vaat + duran iddia | **540** | **0** | −540 | **SIFIRLANDI** |
| **L3** vizyon zamanı | **2** (taban 5) | **0** | −2 | **SIFIRLANDI** |
| **L4** ölü link | **0** | **0** | 0 | sabit |
| **L5** beden dürüstlüğü | **6** | **4** | −2 | DÜŞTÜ |

**Hiçbir L sayısı YÜKSELMEDİ.**

L2'nin kalıp kırılımı (kapının kendi `kalipBasina` çıktısı):

| kalıp | ÖNCE | SONRA |
|---|---|---|
| `byte-identical` | 213 | **0** |
| `your measurements` | 153 | **0** |
| `your own measurements` | 69 | **0** |
| `zero issues` | 36 | **0** |
| `no fixed sizes` | 23 | **0** |
| `0.000000 mm` | 23 | **0** |
| `always` | 21 | **0** |
| `made to measure` | 2 | **0** |
| **TOPLAM** | **540** | **0** |

Kesilmiş taban (`engine/tests/landing-truth-baseline.json`):
```
$ node engine/tests/landing_truth_check.mjs --baseline --note="V10-D: …"
  L1 taban: 937 · L2 taban: 0 · L3 taban: 0 · L4 taban: 0 · L5 taban: 4
$ node engine/tests/landing_truth_check.mjs
  HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4      EXIT=0
```

Site sağlığı (`GECE/log/V10-D.site-health.txt`):
```
$ node engine/tools/site-health.mjs                       → EXIT=0
checked: 127 pages, 2604 internal refs, 124 sitemap urls, 124 indexable pages
OK  site-health: no dead links, sitemap matches the site, one version.
```
Ölü link ÖNCE 0 → SONRA **0**; iç referans 2604 → **2604** (değişmedi).

---

## 2. SIRA 1 — MTM YALANI (L2, kartın birinci işi)

Motorun gerçeği tek satırda: `contract/layers/shape-ratios.json` `sizes` =
**8 beden, EU34–EU48**. Site 284 hit'le ısmarlama satıyordu.

Değişen cümle şablonları (her biri ONLARCA dosyada tekrar ediyordu):

| eski | yeni | nerede |
|---|---|---|
| "stitchu drafts the *X* **to your own measurements** … with **no fixed sizes** and no paywall." | "stitchu drafts the *X* **in the eight fixed sizes EU34 to EU48 that `contract/layers/shape-ratios.json` publishes** … with no paywall." | `web/styles/*.html` 23 sayfa (ld+json + görünen FAQ) |
| "To get it **in your own measurements**, draft it free on the create page and the engine redraws every piece to fit you." | "To get it **in another size**, draft it free on the create page **in any of the eight fixed sizes, EU34 to EU48**." | `web/collections/*.html` 16 sayfa (EN + TR) |
| "stitchu drafts the pattern **to your exact measurements** … every look is benchmark-checked against a sewn reference." | "stitchu **also ships fixed sizes**, the eight in `contract/layers/shape-ratios.json`, EU34 to EU48; the difference is that a real pattern-making engine draws them, not a scanned sheet." | aynı 16 sayfa |
| "the estimate **scales with your measurements** when you draft it" | "the estimate **scales with the size you draft**" | koleksiyon sayfaları |
| "*n* pieces · fabric **scales to your measurements**" | "*n* pieces · fabric **scales with the size**" | `web/collection-60s70s.html` + koleksiyonlar |
| "Draft this **to your measurements**, free" | "Draft this **in a fixed size**, free" | 32 hit |
| "**made to measure**" (başlık + `<h2>`) | "drafted from typed numbers" / "Why draft from typed numbers?" | `web/shop-shift-dress.html:8,122` |
| "**Not your own measurements.**" (V10-C'nin dürüst olumsuzlaması) | "**Fixed sizes, not a body you type in.**" | `web/index.html:176,309` |

### ★ AYRI TUTULAN HAT — kart bunu ADIYLA istiyordu
Kart: *"Bir sayfa gerçekten ölçü alıyorsa o AYRI bir cümledir ve sayfada hangi
hattın ne yaptığı ADIYLA ayrılır."*

**Ölçüldü:** `web/js/create.js:1` create akışı GERÇEKTEN yedi ölçü alıyor
(`web/js/engine.js:183 draft(spec, measurements)` gövdeyi WASM'a geçiriyor).
O yüzden create/print/privacy hattındaki cümleler SİLİNMEDİ, **hattın adıyla**
yeniden yazıldı — pazarlama vaadi olarak değil, ekrandaki alanın tarifi olarak:

| dosya:satır | eski | yeni |
|---|---|---|
| `web/js/i18n.js` `how.2.title` | "Drafted to your measurements" | "Drafted **from the seven numbers you type**" |
| `web/js/i18n.js` `create.measure.title` | "Your measurements" | "Your body numbers" |
| `web/js/i18n.js` `create.spec.addmeasure` | "add your measurements" | "add **the seven body numbers**" |
| `web/js/i18n.js` `hero.sub` / `web/privacy.html` (4 yer) | "your measurements never leave your browser" | "**the numbers you type** never leave your browser" |
| `web/js/print.js` `demoWarn` | "…not drafted to your measurements yet. Add your seven measurements on the site for a pattern that fits you." | "STANDARD EU38 SIZE. **Type the seven body numbers on the create page** to draft it from them instead." |
| `web/js/print.js` `gradeChartTitle` | "pick your size **by your own measurements**" | "pick your size **by comparing your body to this chart**" |
| `web/shop-shift-dress.html:145` | "Type your measurements in centimetres:" | "Type **six body numbers** in centimetres:" |

---

## 3. SIRA 2 — DURAN İDDİA (L2, RULES 6)

| eski | yeni | sağlayıcı ADI | adet |
|---|---|---|---|
| "golden **byte-identical**" | "golden diff unchanged **(golden_check)**" | `add_test(NAME golden_check)`, `engine/CMakeLists.txt` | 44+32 |
| "**byte-identical**" (kalan bütün biçimler) | "unchanged under **golden_check**" | aynı | 137 |
| "bayt bayt aynı / aynen" (TR) | "**golden_check** altında değişmemiş" | aynı | TR karşılıkları |
| "with **zero issues**" | "with **no issue in that run's validator log**" | — (geçmiş koşunun kaydı; uydurma alet adı KONMADI) | 36 |
| "The geometry was **always** correct and sewable" | "The geometry was correct and sewable **in that run**" | — | 12 |
| "Fields with no safe threshold **always** go to the teacher" | "…**go to** the teacher" | — | 4 |
| "**Always** preshrink a natural fibre" | "**Preshrink** a natural fibre" | — | 4 (`web/guide/*.html`) |
| "the same input **always** draws the same line" | "the same input draws the same line" | — | 2 |

### ★ `0.000000 mm` — UYDURMA HASSASİYET, KART HAKLIYDI
23 stil sayfasında aynı tablo satırı duruyordu:
`<tr><td>seam-pair precision</td><td class="v">0.000000 mm</td></tr>`

`engine/tools/precision-report.js` **`toFixed(2)` ile İKİ basamak** basıyor
(V10-C §5'te koşuldu: `precision report: worst pair mismatch 0.00 mm | 0 FAILURES`).
Altı basamak ilan edilemez. Yeni satır:

> `worst pair mismatch 0.00 mm, printed by engine/tools/precision-report.js (the tool prints two decimals)`

⚠ **DÜRÜST SINIR, SAYFADA GİZLENMEDİ AMA BURADA DA YAZILI:** `precision-report.js`
o sayıyı **stil başına değil**, kendi kontrol ettiği dört dikiş çifti üzerinde
basıyor. Satır artık aletin adını taşıyor ama "bu stil için ölçüldü" DEMİYOR.
Alet **bu turda yeniden KOŞULMADI**; sayı V10-C'nin koşusundan alındı.

### ★ KANITSIZ MATRİS SAYILARI ÇIKARILDI (L1'e de dokundu)
V10-C §10 üçünü "hâlâ KANITSIZ" diye kaydetmişti; bu turda da koşulmadılar.
RULES 6: sayı gidiyorsa cümle de gider.

| eski | yeni | dosya |
|---|---|---|
| "**70,200** body × option drafts, 0 failures" | "harness: **engine_check** (the full draft matrix). Not re-run for this page, so no count is printed here." | 23 stil sayfası |
| "web fuzz sweep **19,780 / 0** · vocab sweep **37,800 / 0**" | "tools: `engine/tools/web-fuzz.js` and `engine/tools/vocab-sweep.cpp`. Not re-run for this page…" | 23 stil sayfası |
| "Validated across the full **70,200**-draft matrix" | "Covered by **engine_check**, the full draft matrix; the run was not repeated for this page…" | 23 stil sayfası (ld+json + FAQ + tablo) |
| stat `70,200` + "**zero validation issues, ever**" | stat `engine_check` + "It was not re-run for this page, so no draft count and no issue count is printed here." | `web/benchmark.html:93` |

`grep -rho "70,200\|19,780\|37,800" web/` → **0 hit**.

---

## 4. SIRA 3 — L3 VİZYON ZAMANI

`web/js/i18n.js:14` `hero.platforms` işaretsiz bir blokta "coming soon / yakında"
taşıyordu. Bir **JS stringi `data-vision="1"` ile İŞARETLENEMEZ** (kapının kendi
kuralı, `landing_truth_check.mjs:343`), yani taşıma seçeneği yoktu → vaat SİLİNDİ.

```
eski: 'web · iOS app · Android coming soon'   / 'web · iOS uygulaması · Android yakında'
yeni: 'web only, that is the whole platform list today' / 'yalnız web; bugünkü platform listesi bu'
```
Gerekçe satırın üstüne yorum olarak yazıldı: `App/` klasörü referans kopya,
**sevk edilmiş bir iOS uygulaması YOK** (CLAUDE.md "App/ (old iOS app, reference only)").

Aynı sözlükte duran `hero.title` de MTM satıyordu ("drafted to your body") →
"in eight fixed sizes, EU34 to EU48" oldu.

⚠ **ÖLÇÜM:** `hero.*` anahtarlarının HİÇBİRİ bugün kullanılmıyor —
`grep -rn "hero.platforms\|hero.title\|hero.sub" web/ --include=*.html --include=*.js`
(i18n.js hariç) → **0 hit**. Yani bunlar sevk edilen ama ölü stringlerdi.
Silinmedi, düzeltildi (anahtar `t()` ile çağrılırsa diye).

---

## 5. SIRA 4 — L5 BEDEN DÜRÜSTLÜĞÜ

`web/api.html:151` `<select id="gr-to">` kullanıcıya **EU50** ve **EU52** seçtiriyordu.
Motorda o iki bedenin yayınlanmış şekil oranı YOK. İkisi de kaldırıldı, gerekçe
HTML yorumu olarak satırın üstüne yazıldı. L5 **6 → 4**.

### ★ KAPININ BİLİNEN İSTİSNASI — `web/js/contract.gen.js` (ADIYLA)
Kalan 4 L5 kaçağının HEPSİ tek dosyada ve o dosya **ÜRETİLMİŞ**:

```
web/js/contract.gen.js:1   // GENERATED by engine/tools/gen-contract.mjs from contract/tables.json — DO NOT EDIT.
```

| satır | etiket |
|---|---|
| `web/js/contract.gen.js:28` | `EU50` |
| `web/js/contract.gen.js:29` | `EU52` |
| `web/js/contract.gen.js:104` | `EU50` |
| `web/js/contract.gen.js:113` | `EU52` |

**Kök kaynak `contract/tables.json`:**
- `:13` `"euSizes": [… "EU48", "EU50", "EU52"]` — **10 beden**
- `:82` `"EU50": [116, 98, 122, 41, 43, 61, 40]`
- `:83` `"EU52": [122, 104, 128, 42, 43.5, 61.5, 41]`

Yani `contract/tables.json` **10 beden**, `contract/layers/shape-ratios.json`
**8 beden** ilan ediyor. Bu bir metin hatası değil, **KONTRATLAR ARASI ÇELİŞKİ**.
Elle düzeltmek yasak (üretilmiş dosya) ve `contract/` de bu kartın alanı değil.
→ `DAMLA-KUYRUK.md` **K-V10D**, 3.8.d.

---

## 6. DOKUNULAN HER DOSYA (91 web dosyası + 1 taban)

| grup | adet | ne değişti |
|---|---|---|
| `web/patches.html` + `web/patches/*.html` | 37 | `byte-identical` → `golden_check`, `zero issues`, `always`, MTM cümleleri |
| `web/styles/*.html` | 24 | MTM FAQ şablonu, `0.000000 mm`, 70,200 / 19,780 / 37,800, `byte-identical` |
| `web/collections/*.html` | 16 | MTM FAQ şablonu (EN+TR), kumaş tahmini cümlesi |
| `web/guide/*.html` | 4 | "Always preshrink" → "Preshrink" |
| `web/index.html` | 1 | "Not your own measurements" → "not a body you type in" (2 yer) |
| `web/api.html` | 1 | `:151` EU50/EU52 kaldırıldı + gerekçe yorumu |
| `web/benchmark.html` | 1 | `70,200` stat'ı `engine_check` adına döndü; MTM + gizlilik cümlesi |
| `web/privacy.html` | 1 | 4 yerde "your measurements" → "the numbers you type" |
| `web/shop-shift-dress.html` | 1 | `made to measure` başlık + `<h2>` + "Type six body numbers" |
| `web/showcase.html` | 1 | `zero issues` (4 hit) |
| `web/signature.html` | 1 | "in your measurements" + "always draws" (EN+TR) |
| `web/collection-60s70s.html` | 1 | "fabric scales to your measurements" (24 yer, EN+TR) |
| `web/js/i18n.js` | 1 | `hero.platforms`, `hero.title`, `how.2.title`, `create.measure.title`, `create.spec.addmeasure`, `hero.sub` |
| `web/js/print.js` | 1 | `demoWarn`, `gradeChartTitle` (EN+TR) |
| `engine/tests/landing-truth-baseline.json` | 1 | taban kesildi |

`docs/`, `README.md`, `GECE/KOSU.md`, `web/js/contract.gen.js`, `web/css/**`,
`engine/**` (taban hariç): **TEK BAYT YAZILMADI**.

---

## 7. ★ EN BÜYÜK BULGU — BU TURUN İŞİNİN ÇOĞU ÜRETİLMİŞ DOSYAYA YAZILDI

Bu bir uyarı değil, **ÖLÇÜM**. Düzelttiğim sayfaların büyük bölümü elle yazılmadı,
`engine/tools/` altındaki üreteçlerden çıkıyor ve **üreteçlerin kaynağında yalan
CÜMLE CÜMLE duruyor**:

```
$ grep -nF "your own measurements" engine/tools/*.mjs
engine/tools/gen-style-pages.mjs:810      `Yes. stitchu drafts the ${low} to your own measurements … with no fixed sizes and no paywall.`
engine/tools/gen-collection-pattern.mjs:113  `… To get it in your own measurements, draft it free on the create page …`

$ grep -nF "your exact measurements" engine/tools/*.mjs
engine/tools/gen-collection-pattern.mjs:119  `… stitchu drafts the pattern to your exact measurements …`

$ grep -nF "0.000000" engine/tools/gen-style-pages.mjs
730:  const GOLDEN = '0.000000 mm'; // opt-in features leave the base golden byte-identical

$ grep -rIF "byte-identical" engine/tools/ | wc -l
28
```

Üreteçler ve bastıkları alanlar:
- `engine/tools/gen-style-pages.mjs:971` → `web/styles/*.html` (**24 sayfa**)
- `engine/tools/gen-vintage-page.mjs:159` → `web/collection-60s70s.html` (**1 sayfa**)
- `engine/tools/gen-collection-pattern.mjs` + `gen-collections-page.mjs` → `web/collections/**` (**16 sayfa**)

**SONUÇ: bu turda düzeltilen 41 sayfa, üreteçlerden herhangi biri bir daha
koşarsa YALANLARIYLA GERİ GELİR ve L2 tabanı 0'dan yukarı fırlar.**
Kart `engine/` altına yazmayı açıkça yasakladı (§YASAKLAR), o yüzden üreteçlere
DOKUNULMADI. Bu, bu kartın kapatamadığı en büyük cephe.
→ `DAMLA-KUYRUK.md` **K-V10D**.

---

## 8. YAPILMADI (ADIYLA)

- **L1 SIFIRLANMADI: 937 sağlayıcısız sayısal blok kaldı.** Kart §5 "hepsini
  bitirmek ŞART DEĞİL" diyordu; düşürüldü (1063 → 937), bitirilmedi.
  En yoğun üç dosya, kapının kendi çıktısından:
  - `web/patches.html` **88** — yama notlarındaki "55 of 55 tests" tipi sayılar.
    Bunları basan **tek bir alet adı YOK** (ctest'in suite sayımı). Uydurma
    sağlayıcı yazmak kartın "sahte sayı koyma" yasağını çiğnerdi → dokunulmadı.
  - `web/collection-60s70s.html` **48** — "6 pattern pieces · 2.1 m fabric at
    140 cm". Gerçek motor çıktısı ama **üretilmiş dosya** (§7); elle sağlayıcı
    eklemek üreteçle çelişir → dokunulmadı.
  - `web/js/guide-tr.js` **42** — ★ **KAPININ KUSURU, SAYFANIN DEĞİL.** İçerik
    dikiş TALİMATI: "Trim to 6 mm, clip into the curve every 2 cm", "hem with a
    2 cm double fold". Bunlar motor iddiası değil, el işi tarifi; bir test adı
    eklemek anlamsız olurdu. Kapının `NUM_UNIT` regex'i talimat ile iddiayı
    ayırt edemiyor. **Kapı GEVŞETİLMEDİ**, kusur burada ADIYLA yazıldı.
- **`ctest` KOŞULMADI.** Bu bir `web/` kartı; `engine/` altına yalnız taban
  JSON'u yazıldı, C++ derlenmedi.
- **`engine_check`, `web-fuzz.js`, `vocab-sweep.cpp`, `precision-report.js`,
  `golden-diff.py` BU TURDA KOŞULMADI.** Sayfalara yazılan alet ADLARI o
  aletlerin VARLIĞINI bildiriyor, o sayıyı bugün bastıklarını DEĞİL. Kapının
  kendi kusuru zaten bunu söylüyor (`landing_truth_check.mjs:53`: "L1 sağlayıcı
  eşleşmesi SUBSTRING'dir").
- **Tarayıcıda render YAPILMADI.** Hiçbir sayfa açılıp gözle görülmedi; değişen
  şey metin, ama bazı yeni cümleler eskisinden UZUN (ör. stil sayfası kanıt
  tablosu hücreleri) ve dar ekranda taşma **ÖLÇÜLMEDİ**.
- **`web/js/contract.gen.js` ve `contract/tables.json` DÜZELTİLMEDİ** — §5.
- **DEPLOY YAPILMADI.** `scripts/deploy.sh` çalıştırılmadı, subtree push
  edilmedi, `?v` damgasına elle dokunulmadı. Yayın Damla'nın adımı.

---

## 9. KART DIŞI, DOKUNULMADI (ölçüldü, yazılıyor)

1. **Tabanın `_neden0Degil` metni artık BAYAT.** O metin kapının kodunda SABİT
   (`landing_truth_check.mjs:443-449`) ve hâlâ "L2 540 yasak-vaat hit'i …
   Onaracak kart: V10-C" diyor. Bugün L2=0, L3=0. Kapının kodu yasak olduğu için
   düzeltilemedi; taban dosyası bu yüzden **kendi içinde çelişiyor** (`L2.taban: 0`
   ama `_neden0Degil.L2: "0 yasak-vaat hit'i … Onaracak kart: V10-C"`).
2. **`web/benchmark.html:113` rakip fiyatı hâlâ ayakta:** "Similar tools charge
   **$34–49/mo**". V10-C aynı cümleyi `web/index.html:299`'dan **ölçülmediği için
   SİLDİ**; benchmark sayfasındaki kopyası kaldı. Kartın kalıp listesinde
   olmadığı için dokunulmadı.
3. **`web/showcase.html:109,193`** "the validator stays silent" / "without a
   single validator complaint" — `zero issues` gibi bir duran iddia ama kalıp
   listesinde yok, dolayısıyla kapı görmüyor ve bu turda değiştirilmedi.
4. **`web/index.html:34` `body{overflow-x:hidden}`** hâlâ orada (V10-C de not
   düşmüştü). Bu tur CSS'e girilmedi.
5. **`web/atolye.html`'in 10 `byte-identical` hit'inin hepsi JS YORUMU**
   (`<script>` içi = kapının SKIPPED SCOPE'u). Kullanıcıya görünmüyor, kapı
   saymıyor, kod hakkında doğru bir teknik cümle → bilerek dokunulmadı.
   Aynısı `web/collections/*.html` ve `web/collections/index.html`'deki
   "one source, byte-identical bar" CSS yorumları için de geçerli.
