# V6 — GİRİŞ HATTI: foto + prompt → spec, ve editleme temeli

Şef: tek context, kod yazmadı. 10 kart (R·A·B·C·D·E·F·G·H·I·J), 3 dalga,
2 hakem çağrısı (birincisi **KALDI**), 1 kâtip. Faz **kapandı**; ana dalda
kalan mühendislik 89 satır, reddedilen 2614 satır yan dalda.

Sıra §2.1'in emrettiği gibi işledi: **ÖNCE ÖLÇÜM, SONRA İYİLEŞTİRME.**
Dalga 1'in dördü de ölçümdü, tek satır onarım içermiyordu.

---

## 1. ÖLÇÜLEN (sayı + komut + hash)

### 1.1 Açılış makinesi — ŞEF KENDİ KOŞTU, DEVRALMADI
`cmake --build engine/build -j8 && ctest --test-dir engine/build --output-on-failure`
@ `3fa8002` → `GECE/log/V6.ctest.opening.txt`, 275.47 sn:
```
95% tests passed, 6 tests failed out of 113
9 flat_pattern_agree_check · 12 flat_artifact_census · 13 style_check
20 sizechart_source_check · 91 contract_check · 96 figure_check
114/114 Test #114: vocab_reference_check ... Passed
```
KOSU.md'nin devrettiği "113 test · 6 kırmızı" **DOĞRULANDI**. Ve kritik
ayrıntı: `vocab_reference_check` faz açılışında **YEŞİLDİ** — bu satır §4'ün
sonraki hükmünü belirledi.

### 1.2 Foto→spec isabeti — ÖNCE (V6-A, `29579e5`)
`node engine/tools/foto-spec-olcum.mjs --offline --bank vision/eval/live-2026-08-22.json`
```
FOTO 5 · TAM DOĞRU SPEC 1 (%20.0)
ALAN YARGISI 51 · tutan 47 (%92.2)
GORME 4 · KELIME 0 · MOTOR 0
```
0B ile **birebir aynı çıktı** (banka 22 Ağu'dan beri değişmedi). Payda **5**,
19 değil: kalan 14 etiketli foto ücretli `/api/analyze` çağrısı ister
(§5.3 kalıcı veto) → **ölçülmedi, TAHMİN EDİLMEDİ.**

Dört tahmin dosyası, `node vision/eval.js <dosya>`, paydaları EŞİT DEĞİL:
live-2026-08-22 **47/50 %94** (n=5) · opus **67/78 %86** (n=9) ·
siglip **105/162 %65** (n=19) · clip **71/162 %44** (n=19).
**Bu bir sıralama değil, dört ayrı ölçüm.**

### 1.3 KONUM — 0B'nin hata sınıflamasına giren yeni sınıf
26 serbest kanal teriminin **15'i (%57.7) konum ibaresi taşıyor**, **11'i
spec'te yer bulamıyor**. En sık kayıp konum `front` (5 terim) ve `bodice` (3):
spec'in 16 alanının **hiçbiri ön/arka ayrımı taşımıyor**. Bu KELIME'den ayrı
bir kusur — kelime sözlüğe eklense bile spec onu nereye koyacağını bilmiyor.

★ Tek TAM DOĞRU SPEC olan foto (`05-empire-waist-gown`) aynı zamanda **en çok
KONUM kaybeden** foto: alan isabeti %100 iken serbest kanalda 8 terimin 5'i
yerini kaybediyor. **"Alan isabeti" ile "giysiyi anlamak" aynı şey değil.**

İki sicil kontrolü: `contract/terms.json` (52 kanonik/246 isim) ve
`dataset/vocab-canonical.json` (88 isim) — 26/26 "sicilde yok" sayısı
**değişmiyor**. 0B'nin şüphesi ölçüldü ve çürüdü.

v2 ifade edilebilirliği (`--v2 live-baseline-oldprompt.json`): 68 fotonun
**15'i (%22.1)**; `sleeve` absent tek engel olarak **19'unu (%27.9)** düşürüyor,
engelin geçtiği toplam 35. ⚠ 0B'nin %36.8'iyle KIYASLANMAZ (farklı eşleme) ve
`shoulder`/`closure` eksenleri yargılanmadığı için **%22.1 bir TAVANDIR.**

### 1.4 `edit_locality_check` SÜS MÜ? — HAYIR, ama bir yönü dişsizdi (V6-B, `b9a6d34`)
★ **Kapı sevk edilen hattı yargılıyor** — süs değil. `spec-diff.mjs:152-161`
→ `engine/dist/stitchu-engine.js` = WASM. Kaynak damgası `web/vendor/`
ikilisiyle aynı: **`7023c808195429b3`**. Kullanıcının koştuğu ikili budur.
⚠ `engine/src/surfacepattern.cpp` WASM'a **hiç girmiyor**
(`grep -c surfacepattern engine/build-wasm.sh` = **0**) — yüzey hattı bu
kapının yargısı DIŞINDA. V5-Z'nin "sevk edilen hat başka" uyarısı burada
farklı bir şekle bürünüyor: kapı doğru hattı yargılıyor, ama iki hat var.

12 vaka, `r.locality.checked` = 3·2·1·4·4·4·2·2·3·3·4·3 = **35 panel yargısı**
(taban kalıpta 6 panel). Mutasyon (`GECE/log/V6-B.mutasyon.txt`):
| | ne | sonuç |
|---|---|---|
| M1 | bayt → panel varlığı | **exit 0 — KIRMADI** |
| M2 | dokunulmayan panele +5mm | exit 1, 6 KIRMIZI ✅ |
| M3 | `untouchable = []` | exit 1, A2 kırdı ✅ |
| M4 | sessiz no-op | exit 1, A3 kırdı ✅ |

★ `contract/edit-locality-v1.json` **ELLE YAZILMIŞ**: üreteç yok, `--check`
bekçisi yok. Dosyanın kendi "conflictClass BİREBİR taşındı" iddiası **3
bileşende yanlış** ve 41 alanın **21'i** hiçbir bileşenden gelmiyor. Kapı,
doğrulanmamış bir iddiayı ölçüyor.

### 1.5 ÇIPA ÜRETİLEBİLİR Mİ? (V6-C, `ea836db`)
**Kenar granülaritesinde ÜRETİLEMEZ.** Artefakt **0 adlandırılmış kenar**
taşıyor — 88 spec taraması, 62 ayrı panel adı, `edgeLabel/edges/seam/
seamGraph/stitch` hepsi 0. Kaynakta da: `engine/src/geometry.hpp:40`
`struct PatternPiece` panel adı taşır, kenar adı taşımaz.
**V5'in "0/112" bulgusu bağımsız hattan DOĞRULANDI.**
`primitives-v1.json:primitifler.edge.parametreler.label` şemada TANIMLI,
`PatternPiece`'te karşılığı YOK → RULES md.1 gereği **yok hükmünde**.
Panel granülaritesinde üretilebiliyor (35 bölge×panel çifti).
`contract/figure-landmarks.json` ölü kontrat: **0 tüketici**, GENERATED
başlığı yok, ölçeği ölü flat vitrin motorunun (px/EU36), 10 landmark'ın
**3'ü ÖLÇÜLMEDİ**.
★ Naif "taraf × jeton" üretimi bilerek koşuldu: **54 adayın en az 8'i kendiyle
çelişik** (`frontCenterback`, `backFront`) → kross-çarpım MENÜ'ye çöker.

### 1.6 Dünya emsali (V6-R, `6f92525`)
Kaynak+lisans+hüküm tablosu `GECE/V6-R.md` (549 satır), hepsi arXiv HTML'inden
`grep` ile birebir alıntı — **PDF WebFetch'e özetletilmedi** (o yol yanlış sayı
üretmişti). Öne çıkanlar:
- **ChatGarment** (arXiv:2412.17811, Apache-2.0): şema sadeleştirmesi ortalama
  token'ı **900 → 350** düşürüyor; float'lar **[0,1]**'e normalize.
  ★ **JSON şema DOĞRULAMASI YOK**: 79.834 karakterlik metinde
  `validat|schema|constrain|grammar` **0 eşleşme**. Bizim red yolumuz onlarda yok.
- **Design2GarmentCode** (arXiv:2412.08603): sabit uzunluk **|D| = 122**
  parametre, DressCode'un 1.500'üne karşı **≥10× kompakt**; `λ = 100` = cm.
- **SewFormer**: GarmentX birebir **"%76.3 simulation failure rate"**;
  ChatGarment aynı modele CloSe'de **%8.33** biçiyor. İki sayı da yazıldı.
- **JSONSchemaBench** (arXiv:2501.10868): kısıtlı çözümleme kaliteyi
  DÜŞÜRMÜYOR, YÜKSELTİYOR (GSM8K 80.1% → 83.8%). Bizim ~41 alanımız
  easy/medium sınırında.
- **Edit lokalliği:** giysi alanında ölçen yayın **YOK**; ChatGarment kusuru
  bir FİGÜRLE itiraf ediyor, metrikle değil. Görüntü tarafında olgun
  (**PIE-Bench**, maske dışı PSNR/LPIPS). ★ **Bizim "bayt-aynı" eşiğimiz
  onlarınkinden KATI** — bu bir avantaj, taban değil tavan.
- **Konum dili:** giysi/CAD'de adı konmuş şema YOK; üç bağımsız lisanslı emsal
  aynı yere varıyor — W3C CSS `anchor(<side>, <percentage>)`, FreeSewing
  `shiftFractionTowards` (MIT), GarmentCode `Edge.subdivide_len` (MIT).
  Üçü de `primitives-v1.json._birimler.kesir` ile aynı: **malzeme bizde var,
  spec katmanına çıkmamış.**
- `square↔boat` sınıfı karışma için **yayınlanmış çözüm YOK.**

---

## 2. ANA DALA KALAN İŞ — 89 satır, ve neden bu kadar

### `edit_locality_check` SIKILAŞTIRILDI (V6-E, `572316a`) — gevşetilmedi
Kök sebep, dosya:satır: `engine/tests/edit_locality_check.mjs:75` (eski hâl)
`line(antiCaught > 0, ...)` — eşik **mutlak sıfır tabanıydı**, 12'de 1 yakalama
yetiyordu; granülariteyi ölçen mandal hiç yoktu.
Onarım (§4.6 gereği eski→yeni→gerekçe commit gövdesinde):
- `A1_FLOOR = 10` — bugün ölçülen değer, **iki yönlü ratchet**
- `A1_SKIP_CAP = 1` — sessiz atlama tavanı, atlananlar **ADIYLA** basılıyor
- **A4 (yeni)**: `LOCALITY_GRANULARITY = 'bayt'` ilanı + **0.001mm kontrol
  vakası** + oynatmasız kontrol. İlan bir beyan değil, davranış ölçülüyor.

Mutasyon (`GECE/log/V6-E.mutasyon.txt`, üçü de geri alındı):
M1′ bayt→panel **artık exit 1** (2 KIRMIZI) · M2′ **+0.001mm** exit 1
(`Skirt Front: ae01610930c6 -> 3f66b7e1e632`) · M3′ taban 10→9 exit 1
(`A1 TABANI BAYAT`). **Kırılmayan mutasyon yok.**

★ V6-B'nin tablosunda hiç görünmeyen bir vaka bulundu: **`manşet ekle` hiç
yargılanmıyordu** — rewrite kipi `sleeveStyle`'ı düşürüyor, motor
`invalid spec: cuffStyle requires a sleeve` diyor, eski kod bunu **sessizce
`continue`** ediyordu. Artık adıyla basılıyor ve tavana sayılıyor.

---

## 3. HAKEM **KALDI** DEDİ — ve haklıydı

İlk hakem (temiz oturum, brief görmedi) fazı **düşürdü**, iki gerekçeyle:
1. **RULES md.9 ihlali pushlanmıştı.** Kırmızı AD kümesi **6 → 7** büyüdü
   (`vocab_reference_check`). Hakem KENDİ koştu: `3fa8002` worktree'sinde
   `HUKUM: YESIL` / EXIT=0, HEAD'de EXIT=1 / **10438 → 10448**.
   Geri alınmamış, kuyruğa havale edilmişti. RULES md.9 "geri alınır" der.
2. **`6b3378f` kapıyı geçmek için ŞEKİLLENDİRİLMİŞTİ:** 4 eklenen / 4 silinen
   satırın hepsi YORUM, sıfır davranış; fonksiyonun gerçek girdisi spec alan
   adları olduğu halde örnek `'topLength'` → `'hipBand'` yapılmış. **Sayı
   düştü, yorum yanlışlaştı. Bu bir grep kandırmacasıdır.**

★ Ayrıca bir işçi (V6-F) "ihlal yok" hükmünü **yanlış tabana** dayandırmıştı
(`ada3bf9` — turun KENDİ commit'i, faz öncesi değil). Şef bunu §1.1'deki kendi
açılış ölçümüyle çürüttü ve V6-H kartını kesti. **Faz öncesi = `3fa8002`.**

### Geri alma ölçümle yapıldı (V6-J, `52777a1` + `30d5d39`)
Kapı SCOPE'u okundu (`vocab_reference_check.sh:95-96`): `GECE/` ve
`engine/tests/` **kapsam dışı** → tutanaklar, loglar, kartlar ana dalda kaldı.
En az kaybeden kombinasyon **tahminle değil ölçümle** bulundu:
| adım | toplam | hüküm |
|---|---|---|
| HEAD, geri alma yok | 10448 (+10) | FAIL |
| `revert 6b3378f` (yorumlar dürüstleşti) | **10452 (+14)** | FAIL — **sayı ARTTI, dürüst olan bu** |
| anchors + gen-anchors silindi | 10448 (+10) | FAIL |
| `foto-spec-olcum.mjs` → `3fa8002` | 10438 (**+0**) | **FAIL (4 artan)** |
| `spec-diff.mjs` → `3fa8002` TAM | 10432 (−6) | YESIL |
| **`spec-diff` + `edit_locality` → `572316a`** | **10432 (−6)** | **YESIL — seçilen** |

★ **Adım 4 dersi: toplam tabana TAM eşit (10438 = 10438) olduğu halde kapı
FAIL bastı** — 4 düşüş 4 yükselişi maskeliyordu. Ratchet anahtar bazında
yargılıyor. "Toplamı tutturduk" bir yeşillik iddiası değildir.
★ V6-E ana dalda KORUNDU çünkü **ratchet maliyeti ölçülen sıfır** (onu tutan
ağaç ile atan ağaç aynı 10432'yi basıyor) — inanç değil ölçüm.

**Reddedilen iş SİLİNMEDİ:** `research/v6-cipa-editleme` @ `3d8903c`,
origin'de, **7 dosya / 2614 satır**. Ana dalda asılı referans yok
(`grep -rlnE 'anchors-v1|gen-anchors|anchorNames|operatorSicil'` BOŞ).

### İkinci hakem: **GEÇTİ**
Altı soruyu da kendi komutlarıyla cevapladı:
`git diff 3fa8002..HEAD -- engine/tests/vocab_reference_check.sh
engine/tests/vocab-reference-baseline.json` → **BOŞ** (taban kesilmedi,
scope daraltılmadı). `bash engine/tests/vocab_reference_check.sh` →
`HUKUM: YESIL`, **10432 (−6)**. Boş test kontrolünü ucuza kaçmadan yaptı:
ilan `'bayt'` yerinde bırakılıp `pieceBytes` panel-varlığına indirildiğinde
**2 KIRMIZI** — *"mandal beyanı değil davranışı ölçüyor."*
Kendi ctest'i (271.61 sn): `6 tests failed out of 113`, ad kümesi açılış
loguyla `diff` → **AYNI KÜME**. Hüküm satırı `GECE/KAPI.md`, commit `fff47a8`.

### Kâtip: `7e8a4ee`
`docs/ARCHITECTURE.md` §14 (YENİ — editleme zinciri docs'ta İLK KEZ geçiyor,
öncesinde `grep` 0 hit), §10 payda şerhi, 4 yeni "Known limits" satırı;
`docs/KATMAN-HARITASI.md` boşluk 10; `README.md`; `GECE/INDEX.md`.
Reddedilen iş **"yan dalda, kabul edilmedi"** diliyle yazıldı, şimdiki
zamanla ANLATILMADI.

---

## 4. KAPANAN / AÇILAN KIRMIZI (ad ad)

- **AÇILAN: 0.** (`vocab_reference_check` faz içinde açıldı ve faz içinde
  GERİ ALINARAK kapatıldı — susturularak değil. Ana dal `3fa8002` ile aynı
  ad kümesini taşıyor.)
- **KAPANAN: 0.** Miras 6'nın hiçbirine dokunulmadı: `flat_pattern_agree_check`
  · `flat_artifact_census` · `style_check` · `sizechart_source_check` ·
  `contract_check` · `figure_check`.
- Test sayısı **113 → 113**. Yeni test ADI 0 (yan dala giden
  `anchor_source_check` ana dalda YOK).

---

## 5. DÜRÜST SINIR — V6 ne YAPMADI

1. **Foto→spec isabeti bir milimetre değişmedi: %20 → %20.** Sebep dürüstçe:
   prompt, görü modeli ve sözlük bu gece DEĞİŞMEDİ. Değiştirmek ücretli canlı
   çağrı ister (§5.3 veto). **Ölçüm yapıldı, iyileştirme yapılmadı.**
2. **KONUM ölçüm eklentisi ana dalda YOK** — yan dala gitti. Yani bu gecenin
   en değerli teşhisi (11/26 terim yerini kaybediyor) ana dalda **artık
   yeniden koşulamaz**. Kayıt tutanakta, kod yan dalda.
3. **Çıpa + oran dili kurulmadı.** Yan daldaki 19 çıpa **panel** granülaritesinde;
   `t` oranının üstünde ölçüleceği KENAR üretilemiyor (§1.5). Çıpa bugün bir
   KAPI'dır, bir yerleştirme değil.
4. **`edit_locality_check` dar bir yüzeyde ölçüyor:** 12 vakanın 1'i gerçekten
   yakalanmıyor, 1'i hiç koşulamıyor, kalan 10'u **tek beden + tek taban spec**.
5. **Kapı temiz checkout'ta koşamaz:** `engine/dist/stitchu-engine.js`
   gitignore'da (`engine/.gitignore:5`), HEAD'de yok. Yeşillik yerelde
   derlenmiş artefakta dayanıyor.
6. **`CLAUDE.md`'nin "satın alınabilecek nesne" testine V6'nın katkısı SIFIR.**
   Ana dala kalan 89 satır bir kapıdır, ürün değil. RULES md.3'ün PNG
   artefaktı bu turda üretilmedi.

---

## 6. ★ KAPIDA DELİK — `vocab_reference_check` (iki bağımsız tanık)
`file(1)` `engine/tools/gen-anchors.mjs`'i **"binary data"** sayıyor, kapı
`grep -I` kullanıyor → o **421 satır ratchet'e HİÇ görünmüyordu**. Yani
"10448" sayısı üretecin içeriğini kapsamıyordu. İşçi bunu bilerek KULLANMADI
(borcu görünmez dosyaya taşımak = gevşetme) ve yazdı; hakem bağımsız doğruladı.
**Aynı sınıftan başka dosya var mı — DOĞRULANMADI, kimse taramadı.** Kuyrukta.

---

## 7. SONRAKİ FAZA DEVİR (ÜÇ SAYI)

1. **KIRMIZI = 6 · TEST = 113.** Açılış `GECE/log/V6.ctest.opening.txt`
   (275.47 sn) ve kapanış `GECE/log/V6.ctest.final.txt` (271.31 sn) — **ŞEF
   KENDİ koştu**, hakem bağımsız yeniden koştu, ad kümeleri `diff` ile birebir.
2. **SÖZLÜK TABANI = 10438 @ `495d58a` · bugün 10432 (−6), YEŞİL.**
   `bash engine/tests/vocab_reference_check.sh`. Sayı yalnız DÜŞEBİLİR.
   ⚠ Kapının `grep -I` deliği AÇIK (§6).
3. **YENİ KİLİTLİ BANT — editleme lokalliği** (`engine/tests/edit_locality_check.mjs`):
   `A1_FLOOR = 10` (12 vakada, iki yönlü ratchet) · `A1_SKIP_CAP = 1`
   (atlananlar adıyla) · granülarite ilanı **`'bayt'`**, A4 kontrol vakası
   **0.001mm**. Yalnız SIKILAŞABİLİR; elle gevşetmek yasak.
   ★ İFADE RATCHET **5 UNEXPRESSED** (kol 0/0 · yaka 4/4 · omuz 1/1) —
   **V6 DOKUNMADI**, V5'ten olduğu gibi devrediyor.

**FOTO→SPEC İSABETİ, ÖNCE/SONRA: %20.0 → %20.0** (1/5, alan 47/51 = %92.2).
Değişmedi ve sebebi ölçülü: şema/kapı değişti, prompt ve model değişmedi.
KONUM kapasitesi yan dalda **4/15 → 7/15 (sıkı)** ölçüldü ama **ana dalda
karşılığı YOK**.
