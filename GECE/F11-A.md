# F11-A — KIRMIZI KÜME YENİDEN ÖLÇÜLDÜ (isci-motor)

Bu bir ÖLÇÜM kartıdır. Hiçbir kırmızı kapatılmadı, hiçbir eşik oynatılmadı,
`engine/` `contract/` `web/` altında tek dosya değişmedi, commit atılmadı.

## 0. AĞAÇ

- `git rev-parse HEAD` → **962407d28aac466ebfc0b2f29a4e9e1d7c534f03**
- `git status --porcelain | grep -v '^?? GECE/'` → **boş çıktı**
  (yani GECE/ dışında ağaç temiz; ne değişmiş takipli dosya ne untracked dosya var)
- `git status --porcelain engine/tests` → boş (engine/tests altında untracked test YOK)

## 1. ORTAM (K1'in 16 sahte kırmızısını doğuran şartlar)

Her biri diskte VAR (tek tek `-e` ile ölçüldü):

| yol | durum |
|---|---|
| `engine/dist` | VAR |
| `engine/.venv-dxf` | VAR |
| `engine/pattern-bridge/.venv` | VAR |
| `engine/tools/node_modules` | VAR |
| `core/third_party` | VAR |
| `engine/build` | VAR |

Yani bu koşuda K1'in "ortam eksik → 16 sahte kırmızı" tuzağı ateşlenmedi.

## 2. YÖNTEM — KAPIYLA AYNI KAYNAK

`GECE/kapi.sh:39-48` (`kirmizilar()`) kırmızı ADLARINI
`$B/Testing/Temporary/LastTestsFailed.log` dosyasından, `cut -d: -f2- | sort`
ile okuyor. Bu koşuda birebir aynısı yapıldı:

```
rm -f engine/build/Testing/Temporary/LastTestsFailed.log
cd engine/build && ctest --output-on-failure > GECE/log/F11.ctest.txt 2>&1
cut -d: -f2- engine/build/Testing/Temporary/LastTestsFailed.log | sort > GECE/log/F11.red.after
```

Build: `engine/build/CMakeCache.txt` içinde
`CMAKE_BUILD_TYPE:STRING=Release` ÖLÇÜLDÜ (boş değil — 2684s tuzağı yok).
`cmake --build engine/build -j8` hatasız bitti; toplam ctest süresi **237.61 sn**
(`GECE/log/F11.ctest.txt` son satırları).

## 3. SAYILAR

- Toplam test: **96**
  - `ctest -N` → `Total Tests: 96`
  - `grep -c add_test engine/CMakeLists.txt` → **96**
- Kırmızı: **7** — `GECE/log/F11.ctest.txt`: `93% tests passed, 7 tests failed out of 96`
- ctest exit kodu: **8**

### "F0 96, F9 98 demişti — hangisi?"

HEAD'de (962407d) doğru sayı **96**'dır, iki bağımsız ölçümle:
`ctest -N` 96, `engine/CMakeLists.txt` içinde 96 `add_test` satırı.
98 sayısı HEAD ağacından ÇIKAMAZ: ağaç temiz (§0), `engine/tests` altında
untracked dosya yok, `git ls-files engine/tests` 93 dosya sayıyor. 98'lik bir
sayım ancak HEAD'e girmemiş (ayrı worktree'de duran, +2 `add_test` taşıyan) bir
ağaçta üretilmiş olabilir. **Bu kart o ağacı görmedi — F9'un 98'inin nereden
geldiği burada DOĞRULANMADI**, sadece "HEAD'de değil" ölçüldü.

## 4. KIRMIZI ADLAR (GECE/log/F11.red.after, alfabetik)

```
bugra_bridge_check
contract_check
figure_check
h10_gate_check
preview_truth_check
sizechart_source_check
style_check
```

ctest'in bastığı numaralı hali (`GECE/log/F11.ctest.txt`):
5 style_check · 12 sizechart_source_check · 71 bugra_bridge_check ·
78 contract_check · 82 preview_truth_check · 83 figure_check · 89 h10_gate_check

## 5. TABANLA KARŞILAŞTIRMA

`diff GECE/log/F0v3.red.now GECE/log/F11.red.after` → `GECE/log/F11.reddiff.txt`

- diff çıktısı **BOŞ** (0 satır), exit kodu **0**
- `comm -13 F0v3.red.now F11.red.after` (YENİ kırmızı ad) → **boş, 0 ad**
- `comm -23 F0v3.red.now F11.red.after` (KAPANAN ad) → **boş, 0 ad**
- `diff GECE/log/F0.red.before GECE/log/F11.red.after` → aynı, boş

Cevaplar:
- Kırmızı ad kümesi tabanla **AYNI**, diff **BOŞ**.
- Yeni kırmızı ad sayısı: **0** (§0.6 hedefi tutuyor).
- F0'ın 7 kırmızısından kapanan: **0**. Yedisi de duruyor.

## 6. ÇIKTI DOSYALARI

- `GECE/log/F11.ctest.txt` — tam ctest çıktısı (96 test, 7 fail, 237.61 sn)
- `GECE/log/F11.red.after` — 7 kırmızı ad, alfabetik
- `GECE/log/F11.reddiff.txt` — boş dosya (diff yok)
- `GECE/F11-A.md` — bu tutanak
