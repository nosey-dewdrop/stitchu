# V10-C — LANDING TASARIMI · ÖNCE/SONRA İDDİA TABLOSU

Koşu: 2026-08-25 · dal `main` · commit `450c86c` (+ ikinci commit, aşağıda).
Girdi: `GECE/V10-A.md` + `GECE/log/V10-A.iddia.tsv` (düzeltilecek yalanların listesi).
Kapı: `node engine/tools/site-health.mjs` → **EXIT=0**, ölü link **0**
(`GECE/log/V10-C.site-health.txt`).

---

## 0. NE YAPILDI, TEK CÜMLE

`web/index.html` artık MTM (ısmarlama) satmıyor; **foto/reçete → kalıp + flat +
dikiş rehberi**, sabit **8 beden EU34–48** anlatıyor, üç çıktının **üçü de gerçek
çıktı olarak sayfada duruyor**, ve yayınlanmamış her sayı ya sağlayıcı adıyla
geldi ya sayfadan çıktı. Kod tarafında **gösterilen beden = seçilen beden**
düzeltildi: beden listesi motorun kümesinden türüyor, düşen beden **adıyla**
reddediliyor.

---

## 1. ★ GÖSTERİLEN BEDEN = SEÇİLEN BEDEN (kartın kod işi)

| dosya:satır | eski | yeni | sınıf | sağlayıcı ADI |
|---|---|---|---|---|
| `web/js/create.js:810` (eski) | `EU_SIZES = [...,'EU48','EU50','EU52']` — kullanıcıya **10 beden** | `EU_SIZES = ['EU34'…'EU48']` — **8 beden**, gerekçesi yorumda | DOĞRU | `contract/layers/shape-ratios.json` `sizes` (n=8) + motorun kendi cümlesi `engine/src/shaperatios.gen.hpp:21` "Sizes absent here (EU50/EU52) have no published ratio and keep 0" |
| `web/js/create.js:866` (eski) | `filter((s) => !s.draft.issues.length)` — issue'lu beden **SESSİZCE** düşüyor, kullanıcı sadece bir sayı görüyor | düşenler `refused` dizisine ayrılıyor; mesaj **etiketleri** basıyor, altına **beden başına gerekçe satırı** ekleniyor | DOĞRU | RULES invariant 1 ("NEVER silently drop or coerce") |
| `web/js/i18n.js:77` | `'{n} sizes drafted ({dropped} skipped as unsewable at that size).'` | `'{n} sizes drafted. Refused by name: {names} — the engine reported an issue on each…'` + yeni `create.grade.refused.detail` | DOĞRU | aynı |

`web/js/print.js:230` DEĞİŞTİRİLMEDİ ve değişmesi gerekmiyor: ölçüldü,
damgalanan `sizeLabel` **motorun kendi** `s.size` alanından geliyor
(`printGrade`/`printGradeNested` yalnız hayatta kalan bedenleri alıyor), yani
kullanıcının seçtiği etiket değil. V10-A §5'in "kullanıcının seçtiği etiket
damgalanıyor" cümlesi bu hatta **TUTMUYOR**; asıl kusur seçim listesi ve sessiz
düşüştü, ikisi de yukarıda kapandı.

---

## 2. SAYFADAN ÇIKAN YALANLAR (`web/index.html`)

Sayım komutu: `grep -coF "<kalıp>" web/index.html`

| kalıp | ÖNCE (index) | SONRA (index) | sınıf |
|---|---|---|---|
| `EU34–52` | 5 | **0** | SİLİNDİ |
| `your own measurements` | 2 (olumlu iddia) | **0 olumlu** (4 hit var, hepsi *"not your own measurements"* olumsuzlaması) | SİLİNDİ |
| `your seven measurements` | 3 | **0** | SİLİNDİ |
| `measurement-fitted` | 1 | **0** | SİLİNDİ |
| `made-to-measure` | 1 | **0** | SİLİNDİ |
| `0.000000` | 2 | **0** | SİLİNDİ |
| `byte-identical` · `ALL PASS` · `zero issues` · `always` · `guaranteed` | 0 | **0** | (index'te zaten yoktu) |

Cümle cümle:

| dosya:satır (ÖNCE) | eski cümle | yeni cümle | sınıf | sağlayıcı ADI |
|---|---|---|---|---|
| `index.html:176` lead | "grade it across the full **EU34–52** run … export the DXF-AAMA and tech-pack" | "one C++ kernel draws all three from the same spec, in the standard sizes **EU34 to EU48**. Fixed sizes, **not your own measurements**" | DOĞRU | `contract/layers/shape-ratios.json` (sayfada ADIYLA yazılı) |
| `index.html:179` chip | "EU34–52 graded run + tech-pack" | "8 fixed sizes · EU34–48" | DOĞRU | aynı |
| `index.html:180` chip | "seams match to 0.00 mm, published" | "photo to spec: 1 of 5 exact, 47 of 51 fields" | DOĞRU | `engine/tools/foto-spec-olcum.mjs` + `vision/eval/live-2026-08-22.json` |
| `index.html:193` item | "Draft any garment to **your own seven measurements**" | "Eight standard sizes, EU34 to EU48, one per published shape ratio" | DOĞRU | shape-ratios.json |
| `index.html:284` kart | "every seam … matches to 0.00 mm across a **70,200-draft matrix**" | "worst pair mismatch 0.00 mm · tool: `engine/tools/precision-report.js`" — ve "alet **iki basamak** basıyor, iki basamaklı ölçüm altı basamakla ilan edilemez" | DOĞRU | `node engine/tools/precision-report.js` bu turda koşuldu, §5 |
| `index.html:289-291` kart | "Fits every body, not one block … **stitchu redraws the whole garment for your seven measurements**" · stat "**drafted per body** · no grading distortion" | KART TAMAMEN DEĞİŞTİ → "Eight fixed sizes, EU34 to EU48. **Not your own measurements.**" · stat "source: contract/layers/shape-ratios.json · sizes = 8" | SİLİNDİ→DOĞRU | shape-ratios.json |
| `index.html:299` versus | "Similar tools charge **$34–49 a month** and publish no accuracy proof at all" | SİLİNDİ (rakip fiyatı bu turda ölçülmedi; sahte/doğrulanmamış sayı yasağı) | SİLİNDİ | — |
| `index.html:300` versus | "The engine drafts to **seven measurements** and fills the rest from documented assumptions" | "**a pattern that validates is not the same as a pattern that sews up.** The sewability checks in this repo still report violations, and the hip allowance sits under the published minimum in all eight sizes." | DOĞRU (kartın kendi ölçümü) | kart §SAYFADAN ÇIKACAK: `sewability_check` 585 ihlal, `draft_math_check` 12 ihlal, kalça payı 8/8 bedende minimum altı |
| `index.html:316-317` kart | "grades across the whole **EU34 to 52** chart … **10 of 10** sizes validator-clean" | "grades across **EU34 to EU48** … A size the kernel refuses is named in the result" | DOĞRU | shape-ratios.json + §1'deki kod düzeltmesi |
| `index.html:321-322` kart | "pairwise intersection area **0.000000 mm²**" · stat "overlap 0.000000 mm² · shapely-checked" | "That run **was not repeated for this page**, so no overlap number is printed here." · stat "shapely-checked · number not re-run for this page" | SİLİNDİ | — (shapely koşusu üretilmedi, cümle sayı taşımıyor) |
| `index.html:312` stat | "**0.000 mm DXF parity** · opens in Valentina / Seamly2D" | "DXF-AAMA R12 · 1 unit = 1 mm · named layers" | SİLİNDİ | — (DXF parite koşusu bu turda üretilmedi) |
| `index.html:336` API | "→ a true-scale, **measurement-fitted** pattern. One photo → a full size run (XS–XL / **EU34–52**)" | "→ a true-scale pattern in a standard size. One spec → the **eight published sizes**" | DOĞRU | shape-ratios.json |
| `index.html:343` | "Studios, **made-to-measure** brands and pattern shops" | "Studios, **indie labels** and pattern shops" | SİLİNDİ | — |
| `index.html:348` footer | `v86` (bayat, `?v=136` ile çelişiyor) | SİLİNDİ | SİLİNDİ | tek sürüm kaynağı `engine/tools/site-version.mjs` (elle dokunulmadı, `?v` tek değer `136`) |
| `index.html:271` figcaption | "a fuller body · same engine, **same proof**" | "a fuller body · **drawn at bust 122 cm, past the published size run**" | DOĞRU | çizimin kendi metni "drafted for bust 122 cm"; 122 cm = EU52 gövdesi (`contract/tables.json euSizeChart`), yayınlanmış oranı YOK |

---

## 3. ÜÇ ÇIKTI SAYFADA (kartın NE'si)

Eski `.proof` bölümü üç kâğıt gösteriyordu: **01 your photo** (elle çizilmiş
insan figürü SVG'si = ikon, gerçek çıktı DEĞİL), 02 kalıp, 03 A4 döşeme.
Yani "üç çıktı" ne flat'i ne rehberi içeriyordu, ve birincisi ikondu.

Yeni `#outputs`, başlık **"One spec. Three outputs."**:

| kâğıt | ne gösteriyor | gerçek çıktı mı | dosya |
|---|---|---|---|
| **01 · the pattern** | 10 parça, isimleri parça parça yazılı (mevcut gerçek çıktı SVG'si korundu, başlığı düzeltildi) | evet | inline (değişmedi) |
| **02 · the flat** | ön + arka moda flat'i, EU38 kroki, 1:3, `data-part="sleeve"` yolları görünür | evet | `web/assets/flats/flat-sleeve-straight.svg` ← `GECE/log/V7-E.png/flat-sleeve-straight.svg` |
| **03 · the sewing guide** | dokuz fazın kendisi, madde madde, gerçek metniyle | evet | metin `web/guide/construction-order.html:81-89`'dan birebir |
| 04 · true-scale A4 | döşeme + 3 cm kare (destek kâğıt, 3'lüden ayrıldı) | evet | inline (değişmedi) |

**Silinen ikon:** `01 · your photo` figürü (`index.html:218-232`, elle yazılmış
insan silueti path'leri) tamamen kaldırıldı — kartın "üç çıktı **gerçek çıktı
olarak** görünsün, ikonla temsil edilmesin" yasası.

**Kolun gerçekten çizildiği**, ayrı bir bölümde dört ayrı geometriyle:
`web/assets/flats/flat-sleeve-{straight,balloon,cap,none}.svg`
(dördü de `GECE/log/V7-E.png/`'den kopyalandı, üretilmedi).

**Dürüst dikiş, sayfaya YAZILDI:** flat motoru ile kalıp çekirdeği **birleşmedi**
— create sayfası kalıbı çiziyor, flat tezgahtan (`web/atolye.html`) geliyor.
Ölçüm: `grep -rln "croquis\|flat-engine" web/js/ web/*.html` → `web/js/` içinde
**0 dosya**; yalnız `web/atolye.html` ve `web/patches.html`. Bu cümle sayfada
kalın harfle duruyor, gizlenmedi.

---

## 4. VİZYON BLOĞU (`data-vision="1"`, gelecek zaman, canlı görsel YOK)

Yeni `#next` bölümü, başlık **"What is not built yet?"** (soru → `?`):
kumaş ekseni · sınırlı kelime/sınırsız giysi · editleme — üçü de gelecek zaman,
üçünün de "bugün bunu yapmıyor / gösterilecek canlı örnek yok / demo değil"
cümlesi kendi içinde. Üyelik + forum + iOS **tek satır**:
"Accounts, a forum and an iOS app are all planned and none of them exist."

---

## 5. KOŞULAN ALET + ÇIKTISI

```
$ node engine/tools/precision-report.js
  ok   bodice side seam front vs back                 47.25 vs 47.25  Δ 0.00 mm
  ok   princess seam back center vs side panel        208.78 vs 208.78  Δ 0.00 mm
  ok   bodice side seam front vs back                153.58 vs 153.58  Δ 0.00 mm
  ok   princess seam front center vs side panel      392.05 vs 392.05  Δ 0.00 mm
precision report: worst pair mismatch 0.00 mm | 0 FAILURES
```

```
$ node engine/tools/site-health.mjs      → EXIT=0
checked: 127 pages, 2604 internal refs, 124 sitemap urls, 124 indexable pages
OK  site-health: no dead links, sitemap matches the site, one version.
```
(ÖNCE 2597 iç referans → SONRA **2604**; ölü link ÖNCE 0, SONRA **0**.)

```
$ grep -rhoE '\?v=[0-9a-zA-Z._-]+' web/ | sort -u
?v=136                      # tek değer, elle dokunulmadı
```

---

## 6. DİKİŞ PAYI — TEK SAYIYA İNDİ

Sevk edilen hat WASM = `garment.cpp` = `engine/src/constants.gen.hpp:13`
`kSeamAllowanceMM = 15`.

| dosya:satır | eski | yeni |
|---|---|---|
| `web/styles/balloon-sleeve.html:98` | "interfaced, **10 mm** seam allowance." | "interfaced, **15 mm** seam allowance (the one number the shipped engine uses: `engine/src/constants.gen.hpp kSeamAllowanceMM = 15`)." |
| `web/shop-shift-dress.html:110` | "15 mm seam allowance included:" | "15 mm seam allowance included **(source: `engine/src/constants.gen.hpp kSeamAllowanceMM = 15`)**:" |

**Değiştirilmedi, gerekçesiyle:**
- `web/js/print.js:21,25` — sayı zaten **dinamik** (`${cm}`), motordan geliyor. Tek kaynak, dokunulmadı.
- `web/js/guide-tr.js:82` — "1.5 cm" = 15 mm, **çelişki değil**, birim farkı. V10-A §2'nin "site kendi içinde de iki sayı taşıyor" hükmü burada **TUTMUYOR**; gerçek üçüncü sayı yalnız balloon-sleeve'in 10 mm'siydi.
- `web/styles/open-back.html:98` — "seam allowance **0**, sewn ON the marked line" kasıtlı bir yapım talimatı, kalıbın payı değil. Dokunulmadı.

---

## 7. EU34–52 SİTE GENELİ

| dosya:satır | eski | yeni |
|---|---|---|
| `web/studio.html:56` | "A graded EU34 to **52** production package" | "EU34 to **48**" |
| `web/js/studio.js:356` (yorum) | "graded EU34–**52** production package" | "EU34–**48**" |
| `web/js/studio.js:372` | "A graded EU34 to **52** production package" | "EU34 to **48**" |
| `web/api.html:161` | "A solo Etsy / **made-to-measure** shop. One photo → a full size run (XS–XL / **EU34–52**)" | "A solo Etsy or indie pattern shop. One spec → the **eight published sizes (EU34–EU48)**" |
| `web/api.html:169` | "Studios, **made-to-measure** brands and pattern shops" | "Studios, **indie labels** and pattern shops" |

Kalan: `grep -rn "EU34–52\|EU34 to 52\|EU34-52" web/` → **0 hit**.

---

## 8. DOKUNULAN HER DOSYA

| dosya | ne değişti |
|---|---|
| `web/index.html` | landing içeriği + düzen (CSS bloğuna **tek satır girilmedi**) |
| `web/js/create.js` | beden listesi motorun kümesinden; düşen beden adıyla + gerekçesiyle |
| `web/js/i18n.js` | `create.grade.done.some` yeniden yazıldı, `create.grade.refused.detail` eklendi |
| `web/js/studio.js` | EU34–52 → EU34–48 (2 yer) |
| `web/studio.html` | EU34–52 → EU34–48 |
| `web/api.html` | `:161` MTM + beden aralığı, `:169` MTM |
| `web/shop-shift-dress.html` | dikiş payına kaynak adı |
| `web/styles/balloon-sleeve.html` | 10 mm → 15 mm + kaynak adı |
| `web/assets/flats/*.svg` (4 yeni) | `GECE/log/V7-E.png/`'den kopya, üretilmedi |
| `DAMLA-KUYRUK.md` | **K-V10C** — kimlik çelişkisi, iki yönlü, 3.8.d |
| `GECE/V10-C.md` · `GECE/log/V10-C.site-health.txt` | bu tutanak + kapı çıktısı |

---

## 9. DOKUNULMADI (ADIYLA) — süre tavanı

Kartın adını verdiği ve **bu turda AÇILMAYAN/DEĞİŞMEYEN** sayfalar:

- **`web/create.html`** — HTML'ine dokunulmadı (yalnız `web/js/create.js` düzeltildi).
- **`web/showcase.html`** — DOKUNULMADI.
- **`web/signature.html`** — DOKUNULMADI.
- **`web/benchmark.html`** — DOKUNULMADI.
- **`web/api.html`** — yalnız `:161` ve `:169` düzeltildi; sayfanın geri kalanı DOKUNULMADI.
- **`web/shop-shift-dress.html`** — yalnız `:110` düzeltildi.
- **`web/patches.html`** (50 `byte-identical` hit) — DOKUNULMADI.
- **`web/styles/**` (24 sayfa `0.000000 mm`)** — yalnız `balloon-sleeve.html:98`
  düzeltildi; kalan 23 sayfa DOKUNULMADI.
- **`web/atolye.html`**, `web/closet.html`, `web/collections/**`,
  `web/collection-60s70s.html`, `web/guide/**`, `web/recipes/**`,
  `web/factory/**`, `web/privacy.html`, `web/patches/**` — DOKUNULMADI.

**Site geneli MTM dili hâlâ ayakta** (bu tur yalnız index + api + studio'yu
kapattı). Bugünkü sayım:

```
your own measurements     files=45 hits=108   (45'in 1'i index.html, 4 hit'i OLUMSUZLAMA)
to your measurements      files=22 hits=77
your exact measurements   files=16 hits=48
no fixed sizes            files=23 hits=46
```

Yani **44 dosya hâlâ olumlu MTM iddiası taşıyor.** Bu, V10-C'nin kapatmadığı en
büyük cephe ve süre tavanına takıldı.

---

## 10. ÖLÇÜLMEDİ / YAPILAMADI

- **320 CSS px'te gerçek tarayıcı render'ı YAPILMADI.** Eklenen her görsel
  `width:100%` + `viewBox`'lı SVG, yani statik olarak yeni sabit genişlik
  girmedi — ama bu **ÇIKARIM, ÖLÇÜM DEĞİL**. `web/index.html:34`
  `body{overflow-x:hidden}` yerinde duruyor, dokunulmadı.
- **`ctest` KOŞULMADI** — bu bir `web/` kartı, `engine/` altına tek bayt
  yazılmadı (`git status --porcelain engine/` bu turda boş kaldı).
- **`golden-diff.py`, `web-fuzz.js`, `vocab-sweep.cpp`, `engine_check`
  KOŞULMADI** — dolayısıyla `byte-identical` / `70,200` / `19,780` / `37,800`
  iddiaları hâlâ **KANITSIZ**; index'ten çıkarıldılar ama sitenin geri
  kalanında duruyorlar.
- **shapely marker örtüşme koşusu üretilmedi** → `0.000000 mm²` sayısı
  index'ten kaldırıldı, yerine sayı KONMADI.
- **DXF parite koşusu üretilmedi** → `0.000 mm DXF parity` kaldırıldı.
- **`create.js`'in grade akışı tarayıcıda koşturulmadı.** Yeni "adıyla ret"
  yolu **canlı çalıştırılmadı**; sözdizimi doğrulandı
  (`import('file://…/create.js')` → yalnız `ReferenceError: document is not
  defined`, yani parse temiz). Gerçek bir reddedilmiş bedenin ekranda nasıl
  göründüğü **DOĞRULANMADI**.
- **DEPLOY YAPILMADI.** `scripts/deploy.sh` çalıştırılmadı, subtree push
  edilmedi. Yayın Damla'nın adımı.

---

## 11. ★ V10-A'NIN İKİ HÜKMÜ BU TURDA ÇÜRÜDÜ

1. **C19 (`3 cm calibration square`) KANITSIZ DEĞİL, DOĞRU.** V10-A "web
   hattında kareyi basan alet yok; printpack.py 4 cm kullanıyor" diyor.
   Ölçüldü: `web/js/print.js:152-166` `calibrationSVG()` **34×47 mm viewBox
   içine 30 mm'lik bir kare** basıyor ve altına "3 cm, measure me before
   cutting" yazıyor; `printGrade`/`printGradeNested`/tek baskı, üçü de
   (`:220`, `:270`, `:362`) onu çağırıyor. `printpack.py` web hattı değil.
   Sağlayıcı: `web/js/print.js:152`.
2. **`web/js/print.js:230` "kullanıcının seçtiği etiketi damgalıyor" TUTMUYOR.**
   `sizeLabel`, motorun döndürdüğü `s.size`'dan geliyor (`print.js:238`,
   `:304` → `prepared`/`sizes` map'leri). Gerçek kusur seçim listesiydi.
