# V10-L — TABAN HEAD'DE YENİDEN KESİLDİ + MUHASEBE DÜRÜSTLEŞTİRİLDİ

Girdi ağacı: V10-J'nin bıraktığı hâl (`ade83dc`). Yazılan tek üretim dosyası:
`engine/tests/landing-truth-baseline.json`. `web/**`, kapı kodu, `docs/`,
`engine/src/`, `contract/` — hiçbirine dokunulmadı.

## TABAN KARŞILAŞTIRMASI

| | L1 | L2 | L3 | L4 | L5 |
|---|---|---|---|---|---|
| önceki taban (`c72f650`) | 937 | 0 | 0 | 0 | 4 |
| yeni taban (`ade83dc`) | **937** | **0** | **0** | **0** | **4** |
| fark | 0 | 0 | 0 | 0 | 0 |

**Hiçbir L yükselmedi.** Kapı yeni tabanla `EXIT 0`
(`node engine/tests/landing_truth_check.mjs` → `GECE/log/V10-L.kapi.txt`,
`HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4`).

## MUHASEBE (kendim ölçtüm; komutlar `GECE/log/V10-L.muhasebe.txt`'de)

| | faz öncesi `9b306f9` | HEAD `ade83dc` | fark |
|---|---|---|---|
| sayı+birim taşıyan blok | 1116 | 1106 | **−10 (SİLİNDİ)** |
| sağlayıcısız (L1) | 1074 | 937 | −137 |
| sağlayıcılı | 42 | 169 | **+127 (JETONLA AKLANDI)** |

10 + 127 = 137. Düşüşün **%92.7'si silme değil, jeton**. Bloklar sitede duruyor,
sadece yanlarına `golden_check` / `engine_check` yazıldı.

- `git diff 9b306f9..HEAD -- web/` içinde eklenen jeton geçişi:
  `golden_check` **212**, `engine_check` **60**; SİLİNEN 0.
- HEAD'de **102 blok** yalnız bu iki jetonla aklanıyor (fikstürde jetonlar
  nötrlenince sağlayıcısız 937 → 1039). Ör. `web/patches/1-5.html:82`
  "the benchmark re-classified to exactly 14/54" — `golden_check` 14/54 basmaz.
- Sonda yeniden koşuldu: `"The seam allowance is 12.5 mm on every piece."`
  → 938/937 **KIRMIZI**; aynı cümle + `(site-health)` → 937/937 **YEŞİL**.
  L1 eşleşmesi **SUBSTRING**'dir (`engine/tests/landing_truth_check.mjs:53-54`).
  Kapı sayının DOĞRULUĞUNU ölçmez.

## HAKEMLE TUTMAYAN İKİ SAYI (işçininki geçerli, RULES §6)

| kalem | hakem | işçi | komut |
|---|---|---|---|
| `golden_check` eklenen geçiş | 224 | **212** | `git diff 9b306f9..HEAD -- web/ \| grep '^+' \| grep -o 'golden_check' \| wc -l` |
| yalnız-jetonla-aklanan blok | 81 | **102** | fikstürde jeton nötrlenmesi, muhasebe §5 |

Hakemle TUTAN sayılar: 1116 · 42 · 1106 · 169 · `engine_check` 60 · L4=0 ·
sonda 938/937 → 937/937.

## `_neden0Degil` — ÜÇ DÜZELTME

1. **L3 çelişkisi kapandı.** Eski metin "data-vision=1 taşıyan blok SIFIR"
   diyordu, aynı JSON `isaretliBlok: 1` diyordu. Ölçüm: blok 1 · işaretli cümle 30
   · MUAF 5 · ihlal 0. Metin ölçüme göre yeniden yazıldı.
2. **L5 çelişkisi kapandı (kart dışı, ölçerken çıktı).** Eski metin
   "`web/js/create.js:810` EU_SIZES **10 beden**" diyordu. Ölçüldü: `EU_SIZES`
   **satır 817**'de ve **8 beden** taşıyor (`sed -n '817p' web/js/create.js`),
   `secimListeleri` alanı da `n: 8` diyor, üstküme seçim 0. Dört kaçak
   `web/js/contract.gen.js:28/29/104/113`'te.
3. **L4 artık ölü denetim olduğunu söylüyor:** bu fazda hiçbir borç taşımadı,
   hiçbir şey yakalamadı, faz öncesinde de 0'dı.

L1 maddesi artık "tam tavanda, payı sıfır"ı ve 102'lik jeton aklamasını taşıyor.
L2 maddesi 34 kalıbın 20 EN / 14 TR dağılımını ve faz öncesi 829 hit'i taşıyor.

## YAPILAMAYAN / YAPILMAYAN

- **`_neden0Degil` metinleri kapı KODUNDA gömülü** (`landing_truth_check.mjs:485-491`).
  Kart o dosyayı yasakladığı için metinler yeniden kesimden SONRA JSON üstünde
  elle düzeltildi. Sonuç doğru ama **kalıcı değil**: kapı bir daha
  `--baseline` ile kesilirse bu üç düzeltme silinip bayat metin geri gelir.
  Kalıcı çözüm kapı kodunu düzenlemeyi gerektirir → ayrı kart.
- **L1 937 onarılmadı.** Kart web/ altına yazmayı yasaklıyor; onarım V10-C'de.

## KART DIŞI FARK EDİLEN (dokunulmadı)

1. **Kapının kendi yorumu bayat:** `landing_truth_check.mjs:268` "yukarıdaki 18
   kalıbın 18'i İngilizce" diyor; ölçüm **20 İngilizce** (V10-G'nin 7 kalıbı
   sayılmamış). Toplam kalıp 34, TR 14.
2. **L1 tavanı sıfır paylı.** Site'ye eklenecek TEK bir sayısal cümle kapıyı
   kırar. Bu bir mandal olarak sağlam değil, kaza eseri sıkı.
3. **`ctest -N` ile CMakeLists farkı 1:** `yalnız-cmake 1 [h10_gate_check]`
   (kapı çıktısı satır 7). Kapı bunu basıyor ama hükme bağlamıyor.
4. **`git status`'ta kart dışı untracked dosyalar duruyor:**
   `patterns_real/BUGRA-DEFTER.md`, `patterns_real/geometry/`,
   `patterns_real/tools/bugra-geometry-2026-07-23.json`. Bu kart bunlara
   dokunmadı; CLAUDE.md'nin "patterns_real ASLA push edilmez" yasasıyla
   ağacın çelişkisi (zaten kayıtlı) hâlâ açık.
