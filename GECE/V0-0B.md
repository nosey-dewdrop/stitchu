# V0-0B — görü hattı envanteri (ÖLÇÜM, 2026-08-24)

Ham çıktı: `GECE/log/V0-0B.eval.txt`. Her sayının yanında onu basan komut var.
Hat ONARILMADI, prompt/şema DEĞİŞTİRİLMEDİ. Ağırlık indirilmedi, anahtar kurulmadı,
ücretli çağrı yapılmadı.

---

## 1) HAT — foto/prompt alıp spec üreten dosyalar (yol yol)

Canlı ürün hattı (tek gerçek hat):

| # | dosya | ne yapar |
|---|---|---|
| 1 | `web/js/create.js:410` | kullanıcı fotoyu seçer, `analyzePhoto(file)` çağrılır |
| 2 | `web/js/analyze.js:26-44` | `POST <BACKEND_URL>/api/analyze`, JSON döner, parse edilemezse dürüst red |
| 3 | `backend/worker.js:114` (token'lı) ve `:153` (public) | `/api/analyze` uçları |
| 4 | `backend/worker.js:307-350` | **PROMPT** — 22 alanlık kapalı-liste JSON şeması + eşleme kuralları. Sözlüğü BURASI tanımlar. |
| 5 | `web/js/vision-bridge.js` | okuma → motor spec'i (`pick*` fonksiyonları) |
| 6 | `web/js/vocab.gen.js` / `backend/vocab.gen.js` | **ÜRETİLMİŞ** sözlük — `engine/tools/gen-vocab.mjs` ← `engine/vocab.json` |

Ölçüm/eval hattı (ürün değil):

| dosya | ne yapar |
|---|---|
| `engine/tools/foto-spec-olcum.mjs` | **uçtan uca ölçüm**: foto → canlı worker → bridge → `draft()` → panel; GORME/KELIME/MOTOR sınıflar. `--offline` bankadan okur. |
| `vision/eval.js` | kayıtlı tahmin dosyasını `vision/eval/labels.json` göz etiketine karşı alan-alan puanlar |
| `vision/live-eval.sh` + `vision/live-eval-score.py` | canlı worker'a korpus basar (ÜCRETLİ — koşulmadı), `garment=other/null` oranını sayar |
| `vision/clip-v0.js` · `clip-v0-siglip.js` · `clip-v0-fashion.js` | zero-shot CLIP/SigLIP koşucuları (`@xenova/transformers`) |
| `vision-student/*` | distilasyon iskeleti (eğitim/ONNX) — **hiç koşmadı**, §4 |

---

## 2) KORPUS + İSABET (payda dahil)

### Elde duran korpus

| yer | sayı | etiketli mi | komut |
|---|---|---|---|
| `vision/eval/photos/*.jpg` | **29 foto** | 19'u göz-etiketli, 10'u `_dropped` (giysi değil) | `ls vision/eval/photos/*.jpg \| wc -l` |
| `vision/eval/labels.json` | **19 etiket** (+2 meta anahtar) | göz etiketi, Fable 2026-07-13, PARTIAL | `python3 -c "import json;print(len(json.load(open('vision/eval/labels.json'))))"` → 21 |
| `vision/corpus/manifest.json` | **778 satır kayıt** | etiketsiz | `python3 -c "import json;print(len(json.load(open('vision/corpus/manifest.json'))))"` |
| `vision/corpus/photos/` | **0 foto (DİSKTE YOK)** | — | `ls vision/corpus/photos/*.jpg \| wc -l` → 0 |
| `dataset/labels/` | **KLASÖR YOK** (student ambarı boş) | — | `ls dataset/` → `eval  taste-pool  vocab-canonical.json` |

Kayıtlı tahmin dosyaları: `clip-predictions` (19), `siglip-predictions` (19),
`opus-predictions` (9), `live-2026-08-22` (5), `live-baseline-oldprompt` (68).

### İSABET — alan bazında (`node vision/eval.js <dosya>`)

| tahmin dosyası | payda | isabet | oran |
|---|---|---|---|
| `eval/clip-predictions.json` (CLIP ViT-B/16 zero-shot) | 162 alan | 71 | **44%** |
| `eval/siglip-predictions.json` (SigLIP zero-shot) | 162 alan | 105 | **65%** |
| `eval/opus-predictions.json` (canlı worker, 9 foto) | 78 alan | 67 | **86%** |
| `eval/live-2026-08-22.json` (canlı worker, YENİ prompt, 5 foto) | 50 alan | 47 | **94%** |

### İSABET — FOTO bazında, "TAM DOĞRU SPEC" (asıl ürün sayısı)

`node engine/tools/foto-spec-olcum.mjs --offline` (11 alan: 10 + `shaping`, + motor kapısı):

```
FOTO 5 · TAM DOĞRU SPEC 1 (%20.0)
ALAN YARGISI 51 · tutan 47 (%92.2)
```

Aynı sayı sadece 10 alanla, motor kapısı olmadan (inline python, log §3):
`live-2026-08-22: full-spec exact 2/5` · `opus: 2/9` · `siglip: 2/19` · `clip: 0/19`.

**HÜKÜM: bugün foto→spec hattı, ölçülebilen 5 fotonun 1'inde (%20) tam doğru
spec'e iniyor. Alan bazında 47/51 (%92.2).** Payda 5'tir, 19 değil — kalan 14
etiketli foto için bankalanmış canlı okuma YOK ve okuma ücretli (§ yapılamayan).

### Akış öldüren `garment=other/null`

`python3 vision/live-eval-score.py vision/eval/live-baseline-oldprompt.json`:
```
photos read: 68
flow-killing garment=other/null: 5  (7.4%)
garment spread: {'top': 19, 'skirt': 12, 'dress': 32, 'other': 5}
```
Beşinin de gerekçesi doğru (kumaş yakın çekimi, karikatür, gemi fotoğrafı,
arkeolojik obje, drape edilmiş etnik giysi) — yani ıskalanan giysi değil, korpus kiri.

---

## 3) HATA SINIFI TABLOSU

`node engine/tools/foto-spec-olcum.mjs --offline` (5 foto, 51 alan yargısı):

| sınıf | adet | oran | örnek |
|---|---|---|---|
| **GORME** (kapalı listede var, vision yanıldı) | **4** | %100 | `02-ball-gown`: göz `square` / vision `boat` · `03-wedding-dress`: göz `aLine` / vision `straight` · `04-babydoll`: göz `elbow` / vision `long` · `01-a-line-cocktail`: göz `dart` / vision `princess` |
| **KELIME** (gözün kelimesi sözlükte yok) | **0** | %0 | — (göz etiketleri zaten worker sözlüğüyle yazıldı, tanım gereği içeride) |
| **MOTOR** (spec geçerli, motor reddetti/validator kırmızı) | **0** | %0 | 5 fotonun 5'inde motor kalıp bastı: panel 10/10/10/6/8 |

Dördü de sınır kararı (square↔boat, aLine↔straight, elbow↔long, dart↔princess) —
prompt'un `backend/worker.js:337` ve `:342` satırlarında zaten uyardığı çiftler.

### KELIME sınıfı sıfır DEĞİL — kapalı listede, serbest kanalda patlıyor

```
SERBEST KANAL (outOfVocab): 26 terim, 26 tanesi terim sicilinde YOK (%100.0)
```
5 fotoda vision'ın "bunu görüyorum ama alanlarım anlatamıyor" dediği **26 yapı
terimi** var ve `contract/terms.json` sicilinde **26'sının da karşılığı yok**:
`asymmetric draped pleated overlay at hip`, `lace overlay bodice and sleeves`,
`beaded empire waist sash`, `flared bell lace cuffs`, `empire seam lace trim band`…
Yani foto başına ~5 yapı elemanı düşüyor. Kapalı listede KELIME=0 olması,
sözlüğün geniş olduğu anlamına gelmiyor; göz etiketinin de aynı dar listeyle
yazılmış olması anlamına geliyor.

### İkinci kelime cephesi — sözleşme v2 okumayı İFADE EDEMİYOR

`contract/garment-spec-v2.json` (SEALED 2026-08-17) topolojisi 7 eksen:
`garment · skirtShape · shoulder · sleeve · collar · closure · suppression`.
Vision'ın bastığı 22 alanın çoğunun v2'de **ekseni yok**: `neckline`, `length`,
`waistline`, `fabric`, `hemRuffle`, `keyhole`, `topLength`, `straps`, `yoke`,
`backDetail`, `cupSeams`, `sleeveHead`, `ratios`.

68 fotoluk canlı koşu v2 kapısından geçirildiğinde (log §5):
```
garment not expressible in v2 (other/trousers): 5
skirtStyle value absent from v2 skirtShape enum: 5   (hepsi 'halfCircle'; v2'de 'fullCircle' var)
needs operator "sleeve" (status=absent): 34
expressible AND all needed operators shipped: 25/68 = 36.8%
```
Operatör durumları: `sleeve`, `collarFamily`, `gatheredOverlayLayer`, `skirtFamily`,
`zipperPiece` = **absent**; `shoulderSeam` = **flagged**; kalan 9 = shipped.
Yani **68 fotonun 34'ü (%50) yalnızca "kollu" olduğu için v2'de çizilemez.**
Bu bir MOTOR eksiği ama sözleşme katmanında görünüyor; `foto-spec-olcum.mjs`
eski `vocab.gen.js` hattını koştuğu için MOTOR=0 basıyor. **İki kapı aynı şeyi
ölçmüyor** — bu çelişki DOKUNULMADI, aşağıda kuyruğa yazıldı.

---

## 4) vision-student KELİME LİSTESİNİN KAYNAĞI — HÜKÜM: ELLE YAZILMIŞ

**Kaynak dosya: `vision-student/vocab.py`, satır 17 / 21 / 22 / 23.**

```
vision-student/vocab.py:17  NECKLINE_CLASSES = ["crew","scoop","vNeck","square","boat","sweetheart","halter"]
vision-student/vocab.py:21  GARMENT_CLASSES  = ["skirt","dress","top","trousers","other"]
vision-student/vocab.py:22  SLEEVE_LENGTH_CLASSES = ["short","elbow","long"]
vision-student/vocab.py:23  SKIRT_STYLE_CLASSES   = ["aLine","straight","gathered","halfCircle","pleated"]
```

Dört liste de **düz Python literali**. Üreteç YOK:
`grep -rn --include='*.py' -e NECKLINE_CLASSES -e GARMENT_CLASSES .` sadece
**tüketici** buluyor (`train_val_run.py:28`, `test_onnx_load.py:13`,
`calibrate_cascade.py:28`, `train_field.py:29`) — hiçbiri bu listeyi ÜRETMİYOR.
Bir bekçi/`--check` de yok.

Dosyanın kendi yorumu (`vocab.py:2-3`, `:19-20`) "classes MUST stay identical to
the teacher schema in backend/worker.js" diyor. **Bu garanti kodda uygulanmıyor
→ RULES.md invariant 1'e göre yok hükmünde. Ve zaten SÜRÜKLENMİŞ:**

- `backend/worker.js:309` bugün **9** neckline değeri sayıyor: `crew scoop vNeck
  square boat sweetheart halter` **+ `cowl` + `pussyBow`**.
- `vision-student/vocab.py:17` **7** değer taşıyor. `cowl` ve `pussyBow` yok.
- Etkisi: `label_to_index` (vocab.py:48-53) sözlük dışını `None` döndürüyor, yani
  öğretmen `cowl`/`pussyBow` dediği her örnek "belirsiz" sayılıp **sessizce
  eğitimden düşer** — RULES invariant 1'in yasakladığı sessiz düşürmenin ta kendisi.

**Repoda ÜRETİLEN bir sözlük var, ama student onu kullanmıyor:**
`backend/vocab.gen.js` ilk satırı — `// GENERATED by engine/tools/gen-vocab.mjs
from engine/vocab.json — DO NOT EDIT.` Üreteç komutu: `node engine/tools/gen-vocab.mjs`.
Kaynak: `engine/vocab.json`. `web/js/vocab.gen.js` de aynı üreteçten.
`vision-student/vocab.py` bu zincirin **dışında** duruyor.

---

## YAPILAMAYAN (sebep)

1. **19 etiketli fotonun 14'ü ölçülemedi.** `foto-spec-olcum.mjs` bankasında
   sadece 5 okuma var (`vision/eval/live-2026-08-22.json`). Kalanı ölçmek
   `/api/analyze`'a canlı çağrı gerektirir = **ücretli** → kart YASAKLAR gereği
   yapılmadı. Payda 5 olarak raporlandı, oran tahmin edilmedi.
2. **`vision-student` hiç koşturulamadı.** `dataset/labels/` klasörü YOK (ambar
   boş), `vision-student/.venv` YOK, `vision-student/runs/` YOK (`.gitignore`:
   `.venv/ runs/ *.pt *.onnx`). Ağırlık indirmek/venv kurmak §5.3 kalıcı vetosu
   → koşulmadı. Öğrenci↔öğretmen uyum sayısı (`eval_agreement.py`, go-live
   eşiği %85) **bugün ölçülemez**.
3. **CLIP/SigLIP zero-shot koşucuları yeniden koşturulamadı.** `vision/node_modules`
   diskte yok, `@xenova/transformers` model ağırlığı indirir → veto. Kayıtlı
   `clip-predictions.json` / `siglip-predictions.json` puanlandı.
4. **68 fotoluk `live-baseline-oldprompt` koşusu tekrarlanamaz.** Kaynak
   `vision/corpus/photos/` diskte 0 dosya (gitignore'da).
5. **`live-baseline-oldprompt.json` göz etiketine karşı puanlanamadı** — farklı
   foto seti (korpus), `labels.json` ile kesişmiyor. Sadece `other/null` oranı sayıldı.

## KART DIŞI FARK EDİLEN (dokunulmadı, yazıldı)

1. **`foto-spec-olcum.mjs`'nin bankası tarih damgalı** (`live-<bugün>.json`),
   `--offline` bugün boş banka okuyup `FOTO 0` basıyor. Ölçüm için 22 Ağu bankası
   bugünün adına KOPYALANDI, araç koşuldu, kopya **hemen silindi** — canlı/ücretli
   çağrı yapılmadı, repoda sahte tarihli dosya bırakılmadı. Aracın `--bank <yol>`
   bayrağı yok; kart onarımı yasakladığı için eklenmedi.
2. **İki kapı aynı şeyi ölçmüyor.** `foto-spec-olcum.mjs` eski `vocab.gen.js`
   hattından `draft()` koşuyor ve MOTOR=0 basıyor; aynı okumalar mühürlü
   `contract/garment-spec-v2.json` topolojisine vurulunca 68 fotonun sadece
   %36.8'i ifade edilebiliyor (kol operatörü absent). Hangisinin ürünün gerçeği
   olduğu KARARA BAĞLANMADI.
3. **`vision/README.md` bayat.** "Opus 86%" diyor; `live-2026-08-22.json` yeni
   prompt'la 94% (50 alan) basıyor ve README'de bu koşu hiç geçmiyor.
   Ayrıca "product bar ~95%" cümlesinin kaynağı yok.
4. **`vision/eval/labels.json` kendi içinde PARTIAL olduğunu yazıyor**
   ("labeling paused mid-way for Damla's product-audit directive"), yani %94'ün
   paydası hem küçük hem yarım kalmış bir etiket setinden.
5. **`dataset/vocab-canonical.json` ile `contract/terms.json` iki ayrı sicil.**
   `foto-spec-olcum.mjs` outOfVocab'ı yalnız `terms.json`'a soruyor; kanonikleştirici
   `engine/tools/canonicalize.mjs` / `mine-vocab.mjs` diğerini kullanıyor. 26/26
   "sicilde yok" sayısı bu ayrımdan etkilenmiş olabilir — DOĞRULANMADI.
6. **`vision/live-eval.sh` gitignore'lu bir token dosyasına bağlı**
   (`App/Stitchu/.app-token-note.txt`); diskte var mı KONTROL EDİLMEDİ (açmadım).
