# V0-0D — SÖZLÜK DİLİ ENVANTERİ

Koşu: 2026-08-24. Kart: `GECE/KART/V0-0D-sozluk.md`. Etiket PARALEL.
Ham grep/kOşu çıktısı: `GECE/log/V0-0D.enum-refs.txt` (426 satır).
Bu kart **ÖLÇER, onarmaz** — hiçbir enum silinmedi/taşınmadı, hiçbir kod satırı değişmedi.
Ara değer testi `/tmp/v00d/` altında koştu.

---

## 0. TEK CÜMLELİK HÜKÜM

**Tarayıcının gerçekten koşturduğu hat MENÜ dilinde konuşuyor; malzeme dili
repoda var ama SEVK EDİLMİYOR.** Kullanıcının 33 seçicisinin 33'ü kapalı liste,
sıfırı sürekli girdi (`GECE/log/V0-0D.enum-refs.txt` BÖLÜM D). Sürekli parametre
motorun **içinde** yaşıyor (mm/derece/oran), ama kullanıcı arayüzünden ona giden
sadece **iki kaçış kapısı** var (`skirtLengthMM`, `fabricStretchPct`) ve ikisi de
menüden değil, **fotoğraf/ölçüm hattından** dolduruluyor.

---

## 1. DOSYA DOSYA DİL TABLOSU

Sınıflandırma mekanik ve tekrarlanabilir: bir dosyada
**MENÜ kanıtı** = `^enum class` · JSON `"enum":` · `inline constexpr const char* k…[]`
· `{ key: '…', options:` · `parseEnum` çağrısı;
**MALZEME kanıtı** = `\w+(MM|CM|Deg|Pct|Ratio|Frac|Fullness)` tanımlayıcısı ya da
`double x = <sayı>`. İkisi de varsa KARIŞIK.

Taranan 149 giysi-tanımlayan dosyanın dağılımı:
**MALZEME 113 · KARIŞIK 30 · MENÜ 6.**

### 1a. MENÜ dilinin TAMAMI — sadece 6 dosya

| yol | dil | KANIT (dosya:satır) | sevk? |
|---|---|---|---|
| `engine/vocab.json` | MENÜ | `engine/vocab.json:4` — 37 eksen × kapalı `values[]` | ÜRETEÇ GİRDİSİ (buradan 3 dosya doğar) |
| `engine/src/vocab.gen.hpp` | MENÜ | `engine/src/vocab.gen.hpp:9` `inline constexpr const char* kGarment[]` | **SEVK** (wasm'da) |
| `web/js/vocab.gen.js` | MENÜ | `web/js/vocab.gen.js:10` | **SEVK** (tarayıcıda) |
| `engine/src/specparse.hpp` | MENÜ | `engine/src/specparse.hpp:28` `parseEnum` · `:36` `parseEnumInt` | **SEVK** |
| `engine/src/gather.hpp` | MENÜ | `engine/src/gather.hpp:40` `enum class GatherType` · `:44` `GatherZone` | SEVK |
| `engine/src/hemflounce.hpp` | MENÜ | `engine/src/hemflounce.hpp:42` `enum class HemFlounce` | SEVK |

Yani sözlüğün tamamı **tek bir dosyadan** (`engine/vocab.json`) türüyor; C++ ve JS
tarafları ondan üretilmiş. V2'nin söküm noktası burasıdır, dağınık değil.

### 1b. KARIŞIK — menüyü BAŞLIKTA taşıyıp gövdede mm konuşanlar (30 dosya)

Baskın desen: `X.hpp` bir `enum class` tanımlar (menü), `X.cpp` o enum'u `switch`'e
sokar ve içinde mm/derece hesaplar (malzeme). Örnekler, ilk kanıt satırlarıyla:

| yol | menü kanıtı | malzeme kanıtı | sevk? |
|---|---|---|---|
| `engine/src/measurements.hpp` | `:65` `enum class Neckline` (13 enum tanımı) | `:12` `double bustCM…` (25 sürekli) | SEVK |
| `engine/wasm/bindings.cpp` | `:68` `parseEnum<Neckline>` (37 çağrı) | `:101` `numField` (7) | SEVK |
| `engine/src/fabricease.hpp` | `:69` `enum class Girth` | `:90` `stretchPct` bandı (24) | SEVK |
| `engine/src/cupseam.hpp` | `:46` (4 enum) | 28 sürekli | SEVK |
| `engine/src/pocket.hpp` | `:49` `PocketStyle` · `:54` `PatchCorner` | 19 sürekli | SEVK |
| `engine/src/collar.hpp` | `:48` `CollarType` · `:53` `CollarEdge` | 7 sürekli | SEVK |
| `engine/src/geometry.hpp` | `:17` `enum class CmdType` | 10 sürekli | SEVK |
| `engine/src/hem.hpp` | `:47` `HemShape` | 5 sürekli | SEVK |
| `engine/src/drape.hpp` | `:45` `enum class SpringKind` | 22 sürekli | **SEVK-DIŞI** |
| `web/js/create.js` | `:28` `{ key: 'garment', … options:` (33 picker) | `:336` `skirtLengthMM` (6) | SEVK |
| `web/js/contract.gen.js` | `:256` `"enum":` (20) | `:9` `"skirtLengthMM"` (8) | SEVK |
| `engine/src/recipe.cpp` | `:41`,`:209`,`:370`,`:429`,`:435` (parser içi) | 28 sürekli | SEVK |

Kalan karışık dosyalar aynı `.hpp` enum + `.cpp` mm deseni:
`backdetail · boxpleat · buttonrow · cuff · exposedzip · laceupback · locket ·
offshoulder · openback · peplum · placket · slit · strap · tie · wrapfront · yoke`
(hepsi SEVK; `.hpp` satır numaraları BÖLÜM B'de tek tek listeli).

### 1c. MALZEME — saf sürekli parametre (113 dosya, tepe örnekler)

| yol | KANIT (ilk sürekli satır) | sürekli param # | sevk? |
|---|---|---|---|
| `engine/src/surfacepattern.cpp` | `:15` | **200** | **SEVK-DIŞI** |
| `engine/src/bodice.hpp` | `:20` | 110 | SEVK |
| `engine/src/bodice.cpp` | `:97` | 109 | SEVK |
| `engine/src/skirt.cpp` | `:12` | 83 | SEVK |
| `engine/src/surfacepattern.hpp` | `:51` | 60 | **SEVK-DIŞI** |
| `engine/src/bodysurface.cpp` | `:10` | 52 | **SEVK-DIŞI** |
| `engine/pattern-bridge/walk.py` | `:67` | 46 | SEVK-DIŞI (python hattı) |
| `engine/pattern-bridge/panelcheck.py` | `:26` | 46 | SEVK-DIŞI |
| `engine/pattern-bridge/printpack.py` | `:213` | 36 | SEVK-DIŞI |
| `engine/src/constants.gen.hpp` | `:12` `kSeamAllowanceMM = 15` | 30 | SEVK |
| `engine/src/garmentshell.cpp` | `:10` | 26 | **SEVK-DIŞI** |
| `engine/src/flatten.cpp` | `:19` | 14 | **SEVK-DIŞI** |
| `recipes/bugra-locket-top-front-38.json` | `:15` | 58 | tarif verisi |
| `recipes/shift-dress-square-spaghetti.json` | `:8` | 41 | tarif verisi |
| `recipes/skirt-aline-dart.json` | `:8` | 18 | tarif verisi |
| `knowledge/dial-seam-table.json` | `:562` | 1 | okuyucu YOK (§5) |

`contract/` altındaki 23 dosyanın hiçbiri bu taramada MENÜ çıkmadı; şema dosyaları
(`garment-spec.schema.json` 82 adet `"enum"`, `garment-spec-v2.schema.json` 8,
`primitives-v1.json` 8) menüyü **doğrular** ama motor yolu onları okumaz — motorun
okuduğu tek sözlük `engine/vocab.json`.

---

## 2. SEVK EDİLEN TARAF HANGİSİ? — ÇAĞRI ZİNCİRİ

Kullanıcının tarayıcıda tetiklediği yol, dosya:satır:

```
web/js/create.js:28-125   33 picker, hepsi kapalı options[] listesi   → MENÜ
web/js/create.js:730      const result = await draft(spec, values)
web/js/engine.js:21-43    intValue('collarType', …)  — kelime → indeks   → MENÜ
                          (indeks tablosu: web/js/vocab.gen.js:10)
web/js/engine.js:56       script.src = 'vendor/stitchu-engine.js?v=136'
web/js/engine.js:57       window.createStitchuEngine()
engine/wasm/bindings.cpp:392   emscripten::function("draftJSON", &draftJSON)
engine/wasm/bindings.cpp:264   draftJSON(val specObj, val bodyObj)
engine/wasm/bindings.cpp:109   buildSpec(o)
engine/wasm/bindings.cpp:68-78 parseEnum<E>(...)  ← 37 eksenin 37'si burada
engine/src/specparse.hpp:28    parseEnum  /  :36 parseEnumInt   → MENÜ KAPISI
engine/src/measurements.hpp:279 GarmentSpec  (44 alan)
engine/src/garment.cpp          switch(enum) → mm çizim              → MALZEME
```

### HÜKÜM: hâkim dil MENÜ, iki yerde sayıyla

**(a) Giriş yüzeyi %100 menü.** `GarmentSpec` (measurements.hpp:279-490) 44 alan:
- **37 alan kapalı enum** (= `engine/vocab.json`'daki 37 eksenin birebir karşılığı)
- **3 alan sürekli `double`**: `skirtLengthMM` (`:302`), `ruffleFullness` (`:306`),
  `ruffleDepthMM` (`:307`)
- 3 `bool` (`ruffleHem`, `keyhole`, `frontPlacket`), 1 tam sayı (`ruffleTiers`)

Yani **37/44 = %84 menü, 3/44 = %6.8 sürekli.**

**(b) Tarayıcı arayüzü sürekli girdiyi HİÇ sunmuyor.** `web/js/create.js`'te
`type="range"` / `type="number"` / slider sayısı **0** (BÖLÜM D).
`skirtLengthMM`'i yazan tek yer `create.js:336`/`:446` — `refreshSkirtLengthMM(...)`,
yani **fotoğraf oranından** gelir; kullanıcı elle menüden bir beden seçerse
`create.js:710` onu **0'a geri düşürür** (`spec.skirtLengthMM = 0; photoLenHandPicked = true`).
`fabricStretchPct`'yi web'de hiçbir picker yazmıyor — sadece `engine.js:92` iletiyor.

**(c) Repodaki en yoğun malzeme dili SEVK EDİLMİYOR.** `engine/build-wasm.sh`
43 `.cpp`'nin **35'ini** derliyor. Dışarıda kalan 8'i:

```
bodysurface.cpp  curvefit.cpp  drape.cpp  flatten.cpp
garmentshell.cpp  nest.cpp  surfacepattern.cpp  volume.cpp
```

Bu 8 dosya tam olarak KOŞU 4'ün "tek yüzey motora indi" hattı + ARAP flatten +
Verlet drape + nest. Sürekli parametre yoğunluğu en yüksek dosya
(`surfacepattern.cpp`, 200 sürekli param) tarayıcıya **hiç girmiyor**.
Tarayıcıdaki motor hâlâ 2B-formül hattı (`bodice.cpp` + `skirt.cpp` + `sleeve.cpp`).

---

## 3. KAPALI ENUM SAYIMI — V2'nin `vocab_reference_check` TABANI

Tam tablo `GECE/log/V0-0D.enum-refs.txt` BÖLÜM A.1 + A.2'de. Özet:

| ölçü | sayı | KOMUT |
|---|---|---|
| kapalı eksen (alan) sayısı | **37** | `python3 -c "import json;d=json.load(open('engine/vocab.json'));print(len(d['fields']))"` |
| toplam kapalı enum DEĞERİ | **132** | `python3 -c "import json;d=json.load(open('engine/vocab.json'));print(sum(len(v['values']) for v in d['fields'].values()))"` |
| eksen ADI referansı (37 eksen toplamı) | **7524** | A.1 tablosu (aşağıdaki grep, eksen başına) |
| enum DEĞERİ ham referansı (132 kelime) | **29703** | A.2 tablosu |
| bunun ekseni-TEKİL kelimelerden geleni | **2833** | A.2 alt toplam |
| paylaşılan kelimelerden gelen (KİRLİ) | **26870** | A.2 alt toplam |

**Taban komutu (kopyala-yapıştır, aynı sayıyı basar):**

```bash
cd /Users/damummyphus/damla_projects_2026/stitchu
# eksen ADI referansı — X yerine eksen adı
grep -rIn --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=node_modules \
  --exclude-dir=probe -w 'X' \
  contract engine/src engine/wasm engine/tools engine/pattern-bridge \
  engine/vocab.json web/js recipes backend knowledge | wc -l

# enum DEĞERİ referansı — v yerine tek kelime
grep -rIn --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=node_modules \
  --exclude-dir=probe -F '"v"' \
  contract engine/src engine/wasm engine/tools engine/pattern-bridge \
  engine/vocab.json web/js recipes backend knowledge | wc -l
```

⚠ **KAPSAM UYARISI, V2 buna dikkat etsin.** İlk koşu kapsamı daraltmamıştı ve
`garment` için **8978** bastı; `Logs/ docs/ reports/ .git/` çıkarılınca **1167**.
Yukarıdaki dar kapsam kanonik sayıdır; geniş kapsam ~7.7× şişik.

⚠ **`"none"` KİRLİLİĞİ — VALREF sütununu tek başına kullanma.** `"none"` kelimesi
**22 ayrı eksende** ortak ve tek başına **1178** referans veriyor; 29703'ün 26870'i
(%90.5) paylaşılan kelimelerden geliyor. V2 kelime bazlı sayarsa
`grep -F '"none"'` **22 eksenin hepsini birden** sayar. Sağlıklı taban:
A.2'nin **PAYLASIM=1** satırları (2833 referans).

**En yoğun 8 eksen (REF, dar kapsam):**
`garment` 1167 · `neckline` 984 · `fabric` 632 · `shaping` 385 · `sleeveStyle` 352 ·
`peplum` 332 · `skirtStyle` 329 · `yoke` 315.
**En seyrek 8:** `hemFlounce` 24 · `boxPleat` 25 · `locketTop` 28 · `laceUpBack` 29 ·
`bardotStyle` 33 · `exposedZip` 39 · `shoulderStyle` 40 · `cupSeam` 43.

**Sözlük dışı `enum class`'lar (giysi tanımlamıyorlar, V2 sökümünde ayrı tutulmalı):**
`PatchCorner` (`engine/src/pocket.hpp:54`) · `Girth` (`engine/src/fabricease.hpp:69`) ·
`SpringKind` (`engine/src/drape.hpp:45`) · `CmdType` (`engine/src/geometry.hpp:17`) ·
`recipe.cpp`'nin 4 parser enum'u (`:41 :209 :370 :429 :435`).

---

## 4. ARA DEĞER TESTİ — ÖLÇÜM

Denenen motor: `engine/dist/stitchu-engine.js` (tarayıcının yüklediği baytın
kendisi — `web/js/engine.js:56` onu çağırıyor, `build-wasm.sh` oraya kopyalıyor).
Gövde EU38. Parmak izi = `sha256(JSON.stringify(pattern))` ilk 12 hane.
Ham çıktı: `GECE/log/V0-0D.enum-refs.txt` BÖLÜM E.

### 4a. int enum ekseni: SESSİZ İKAME VAR (kanıtlı)

`sleeveCap` ekseni, `gathered`(1) ile `puffed`(2) arasına 1.5 verildi:

```
sleeveCap=1     -> sha=eaba7a7b64ed
sleeveCap=1.5   -> sha=eaba7a7b64ed     <-- 1 ile BAYT AYNI
sleeveCap=2     -> sha=6f560bbfe0aa
   1.5 sha == 1 sha ? true    |    1.5 sha == 2 sha ? false
```

Tarama, kesire duyarsız — aşağı doğru kırpıyor, sıfıra doğru kırpıyor:

```
0.1 / 0.9   -> ac4d2356449c   (= 0'ın çizimi)
1.0/1.5/1.9 -> eaba7a7b64ed   (= 1'in çizimi)
2.0/2.999   -> 6f560bbfe0aa   (= 2'nin çizimi)
3.0/3.0001  -> 81c0cd4c10e5   (= 3'ün çizimi)
-0.5        -> ac4d2356449c   (= 0'ın çizimi)
```

**Motorun cevabı: SESSİZ İKAME. Hata YOK, uyarı YOK, `issues` artmıyor.**

Kök yer: `engine/wasm/bindings.cpp:94` — `intField()` içinde `v.as<int>()`,
`parseEnumInt`'e ulaşmadan ÖNCE 1.5'i 1'e çeviriyor. `specparse.hpp:36`'nın
aralık kontrolü doğru çalışıyor ama artık ara değeri göremiyor.
Bu, `RULES.md` değişmez 1'in ("Unknown or unsupported enum value → Err.
NEVER silently drop or coerce") **sevk edilen hatta ihlalidir**. `bindings.cpp:82`
yorumu bunun tam tersini iddia ediyor ("present-but-unknown value = error via
specparse"). **Onarılmadı — kart YASAKLAR gereği dokunulmadı.**

Aynı kırpma 26 int-enum alanının hepsinde geçerli (`bindings.cpp:146-172`,
hepsi `intField(o, …)` üzerinden). Ölçüldü: `collarType=4.5` ile `collarType=4`
aynı çıktıyı veriyor.

### 4b. string enum ekseni: DÜRÜST HATA (sessiz ikame YOK)

```
neckline="scoopVNeck"  -> ERROR: invalid neckline 'scoopVNeck'
                          (valid: crew, scoop, vNeck, square, boat, sweetheart,
                           halter, cowl, pussyBow)
skirtLength="midiMaxi" -> ERROR: invalid skirtLength 'midiMaxi' (valid: mini, midi, maxi)
fabric="wovenKnit"     -> ERROR: invalid fabric 'wovenKnit' (valid: woven, knit)
neckline=1.5           -> THROW: Cannot pass non-string to std::string
```

11 string alanı (`garment shaping waistline fabric neckline sleeveStyle
sleeveLength skirtStyle skirtLength topLength` + `collarEdge`'in web adı)
ara değeri **reddediyor** ve geçerli listeyi basıyor. Kural burada tutuyor.

### 4c. İKİ eksende ara değer GERÇEKTEN ÇALIŞIYOR (kaçış kapıları)

`skirtLength` menüsü (mini 450 / midi 650 / maxi 900 mm, `contract.gen.hpp:11-13`)
yanında sürekli `skirtLengthMM` duruyor:

```
skirtLengthMM=650   -> maxAbsY=665.0059mm
skirtLengthMM=700   -> maxAbsY=715.0059mm
skirtLengthMM=775   -> maxAbsY=790.0059mm    <-- midi ile maxi ARASINDA
skirtLengthMM=776   -> maxAbsY=791.0059mm    <-- 1mm ayrı, ayrı sha
skirtLengthMM=776.5 -> maxAbsY=791.5059mm    <-- 0.5mm ayrı, ayrı sha
skirtLengthMM=900   -> maxAbsY=915.0059mm
```

Gerçek süreklilik: 0.5mm çözünürlükte ayrı geometri. Motor `[250,1200]` kelepçeliyor
(`bindings.cpp:135`).

`fabric` menüsü (woven/knit) yanında `fabricStretchPct`:

```
stretchPct=0     -> 29ad5eb0f7a2
stretchPct=6.25  -> a921f34a8931    <-- woven(%0) ile knit(%12.5) ARASINDA
stretchPct=6.26  -> 5291195aceae    <-- 0.01 puan ayrı, ayrı sha
stretchPct=12.5  -> 3ebef1d59a1a
stretchPct=25    -> 04c4a25e1ed9
```

`FabricAxis` (`measurements.hpp:90-107`) bu iki dilin repodaki **tek temiz melezi**:
enum sınıf + sürekli yüzde, ve `declared()` ile "beyan edilmedi" hâli
0'dan ayrı tutuluyor. V2 için taşınabilir desen budur.

### 4d. Özet: 37 eksenin ara-değer davranışı

| grup | eksen sayısı | ara değere cevap |
|---|---|---|
| string enum | 10 | **dürüst hata** + geçerli liste |
| int enum | 26 | **SESSİZ İKAME** (aşağı kırpma, `bindings.cpp:94`) |
| sürekli kaçış kapısı olan | 2 (`skirtLength`→`skirtLengthMM`, `fabric`→`fabricStretchPct`) | **çalışıyor**, gerçek ara geometri |

(10+26 = 36; `collarEdge` web'de string, motorda int — iki grupta da sayılabilir,
bu yüzden toplam 37'yi aşmaz.)

---

## 5. KART DIŞI FARK EDİLENLER (DOKUNULMADI)

1. ⚠ **`collarType` 1..6'nın HEPSİ NaN koordinat üretiyor — yaka ailesi sevk
   edilen hatta KIRIK.** `draftJSON` çıktısı `"x":nan,"y":nan` içeriyor, bu
   geçersiz JSON, tarayıcıdaki `JSON.parse` fırlatıyor
   (`SyntaxError: Unexpected token 'a'`). 5 ayrı taban spec'te doğrulandı
   (dress+crew+straight, dress+crew+sleeveless, top+crew, dress+scoop,
   dress+vNeck) — hepsinde **8 adet nan**. `collarType=0` temiz.
   Komut: `cd /tmp/v00d && node collar.mjs`. Ham çıktı BÖLÜM E sonu.
   **Bu bir V0-0D işi değil; onarılmadı, sadece bildiriliyor.**

2. ⚠ **Menü kayması: motor 7 yaka tanıyor, web 6 gösteriyor.**
   `engine/vocab.json:18` ve `engine/src/collar.hpp:42` `crescent` taşıyor
   (Buğra Locket Top'ın U yakası). `web/js/create.js:34`'ün options listesinde
   `crescent` **yok** (`grep -c crescent web/js/create.js` → **0**).
   Yani kullanıcı hedef giysinin yakasını arayüzden seçemiyor.

3. ⚠ **`knowledge/dial-seam-table.json` yalnız SEVK EDİLMEYEN Python hattına bağlı.**
   `grep -rIn "dial-seam-table" contract engine web recipes backend` → **5 satır**:
   `engine/pattern-bridge/dials.py:14,24,40` (üreteci ve okuyucusu),
   `engine/pattern-bridge/matrix.py:183` (hata mesajı içinde),
   `contract/garment-spec-v2.md:28` (doküman alıntısı).
   **`engine/src/` ve `web/js/` içinde sıfır referans** — yani 68 sürekli kadranın
   6'sının dikişi oynattığını ölçen tablo tarayıcıdaki motora hiç ulaşmıyor.
   İçeriği MALZEME dili. `contract/garment-spec-v2.md:28` bu tabloya dayanarak
   "parametre uzayının %91'i ölü" diyor.

4. ⚠ **`contract/garment-spec-v2*.json` üçlüsü motor yolunda okunmuyor.**
   Motorun okuduğu tek sözlük `engine/vocab.json`; `contract/`'taki üç şema
   (82 + 8 + 8 = 98 `"enum"` bloğu) paralel bir menü tanımı. Hangisinin
   otorite olduğu **DOĞRULANMADI** — V2 karar vermeden söküme başlamamalı.

5. ⚠ **`engine/dist/` gitignore'da ama diskte 23 Ağu 05:51 tarihli.**
   Ara değer testi bu bayta koştu. Bu baytın `engine/src/`'nin **bugünkü** hâlinden
   üretildiği **DOĞRULANMADI** (yeniden derlenmedi — kart kod değiştirmeyi yasaklıyor
   ve `emsdk` kurulumu doğrulanmadı). 4a/4c sonuçları sevk edilen bayt için kesin;
   kaynak için "muhtemelen aynı" demiyorum.

6. ℹ️ **Tarama filtresi uyarısı (kendi ölçüm aletimin kusuru).** §1'in tablosu
   "ne menü ne sürekli kanıtı olan" dosyaları atıyor. `engine/src/keyhole.cpp`
   diskte VAR ve `build-wasm.sh:14`'te derleniyor, ama tabloda görünmüyor —
   çünkü mm/enum deseni eşleşmedi. Tablodaki 149 dosya, taranan dosyaların
   TAMAMI değil; dil kanıtı taşıyan alt kümesi. V2 bunu tam envanter sanmasın.

---

## 6. GÖREMEDİĞİM / ERİŞEMEDİĞİM

- `contract/vocab-resolution-v1.json` (3096 satır) **açılmadı** — mekanik taramaya
  girdi ama içeriği okunmadı; adı "çözümleme" diyor, V2 için önemli olabilir.
- `engine/pattern-bridge/` Python hattının ara-değer davranışı **koşulmadı**
  (kendi venv'i gerekiyor, kart süre tavanı). Sadece statik olarak MALZEME
  sınıflandı.
- `contract/layers/*` (4 dosya) tabloda görünmüyor: taramada ne menü ne sürekli
  deseni eşleşti. İçerikleri okunmadı.
- `mocks/`, `vision/`, `App/`, `backend/worker.js` kart girdisi değil, taranmadı.
