# V10-F — YALANIN KÖKÜ: ÜRETEÇLER (rapor)

Kanıt dosyası: `GECE/log/V10-F.uretec.txt` (komutlar + çıktılar orada).

---

## 1. NE YAPILDI

V10-D 91 sayfadan MTM ve duran-iddia dilini sildi, ama o sayfaları basan
üreteçlerin şablon metnine dokunamadı (kartı `engine/` altına yazmayı
yasaklıyordu). Bu turda üreteçlerin **kaynak metni** düzeltildi.

Ölçüldü, tahmin değil: HEAD'deki `gen-style-pages.mjs` geçici bir dizine
koşturuldu (`/tmp/v10f-before/styles`) ve çıktısı canlı `web/styles/` ile
karşılaştırıldı — **24 dosyanın 24'ü farklı.** Yani üreteç bugün koşsaydı
V10-D'nin bütün işini silecekti. Düzeltmeden sonra fark **8 dosya / 30 satır**,
ve o 30 satırın hepsi üretecin canlı sayfadan **daha dürüst** olduğu yerler.

---

## 2. KALIP BAŞINA ÖNCE / SONRA — ÜRETİLEN HTML (24 sayfa)

`gen-style-pages.mjs`, iki kez geçici dizine koşturuldu, çıktı HTML'i grep'lendi.
Komut: `cat /tmp/v10f-{before,out}/styles/*.html | grep -cF "<kalıp>"`

| kalıp | ÖNCE | SONRA |
|---|---:|---:|
| `made to measure` | 0 | 0 |
| `your own measurements` | 46 | **0** |
| `your measurements` | 10 | **0** |
| `no fixed sizes` | 46 | **0** |
| `byte-identical` | 13 | **0** |
| `zero issues` | 0 | 0 |
| `0.000000 mm` | 23 | **0** |
| `always` (kelime) | 1 | **0** |

`0.000000 mm` uydurma hassasiyetti: `engine/tools/precision-report.js`
`toFixed(2)` ile **iki** basamak basıyor. Yerine RULES 6 karşılığı kondu —
"worst pair mismatch 0.00 mm, printed by engine/tools/precision-report.js
(the tool prints two decimals)".

Kanıt kutusundan silinen dört donmuş sabit: `FUZZ='19,780'`, `VOCAB='37,800'`,
`MATRIX='70,200'`, `GOLDEN='0.000000 mm'`. Hiçbiri sayfa başına yeniden
koşulmuyordu. Yerlerine sayıyı basan **aletin adı** yazıldı
(`engine_check`, `engine/tools/web-fuzz.js`, `engine/tools/vocab-sweep.cpp`,
`engine/tools/precision-report.js`) + "not re-run for this page".

---

## 3. KAYNAK TARAFI — ÖNCE / SONRA (koşturulamayan üreteçler için tek kanıt)

`git show HEAD:<dosya>` vs çalışma ağacı.

| dosya | kalıp | ÖNCE | SONRA |
|---|---|---:|---:|
| `engine/tools/gen-style-pages.mjs` | your own measurements | 1 | 0 |
| | your measurements | 4 | 0 |
| | no fixed sizes | 1 | 0 |
| | byte-identical | 6 | 0 |
| | 0.000000 mm | 1 | 1 (¹) |
| | always | 1 | 0 |
| `engine/tools/gen-collection-pattern.mjs` | your own measurements | 1 | 0 |
| | your measurements | 3 | 0 |
| | your exact measurements | 1 | 0 |
| | byte-identical | 2 | 0 |
| `engine/tools/gen-collections-page.mjs` | byte-identical | 1 | 0 |
| `engine/tools/gen-gore-contact.mjs` | byte-identical | 2 | 0 |
| `engine/tools/gen-wrap-contact.mjs` | byte-identical | 2 | 0 |
| `engine/tools/gen-vintage-page.mjs` | (hiçbiri) | 0 | 0 |

(¹) Kalan tek hit `gen-style-pages.mjs:725`'te, **sabitin neden silindiğini
anlatan yorum satırı**. Üretilen HTML'de 0 (bölüm 2).

Türkçe karşılıkları da düzeltildi (`gen-collection-pattern.mjs` `a_tr` / `data-tr`):
"kendi ölçülerinizde", "ölçülerinize göre değişir", "Bunu ölçülerine göre çiz"
→ sekiz sabit beden dili. Aynı yalanın TR yüzüydü.

---

## 4. DOKUNULAN DOSYALAR

- `engine/tools/gen-style-pages.mjs` — metin + kanıt kutusu + `--out=DIR`
- `engine/tools/gen-collection-pattern.mjs` — EN/TR metin + `--out=DIR`
- `engine/tools/gen-collections-page.mjs` — CSS yorumu + `--out=DIR`
- `engine/tools/gen-gore-contact.mjs` — `ctest 48/48` / `golden byte-identical`
  duran iddiaları → alet adı
- `engine/tools/gen-wrap-contact.mjs` — aynısı

`--out=DIR` üçünde de **geriye dönük uyumlu**: bayrak verilmezse eski yol
(`web/styles`, `web/collections`) aynen kullanılır. Kanıt koşusu bu bayrakla
yapıldı, `web/` altına **tek bayt yazılmadı** (`git status --porcelain web/` → 0 satır).

---

## 5. YAN DÜZELTME — ÜRETEÇ CANLI SAYFAYI GERİLETİYORDU (ölçüldü)

`gen-style-pages.mjs:317` `balloon-sleeve` manşetini **"10 mm seam allowance"**
diye basıyordu. V10-D canlı sayfayı **15 mm**'ye düzeltmişti. Doğrulandı:
`engine/src/constants.gen.hpp:13` → `inline constexpr double kSeamAllowanceMM = 15`.
Üreteç 15'e çekildi; yoksa ilk koşuda sayfa yanlış paya geri dönerdi.

---

## 6. KOŞTURULAMAYAN ÜRETEÇLER (ADIYLA, sebebiyle)

Üçü de aynı sebeple: girdi dizini `web/patterns/**` repoda YOK.

```
$ node engine/tools/gen-vintage-page.mjs
  ENOENT  web/patterns/vintage6070/meta.json
$ node engine/tools/gen-collections-page.mjs --out=/tmp/v10f-out/collections
  ENOENT  web/patterns/svg/meta.json        (gen-taste-collections.mjs:35)
$ node engine/tools/gen-collection-pattern.mjs --out=/tmp/v10f-out/collections
  ENOENT  web/patterns/vintage6070/meta.json
$ git ls-files web/patterns | wc -l  → 0
$ git log --oneline -1 -- web/patterns/vintage6070/meta.json
  af49514 delete fake pattern gallery (67 svgs + 22 product pages) …   (29 Tem)
```

Dizin gitignore'da **değil**, 29 Temmuz'da silinmiş. Bu üç üreteç bugün
koşturulamıyor; metinleri yine de düzeltildi ve kanıt kaynak-tarafı grep
(bölüm 3). **`web/collections/` altındaki 22 HTML canlı duruyor ama üretecini
bugün kimse koşturamaz** — bölüm 8/1.

Koşturulan diğer ikisi (`reports/gate/` altına yazıyor, `web/` değil):
`gen-gore-contact.mjs` → `MIHENK-08-contact.html`, `gen-wrap-contact.mjs` →
`MIHENK-07-contact.html`; ikisinde de `grep -cF "byte-identical"` → **0**.

---

## 7. KONTRAT ÇELİŞKİSİ — ÖLÇÜLDÜ, DEĞİŞTİRİLMEDİ

`contract/tables.json:13` 10 beden (EU34–EU52) diyor, `contract/layers/shape-ratios.json`
8 beden (EU34–EU48) diyor. Kontrat kararı olduğu için **tek taraflı dokunulmadı**;
`DAMLA-KUYRUK.md` → **K-V10F (3.8.d)** satırı düşüldü, varsayılan (A) 8 beden,
gerekçe `shape-ratios.json`'un kendi `drift_note.EU50_EU52` alanı
("EU50 … it ABORTS") ve kapının 4 L5 kaçağının 4'ünün de `web/js/contract.gen.js`
EU50/EU52 satırları olması.

---

## 8. YAPILMADI / GÖRÜLMEDİ (adıyla)

1. **`web/` DÜZELTİLMEDİ** — kartın yasağı (V10-E orada çalışıyor). Sonuç:
   üretecin bugünkü çıktısı 8 sayfada canlı sayfadan daha dürüst. Canlıda hâlâ
   duran ve üretecin artık basmadığı cümleler:
   - 6 sayfada FAQ: *"The engine drafts it from your body, not fixed sizes."*
     Bu, motorun gerçeğinin TAM TERSİ ve V10-D'nin kalıp listesinde olmadığı için
     kaçmış. Üreteç yeniden koşturulunca kendiliğinden düzelir.
   - `web/styles/index.html` hub lead: *"computed against your seven measurements"*.
   - `web/styles/v-neck.html`: *"to your own neck measurement"* / *"your neck measurement × 0.17"*.
   - `web/collections/*.html`: 17 `byte-identical` hit'i (CSS/JS yorumu) hâlâ orada.
2. **`ctest` KOŞULMADI.** Bu bir metin/şablon kartı; C++ derlenmedi, `engine/src/`e
   dokunulmadı.
3. **`engine_check`, `web-fuzz.js`, `vocab-sweep.cpp`, `precision-report.js`,
   `golden-diff.py` BU TURDA KOŞULMADI.** Sayfalara yazılan alet ADLARI o
   aletlerin varlığını bildiriyor, bugün o sayıyı bastıklarını DEĞİL — sayfa
   metni bunu açıkça söylüyor ("not re-run for this page").
4. **Tarayıcıda render YAPILMADI.** Üretilen HTML gözle görülmedi; bazı yeni
   hücre metinleri eskisinden uzun (kanıt tablosu), dar ekranda taşma **ÖLÇÜLMEDİ**.
5. **`landing_truth_check.mjs` DEĞİŞTİRİLMEDİ**, sadece koşturuldu:
   `HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4`.
   Bu kart `web/`e yazmadığı için kapı kımıldamadı; **kapı üreteç kaynağını
   hiç okumuyor**, yani bu turun düzeltmesini kapı DOĞRULAYAMAZ. Doğrulayan şey
   bölüm 2'deki üreteç koşusudur.
6. **DEPLOY YAPILMADI.**

---

## 9. KART DIŞI, DOKUNULMADI (ölçüldü, yazılıyor)

1. **`web/collections/` (22 HTML) ÖKSÜZ.** Üreteçleri `gen-collection-pattern.mjs`
   / `gen-collections-page.mjs` / `gen-taste-collections.mjs`, girdileri
   29 Tem'de silinmiş `web/patterns/**`. Sayfalar canlı, üreteç ölü: bu sayfalarda
   bir daha hiçbir düzeltme üreteçten geçemez, elle yapılmak zorunda. Aynı şey
   `web/collection-60s70s.html` (48 sağlayıcısız blokla V10-D'nin en yoğun ikinci
   dosyası) ve `gen-vintage-page.mjs` için de geçerli.
2. **`gen-style-pages.mjs`'in kalan MTM'i tam bitmedi.** Kalıp listesinde
   olmadığı için dokunulmayanlar: `:188` *"Waist and hip ease are 2% of YOUR
   measurement"*, `:375` *"sized from your body"*, `:480`/`:483` *"suppressed to
   your underbust girth"*. Bunlar bir formülü anlatıyor (o bedenin ölçüsü) ama
   "your" zamiri MTM okunuyor. **KARARA BAĞLANMADI.**
3. **V10-D'nin yerleştirdiği iki cümle bozuk İngilizce** ve üreteç artık onları
   birebir basıyor (canlı sayfayla aynı kalsın diye bilerek kopyalandı):
   *"facing stitch line **unchanged under golden_check to** the opening"* ve
   *"ratio ordering … **unchanged under golden_check base**"*. Anlam doğru,
   gramer kırık. Düzeltmek canlı sayfayla farkı büyütür → **dokunulmadı.**
4. **`byte-identical` kalan 9 hit'i** `engine/tools/` altında: `build-atolye.mjs:9`,
   `gen-constants.mjs:3,4,66`, `gen-contract.mjs:4,8,28`, `gen-sitemap.mjs:84`,
   `gen-vocab.mjs:3`, `header-diff.mjs:1`, `render-listing-sheet.mjs:13`,
   `render-garment-flat.mjs:852`, `wasm-baseline.mjs:185`. Hepsi ya alet kodunun
   kendi yorumu ya da üretilen C++ başlığındaki yorum — kullanıcıya gösterilen
   metin DEĞİL, ve golden mekanizması hakkında doğru cümleler. `wasm-baseline.mjs:185`
   bir ÖLÇÜM etiketi (aletin bastığı sayı), duran iddia değil. Bilerek bırakıldı.
   `engine/src/contract.gen.hpp:1` aynı sınıf.
5. **`web/atolye.html`'in 10 `byte-identical` hit'i** paketlenmiş çizim kodunun
   yorumlarından geliyor (`<script>` içi). `build-atolye.mjs` **KOŞTURULMADI**:
   `web/atolye.html`e yazıyor, kart `web/`i yasaklıyor ve `--out` eklemek için
   paketleyici yolunu da taşımak gerekirdi. Sebep bu, unutulduğu için değil.
6. **`engine/tools/README.md`** içinde 1 `byte-identical` var. `README.md`
   kartın yasak listesinde → dokunulmadı.
