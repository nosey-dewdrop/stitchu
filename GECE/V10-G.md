# V10-G — KAPIDAN KAÇAN MTM CÜMLELERİ (rapor)

Kart: `GECE/KART/V10-G-kacan-yalan.md`. Girdi: V10-F "KART DIŞI" md.2/md.4.
Kapı: `engine/tests/landing_truth_check.mjs` (ctest `landing_truth_check`).
Log: `GECE/log/V10-G.kapi.txt`, `GECE/log/V10-G.mutasyon.txt`, `GECE/log/V10-G.mutasyon.sh`.

---

## 1. L1..L5 ÖNCE / SONRA

Basan komut: `node engine/tests/landing_truth_check.mjs`

| | L1 | L2 | L3 | L4 | L5 |
|---|---|---|---|---|---|
| ÖNCE (kart öncesi, 13 kalıplı kapı) | 937 | **0** | 0 | 0 | 4 |
| ÖNCE (20 kalıplı kapı, ONARIMSIZ web/) | 937 | **23** | 0 | 0 | 4 |
| SONRA (20 kalıplı kapı, onarılmış web/) | 937 | **0** | 0 | 0 | 4 |

Orta satır ölçüldü: mutasyon fikstürü HEAD `c72f650`'in web/'ini taşıyor ve yeni
kalıplarla kesilen fikstür tabanı **L2 = 23**'tür (`GECE/log/V10-G.mutasyon.txt`
"0. MUTASYONSUZ FİKSTÜR"). Yani **13 kalıplı kapı 23 MTM hit'ini görmüyordu.**

Hiçbir L sayısı yükselmedi. L1 tam olarak 937'de kaldı (yeni cümleler sayı+birim
dengesini bozmadı). Taban yeniden kesildi:
`node engine/tests/landing_truth_check.mjs --baseline --note="V10-G: kapıdan kaçan MTM kalıpları eklendi"`
→ exit 0, `engine/tests/landing-truth-baseline.json`.
`ctest -R landing_truth_check` → Passed (0.88 s).

---

## 2. BULUNAN HER CÜMLE (dosya:satır · eski · yeni)

Tarama: kartın verdiği grep + kapının kendi 7 yeni kalıbı.
Her hit YARGILANDI; hiçbiri "gerekçeyle bırakıldı" kutusuna düşmedi (bir istisna: §4).

### (a) 6 stil sayfasında FAQ — kartın 1. bilinen yalanı
`web/styles/{halter-neckline, straight-skirt, a-line-skirt, keyhole, gathered-skirt, open-back}.html`
her birinde **2 yerde** (görünür `<p class="faq-a">` + `<script type="application/ld+json">` FAQPage) = 12 yer.

- eski: `The engine drafts it from your body, not fixed sizes. Key numbers: …`
- yeni: `The engine drafts it in eight fixed sizes, EU34 to EU48 (contract/layers/shape-ratios.json). Key numbers: …`

Kaynak zaten V10-F'te düzeltilmişti (`gen-style-pages.mjs:824`); **sayfalar bayattı**,
üreteç yeniden koşturulmamıştı. Metin üretecin BUGÜNKÜ çıktısıyla birebir aynı.

### (b) `web/styles/index.html:91` — kartın 2. bilinen yalanı
- eski: `… geometry computed against your seven measurements, validated on …`
- yeni: `… geometry computed against the eight fixed sizes EU34 to EU48 published in contract/layers/shape-ratios.json, validated on …`
- (üreteçte zaten doğruydu: `gen-style-pages.mjs:963`)

### (c) `web/styles/v-neck.html:92` ve `:95` — kartın 3. bilinen yalanı
- `:92` eski: `The engine cuts it to your own neck measurement, then guards …`
  yeni: `The engine cuts it to the neck girth of the size you pick, then guards …`
- `:95` eski: `Front neck width is your neck measurement × 0.17, capped at …`
  yeni: `Front neck width is the neck girth of the drafted size × 0.17, capped at …`
- (üreteçte zaten doğruydu: `gen-style-pages.mjs:77,79`)

### (d) grep'in ÇIKARDIĞI, kartta ADI GEÇMEYEN dört yalan
Kart §1'in son cümlesi ("çıkan HER hit'i yargıla") gereği:

1. `web/styles/fabric-ties.html` — 7 yer (title ×3, description ×3, `<p class="fact">`)
   - eski: `Fabric tie & sash pattern pieces, sized to you · stitchu`
     yeni: `Fabric tie & sash pattern pieces, sized to the size you print · stitchu`
   - eski: `… back sash reaches from your waist, tie-back, neck bow and cuff ties.`
     yeni: `… back sash length follows that size's waist, tie-back, neck bow and cuff ties.`
   - eski: `The back waist sash is sized from your body: each half is max(300, waist × 0.5 + 250) mm long, …`
     yeni: `The back waist sash is sized from the waist of the size you print: each half is …`
   - ★ Üreteçte de düzeltildi: `gen-style-pages.mjs:374,375,379`. (V10-F md.2 bunu
     "KARARA BAĞLANMADI" bırakmıştı — karar: MTM okunuyor, dürüstleştirildi.)
2. `web/styles/balloon-sleeve.html:92` + `web/styles/index.html:112` (kart özeti)
   - eski: `… the cuff sized from your arm, not a standard.`
     yeni: `… the cuff sized from the biceps of the drafted size, not a standard.`
   - Üreteçte de: `gen-style-pages.mjs:312`.
3. `web/styles/empire-waist.html:92` + `web/styles/index.html:128` (kart özeti)
   - eski: `… the empire bodice is suppressed to your underbust girth, not your waist.`
     yeni: `… the empire bodice is suppressed to the underbust girth of the drafted size, not to its waist.`
   - Üreteçte de: `gen-style-pages.mjs:484`. (V10-F md.2'nin üçüncü açık kalemi.)
4. `web/js/i18n.js:110` (`result.blocked`)
   - eski: `… please re-check your seven measurements. If they are right …`
     yeni: `… please re-check the seven measurements you entered. If they are right …`
   - TR tarafı zaten "girdiğin ölçü" anlamındaydı, dokunulmadı.

### (e) YENİ KAPININ KENDİ YAKALADIĞI — kart listesinde de grep'te de YOKTU
Kalıplar eklendikten sonra kapı **1 hit** daha bastı ve o hit gerçek bir yalandı:

- `web/js/i18n.js:23` (`how.2.body`)
  - eski EN: `The pattern is calculated from your own seven measurements, right here in the browser. Nothing is uploaded.`
    yeni EN: `The pattern is calculated in the eight fixed sizes EU34 to EU48, right here in the browser. Nothing is uploaded.`
  - eski TR: `Kalıp, yedi ölçünden bu tarayıcının içinde hesaplanır. …`
    yeni TR: `Kalıp, EU34 ile EU48 arası sekiz sabit bedende, bu tarayıcının içinde hesaplanır. …`
  - Kartın grep'i bunu kaçırmıştı ("your **own** seven measurements"); kapının
    `your (?:own )?seven measurements` kalıbı yakaladı. **Kapı grep'ten geniş çıktı.**

---

## 3. KAPI NASIL SIKILAŞTIRILDI (7 yeni L2 kalıbı)

`engine/tests/landing_truth_check.mjs` `BANNED` listesi 13 → **20**. Silinen kalıp
YOK, eşik gevşetilmedi, muafiyet eklenmedi.

| kalıp adı | regex |
|---|---|
| `from your body` | `/from your body/gi` |
| `your seven measurements` | `/your (?:own )?seven measurements/gi` |
| `sized from your <x>` | `/sized from your \w+/gi` |
| `not fixed sizes` | `/not fixed sizes/gi` |
| `per body` | `/\bper body\b/gi` |
| `drafted per <non-size>` | `/drafted per (?!size\b)/gi` |
| `your <ölçü> measurement/girth` | `/your (?:own )?(?:neck\|bust\|waist\|hip\|underbust\|arm\|body) (?:measurement\|girth)s?/gi` |

Üçü kartın verdiğinden GENİŞ (`sized from your \w+` "arm"i de yakalar;
`your (?:own )?seven measurements` "own"lı hali de; `your <ölçü>` ailesi kartın
tek tek saydığı v-neck cümlesini sınıf olarak kapatır). Bu bir gevşetme değil,
kart §2'nin listesinin üstüne çıkmadır.

**`drafted per` neden çıplak değil?** `web/index.html:346` `EU34–48 · re-drafted per
size · refusals named` cümlesini taşıyor ve bu cümle motorun GERÇEĞİNİ söylüyor
(8 sabit beden, beden başına yeniden çizim). Çıplak `drafted per` bu DOĞRU cümleyi
kırmızıya çevirirdi — V9-A §2B'nin uyardığı hata. Bu yüzden kalıp `size` jetonunu
dışlar. Muafiyet listesi YOK; dışlama kalıbın kendi tanımının içinde ve
NEGATİF KONTROL ile ölçülüyor (§4).

---

## 4. MUTASYON (§4.5) — 7/7 KIRMIZI, 7/7 GERİ YEŞİL

Koşucu `GECE/log/V10-G.mutasyon.sh`, çıktı `GECE/log/V10-G.mutasyon.txt`.
Fikstür `/tmp/v10g-mutasyon` = `git worktree` @ `c72f650`, **`web/` DIŞINDA**;
kapıya `--dir=/tmp/v10g-mutasyon/web` ile gösterildi. Geri alma `git checkout` ile
DEĞİL, koşu başında alınan bayt kopyasıyla yapıldı. Ana ağacın `web/`'ine mutasyon
yazılmadı (log'un başında ve sonunda `git status --porcelain web/` basılı).

Fikstür tabanı (mutasyonsuz): `L1 937 · L2 23 · L3 0 · L4 0 · L5 4`, exit 0.

| # | kalıp | sokulan yalan | mutasyonlu | geri alınınca |
|---|---|---|---|---|
| 1 | from your body | `Every panel is drafted from your body.` | exit 1 · L2 24/23 AŞTI | exit 0 · 23/23 |
| 2 | your seven measurements | `The draft follows your seven measurements exactly.` | exit 1 · 24/23 | exit 0 |
| 3 | sized from your <x> | `The cuff is sized from your arm.` | exit 1 · 24/23 | exit 0 |
| 4 | not fixed sizes | `We cut to your shape, not fixed sizes.` | exit 1 · 24/23 | exit 0 |
| 5 | per body | `The block is recomputed per body.` | exit 1 · 24/23 | exit 0 |
| 6 | drafted per <non-size> | `Each panel is drafted per customer.` | exit 1 · 24/23 | exit 0 |
| 7 | your <ölçü> measurement/girth | `The neckline is cut to your own neck measurement.` | exit 1 · 24/23 | exit 0 |

**NEGATİF KONTROL (yanlış pozitif avı):** dürüst cümle
`EU34 to EU48, re-drafted per size, refusals named.` sokuldu → **exit 0, YEŞİL**.
Yani `drafted per <non-size>` kalıbı doğru cümleyi cezalandırmıyor.

Koşu sonunda taban gerçek `web/` ağacına geri kesildi → exit 0,
`HÜKÜM: YEŞİL — L1 937/937 · L2 0/0 · L3 0/0 · L4 0/0 · L5 4/4`.

---

## 5. YAPILMAYAN / SINIR

1. **`gen-style-pages.mjs` KOŞTURULMADI.** Sayfalar elle, üretecin bugünkü
   çıktısıyla BİREBİR aynı metinle düzeltildi. Sebep: üreteç `web/` altına toplu
   yazar ve kart `?v` damgasına dokunmayı yasaklıyor. **DOĞRULANMADI:** üreteci
   koşturunca bu 12 sayfanın bayt bayt aynı çıkacağı ölçülmedi; yalnız değiştirilen
   cümlelerin eşitliği grep'le doğrulandı.
2. **Deploy YAPILMADI.** `git add -A` kullanılmadı; dosyalar tek tek staging'e alındı.
3. **`GECE/log/V10-E.png/index-320.png`** oturum başında zaten `M` durumundaydı,
   bu karta ait değil, **staging'e ALINMADI.**
4. L1 (937), L5 (4) borçları bu kartın işi değil, dokunulmadı.
