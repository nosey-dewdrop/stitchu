# F11-D — VİTRİNİN ÖNCE/SONRA HÂLİ (isci-vitrin, sayım tutanağı)

Tarih: 2026-08-22 · Kart: `GECE/KART/F11-D-vitrin.md`
Yazma alanı: yalnız `GECE/`. `docs/`, `web/`, `README.md`'ye tek karakter yazılmadı
(kanıt: bu tutanağın §6 son satırı — `git status --porcelain | grep -v '^?? GECE/'` BOŞ).
Commit atılmadı.

---

## 1. ÖNCE/SONRA FARKI VAR MI? → **YOK. ÖNCE == SONRA, BAYT BAYT.**

| ölçüm | komut | sonuç |
|---|---|---|
| commit var mı | `git log --oneline 962407d..HEAD` | **BOŞ** (0 commit) |
| HEAD nerede | `git rev-parse HEAD` | `962407d28aac466ebfc0b2f29a4e9e1d7c534f03` |
| vitrin diff | `git diff --stat 962407d..HEAD -- docs web README.md` | **BOŞ** |
| çalışma ağacı | `git status --porcelain -- docs web README.md` | **BOŞ** |

Yani gecenin başındaki `docs/` + `web/` + `README.md` ile şu andaki AYNI baytlar.
**F11 brief'inin istediği "önce/sonra ekran görüntüsü" için çekilecek İKİ ayrı
görüntü YOKTUR** — tek bir hâl var. Sahte bir "sonra" üretilmedi.

### 1b. Ama "sonra" bir yerde VAR: `/tmp`'de öksüz duruyor

F9 ve F10 işçileri gerçek çalışma ağacını düzenlemiş, sonra geri almışlar.
Geri alınan hâl `/tmp`'de duruyor ve **commitli hâlden farklı**:

| dosya | commitli (962407d) ile fark |
|---|---|
| `/tmp/f10-index-yedek.html` | `diff` **126 satır** farklı |
| `/tmp/f9-arch-backup.md` | `diff` **42 satır** farklı |

Örnek (ölçüldü, `diff <(git show 962407d:web/index.html) /tmp/f10-index-yedek.html`):

```
< <title>stitchu · a fixed-size pattern CAD</title>
> <title>stitchu · a photo becomes a pattern, a flat and a sewing guide</title>
```

Yani F10-C'nin "CAD lafını kaldırdım" işi **YAZILDI, ana dala GİRMEDİ**.
Bugün canlıya giden başlıkta `CAD` hâlâ duruyor (`web/index.html:7`).
Bu iki `/tmp` dosyası bir sonraki makine temizliğinde kaybolacak — **DOĞRULANMADI:
başka bir yerde yedeği var mı.**

---

## 2. F9'un "16+36 → 0+0" İDDİASI ANA DALDA GEÇERLİ Mİ? → **HAYIR.**

`GECE/F9.md:57` şunu iddia ediyor: faz öncesi `16 standing-claim | 36 unwitnessed`,
faz sonrası `0 standing-claim | 0 unwitnessed`, EXIT=0.

Diskte ölçüldü:

| dosya | durum |
|---|---|
| `engine/tests/docs_truth_check.sh` | **YOK** (`ls` → No such file or directory) |
| `git ls-files \| grep truth_check` | **BOŞ** — takipli değil |
| `grep -n -i truth engine/CMakeLists.txt` | yalnız `preview_truth_check` (satır 555-560). `docs_truth_check` kaydı **YOK** |

F9 tutanağı kapının `engine/CMakeLists.txt:93-94`'e yazıldığını söylüyor; o satırlarda
bugün böyle bir kayıt yok, dosyanın kendisi de yok.

**HÜKÜM ADAYI: F9'un `0|0`'ı ana dalda GEÇERSİZ.** Ne kapı ne düzeltilmiş docs girdi.

**Bugünkü gerçek sayı:** `docs/` baytları 962407d ile birebir aynı olduğu için,
`GECE/log/F9A.gate.before.txt`'nin **962407d worktree'sinde** ölçtüğü sayı
BUGÜNÜN sayısıdır:

```
docs truth: 16 standing-claim violation(s) | 36 unwitnessed numeric claim(s)
EXIT=1
```

→ **BUGÜN de 16 + 36 = 52 ihlal.** Ölçülemedi değil, ÖLÇÜLDÜ (bayt eşitliği yoluyla).

Bağımsız kaba doğrulama (kapı dosyası olmadığı için KURAL A regex'i elle koşuldu,
kod çiti filtresi YOK → kaba üst/alt sınır, satır sayısı):

| dosya | KURAL A eşleşen satır |
|---|---|
| `README.md` | 3 |
| `docs/ARCHITECTURE.md` | 6 |
| `docs/KATMAN-HARITASI.md` | 0 |
| `docs/SATIS-SARTNAMESI.md` | 4 |
| `docs/loop-engineering.md` | 0 |
| **toplam** | **13 satır** |

13 satır ≠ 16 ihlal değildir; kapı SATIR değil GEÇİŞ sayıyor (aynı satırda iki
duran-iddia olabiliyor — bkz. `F9A.gate.before.txt`'de `docs/SATIS-SARTNAMESI.md:235`
ve `:241` hem KURAL A hem KURAL B ihlaliyle iki kez listeleniyor). İki ölçüm çelişmiyor.

---

## 3. F10'un "18 iddia · DOĞRU 0 · YALAN 1 · KANITSIZ 17" SAYISI BUGÜN GEÇERLİ Mİ? → **EVET, 18/18 SAYFADA DURUYOR.**

`web/index.html` bugün **408 satır**. F10-A'nın 18 satırlık iddia tablosunun
her biri grep'le arandı, hepsi bulundu. `contract/generated-paths.sha256` (61 yol)
içinde `web/index.html` **YOK** → dosya **ELLE YAZILMIŞ**, üreteç çıktısı değil.

| # | iddia (birebir) | bugünkü dosya:satır | hâlâ doğru mu | kanıtlayan test/alet | hüküm adayı |
|---|---|---|---|---|---|
| 1 | `seams match to 0.00 mm, published` | web/index.html:180 (chip) · 284 (paragraf) | ÖLÇÜLMEDİ | `engine/tests/engine_check.cpp` sayıyı üretiyor; sayfaya bağlayan kayıt YOK (`web/landing-claims.json` diskte YOK) | güncelle |
| 2 | `0.00 mm seam match (internal) · 70,200-draft matrix` | web/index.html:285 | ÖLÇÜLMEDİ | YOK (bağlayıcı kayıt) | güncelle |
| 3 | `70,200-draft matrix` | web/index.html:284, 285 | ÖLÇÜLMEDİ | `engine/tests/engine_check.cpp` (CMakeLists:63) | güncelle |
| 4 | `$34–49 a month` (rakip fiyatı) | web/index.html:299 | ÖLÇÜLMEDİ | **YOK** — rakip fiyatını ölçen alet yok | güncelle |
| 5 | `EU34–52 graded run + tech-pack` | web/index.html:179 | ÖLÇÜLMEDİ | `engine/tests/grade_check.cpp` (CMakeLists:266) | güncelle |
| 6 | `EU34 to 52` | web/index.html:316 | ÖLÇÜLMEDİ | `engine/tests/grade_check.cpp` | güncelle |
| 7 | `10 of 10 sizes validator-clean` | web/index.html:316 | ÖLÇÜLMEDİ | `engine/tests/grade_check.cpp:62` | güncelle |
| 8 | `EU34–52 · 10/10 sizes clean · dart topology fixed` | web/index.html:317 | ÖLÇÜLMEDİ | `engine/tests/grade_check.cpp` | güncelle |
| 9 | `0.000 mm DXF parity · opens in Valentina / Seamly2D` | web/index.html:312 | ÖLÇÜLMEDİ | `engine/tests/dxf_check.sh` — gerçek tolerans `dxf-verify.py:25 TOL_MM=1e-4` = 0.0001mm, sayfa "0.000 mm" diyor | güncelle |
| 10 | `pairwise intersection area 0.000000 mm²` | web/index.html:321 | ÖLÇÜLMEDİ | `engine/tests/nest_check.cpp` (CMakeLists:452) | güncelle |
| 11 | `1 unit = 1 mm` | web/index.html:311 | ÖLÇÜLMEDİ | `engine/tests/dxf_check.sh:8` | kal |
| 12 | `+4 to +6 cm jump` | web/index.html:316 | ÖLÇÜLMEDİ | `engine/tests/grade_check.cpp:63` | güncelle |
| 13 | `Draft any garment to your own seven measurements` | web/index.html:193 (ayrıca 211, 290, 300) | ÖLÇÜLMEDİ | `engine/tests/body_volume_check.cpp` — ama o "seven size-chart numbers"; kullanıcı girdisi değil | güncelle |
| 14 | `a real, editable pattern` | web/index.html:191 | ÖLÇÜLMEDİ | **YOK** | güncelle |
| 15 | `all 10 pieces, real output` | web/index.html:236 (figcaption) | ÖLÇÜLMEDİ | **YOK** — figcaption'ı ölçen test yok | güncelle |
| 16 | `Your body measurements are never uploaded anywhere` | web/index.html:295 | ÖLÇÜLMEDİ | **YOK** — ağ analizi yapan test/alet yok | güncelle |
| 17 | `Take the AI away and a full CAD remains` | web/index.html:192 | ÖLÇÜLMEDİ | **YOK**; ayrıca `CAD` kelimesi `CLAUDE.md:20` yasağıyla çelişiyor | güncelle |
| 18 | `Every drawing on this page is real engine output, drafted end-to-end and live` | **web/index.html:212** | **HAYIR — YALAN (bu turda ÖLÇÜLDÜ, §4)** | **YOK** | güncelle veya sil |

**SAYIM: 18 iddia · DOĞRU 0 · YALAN 1 · ÖLÇÜLMEDİ 17 · KANITSIZ (bağlayıcı kayıt/test YOK) 18/18.**
F10-A'nın sayısı bugün aynen geçerli.

`web/landing-claims.json` bugün **DİSKTE YOK** ve takipli değil — F10-B'nin
"bilerek boş bıraktım" dediği dosya da ana dala girmedi. `engine/tests/landing_truth_check.sh`
de **YOK**. Yani sayfayı testlere bağlayan mekanizmanın **tamamı** ana dal dışında.

---

## 4. TEK YALAN — ADIYLA, SATIRIYLA, KANITIYLA

> **`web/index.html:212`**
> `Every drawing on this page is real engine output, drafted end-to-end and live.`
> (TR karşılığı aynı satırda: `Bu sayfadaki her çizim gerçek motor çıktısı, uçtan uca ve canlı çizildi.`)

**Neden yalan — ölçüm, iddia değil:**

`web/index.html` içinde **9 inline `<svg>`** var (satır 165, 171, 221, 237, 243,
254, 260, 266, 272). Bunlar HTML'e gömülü sabit `path d="..."` koordinatları.
Sayfa hiçbir üretecin çıktısı değil (`contract/generated-paths.sha256`'da yok).

Landing çizimlerini üreten alet `engine/tools/gen-landing.js`. Bugün koşuldu
(izole worktree'de, gerçek ağaca yazılmadan), **EXIT=0**, 7 anahtar üretti:
`heroPair, heroTiling, proofSheet, tiling, exBabydoll, exTop, exKnit`.

Bugünkü çıktı sayfadaki baytlarla karşılaştırıldı:

| anahtar | tam eşleşme | ilk `path d` eşleşmesi |
|---|---|---|
| heroPair | HAYIR | HAYIR |
| heroTiling | HAYIR | HAYIR |
| proofSheet | HAYIR | HAYIR |
| tiling | HAYIR | HAYIR |
| exBabydoll | HAYIR | HAYIR |
| exTop | HAYIR | HAYIR |
| exKnit | HAYIR | HAYIR |

**7/7 eşleşmiyor. Tek bir path bile tutmuyor.** Aynı `viewBox`, farklı koordinat:

```
gen-landing.js proofSheet  : <svg viewBox="0 0 300 260" fill="none"><path d="M20.2 15.8C28.8 15.2 35.7 14.3 40.6 14.0C43.5 25.9 ...
web/index.html:237         : <svg viewBox="0 0 300 260" fill="none"><path d="M19.1 18.1C27.3 17.5 33.9 16.5 38.6 16.2C41.8 28.7 ...
```

Yani sayfadaki çizimler **eski bir motor koşusunun dondurulmuş baytları**;
bugünkü motor başka geometri veriyor. "live" (canlı) kelimesi sayfada duruyor,
sayfada duran çizim ise canlı değil. Bu farkı ölçen bir test **YOK** —
`landing_truth_check.sh` diskte olmadığı için hiç kimse bunu bekçilemiyor.

**Damla bunu dışarı söyleyebilir:** *"Landing'de 'bu sayfadaki her çizim gerçek
motor çıktısı, uçtan uca ve canlı' yazıyor; üretecin bugünkü çıktısıyla sayfadaki
9 SVG'nin 7 anahtarının hiçbiri eşleşmiyor, tek path bile tutmuyor."*

---

## 5. SITE-HEALTH — BUGÜNKÜ SAYILAR

Komut: `node engine/tools/site-health.mjs` → `GECE/log/F11D.site-health.txt`

```
checked: 127 pages, 2597 internal refs, 124 sitemap urls, 124 indexable pages
OK  site-health: no dead links, sitemap matches the site, one version.
EXIT=0
```

| ölçüt | sayı |
|---|---|
| kırık iç link | **0** (2597 iç referansta) |
| sitemap 404 | **0** (124 URL) |
| sitemap eksiği | **0** (124 indexlenebilir sayfa = 124 URL) |
| `?v` gerilemesi | **0** ("one version") |

Bu sayılar `GECE/log/F10A.site-health.txt` (gece başı) ile **birebir aynı** —
beklenen, çünkü `web/` baytları değişmedi.

⚠ site-health'in ÖLÇMEDİĞİ şeyler: iddia doğruluğu, `lastmod` tazeliği,
SVG'nin motor çıktısıyla eşleşmesi, dış linkler.

---

## 6. ÜRETEÇ ↔ ÜRETİLMİŞ AYRIMI + ÜRETEÇLER BUGÜN KOŞUYOR MU

Üreteçler gerçek ağaca yazmasın diye `/tmp/f11d-tree` adlı detached worktree'de
koşturuldu, sonra `git worktree remove --force` ile silindi.

| sayfa | üreteç | elle mi / üretilmiş mi | üreteç bugün koşuyor mu |
|---|---|---|---|
| `web/index.html` | **hiçbiri** | **ELLE YAZILMIŞ** (generated-paths.sha256'da yok) | — |
| landing SVG'leri (index.html:165…272) | `engine/tools/gen-landing.js` | çıktı stdout'a JSON basıyor, dosyaya YAZMIYOR → SVG'ler index.html'e **elle yapıştırılmış** | **EXIT=0 koşuyor**, ama çıktısı sayfadakiyle eşleşmiyor (§4) |
| `web/styles/*.html` (23 sayfa + hub) | `engine/tools/gen-style-pages.mjs` | ÜRETİLMİŞ | **EXIT=0** · `generated 23 style pages + hub at ?v=136` · çıktı commitli hâlle **bayt bayt aynı** (git status boş) |
| `web/guide/*` (6 sayfa) | `engine/tools/gen-guide.mjs` | ÜRETİLMİŞ | **EXIT=0** · `wrote 6 pages to web/guide/` · commitli hâlle **aynı** |
| `web/sitemap.xml` + `web/robots.txt` | `engine/tools/gen-sitemap.mjs` | ÜRETİLMİŞ | **EXIT=0** · `124 urls (skipped 3: atolye.html, shop-shift-dress.html, studio.html — noindex)`. robots.txt aynı. **sitemap.xml 124 satırda FARKLI: tek fark `lastmod` `2026-08-17` → bugün.** URL kümesi birebir aynı. |
| `web/collections/index.html` | `engine/tools/gen-collections-page.mjs` | ÜRETİLMİŞ | **KOŞMUYOR — EXIT=1, ENOENT `web/patterns/svg/meta.json`** |
| `web/collection-60s70s.html` | `engine/tools/gen-vintage-page.mjs` | ÜRETİLMİŞ | **KOŞMUYOR — EXIT=1, ENOENT `web/patterns/vintage6070/meta.json`** |
| koleksiyon sayfaları (taste) | `engine/tools/gen-taste-collections.mjs` | ÜRETİLMİŞ | **KOŞMUYOR — EXIT=1, ENOENT `web/patterns/svg/meta.json`** |

**`web/patterns/` dizini gerçek repoda DİSKTE YOK** (`ls web/patterns/` →
No such file or directory) ve `.gitignore`'da da anılmıyor (satır 63/65 başka şeyler).
Sebep koddaki yorumda yazılı: `engine/tools/gen-sitemap.mjs:15` —
`"plus web/patterns/svg/meta.json. af49514 deleted that tree, so the try/catch"`.
`engine/tools/gen-legacy-redirects.mjs:34-36` aynı şeyi söylüyor ve
**"nobody has"** diye devam ediyor (restore eden olmamış).

→ **Sitede yayında olan koleksiyon sayfaları ÖKSÜZ: üretilmiş dosyalar duruyor,
onları üreten 3 üreteç bugün koşmuyor.** Bunu ölçen kapı: `engine/tests/generated_ratchet_check.sh`
diskte VAR — ama koşmadım (kart dışı, **DOĞRULANMADI**: bu 3 ölü üreteci yakalıyor mu).

---

## 7. BU TURDA GÖRDÜĞÜM, SORULMAMIŞ AMA ÖNEMLİ OLAN

**7.1 — Sayfa kendi içinde çelişiyor: EU34–48 vs EU34–52.**
`web/index.html:8` (meta description) ve `:22` (JSON-LD, 3 yerde) hâlâ
`grade across EU34–48` diyor. Gövde metni `:179`, `:316`, `:317` ise `EU34–52`
diyor. Aynı dosyada iki farklı beden aralığı. Google'ın gördüğü (meta+schema)
EU34–48, insanın gördüğü EU34–52. Bunu ölçen test YOK.

**7.2 — `CAD` yasağı 5 yerde ihlal, ölçen kapı yok.**
`web/index.html:7` (title), `:8` (meta), `:12` (og:title), `:20` (twitter:title),
`:22` (JSON-LD ×3), `:176`, `:191`, `:192`. `CLAUDE.md:20`: *"CAD lafı yasak."*
F10-C bunu düzeltmiş ama iş `/tmp/f10-index-yedek.html`'de kaldı, ana dala girmedi (§1b).

**7.3 — sitemap `lastmod` 5 gün bayat.** Commitli `web/sitemap.xml` 124 URL'in
hepsinde `2026-08-17`; üreteç bugün koşunca `2026-08-22` basıyor. site-health
bunu ihlal saymıyor (yalnız URL kümesini karşılaştırıyor). Zararı ölçülmedi —
**DOĞRULANMADI**: Google `lastmod` bayatlığını cezalandırıyor mu.

**7.4 — F10-D'nin "rule D: 16 proof-claims, 15 declared" satırı ana dalda YOK.**
`GECE/log/F10D.gate.after.txt` YEŞİL diyor, ama o kapı dosyası (`landing_truth_check.sh`)
ve `landing-claims.json` diskte yok. Yeşil damga öksüz.

**7.5 — F10-A'nın "landing_truth_check ctest'te 97/97 FAIL" notu bugün geçersiz.**
Kapı ana dala girmediği için bugünkü ctest'te böyle bir test yok. Bugün ctest
koşulmadı (kart dışı) — **DOĞRULANMADI**: bugünkü kırmızı sayısı.

**7.6 — `gen-landing.js` dosyaya hiç yazmıyor.**
`grep -n "writeFile\|index.html\|OUT" engine/tools/gen-landing.js` → **0 eşleşme**.
Yani landing SVG'lerini sayfaya taşıyan otomatik hiçbir yol yok; birileri elle
kopyalıyor. "live" iddiasının mimari olarak imkânsız olmasının sebebi bu.

**7.7 — F9/F10 işçileri gerçek çalışma ağacını düzenlemiş.**
`/tmp/f9-arch-backup.md`, `/tmp/f10-index-yedek.html`, `/tmp/f9-clean` dizini
buna tanık. Geri alma başarılı olmuş (`git status` temiz), ama yöntem riskli:
izole worktree yerine canlı ağaç. Bu turda ben `git worktree` kullandım.

**7.8 — `gece/F1-reddedildi` adlı bir dal duruyor** (`git branch -a`).
İçeriği bu turda incelenmedi — **DOĞRULANMADI**.

## GÖREMEDİĞİM / ERİŞEMEDİĞİM

- **Canlı site** (`stitchu.noseydewdrop.com`) — ağ çağrısı yapılmadı. Repodaki
  `web/` ile canlı baytların aynı olduğu **DOĞRULANMADI**.
- **`web/create.html`, `web/closet.html`, `web/studio.html`** — iddia sayımı
  yapılmadı. F10-A da yapmamış. Orada başka iddialar olabilir.
- **`docs/` içindeki 5 kapsam dosyası dışındaki dokümanlar** — F9'un kapsamı 5
  dosyaydı; `docs/` altında başka dosyalar var ve hiç ölçülmedi.
- **ctest bugünkü kırmızı sayısı** — koşulmadı (kart dışı, süre).
- **`generated_ratchet_check.sh`'ın 3 ölü üreteci yakalayıp yakalamadığı** — koşulmadı.
- **`web/patterns/` tree'sini silen `af49514` commit'i** — incelenmedi; geri
  yüklemenin maliyeti/mümkünlüğü **DOĞRULANMADI**.
