# F11-B — GECENİN İŞİ NEREDE? ADLİ ÖLÇÜM

Ham çıktı: `GECE/log/F11B.kurtarma.txt` (blok numaraları [1]…[37] aşağıda anılıyor).
Bu bir ÖLÇÜM tutanağıdır. Silinen iş "hatırlanıp" yeniden yazılmadı; `GECE/gece.sh`
değiştirilmedi, yama aşağıda yalnız DİFF olarak duruyor.

---

## 0. TEK CÜMLE

`GECE/gece.sh:158` makine kapısını `:164-165` commit'ten ÖNCE koşturuyor; kapı
kırmızı düştüğünde `HEAD` hâlâ `$ONCE` olduğu için `DUR()`'un `:82` `if`'i
**her zaman** yanlıştır ve `:93-94`'teki `git checkout -- .` + `git clean -fdq --
engine contract web knowledge docs vision-student` fazın bütün emeğini
**hiç git nesnesi üretmeden** siler.

---

## 1. KÖK SEBEP — SATIR SATIR (soru 1)

`GECE/gece.sh` içindeki sıra:

| satır | ne |
|---|---|
| 71 | `ONCE=$(git rev-parse HEAD)` |
| 107-136 | ŞEF oturumu — işçiler kod yazar, **kimse commit atmaz** (:126 açıkça yasaklıyor) |
| **158** | `bash GECE/kapi.sh` — kırmızıysa `DUR "KAPI KIRMIZI"` |
| **164-165** | `git add -A` + `git commit` — **kapıdan SONRA** |
| 168, 178, 210 | mutasyon / hakem / katip kapıları — bunlar commit'ten SONRA |

`DUR()` gövdesi (`:82`):

```sh
if [ "$(git rev-parse HEAD)" != "$ONCE" ]; then
    git branch -f "gece/$F-reddedildi" HEAD     # :83  — iş DALA alınır
    ...
    git reset --hard "$ONCE"                     # :89
else
    git checkout -- .                            # :93  — iş SİLİNİR
    git clean -fdq -- engine contract web knowledge docs vision-student   # :94
fi
```

Kapı kırmızısında commit hiç atılmamıştır → `git rev-parse HEAD` = `$ONCE` →
**koşul yanlış → `else` dalı → silme.** Ölçüldü: `GECE/log/F11B.kurtarma.txt`
bloğu **[34]**, bu reponun izole bir klonunda mevcut `:82-95` bloğu aynen
koşturuldu:

```
HEAD != ONCE ?  HAYIR-ELSE-DALINA-GIRER
sahte test: SİLİNDİ
sleeve.cpp diff: 0 satır (geri alındı)
açılan dal: ''   (boş = dal AÇILMADI)
```

**Hüküm:** `git branch -f "gece/$F-reddedildi"` bir **kapı kırmızısında HİÇ
çalışamaz**. Sadece mutasyon (:169), hakem (:179) ve katip kapısı (:211)
kırmızılarında çalışabilir — çünkü onlar commit'ten sonradır.
Bu gecenin iki kırmızısının **ikisi de kapı kırmızısıydı**.

→ §3.1'in *"reddedilen iş `gece/F#-reddedildi` dalına alınır, ana dal temiz
kalır"* sözü **TUTULMUYOR.** Sözün tuttuğu hal (dal + `reset --hard`) yalnız
fazın ikinci yarısındaki kapılar için geçerli; asıl kapı için sözleşme
"iş dalda durur" diyor, kod "iş silinir" yapıyor. `gece/F1-reddedildi` dalının
var olması yanıltıcı: o dal **v2'de** bir commit'ten sonraki reddedişten kaldı
(`git reflog HEAD@{10}` → `commit: F1: ...` sonra `HEAD@{9} reset: moving to b874e87`).

---

## 2. KİM, HANGİ ANDA, NEYİ SİLDİ (soru 2)

`GECE/log/gece.txt` zaman çizelgesi ([7]) + kapı log mtime'ları ([8]):

| saat | faz | dönüş | ağaca ne oldu |
|---|---|---|---|
| 15:20→15:46 | F0 (1) | `return 2` (API hatası, :154) | **DUR() ÇAĞRILMADI** — iş ağaçta kaldı |
| 15:46→15:52 | F0 (2) | `return 2` | iş ağaçta kaldı |
| 15:52→16:26 | F6 | `return 2` | F6'nın işi F0'ınkinin ÜSTÜNE birikti |
| 16:26→16:57 | F9 (1) | `return 2` | birikti |
| 16:57→**17:08:19** | F9 (2) | **KAPI KIRMIZI** → `DUR()` → `:93-94` | **F0 + F6 + F9'un BİRİKMİŞ İŞİ TOPTAN SİLİNDİ** |
| 17:08→17:59 | F10 (1) | `return 2` | F10'un işi birikti |
| 17:59→**18:09:49** | F10 (2) | **KAPI KIRMIZI** → `DUR()` → `:93-94` | **F10'un işi silindi** |

**"Birikmiş iş de silindi" iddiası tahmin değil, F9'un kendi kapı logundan
okunuyor** (`GECE/log/F9.kapi.txt`, [9]) — F9 kapısı 17:08'de ağaçta şunları
GÖRÜYORDU:

- `K2 KIRMIZI -- docs_truth_check ...` → **F9'un** testi ağaçtaydı
- `K2 KIRMIZI -- sleeve_armhole_agree_check ...` → **F6'nın** testi ağaçtaydı
- `K4 KIRMIZI -- ... + // GECE/F6-B.md S3 / GECE/log/F6C.gate.before.txt` →
  **F6'nın `engine/src/sleeve.cpp` düzenlemesi** ağaçtaydı

Ve `/tmp/stitchu-gece/F9.ctest.b-now-F9.txt` ([31b]) `98/98` test sayıyor —
taban `962407d` 96 test ([29c]). Fark tam olarak `docs_truth_check` +
`sleeve_armhole_agree_check`. F10'un ağacı 97 test = 96 + `landing_truth_check`.

**Yani F6 saat 16:26'da bitmiş işini 17:08'e kadar taşıdı ve F9'un kapısı
onu da öldürdü.** F6 kendi kapısına HİÇ girmedi (`return 2` ile çıktı) —
kendi kusuru yüzünden değil, başka bir fazın kırmızısıyla silindi.

**İkinci sonuç:** F9'un K2/K4 kırmızısı da kısmen F6'nın ağaçta duran işinden
geliyor (K4 satırlarının biri F6'nın `sleeve.cpp` YORUM satırı). Yani birikme
sadece işi kaybettirmedi, kapıyı da yanlış faza fatura etti.

---

## 3. NEDEN `GECE/` SAĞ KALDI (soru 3)

`:94`'ün yol listesi: `engine contract web knowledge docs vision-student`.
`GECE/` bu listede **yok**, ve `:93` `git checkout -- .` yalnız İZLENEN
dosyaları geri alır — `GECE/*.md` ve `GECE/log/*` izlenmiyor (`git status`
hepsini `??` ile gösteriyor, [2]).

Sonuç: **tutanaklar yaşıyor, kod öldü.** 19 faz tutanağı + 50 log dosyası
diskte ([ls GECE/]), sıfır satır kod ağaçta ([2] boş).

---

## 4. KURTARILABİLİR Mİ? — KAYNAK KODU İÇİN **HAYIR**, ARTEFAKTLAR İÇİN **KISMEN EVET**

### 4.1 Git tarafı: KURTARILAMAZ (kanıtlı)

| deneme | çıktı | blok |
|---|---|---|
| `find .git/objects -type f -newermt "2026-08-22 15:20" ! -path '*/pack/*'` | **0 dosya** | [20] |
| `git reflog -20` | en yeni giriş `HEAD@{0} 962407d` = 21 Ağu'nun commit'i; bu gece HİÇ commit yok | [17] |
| `git fsck --lost-found --unreachable --dangling` | 60+ unreachable nesne, hepsi ESKİ; `.git/objects` içindeki en yeni loose dosya **20 Ağu 23:09** | [12],[27] |
| `ls .git/lost-found/` | var ama **17 Ağu 06:21** tarihli, bu geceyle ilgisiz | [13] |
| `git stash list` | `stash@{0}: On main: onceki oturum artigi` · `stash@{1}: On main: wip pen revision _engine-full` — İKİSİ DE BU GECEDEN ÖNCE, içleri AÇILMADI | [14] |
| `ls engine/src/*.orig engine/src/*~` | `no matches found` | [15] |

**Kesin kanıt:** işçiler `git add` çağırmadı, `gece.sh` de kapıdan önce
`git add` çağırmıyor. `git clean -fd` izlenmeyen dosyayı nesne veritabanına
YAZMADAN siler. 15:20'den sonra **sıfır** loose git nesnesi oluştu.
Silinen kaynak dosyaların hiçbir baytı git'e girmedi → **git'ten KURTARILAMAZ.**

### 4.2 Kurtarılan artefaktlar (git dışı — kapı kendi çöplüğünde tuttu)

`GECE/kapi.sh:104` kirli ağaçtan `$TMP/b-now-$F` derleme dizini kuruyor ve
`kapi.sh` kendi TMP'sini **silmiyor** (`:105` yalnız `b-once` siliyor). Bu
dizinler diskte duruyor:

| dosya | ne veriyor | blok |
|---|---|---|
| `/tmp/stitchu-gece/b-now-F9/sleeve_armhole_agree_check` (553688 B, 17:04) | **F6'nın düzeltilmiş `sleeve.cpp`'siyle derlenmiş, ÇALIŞAN test ikilisi** | [28],[30] |
| `/tmp/stitchu-gece/b-now-F9/CMakeFiles/sleeve_armhole_agree_check.dir/tests/*.o` + `.o.d` | testin include zinciri (garment/bodice/geometry/sleeve.hpp…) | [29b] |
| `/tmp/stitchu-gece/b-now-F9/Testing/Temporary/LastTest.log` (334675 B) | **98 testin TAM çıktısı**, `docs_truth_check`'inki dahil | [32a] |
| `/tmp/stitchu-gece/b-now-F10/Testing/Temporary/LastTest.log` (325538 B) | `landing_truth_check`'in TAM çıktısı (A/B/C/D kural adları + sayıları) | [32b] |
| `/tmp/stitchu-gece/{F9,F10}.ctest.b-{now,once}-*.txt` | önce/sonra kırmızı setleri (ikisi de 7) | [31] |
| `/tmp/stitchu-gece/b-now-{F9,F10}/CTestTestfile.cmake` | silinen `add_test` satırlarının **CMakeLists.txt satır numaraları**: 93 (`docs_truth_check`), 256 (`sleeve_armhole_agree_check`), 707 (`landing_truth_check`) | [29a] |
| `/private/tmp/F10D.index.orig` (77548 B, 17:53) | **F10-A/B/C'nin düzenlediği `web/index.html`** — bugünkü HEAD sürümüne göre 192 satır fark; `.kit`/`.viz` CSS blokları ve `GECE/F10.md:101`'in ilan ettiği yeni başlık içinde | [36] |

**Bunlar KAYNAK KOD DEĞİLDİR.** İkiliden kaynak çıkmaz: Release derlemesi,
`dwarfdump --debug-info` boş ([29a]). Verdiği şey testin **sözleşmesi**:
izole `/tmp/f11b-scratch` içinde koşturuldu ([30], `EXIT=0`, 103 satır):

```
cells=96
worst H1 = 0.015479 mm   (EU48 Princess Woven Plain)   limit 0.79375 mm
worst H2 = 0.001297      (EU36 Princess Woven Plain)   limit 0.0015
worst H3 = 0.000000      (EU42 Dart Knit Puffed)       limit 0.0200
sleeve_armhole_agree_check OK (0 failures)
```

Yani F6'nın düzeltmesi ölçülmüş olarak **çalışıyordu** ve o ikili bugün hâlâ
koşuyor. Kaybolan şey, o sonucu üreten kaynak.

### 4.3 YANLIŞ ALARM — bunları bu gece SİLMEDİ ([37])

`web/patterns/`, `web/blog/`, `engine/STYLE-PIN` bugün diskte yok ama
`git clean` kurbanı DEĞİL: `af49514` (29 Tem) silmiş, `GECE/F0-C.md:240`
zaten öyle yazıyor; `style_check` `962407d`'de de kırmızıydı (7 devralınan
kırmızının biri). Bunları kayıp listesine YAZMADIM.

### 4.4 KESİN KAYIP LİSTESİ (kaynak, kurtarılamaz)

| yol | fazı | tutanakta tarifi var mı |
|---|---|---|
| `engine/tests/sleeve_armhole_agree_check.cpp` | F6 | kısmen (H1/H2/H3 tanımı `GECE/F6-C.md:60`, limitler ikiliden okundu) |
| `engine/src/sleeve.cpp` düzenlemesi (104-107) | F6 | **EVET, tam** (`GECE/F6-C.md:103-120` önce+sonra kod) |
| `engine/tools/f6-armhole-cap.cpp` | F6 | çıktısı `GECE/log/F6.armhole-cap.json` yaşıyor, kaynağı yok |
| `knowledge/kol-kapak-yedirme-2026-08-22.md` | F6 | hayır |
| `engine/tests/docs_truth_check.sh` | F9 | kısmen (`GECE/F9-A.md:22-44`) |
| `engine/tests/landing_truth_check.sh` | F10 | kısmen (`GECE/F10.md:57-65`) |
| `web/landing-claims.json` | F10 | şema `GECE/F10.md:151`, 16 kaydın içeriği `GECE/F10-A.md` tablosunda |
| `web/index.html` düzenlemeleri | F10 | **/private/tmp/F10D.index.orig ile bayt olarak kurtarıldı** |
| `engine/CMakeLists.txt` 3 × `add_test` | F6/F9/F10 | satır numaraları CTestTestfile'dan kurtarıldı |

---

## 5. FAZ FAZ: TUTANAKTAN NE YENİDEN İNŞA EDİLİR (soru 5)

- **F0** — yeniden inşa için elde olan: `GECE/F0.md` + `F0-A/B/C/D1/D2.md`
  (envanter/ölçüm fazı; ürettiği şey zaten tutanaktı). · eksik olan: **hiçbir
  şey** — F0 kod üretmedi, kayıp yok. F9 kapısının K3'ü F0'ın andığı iki yolu
  (`engine/SPECS-next-vocabulary.md`, `flatten-research/FINDINGS.md`) diskte
  bulamadı, bu ayrı bir bayat-yol kırmızısı.
- **F6** — elde olan: `GECE/F6-C.md:87-120` (kök sebep + ÖNCE/SONRA kodu tam),
  `GECE/F6.md:38-46`, ölçülen limitler `/tmp/stitchu-gece/b-now-F9/sleeve_armhole_agree_check`
  koşusundan (H1 0.79375mm · H2 0.0015 · H3 0.02 · 96 hücre). · eksik olan:
  `sleeve_armhole_agree_check.cpp`'nin gövdesi (96 hücrelik döngünün kurulumu)
  ve `f6-armhole-cap.cpp`. **F6 tek satır tahmin gerektirmeden yeniden
  yazılabilecek tek fazdır** — evet, `capSpreadFrac` kök sebebi tutanakta yazılı.
- **F9** — elde olan: `GECE/F9-A.md:22-44` (dosya adı, chmod, add_test satırı,
  ilk kırmızı çıktısı), `GECE/F9.md:52` faz-ortası çıktısı `13 | 31` (44 ihlal),
  `GECE/log/F9.mutasyon-kanit.txt`, ve yeşil çıktının tam metni LastTest.log'dan
  (`docs truth: 0 standing-claim violation(s) | 0 unwitnessed numeric claim(s)`).
  · eksik olan: gömülü perl'in kendisi (hangi kalıpla "duran iddia" ve "tanıksız
  sayı" arıyordu) — tutanak kuralı anlatıyor, regex'i vermiyor.
- **F10** — elde olan: `GECE/F10-A.md`'nin iddia-iddia tablosu (16 satır, her
  birinin `web/index.html` satır numarası ve kanıtlayan test adıyla),
  `GECE/F10.md:57-65,151,178`, `/private/tmp/F10D.index.orig` (HTML **bayt olarak
  elde**), ve `landing_truth_check`'in A/B/C/D kural çıktısı LastTest.log'dan.
  · eksik olan: `landing_truth_check.sh`'in parse kodu ve `landing-claims.json`
  kaydının tam şeması (`provenBy` alan adı biliniyor, gerisi bilinmiyor).

---

## 6. DÜZELTME ADAYI — ÖLÇÜLDÜ, UYGULANMADI

### 6.1 Reddedilen aday

*"Commit'i kapıdan ÖNCE atıp kapı kırmızısında `git reset --hard $ONCE` yap"* —
**reddedildi, ölçülmedi bile gerekmedi:** `gece.sh:164` `git add -A` GECE/'yi de
alır; `reset --hard` sonra `GECE/log/gece.txt`'yi diskten siler. Bu tam olarak
`:84-90`'daki yorumun anlattığı 21 Ağu kazasıdır (kabuk silinmiş inode'a yazmaya
devam etti). Aynı tuzağa geri dönmek olurdu.

### 6.2 Seçilen aday: `DUR()` içinde, SADECE kod yollarını kapsayan anlık commit

Klonda sahte bir `FTEST` fazıyla koşturuldu ([33] — yeni izlenmeyen test +
`sleeve.cpp` değişikliği + `knowledge/` notu + `GECE/FTEST.md` tutanağı):

```
HEAD == ONCE ?  EVET
main temiz mi:  ''
ağaçta sahte test var mı: YOK-doğru
sleeve.cpp geri alındı mı: 0 satır diff
GECE/FTEST.md tutanak sağ mı: SAĞ
GECE/log/gece.txt sağ mı:     SAĞ (kutuk-canli)
76302cf REDDEDILDI(kapi) FTEST
 engine/src/sleeve.cpp           | 1 +
 engine/tests/sahte_yeni_test.sh | 1 +
 knowledge/sahte-not.md          | 1 +
git checkout gece/FTEST-reddedildi -- engine/tests/sahte_yeni_test.sh
    GERİ GELDİ: #!/bin/sh
```

Yani: ana dal temiz, HEAD kımıldamıyor, tutanaklar ve canlı kütük sağ, iş dalda
duruyor ve tek dosya olarak geri çekilebiliyor.

### 6.3 DİFF (UYGULANMADI — `GECE/gece.sh` bu iş sırasında değiştirilmedi)

```diff
--- a/GECE/gece.sh
+++ b/GECE/gece.sh
@@ -75,22 +75,29 @@
   DUR(){ # <baslik> <log>
     { echo "## $1: $F  ($(date '+%H:%M'))"
       echo "once commit: $ONCE"
       sed -n '1,80p' "$2"
       echo
     } >> GECE/STOP.md
-    # ajanin emegi silinmez, ana daldan ALINIR: reddedilen is kendi dalinda durur
-    if [ "$(git rev-parse HEAD)" != "$ONCE" ]; then
-      git branch -f "gece/$F-reddedildi" HEAD
-      # `git add -A` bu dosyanin KENDI logunu da commit'e almisti; reset --hard
-      # sonra onu siliyor ve gece boyunca yazdigimiz kutuk yok oluyordu
-      # (21 Agu: F1 reddedilince GECE/log/gece.txt diskten silindi, kabuk
-      # silinmis inode'a yazmaya devam etti). Once kenara al, sonra geri koy.
-      cp GECE/log/gece.txt "$TMP_KUTUK" 2>/dev/null
-      git reset --hard "$ONCE" >/dev/null
-      mkdir -p GECE/log; cp "$TMP_KUTUK" GECE/log/gece.txt 2>/dev/null
-      echo "    $F reddedildi -> is 'gece/$F-reddedildi' dalinda, $DAL temiz"
-    else
-      git checkout -- . >/dev/null 2>&1
-      git clean -fdq -- engine contract web knowledge docs vision-student >/dev/null 2>&1
-    fi
+    # 22 Agu (F11-B): kapi (:158) commit'ten (:165) ONCE kosuyor. Kapi
+    # kirmizisinda HEAD hep $ONCE'a esitti, yani asagidaki `if` HIC dogru
+    # olmuyordu ve `git clean` fazin butun emegini -- ustelik onceki
+    # `return 2` fazlarindan BIRIKMIS emegi de -- git nesnesi birakmadan
+    # siliyordu (F9 17:08: F0+F6+F9; F10 18:09). Olculdu: GECE/F11-B.md.
+    # Cozum: temizlemeden ONCE emegi bir anlik commit'e muhurle.
+    # `git add`in yol listesi `git clean`inkiyle AYNI -- GECE/ bilerek disarida:
+    # tutanaklar ve canli kutuk izlenmeyen kalir, reset --hard onlara dokunmaz.
+    local KOD="engine contract web knowledge docs vision-student"
+    if [ -n "$(git status --porcelain -- $KOD)" ] || [ "$(git rev-parse HEAD)" != "$ONCE" ]; then
+      git add -A -- $KOD
+      git commit -q -m "REDDEDILDI($1) $F -- gece.sh anlik kaydi, ana dala girmez" || true
+      git branch -f "gece/$F-reddedildi" HEAD
+      cp GECE/log/gece.txt "$TMP_KUTUK" 2>/dev/null
+      git reset --hard "$ONCE" >/dev/null
+      mkdir -p GECE/log; cp "$TMP_KUTUK" GECE/log/gece.txt 2>/dev/null
+      git clean -fdq -- $KOD >/dev/null 2>&1
+      echo "    $F reddedildi -> is 'gece/$F-reddedildi' dalinda, $DAL temiz"
+      echo "## REDDEDILEN IS DALI: gece/$F-reddedildi ($(git rev-parse --short "$(git rev-parse "gece/$F-reddedildi")"))" >> GECE/STOP.md
+    else
+      echo "    $F: agacta kod degisikligi yok, alinacak emek yok"
+    fi
   }
```

`git add -A -- $KOD` + `git commit` sonrası `git reset --hard "$ONCE"` o
dosyaları çalışma ağacından **kendisi** siliyor ([33]'te ölçüldü); sondaki
`git clean` yalnız artık dizinler için bir emniyet kemeri.

### 6.4 Bu yamanın KAPATMADIĞI şey (dürüstlük payı)

Yama işi kurtarır, **birikmeyi kurtarmaz**. Asıl ikinci kusur:
`gece.sh:154` `return 2` (ŞEF API hatası) yolu `DUR()` çağırmıyor, yani
koşmamış bir fazın ağaçta bıraktığı iş bir sonraki faza taşınıyor ve o fazın
kapısına fatura ediliyor (F6'nın `sleeve.cpp`'si F9'un K4 kırmızısında görünüyor,
[9]). Bunu kapatmak için `:154`'ten önce de aynı mühürleme gerekir —
`DUR "AJAN KOSMADI" ...` çağrısı. Ama o zaman "ajan öldü, bir kez daha dene"
(`:233`) tekrarı işi kaybeder. **Bu bir tasarım kararıdır, ölçümle
çözülmez → DAMLA-KUYRUK.** Ölçtüğüm hâliyle: bu gece iki kaybın DÖRT fazı
vurmasının sebebi tam olarak bu birikme.

---

## 7. BU TURDA GÖRDÜĞÜM, SORULMAMIŞ AMA ÖNEMLİ OLAN

1. **`GECE/kapi.sh` TMP'sini temizlemiyor** (`:105` yalnız `b-once`'ı siliyor).
   Bu bir dağınıklık kusuru — ama bu gece **tek kurtarma vektörü** oydu.
   Temizleseydi F6/F9/F10'dan geriye hiçbir şey kalmazdı. Silmeden önce
   düşünülmeli.
2. **F9'un K4 kırmızısı büyük olasılıkla YANLIŞ POZİTİF.** Yakaladığı iki satır
   ([9]) bir Türkçe YORUM satırı ve bir `// GECE/F6-B.md ...` yorumu; ikisi de
   `kapi.sh:195`'in `tolerance|threshold|EPS|kapi|gate` grep'ine **"kapı"/"gate"
   kelimesi yorumda geçtiği için** takılmış. Eşik sabiti oynatılmamış
   (`GECE/F6-C.md:99-101` hangi sabitlerin AYNI kaldığını tek tek sayıyor).
   K4 kod ile yorumu ayırmıyor. DOĞRULANMADI — kapı mühürlü, ben açmadım.
3. `stash@{1}: wip pen revision _engine-full` bu geceden değil; `stash@{0}`
   Damla'nın kararında. İkisinin de içi AÇILMADI.
4. `git status`'ta ağaç tertemiz görünüyor ([2]) — yani bu gecenin 4 fazlık
   emeğinden repoda **hiçbir iz yok**; tek iz `GECE/` tutanakları ve
   `/tmp/stitchu-gece/`.

## 8. GÖREMEDİĞİM / ERİŞEMEDİĞİM

- `stash@{0}` ve `stash@{1}` içerikleri (yasak, açılmadı).
- `sleeve_armhole_agree_check.cpp`'nin kaynağı — ikili Release, DWARF yok ([29a]).
- `docs_truth_check.sh` / `landing_truth_check.sh` kaynağı: `.sh` testleri
  CMake tarafından build dizinine kopyalanmıyor, mutlak kaynak yoluyla
  çağrılıyor ([29a] `Command: "/Users/.../engine/tests/docs_truth_check.sh"`).
  Yalnız ÇIKTILARI kurtarıldı.
- Bu yamanın 16 saatlik gerçek bir koşuda davranışı: yalnız tek sahte fazda
  ölçüldü ([33]).
