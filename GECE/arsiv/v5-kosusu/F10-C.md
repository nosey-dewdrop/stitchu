# F10-C — web/index.html yeniden yazıldı (22 Ağu)

Dosya: `web/index.html` (408 → 468 satır). Tek Write yerine hedefli düzenlemeler
yapıldı: sayfanın kütlesinin ~%70'i gövdeye gömülü statik SVG'ler ve onları
yeniden basmak çıktı sınırını yakıyordu. SVG'lere DOKUNULMADI (kapı zaten
`<svg>` bloklarını görünür metinden düşürüyor).

## 1. "CAD" — 8 → 0
`grep -c -i "CAD" web/index.html` → **0**.
Temizlenen yerler: `<title>`, `meta description`, `og:title`, `og:description`,
`twitter:title`, `twitter:description`, ld+json (WebSite / Organization /
WebApplication = üç açıklama), gövdedeki "deterministic pattern CAD" cümleleri
(hero lead + "what is it?" maddesi + "under the hood?" maddesi).
Yerine geçen tanım: **"bir foto ya da bir cümle → kalıp + teknik çizim (flat) +
dikiş rehberi"**. Yeni title: `stitchu · a photo becomes a pattern, a flat and a
sewing guide`.

## 2. Sayılar — 12 beyansız sayı vardı, 11'i SİLİNDİ, 1'i BEYAN EDİLDİ
Silinenler (ve yerine gelen sayısız ifade):
- `$34–49 a month` → "sold by subscription"
- `0.00 mm` (kart metni + `.stat` + hero chip) → "seams that must sew together
  are equalised by the kernel ... the build goes red"
- `70,200-draft` → silindi (matris sayısı hiçbir yerde tekrarlanmadı)
- `1 mm` ("1 unit = 1 mm") → "in millimetres"
- `0.000 mm` (DXF paritesi) → "vertex-for-vertex DXF parity"
- `0.000000 mm²` (×2: metin + `.stat`) → "no overlap · shapely-checked"
- `6 cm` ("+4 to +6 cm jump") → "the size chart's own steps"
- `10 pieces` (figcaption "all 10 pieces, real output") → "the full set of panels"
- `EU34–52`, `EU34 to 52` → "the full EU size run"
- `10 of 10 sizes`, `10/10 sizes` → "Every size in the run comes out
  validator-clean"

Beyan edilen tek sayı: **`drafted for EU38`**
→ `web/landing-claims.json` `claims[0]`,
`provenBy: contract/layers/body.EU38.json` (diskte var).
Not alanında sınırı da yazıldı: o dosya EU38'in ne demek olduğunu sabitler,
sayfaya yapıştırılmış SVG'lerin o gövdeden çıktığını KANITLAMAZ.

## 3. Yalan kaldırıldı
Eski: *"Every drawing on this page is real engine output, drafted end-to-end and
live."* SVG'ler HTML'e gömülü statik vektör; "live" yanlıştı.
Yeni cümle (proof bölümü başı): çizimlerin motor tarafından çizilip **statik SVG
olarak yapıştırıldığı**, sayfanın çekirdeği ÇALIŞTIRMADIĞI (onu stüdyo yapıyor)
ve **hangi bedende çizildikleri (EU38)** açıkça yazıyor.
Hero'daki iki küçük kağıdın "real output" başlığı da "engine drawing, pasted in"
oldu; "03 · ... · real layout" → "03 · true-scale, tiled to A4".

## 4. VİZYON bölümleri (4 adet, hepsi `data-vizyon`)
Hepsi kesikli çerçeve + "planned · not built yet" rozetiyle görsel olarak ayrı
(`.viz` / `.vizbadge`, yeni CSS sınıfları; mevcut kurallara dokunulmadı):
1. `#kitchen` sonrası — **"A longer menu, from the same short list."** (aynı
   primitiflerden palto/pantolon/ceket — mutfağın SINIRSIZLIĞI vizyondur)
2. **"Say “put a bow here.”"** — düzenleme katmanı
3. `#fabric` sonrası — **"One dress, two fabrics, two patterns."** — kumaşın
   geometriye girdi olması
4. **"Further out."** — üyelik/hesap, dikenlerin paylaştığı yer, iOS (tek blok)

Kapı B kuralı: 4 `data-vizyon` elemanında **0 ihlal** — hepsi "will"/"planned"
ile konuşuyor, ` is / are / does / exports / runs / publishes / drafts /
matches / already / today` kalıplarının hiçbiri geçmiyor.

## 5. Bugün GERÇEK olan, şimdiki zamanla kalan yeni bölümler
- `#kitchen` — "A small kitchen, a long menu.": motor elbise değil PARÇA tutuyor
  (panel · pens · dikiş · kol kapağı · tela · manşet · kemer · çentik ·
  grainline), sayfadaki dört elbise dört program değil dört diziliş.
- `#fabric` — bugünkü hat: `web/js/fabrics.js` (+ `web/data/fabrics.json`)
  bir siluete yakışan / ters düşen kumaşları ADLANDIRIYOR. Sayfada sadece bu
  kadarı şimdiki zamanla söylendi; "kalıp kumaşa göre değişir" vizyona bırakıldı.

## 6. Korunanlar (doğrulandı)
`?v=136` damgası 4 yerde · `css/theme-transitions.css` `css/shared-button.css`
`css/shared-header.css` link'leri aynen · `.betaform` + worker endpoint
(`stitchu-api.damummyphus.workers.dev/api/waitlist`) aynen · `assets/og-card.png`
og+twitter · `#silk` gingham canvas + inline dokuma scripti · header/nav yedi
bağlantı · `@media (max-width:460px)` ×2 ve `(max-width:1240px)` ×1 · renk
değişkenleri ve Didot/Bodoni başlık ailesi. Etiket dengesi: section 12/12,
div 22/22, figure 9/9, p 33/33.

⚠ Kartta "`web/js/landing.js` çağrısı kalacak" yazıyordu; **o çağrı bugünkü
sayfada YOKTU** (`grep -rl "js/landing.js" web/` → hiç). `landing.js`
`#hero-sew` elemanını arıyor, sayfada o eleman da yok. Hero'nun görseli inline
gingham canvas'ı. Ölü bir script eklemek yerine mevcut çalışan canvas korundu.

## 7. Kapılar
| kapı | komut | EXIT |
|---|---|---|
| landing truth | `bash engine/tests/landing_truth_check.sh` | **0** (`A=0 B=0 C=0`, HUKUM: YESIL) |
| site health | `node engine/tools/site-health.mjs` | **0** (127 sayfa, 2599 iç referans, 124 sitemap URL, ölü link 0) |

Loglar: `GECE/log/F10C.gate.after.txt` · `GECE/log/F10C.site-health.txt`

`web/index.html` `contract/generated-paths.sha256` içinde DEĞİL (kontrol edildi),
yani elle yazılması meşru.
