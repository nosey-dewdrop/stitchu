# F10-D — kural D: kanıt-iddiası beyansız olamaz

## Kusur
`engine/tests/landing_truth_check.sh` sadece RAKAM arıyordu. F10-C sayfadan sayıları
sildi, cümleleri bıraktı; bazıları sayısız haliyle daha iddialı oldu ve kapı üçünü de
geçirdi:

| silinen sayı | yerine kalan cümle |
|---|---|
| `0.000000 mm²` | "it reports no overlap at all" |
| `10 of 10 sizes` | "Every size in the run comes out validator-clean" |
| `0.000 mm DXF parity` | "vertex-for-vertex DXF parity" |

Kapı belirsizliği ödüllendiriyordu.

## Kural D
`engine/tests/landing_truth_check.sh` (başlık satır 26-40, kod satır 171-213).
Görünür metin cümlelere bölünür (`.!?` ve sayfanın rozet ayıracı `·`). Bir cümlede
şu kalıplardan biri geçerse — `validator-clean` `no (pairwise) overlap` `parity`
`machine-checked` `cross-checked` `equalised` `equalized` `verified` `proof(s)`
`proven` `checked by` `fails the verifier` `every size` `vertex-for-vertex` —
`web/landing-claims.json`'da bir kayıt olmak zorunda; kaydın `claim` metni O CÜMLENİN
İÇİNDE geçecek, AYNI kanıt kelimesini taşıyacak ve `provenBy` diskte var olacak.
Tek bir kayıt bütün paragrafı aklayamaz.

Çıktı: `rule D: N proof-claims on the page, M declared, X violation(s)`, özet satırı
`landing_truth_check: A=..  B=..  C=..  D=..`.

## Kaç ihlalle başladı
**23 ihlal / 23 kanıt-kelimesi vuruşu.** Kanıt: `GECE/log/F10D.gate.before.txt`
(`landing_truth_check: A=0  B=0  C=0  D=23`, `EXIT=1`).
Kural boş değil: aynı sayfa A/B/C'de sıfır ihlalle yeşildi.

## Karar tablosu — 8 YUMUŞATMA, 15 BEYAN

### YUMUŞATILANLAR (kanıtlayan dosya YOK)

| # | eski metin | yeni metin | neden yumuşatıldı |
|---|---|---|---|
| 1 | `a fuller body · same engine, same proof` | `… same engine, same kernel` | O SVG sayfaya elle yapıştırılmış statik bir çizim. Hiçbir dosya o çizimin hangi koşudan geldiğini ölçmüyor; `proof` kelimesinin arkasında hiçbir şey yok. |
| 2 | `so the proportions hold at every size` | `so the shoulders and armholes are drawn for that body instead of stretched from a smaller one` | "oranlar korunur" bir UYUM hükmü. `grade_check.cpp` ve `size-coverage-check.mjs` okundu: ikisi de beden serisinin çizildiğini/kapsandığını ölçüyor, "oran korunuyor" diye bir yargı basmıyor. Yeni cümle sadece MEKANİĞİ söylüyor (yeniden çizim vs ölçekleme). |
| 3 | `Tools in this corner … publish no accuracy proof at all` | `… keep their geometry closed` | Rakip ürünler hakkında bir iddia. Bu depoda başka ürünü ölçen tek dosya yok. |
| 4 | `each one machine-checked against third-party tools` | `machine-checked against third-party tools where one exists` | DXF/nesting/tech-pack'in üçünün de üçüncü-taraf hakemi var (ezdxf, shapely, poppler). **Grading'in yok** — `techpack-verify.py:140-143` monotonluğu KENDİ içinde kontrol ediyor. "each one" 4/4 iddia ediyordu, gerçek 3/4. Kelime BEYAN edildi, kapsam daraltıldı. |
| 5 | `Every size in the run comes out validator-clean` | `Each size gets its own draft rather than a scaled copy … The manifest carries the clean count per run, and prints the misses next to it.` | `techpack-verify.py:134-135` sadece `gradedSizesClean == sayılan temiz` eşitliğini kuruyor; **`clean == total` diye bir şart YOK**. Yani manifest 3/10 temiz de diyebilir ve doğrulayıcı yeşil geçer. "Her beden temiz" kanıtsızdı. |
| 6 | `a tampered value fails the verifier loudly` | (silindi; cümle `cross-checked against the manifest's own reported numbers` ile bitiyor) | `grep -rn -i tamper engine contract scripts` → sadece pip'in kendi kaynağında geçiyor, depoda bir değeri KURCALAYIP doğrulayıcının kırmızıya düştüğünü gösteren tek test yok. Mutasyon kanıtı olmayan bir mutasyon iddiası. |
| 7 | `These are the machine-checked gates: DXF, grading, nesting, tech-pack` | `These are the gates that can go red: …` | #4 ile aynı kök: grading üçüncü-taraf araçla doğrulanmıyor. "kırmızıya düşebilen kapı" dördü için de DOĞRU ve ölçülebilir. |
| 8 | (#3'ün ikinci yarısı olan `no accuracy proof` vuruşu) | — | #3 ile birlikte düştü. |

> Yumuşatmada iddia SİLİNMEDİ, kanıt dilinden çıkarıldı. Türkçe `data-tr` karşılıkları
> da aynı anda güncellendi (sayfa iki dilli; İngilizcesi yumuşayıp Türkçesi iddialı
> kalsa yalan orada dururdu).

### BEYAN EDİLENLER (dosya okundu, ölçtüğü doğrulandı)
Hepsi `web/landing-claims.json`'da, `{"claim","provenBy","note"}` şemasıyla.

| sayfadaki iddia | provenBy | o dosya iddiayı NEREDE basıyor |
|---|---|---|
| `seams that must sew together are equalised` | `engine/tests/walkgate_check.sh` | satır 59-88: 8 bedende walk.py koşar; satır 90-94 tek hüküm-FAIL'de `exit 1` |
| `are equalised by the kernel` | `engine/tests/walkgate_check.sh` | satır 38: yargılanan spec'ler C++ `surface-pattern` ikilisinden TAZE üretiliyor |
| `equalised seams` | `engine/tests/walkgate_check.sh` | aynı kapı (satır 90-94) |
| `checked by a test that can fail` | `engine/tests/walkgate_check.sh` | satır 73-80: walk.py'ın exit kodu ile bastığı FAIL sayısı birbirini tutmazsa kapı yine kırmızı — "düşebilir" iddiasının kendisi mühürlü |
| `stitchu publishes the proof` | `web/benchmark.html` | 22418 bayt, yayında: `27 / 54` gerçek fotoğraf başlığı + "Internal consistency (not a fit proof)" etiketi + eksikler. İddia YAYINLAMA hakkında, dosya da yayının kendisi. |
| `Read the engineering proofs + textbook audit` | `web/benchmark.html` | aynı sayfa: "an audit against two published pattern-cutting textbooks" |
| `stitchu publishes its engineering proofs` | `web/benchmark.html` | aynı yayın |
| `See the Engineering Proofs` | `web/benchmark.html` | linkin açtığı sayfa |
| `machine-checked against third-party tools` | `engine/tests/dxf_check.sh` | satır 47: `"$PY" "$VERIFY" …` ezdxf venv'inde koşar; satır 13: ezdxf yoksa SESSİZ ATLAMAZ, FAIL |
| `measured vertex for vertex against the engine's own millimetres` | `engine/tools/dxf-verify.py` | satır 181-189: her vertex için `dx/dy > TOL_MM` → `return 1`; TOL_MM = 1e-4 (satır 25) |
| `vertex-for-vertex DXF parity` | `engine/tools/dxf-verify.py` | aynı karşılaştırma + satır 126-162 layer tablosu ve `$INSUNITS=4 (mm)` |
| `the overlap is checked by an independent geometry engine` | `engine/tools/nest-verify.py` | satır 21 `from shapely.geometry import Polygon` (kurulu: `engine/.venv-dxf` → shapely 2.1.2, ezdxf 1.4.4, ölçüldü); satır 89 kesişim alanı. ⚠ satır 91: shapely yoksa saf-python'a düşer — not alanında yazılı. |
| `it reports no pairwise overlap at all` | `engine/tools/nest-verify.py` | satır 86-95: her çift için kesişim alanı > 1e-3 mm² ise `assert` düşer |
| `no overlap` | `engine/tools/nest-verify.py` | satır 95 |
| `The weight and efficiency maths are cross-checked` | `engine/tools/techpack-verify.py` | satır 113-114 verim = `pieceAreaMM2/(w*roll)`; satır 121-125 ağırlık = `metre × 1.40 × gsm` |

## Kapılar
| kapı | komut | EXIT | log |
|---|---|---|---|
| landing_truth_check (önce) | `bash engine/tests/landing_truth_check.sh` | **1** (`A=0 B=0 C=0 D=23`) | `GECE/log/F10D.gate.before.txt` |
| landing_truth_check (sonra) | aynı | **0** (`A=0 B=0 C=0 D=0`, `rule D: 16 proof-claims, 15 declared`) | `GECE/log/F10D.gate.after.txt` |
| site-health | `node engine/tools/site-health.mjs` | **0** (127 sayfa, 2599 iç referans, 124 sitemap url) | `GECE/log/F10D.site-health.txt` |
| ctest | `ctest --test-dir engine/build -R landing_truth_check --output-on-failure` | **0** | aşağıda |

```
Test project /Users/damummyphus/damla_projects_2026/stitchu/engine/build
    Start 97: landing_truth_check
1/1 Test #97: landing_truth_check ..............   Passed    0.53 sec

100% tests passed, 0 tests failed out of 1
```

## Kural D boş değil — MUTASYON
`GECE/log/F10D.mutasyon.txt`, üç adım:

1. temiz sayfa → `D=0`, `EXIT=0`
2. `perl -0pi -e 's|</body>|<p>every seam is verified.</p></body>|' web/index.html`
   → `rule D: 17 proof-claims on the page, 15 declared, 1 violation(s)` ·
   `landing_truth_check: A=0 B=0 C=0 D=1` · `REAL_EXIT=1`
3. geri alındı (`revert byte-identical: yes`) → `D=0`, `REAL_EXIT=0`

Kapı 23 → 0 → (mutasyon) 1 → 0 hareket etti; sabit yeşil basan bir kural değil.

## Dokunulmayanlar
- `engine/tests/` altında SADECE `landing_truth_check.sh` değişti.
- `engine/CMakeLists.txt` değişmedi (test zaten kayıtlı, #97).
- `contract/` altına yazılmadı. `web/index.html` `contract/generated-paths.sha256`'da
  DEĞİL (kontrol edildi: manifestteki `index.html` girdileri
  `web/collections/`, `web/guide/`, `web/styles/` altındakiler) — elle düzenlenmesi
  yasaklı bir üretilmiş dosya değil.
- Görsel kimlik: sadece metin düğümleri ve `data-en`/`data-tr` çiftleri düzenlendi.
  `?v=136`, `css/*`, `assets/og-card.png`, `.betaform`, renk/font/düzen kuralları,
  hiçbir `<svg>` kaymadı.
- `data-vizyon` bölümlerine dokunulmadı (kural B hâlâ `4 element, 0 violation`).

## ⚠ Not — çalışma ağacı
`web/index.html` bu vardiya BAŞLARKEN zaten commit'siz değişikliklerle duruyordu
(F10-C'nin metin yazımı; `git diff --stat web/index.html` benim 7 metin düzeltmemden
çok daha büyük bir diff gösteriyor, hero lead ve `.viz` CSS bloğu dahil). Benim
düzenlemelerim onun ÜSTÜNE, sadece metin olarak yazıldı. Commit atılmadı.
