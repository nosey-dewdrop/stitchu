# F0-B — SÖZLÜK ENVANTERİ (ölçüm kartı, 2026-08-22)

Bu dosya ÖLÇER. Hiçbir sözlük düzenlenmedi, birleştirilmedi, terim eklenmedi.
Her hüküm `dosya:satır` ile gelir.

---

## 1. SAYILAR — kaç kelime, kaçı kapalı enum, kaçı sürekli parametre

Sayan komut (tek satır, dosya yazmaz):
`python3 -c "import json; ..."` — aşağıdaki her sayı bu koşudan.

### 1.1 `engine/vocab.json`
| ölçü | sayı | kanıt |
|---|---|---|
| alan (field) | **37** | `engine/vocab.json:3-44` (`fields` objesi) |
| toplam enum DEĞERİ | **132** | aynı, `values[]` dizilerinin toplamı |
| kapalı enum alan | **37 / 37** | her alanda `"enum": "<C++ tip>"` var; `engine/vocab.json:4` … `:43` |
| sürekli (sayısal aralık) parametre | **0** | `type` değerleri yalnız `"string"` (10) ve `"int"` (27); `engine/vocab.json:4` (string) vs `:16` (int). Hiçbir alanda min/max/aralık YOK. |
| eş anlamlı (synonym) | **9** | `engine/vocab.json:10` (4 adet), `:13` (4 adet), `:31` (1 adet) |

**Hüküm: `engine/vocab.json` %100 YEMEK DİLİ.** 37 alanın 37'si kapalı enum,
sürekli parametre sayısı sıfır. Malzeme dili (mm, oran, açı) bu dosyada hiç geçmiyor.

### 1.2 `contract/spec-grammar.json`
| ölçü | sayı | kanıt |
|---|---|---|
| slot | **14** | `contract/spec-grammar.json:11-139` (`slots`) |
| aktif değer | **51** | slot dökümü aşağıda |
| PARK (gramerde YOK) değer | **26** | `:17`, `:30`, `:39`, `:49`, `:59`, `:66`, `:73`, `:83`, `:91`, `:99`, `:110`, `:118`, `:128` |
| kapalı enum slot | **14 / 14** | her slot `values{}` + `PARK[]` ikilisi, sayısal alan yok |
| sürekli parametre | **1** | `contract/spec-grammar.json:97` → `"gatherRatio": [1.8, 3.2]` (shirred.physics.alt) — dosyadaki TEK sayısal aralık |
| geçersiz kombinasyon kuralı | **6** | `:141-148` |
| kumaş sınıfı | **2** | `:150-154` (`woven`, `knit`) |

Slot dökümü (`values` / `PARK`): garment 2/2 (`:12`) · neckline 7/3 (`:20`) ·
shaping 3/0 (`:33`) · sleeve 4/0 (`:42`) · skirt 4/2 (`:52`) · length 2/1 (`:62`) ·
topLength 2/1 (`:69`) · peplum 4/0 (`:76`) · hemRuffle 2/2 (`:86`) · shirred 2/2 (`:94`) ·
closure 5/4 (`:102`) · collar 2/5 (`:113`) · straps 4/4 (`:121`) · detay-pinli 8/0 (`:131`).

**Hüküm: 51 değerin 50'si kapalı enum, 1'i (gatherRatio) sürekli aralık = %1.96.**
Bu dosya da yemek dili; tek malzeme-dili kırıntısı `:97`.

### 1.3 `contract/terms.json`
| ölçü | sayı | kanıt |
|---|---|---|
| terim | **52** | `contract/terms.json:5-129` (`terms[]`) |
| `drawable` | **24** | `:6` … `:53` bloğu |
| `honest` (kayıtlı, ÇİZİLMİYOR) | **28** | `:55` … `:128` bloğu |
| eş anlamlı (synonym) toplamı | **194** | tüm `synonyms[]` |
| kategori | **14** | `category` alanının tekil sayısı |
| `capability` taşıyan | **24** | = drawable sayısı; honest'ların hepsinde `capability: null` |
| sürekli parametre | **0** | dosyada tek sayı yok; `statuses` da 2 elemanlı kapalı enum (`:4`) |

**★ BAYAT SAYI BULUNDU (ölçüldü, düzeltilmedi — kart düzenlemeyi yasaklıyor):**
`contract/gusto-corpus.json:7` şunu iddia ediyor: *"contract/terms.json (51 terim,
29 drawable / 22 honest)"*. Bugünkü dosya **52 terim, 24 drawable, 28 honest**.
Üç sayı da yanlış. (`GECE/F0.md:245`'teki "24 drawable / 28 honest" DOĞRU.)

### 1.4 `vision-student/vocab.py` (kart md.11'in aradığı kelime listesi)
Glob sonucu: `vision-student/` altında kelime listesi taşıyan TEK dosya
`vision-student/vocab.py` (dizin listesi: 11 .py + README + requirements).

| alan | sınıf | kanıt |
|---|---|---|
| neckline | 7 | `vision-student/vocab.py:17` |
| garment | 5 | `:21` |
| sleeveLength | 3 | `:22` |
| skirtStyle | 5 | `:23` |
| **toplam** | **4 alan / 20 sınıf** | `:26-31` (`FIELDS`) |
| sürekli parametre | **0** | dosyada sayısal aralık yok |
| "bilinmiyor" kovası | 6 değer | `:34` `UNCERTAIN_VALUES` |

---

## 2. SEVK EDİLEN HAT HANGİSİNİ OKUYOR — ölü olan hangisi

### `engine/vocab.json` = SEVK EDİLİYOR (tek gerçek sözlük)
- `engine/wasm/bindings.cpp:16` → `#include "../src/vocab.gen.hpp"`
- `engine/src/vocab.gen.hpp:2` → `GENERATED by engine/tools/gen-vocab.mjs from engine/vocab.json — DO NOT EDIT.`
- `engine/tools/gen-vocab.mjs:14` → `readFileSync(join(root,'engine/vocab.json'))`
- Sınırda gerçekten koşuyor: `engine/wasm/bindings.cpp:68-79` (11 string enum
  `parseEnum<...>`), `:134-140` (int enum `parseEnumInt` aralık kontrolü).
- **Ölçüldü: 37 alanın 37'si bindings.cpp'de adıyla geçiyor** (`0` eksik).
- Aynı kaynaktan üretilen diğer iki tüketici: `web/js/vocab.gen.js:1`,
  `backend/vocab.gen.js:1`; backend sınırı `backend/spec-core.js:19`.

### `contract/spec-grammar.json` = SEVK EDİLMİYOR, tek tüketicisi bir TEST
- Tek okuyucu: `engine/compiler/parse.mjs:14`.
- Kullanım noktaları: `parse.mjs:91`, `:101`, `:211`.
- Zincirin tamamı: `parse.mjs` → `engine/compiler/compile.mjs:13`
  (`import {validateSpec} from './parse.mjs'`) → `engine/tests/compile_dialect_check.mjs:17`
  → `engine/CMakeLists.txt:106` (`add_test(NAME compile_dialect_check ...)`).
- `compile.mjs`'in ikinci çağıranı `engine/compiler/gate.mjs:106`; **web/, backend/,
  wasm/ içinde `compiler/parse` ya da `compiler/compile` geçen SIFIR satır**
  (Grep, glob `*.{mjs,js,html,cpp,sh}`, tüm repo: tek isabet `compile_dialect_check.mjs:17`).
- `engine/wasm/bindings.cpp` içinde `grammar` geçen **sıfır satır** (grep).
- Dosyanın kendi damgası bunu zaten söylüyor: `contract/spec-grammar.json:2`
  (*"kelime dağarcığı olarak koşar, kapsam beyanı olarak KOŞMAZ"*).
→ **Hüküm: spec-grammar.json sevk edilen giysi hattında ÖLÜ; yalnız bir ctest'i besliyor.**

### `contract/terms.json` = SEVK EDİLMİYOR, iki alet okuyor
- `engine/tools/benchmark-58.mjs:106` (`terms` dizisini okur; `:99`, `:194`, `:404`).
- `engine/tools/validate-contract.mjs:36` (yanında `:37` vocab.json'u da okur).
- Kapı durumu: `validate-contract.mjs` ctest'te → `engine/CMakeLists.txt:522`
  (`add_test(NAME contract_check ...)`). **`benchmark-58` CMakeLists'te ve
  `.github/` altında GEÇMİYOR** (grep boş) → kapısız alet.
- `engine/wasm/bindings.cpp` içinde `terms` geçen **sıfır satır**.
→ **Hüküm: terms.json çizim hattına değmiyor; bir sözleşme-kapısı + bir kapısız benchmark girdisi.**

### Sürekli parametreler sözlüklerin DIŞINDA
Sevk edilen sınır 9 sürekli sayı alıyor ve **hiçbiri üç sözlüğün hiçbirinde tanımlı değil**:
`engine/wasm/bindings.cpp:123` (`skirtLengthMM`), `:231-233`
(`bust, waist, hip, shoulder, backLength, armLength, neck`), `:234` (`upperBust`).
Bunların bandı yalnız `contract/garment-spec.schema.json:673` (`bust`) ve `:715`
(`skirtLengthMM`) içinde. Yani **malzeme dili repoda var ama SÖZLÜKTE yok** —
sözlükler yemek dilini, şema malzeme dilini tutuyor, ikisi ayrı dosyada.

---

## 3. ÇAKIŞMA — aynı kavram iki ayrı adla

### 3.1 Slot/alan adı çakışması (aynı kavram, farklı ad)
| kavram | `contract/spec-grammar.json` | `engine/vocab.json` |
|---|---|---|
| kol stili | `sleeve` (`:42`) | `sleeveStyle` (`:9`) |
| etek stili | `skirt` (`:52`) | `skirtStyle` (`:12`) |
| etek boyu | `length` (`:62`) | `skirtLength` (`:14`) |
| yaka (parça) | `collar` (`:113`) | `collarType` (+`collarEdge`) (`:18`,`:19`) |
| askı | `straps` (`:121`) | `ruffledStraps` (`:26`) |
| büzgü | `shirred` (`:94`) | `gatherType` + `gatherZone` (`:20`,`:21`) |
| hem fırfırı | `hemRuffle` (`:86`) | `hemFlounce` (`:28`) |
| kapanış | tek slot `closure` (`:102`) | **dört ayrı alan**: `tieClosure` (`:16`), `placketStyle` (`:29`), `exposedZip` (`:37`), `wrapFront` (`:24`) |

Aynı adla geçen 5 slot: `garment`, `neckline`, `shaping`, `peplum`, `topLength`.
Yani 14 slotun **9'u iki sözlükte iki ayrı adla** duruyor.

### 3.2 DEĞER düzeyinde çakışma (aynı şey, iki yazım)
- **`fullCircle` vs `halfCircle`**: `contract/spec-grammar.json:56` değeri `fullCircle`,
  ve kendi `alt_ad` alanında `["circle","full","halfCircle"]` yazıyor;
  `engine/vocab.json:12` ise kanonik adı **`halfCircle`** tutuyor. Motorun kabul ettiği
  tek yazım vocab'ınki (`bindings.cpp:69`); gramerin kanonik adı sınırda REDDEDİLİR.
- **`straight` iki kavramda**: `spec-grammar.json:28` `neckline: straight`
  (= strapless bandeau, `alt_ad: strapless`) **ve** `:45` `sleeve: straight` (= düz set-in)
  **ve** `engine/vocab.json:12` `skirtStyle: straight`, `:34` `hemShape: straight`.
  vocab'ın `neckline` listesinde (`:8`) `straight` **YOK** → gramerin bandeau'su
  WASM sınırından geçemez.
- **`balloon` vs `puff` vs `puffed`**: `spec-grammar.json:46` `sleeve: balloon`;
  `engine/vocab.json:9-10` kanonik `balloon` + synonym `puff`/`bishop`;
  `engine/vocab.json:17` ayrıca `sleeveCap: puffed`; `contract/terms.json:14`
  aynı şeyi `gathered / puffed sleeve head` diye üçüncü bir adla kaydediyor.
- **`cap`**: `spec-grammar.json:47` `sleeve: cap` (bir kol STİLİ) ama
  `engine/vocab.json:17` `sleeveCap: cap` (bir kol BAŞI) — aynı kelime iki farklı eksende.
- **`wrapFront`**: `spec-grammar.json:108` `closure` altında bir DEĞER;
  `engine/vocab.json:24` başlı başına bir ALAN; `:16` `tieClosure` içinde de bir DEĞER.
  Aynı ad üç yerde, iki farklı düzeyde.

### 3.3 PARK ↔ vocab çelişkisi (gramer "yok" diyor, motor sözlüğü tanımlı tutuyor)
Gramerin PARK'ındaki 26 değerin **13'ü `engine/vocab.json`'da geçerli enum değeri**:
`garment: skirt` (`:17` ↔ `vocab:4`) · `neckline: halter, cowl, pussyBow`
(`:30` ↔ `vocab:8`) · `skirt: straight, pleated` (`:59` ↔ `vocab:12`) ·
`length: maxi` (`:66` ↔ `vocab:14`) · `topLength: tunic` (`:73` ↔ `vocab:15`) ·
`shirred: smocked` (`:99` ↔ `vocab:20`) · `collar: stand, shirt` (`:118` ↔ `vocab:18`) ·
`straps: halter` (`:128` ↔ `vocab:26`).

### 3.4 `terms.json` ↔ diğer ikisi: STATÜ çelişkisi
`terms.json` "çizilmiyor" (honest) dediği halde öbür sözlükte tanımlı olanlar:
| terim | terms.json | çelişen kayıt |
|---|---|---|
| `boxy-fit` honest, *"fit descriptor, not construction"* | `:106-108` | `spec-grammar.json:37` `shaping: boxy`, `kanit: "hakem"` |
| `strapless` honest | `:89-91` | `spec-grammar.json:28` `neckline: straight`, `alt_ad: strapless`, `kanit: "hakem"` |
| `box-pleats` honest, *"pleat folding is not drafted"* | `:61-63` | `engine/vocab.json:43` `boxPleat: centerInverted`, `:34` `hemShape: boxPleatHem` |
| `yoke-plain` honest | `:55-57` | `engine/vocab.json:42` `yoke: plain` |
| `lace-up-closure` honest | `:86-88` | `engine/vocab.json:23` `laceUpBack: corset` |
| `bust-cups` honest | `:92-94` | `engine/vocab.json:40` `cupSeam: horizontal, bugra` |
Ters yön: `terms.json:28` `cowl-neckline` **drawable** (`capability: neckline=cowl`) —
ama `spec-grammar.json:30` `cowl`'ü PARK'ta (= çizilemez) tutuyor. Doğrudan zıt hüküm.

**Toplam: 9 alan-adı çakışması + 5 değer-adı çakışması + 13 PARK/vocab çelişkisi
+ 7 terms.json statü çelişkisi.**

---

## 4. `vision-student` KELİME LİSTESİ NEREDEN KOPYALANDI — kanıtlandı

**Kaynak: `backend/worker.js`, `27999b5` (2026-07-16) commit'indeki hali.** Tahmin değil,
dosya karşılaştırması:

1. Dosyanın kendi beyanı: `vision-student/vocab.py:3-5`
   → *"MUST stay identical to the teacher schema in backend/worker.js. neckline (worker.js line ~296)"*
   ve `:19-20` → *"Classes copied verbatim from the teacher schema"*.
2. **O satır numarası tutuyor.** `git show 27999b5:backend/worker.js` → **satır 296**:
   `"neckline": "crew" | "scoop" | "vNeck" | "square" | "boat" | "sweetheart" | "halter" | null`
   `vision-student/vocab.py:17` → `["crew","scoop","vNeck","square","boat","sweetheart","halter"]`
   — **aynı 7 kelime, aynı sıra, null düşürülmüş.**
3. Aynı commit'te `worker.js:295` `garment` = `skirt|dress|top|trousers|other` →
   `vocab.py:21` **aynı sıra**; `worker.js:298` `sleeveLength` → `vocab.py:22` aynı;
   `worker.js:299` `skirtStyle` = `aLine|straight|gathered|halfCircle|pleated` →
   `vocab.py:23` **aynı sıra**.
4. **Kaynağın `engine/vocab.json` OLMADIĞI da kanıtlı** (ayırt edici iki fark):
   - `vocab.py:21` `garment`'ta `trousers`,`other` var; `engine/vocab.json:4` bunları
     TAŞIMIYOR (`["skirt","dress","top"]`). worker.js taşıyor.
   - `vocab.py:23` `skirtStyle`'da `gore` YOK; `engine/vocab.json:12` `gore`'u TAŞIYOR.
     `gore` şemaya `669f14d` (2026-07-19) ile girdi — vocab.py'den 3 gün SONRA.

### ★ Liste BAYAT — ölçüldü
- `vocab.py` doğdu: `27999b5`, **2026-07-16** (`git log --diff-filter=A -- vision-student/vocab.py`).
- `worker.js` `cowl` + `pussyBow` kazandı: `c0f30bd`, **2026-07-17** (`git log -S"pussyBow" -- backend/worker.js`).
- Bugünkü `backend/worker.js:309` neckline = **9 sınıf** (`...|halter|cowl|pussyBow|null`);
  `vision-student/vocab.py:17` hâlâ **7 sınıf**.
- Aynı sapma şemada da: `contract/garment-spec.schema.json:41` visionReading neckline
  = `[crew, scoop, vNeck, square, boat, sweetheart, halter, cowl, pussyBow, null]`,
  `:71` skirtStyle `gore` içeriyor. Öğrenci ikisini de görmüyor.
- **Sonuç: öğrenci CV kafası, öğretmenin 2 yaka sınıfını (`cowl`,`pussyBow`) ve
  1 etek sınıfını (`gore`) hiç öğrenemez.** `vocab.py:52-53` gereği bu etiketler
  "out-of-vocab → uncertain" sayılıp örnek EĞİTİMDEN DÜŞÜRÜLÜR (sessiz veri kaybı,
  gürültü değil). Kaç örnek düştüğü bu kartta ÖLÇÜLMEDİ (etiket dosyaları okunmadı).

---

## ÖZET TABLO

| sözlük | birim | sayı | kapalı enum | sürekli param | sevk ediliyor mu |
|---|---|---|---|---|---|
| `engine/vocab.json` | alan | 37 (132 değer) | 37/37 | **0** | **EVET** — `bindings.cpp:16,68-79,134-140` |
| `contract/spec-grammar.json` | slot | 14 (51 değer + 26 PARK) | 14/14 | **1** (`:97` gatherRatio) | HAYIR — tek tüketici `parse.mjs:14` → ctest `compile_dialect_check` |
| `contract/terms.json` | terim | 52 (24 drawable / 28 honest) | 52/52 | **0** | HAYIR — `benchmark-58.mjs:106` (kapısız) + `validate-contract.mjs:36` (ctest `contract_check`) |
| `vision-student/vocab.py` | alan | 4 (20 sınıf) | 4/4 | **0** | eğitim tarafı; kaynağından 3 sınıf geride |

**Tek cümlelik hüküm:** repoda "malzeme dili" konuşan bir sözlük YOK — 4 dosyanın
205 sözlük değerinin **204'ü kapalı enum**, tek sürekli parametre
`spec-grammar.json:97`'deki `gatherRatio [1.8, 3.2]`, o da sevk edilmeyen dosyada.
Sevk edilen sınırın aldığı 9 sürekli sayı (`bindings.cpp:123,231-234`) hiçbir
sözlükte tanımlı değil, yalnız `garment-spec.schema.json`'da bandı var.

## KALAN İŞ
- `vision-student` etiket dosyalarında kaç örneğin `cowl`/`pussyBow`/`gore` yüzünden
  düştüğü ÖLÇÜLMEDİ (kartın girdi listesinde etiket dosyası yok).
- `contract/garment-spec-v2.json` `operators` sicili bu kartın girdisi değildi;
  spec-grammar↔v2 karşılaştırması yapılmadı.
