# F11-C — damar yüzdesinin yeniden ölçümü + F0'ın iki sayısının ayrıştırılması

DURUM: KAPALI (ölçüm bitti)
Ağaç: `962407d` · `git status --porcelain | grep -v '^?? GECE/'` → **0 satır**
ctest: `GECE/log/F11.ctest.txt` (22 Ağu 18:18) → **93% · 7 kırmızı / 96 test**,
kırmızı isimleri F0'ın tabanıyla **birebir aynı küme**:
`style_check · sizechart_source_check · bugra_bridge_check · contract_check ·
preview_truth_check · figure_check · h10_gate_check`.
→ Bu koşuda ana dala tek satır kod girmedi; damar yüzdesinin kımıldamaması
BEKLENEN sonuçtur. Aşağıda kımıldamadı, yöntem kaymadı.

---

## 1. YÖNTEM — F0-A §3.1'den aynen alındı, gevşetilmedi

PAYDA = **21 kalem** (ANAYASA.md:42-46 + :56 çıpalı; B1..B21). Yeni kalem
icat edilmedi, kalem çıkarılmadı.

Kriter (garment hattı):
- **G1** `engine/vocab.json` `fields` içinde detayı ADIYLA taşıyan eksen+değer
- **G2** o alan `engine/wasm/bindings.cpp` `buildSpec()` içinde okunuyor
- **G3** o değeri çizip geometrisini iddia eden ADLI ctest kapısı var ve YEŞİL

Puan 1.0 = G1+G2+G3 · 0.5 = G1+G2, G3 yok · 0.0 = G1 yok.

Flat kriteri F1+F2 (styles.json anahtarı + `_engine-full.mjs` okuması),
surfacepattern kriteri = kalemin gerektirdiği operatör `contract/garment-spec-v2.json`
içinde `shipped` mi.

---

## 2. ÖLÇÜM — üç hat, F0-A ile karşılaştırma

### 2.1 G1/G2 yeniden ölçüldü
```
python3 -c "import json;v=json.load(open('engine/vocab.json'))['fields'];print(len(v))"
→ 37 eksen
buildSpec() gövdesi (bindings.cpp:109, 53 satır) içinde OKUNMAYAN eksen: []  → 37/37
```
G2 tüm kalemler için sağlanıyor. F0-A ile aynı.

### 2.2 G3 — 26 adlı kapının 26'sı YEŞİL
`GECE/log/F11.ctest.txt` içinde tek tek arandı, hepsi `Passed`:
`tie · fronttie · gather · buttonrow · placket · placket_asym · hemflounce ·
backdetail · ruffle · tiered_ruffle · peplum · compose · cutline · laceupback ·
neckline_ext · sweetheart · collar · halter · strap · sleeve · cap_sleeve ·
cuff · skirtlen · hem · boxpleat · gore` (`_check` ekli).
F0-A 24 kapı saymıştı; ben 26 saydım (aynı liste, F0-A `compose`/`cutline`'ı
tek satırda göstermişti). Hüküm değişmedi.

### 2.3 Garment hattı = **20.0 / 21 = %95.2** — F0-A ile AYNI
Tek 0.0: **B8 dantel/fisto**. Yeniden doğrulandı:
`grep -icE "lace|eyelet|broderie|scallop" engine/vocab.json` → **3 hit**, üçü de
başka şey: `tieClosure` (satır 16, "wrapFront" içindeki `lac` değil — hit
`collarEdge`), `collarEdge::scallop` (satır 19, SADECE yaka kenarı),
`laceUpBack::corset` (satır 23, bağcık — kumaş değil).
**Dantel/fisto motorda ADIYLA yok.** Yukarı yuvarlanmadı.

### 2.4 Flat hattı = **17.0 / 21 = %81.0** — F0-A ile AYNI
`styles.json` / `_engine-full.mjs` hit sayıları (ham grep -ci):
```
button    0/0     placket 0/0    laceUp 0/0   lacing 0/0   eyelet 0/0
halter    0/0     pleat   0/0    boxPleat 0/0
tie      46/20    shirr  56/6    ruffle 13/5  peplum 22/21 princess 29/3
lace     15/8     gore    4/1    strap  57/13 capPuff 10/2 cuffGather 11/1
skirtFull 21/2    corset  1/0  (yalnız `label` metninde — puan vermedi)
```
ADIYLA EKSİK 4: **mini-düğme sırası · lace-up · halter · kutu-pili**.

### 2.5 Surfacepattern hattı = **2.5 / 21 = %11.9** — F0-A ile AYNI
Sicil statüleri makineyle sayıldı (`contract/garment-spec-v2.json`):
```
shipped : bodiceSurface skirtSurface hemSweepCone princessCut waistAnchoredDart
          topAnchoredDart necklineDraft armholeNotch backOpening      (9)
flagged : shoulderSeam                                                (1)
absent  : gatheredOverlayLayer sleeve collarFamily skirtFamily zipperPiece (5)
```
`grep -o '"absent"' … | wc -l` → **6** = 5 operatör + `_statuses` tanım bloğu.
(KOSU.md:41 "gerçek absent sayısı **4**" diyor — **BEŞ**. `zipperPiece` sayılmamış.
Ufak, ama sayı sayıdır; §4'te öneriye eklendi.)

Puanlar: B6 prenses 1.0 (`princessCut` shipped) · B19 A-line 1.0
(`hemSweepCone` shipped) · B15 kolsuz/askılı 0.5 · kalan 18 kalem 0.0.

**B15'in 0.5'inin gerekçesi DÜZELTİLDİ.** F0-A "askı/strap operatörü sicilde
ADIYLA yok" demiş. Ölçüm: `grep -icE strap engine/src/surfacepattern.hpp` → 7,
ama **yedisi de yorum satırı**; `contract/garment-spec-v2.json` içinde tek
`strap` hit'i `topology.shoulder.strapless` (satır 151). Doğru gerekçe kontratın
kendi `requires` kuralından çıkıyor:
`sleeve.none → requires [armholeNotch]` = shipped (kolsuz İFADE EDİLİR) ama
`shoulder.shoulderSeam → requires [shoulderSeam]` = **flagged**, shipped değil
(askılı/omuzlu İFADE EDİLEMEZ). Yarısı geçiyor → 0.5 AYNEN durur, gerekçesi
artık ölçülü.

Yaka şekil ailesi yeniden doğrulandı:
`grep -icE "sweetheart|vNeck|boat|square|halter" engine/src/surfacepattern.hpp` → **0**.
`necklineDraft` shipped ama yaka bir GENİŞLİK+DERİNLİK açıklığı, şekil değil.

### 2.6 ÜÇ SAYI — F0 ile fark YOK

| hat | F0-A (15:26) | F11-C (18:1x) | fark |
|---|---|---|---|
| garment (`bindings.cpp`→`garment.hpp`) | %95.2 (20.0/21) | **%95.2 (20.0/21)** | 0 |
| flat (`engine/flat-engine/`) | %81.0 (17.0/21) | **%81.0 (17.0/21)** | 0 |
| surfacepattern (sicilin "shipped"i) | %11.9 (2.5/21) | **%11.9 (2.5/21)** | 0 |

Yöntem kaymadı, kod girmedi, sayı kımıldamadı. Üçü tutarlı.

---

## 3. ÇELİŞKİNİN ÇÖZÜMÜ — iki sayı, iki AD, iki farklı NİCELEYİCİ

`GECE/F0.md:93` %0 ile `GECE/F0-A.md:241` %95.2 çelişmiyor; **farklı
niceleyici** taşıyorlar. Fark "yetenek vs sevkiyat"tan da keskin:

> **DAMAR-YETENEK (repertuar).** *"EN AZ BİR yapılandırmada motor bu detayı
> ADIYLA çizebiliyor mu?"* — VAR-BİR niceleyicisi.
> Payda 21, kriter G1+G2+G3.
> **garment %95.2 · flat %81.0 · surfacepattern %11.9.**
> Ölçüldüğü yer: `engine/vocab.json` + `engine/wasm/bindings.cpp` + ctest.

> **DAMAR-SEVKİYAT (varsayılan nesne).** *"Alıcının BUGÜN eline geçen
> VARSAYILAN nesnede bu detay VAR mı?"* — TEK-BELİRLİ niceleyici.
> Payda aynı 21.
> **garment %9.5 (2/21) · surfacepattern %0 (0/21).**

**DAMAR-SEVKİYAT garment hattında ilk kez ölçüldü** (F0 bunu yalnız
surfacepattern'de ölçmüştü). Kaynak `engine/src/measurements.hpp:248-320`,
`struct GarmentSpec` üye başlangıç değerleri:
```
garment=Dress · shaping=Dart · neckline=Crew · sleeveStyle=None
skirtStyle=ALine · skirtLength=Midi
ruffleHem=false · keyhole=false · frontPlacket=false · placketStyle=0
tieClosure=0 · collarType=0 · gatherType=0 · backOpening=0 · backSlit=0 …
```
Yorumların kendi dili: *"Off by default → byte-identical"*, *"Opt-in"*.
21 kalemin **19'u varsayılanda KAPALI**. Varsayılanda AÇIK olan iki kalem:
**B15 kolsuz** (`SleeveStyle::None`) ve **B19 A-line** (`SkirtStyle::ALine`).
B6 prenses varsayılanda DEĞİL (`Shaping::Dart`), B9-B12 yakalar varsayılanda
DEĞİL (`Neckline::Crew` damar listesinde yok).
→ **DAMAR-SEVKİYAT (garment, varsayılan) = 2/21 = %9.5.**

**Üçüncü bir sayı da vardı ve o da ayrı bir ad hak ediyor.**
`GECE/KOSU.md:48` devrettiği `%0` satırının parantezindeki *"flat yolu 9/31 =
%29"* **21'lik paydadan gelmiyor** — F0.md §2'nin 6-primitifli ANAYASA üyelik
testinden geliyor (31 stilin kaçı en az İKİ yumuşak primitif taşıyor). Yani
repoda dolaşan üç farklı ölçü var: 21-kalem/YETENEK, 21-kalem/SEVKİYAT ve
6-primitif/ÜYELİK. Üçü de meşru, ama adsız oldukları için tek sayı sanılıyorlar.
Üçüncüsünün adı: **DAMAR-ÜYELİK (stil başına)** = flat 9/31 = %29.

**F0.md'nin %0'ı yanlış DEĞİL, ama yanlış HATTA ölçülmüş.** Üç bacağının
bugünkü durumu:
1. *"Flat SVG ölçeksiz, satılabilir nesne değil"* — AYAKTA (`unitDeclared:false`,
   KOSU.md:44).
2. *"Sevk edilen kalıp strapless"* — bacak **surfacepattern**'e dayanıyordu
   (`sleeve` absent + `shoulderSeam` flagged). HAT VARSAYIMI garment diyor;
   garment hattında `sleeveStyle` gerçek bir eksen ve `sleeve_check` yeşil.
   AMA `h10_gate_check` bu gece de KIRMIZI ve kırmızısının bir bacağı
   *"shoulder-seam 0 dikiş (kapı ≥2)"* (KOSU.md:36). Yani omuz sorunu iki hatta
   da duruyor; hangi hatta hangi sebeple durduğu bu kartta AYRIŞTIRILMADI —
   **DOĞRULANMADI**.
3. `ANAYASA.md:19` *"test yeşil + damar dışı = kötü çıktı"* — hüküm, ölçü değil.

---

## 4. HAT VARSAYIMI (§KOSU.md:19-21) — HÂLÂ GEÇERLİ, ölçüldü

```
grep -c surfacepattern engine/wasm/bindings.cpp   → 0
grep -n "garment.hpp" engine/wasm/bindings.cpp    → 10:#include "../src/garment.hpp"
```
İkisi de KOSU.md'nin yazdığı gibi. Ek olarak zincirin geri kalanı da izlendi:
`EMSCRIPTEN_BINDINGS` 4 fonksiyon veriyor (`draftJSON` `gradeJSON`
`draftRecipeJSON` `dxfRecipeJSON`); `draftJSON` (bindings.cpp:238) →
`buildSpec` → `GarmentDrafter::draft` → `patternJSON`. Web tarafında
`web/js/create.js:9` ve `web/js/studio.js:11` `printPattern`'ı `print.js`'ten
alıyor, `print.js` `mm` boyutlu 1:1 A4 sayfa basıyor (`print.js:145-146,187`).
→ **Alıcının eline geçen basılabilir nesne garment hattından çıkıyor.**
HAT VARSAYIMI geçerli; bu kartta onu çürüten tek ölçüm çıkmadı.

---

## 5. SORULMAMIŞ AMA ÇIKAN BULGULAR (dökülüyor, §5.5)

1. **`web/js/missing.js` dürüstlük katmanı motorun GERİSİNDE olabilir.**
   Dosya alıcıya *"lace-up (kuşgözü+boşluk) çizili değil"*, *"kumaş bağlar /
   fiyonk parça olarak çizili değil"*, *"düğme patı + ilikler çizili değil"*
   diyor (missing.js:30-56). Oysa `engine/src/` içinde `laceupback.cpp`
   `tie.cpp` `buttonrow.cpp` `collar.cpp` `gather.cpp` VAR ve
   `laceupback_check` / `tie_check` / `buttonrow_check` / `collar_check` /
   `gather_check` bu gece YEŞİL. `create.js:849` bu katmanın **vision** yolunda
   koştuğunu söylüyor, `print.js:9` de onu import ediyor. Dolayısıyla vision'dan
   gelen bir spec'te alıcıya *"çizemedim"* denip motorun çizebildiği bir detayın
   düşürülüyor olması MÜMKÜN. **Koşturarak DOĞRULANMADI** — bu kart engine/web
   altında dosya çalıştırmadı. Ölçülmesi gereken tek şey: vision yolu
   `tieClosure`/`laceUpBack`/`buttonRow` eksenlerini set ediyor mu, yoksa
   sadece missing.js'e mi düşüyor.
2. **`skirtStyle` ekseninde `fullCircle` YOK** (`engine/vocab.json:12` →
   aLine, straight, gathered, halfCircle, pleated, gore) ama
   `contract/garment-spec-v2.json` `topology.skirtShape` içinde `fullCircle`
   VAR ve `skirtFamily`'ye (absent) bağlı. Flat tarafında 6 stil "full-circle"
   etiketli. Üç katman üç farklı etek sözlüğü konuşuyor.
3. **KOSU.md:41 `absent` sayısını 4 yazıyor, gerçek 5** (`zipperPiece`
   sayılmamış). `closure.centreBackZip` ve `closure.buttonFront` ikisi de
   `zipperPiece` istiyor → ikisi de İFADE EDİLEMEZ.
4. `GECE/log/F11.ctest.txt` ile `GECE/log/F0v3.ctest.txt` **bayt olarak farklı**
   (aynı boyut, aynı 7 kırmızı) — süre satırları farkı. Yani F11 gerçekten
   yeniden koşmuş, kopyalanmamış.

---

## 6. KOSU.md'YE ÖNERİ — DEĞİŞTİRİLECEK TEK SATIR

**Satır:** `GECE/KOSU.md:48`
**Bugünkü hali:**
```
1. **DAMAR = %0** (kalıp yolu %0 · flat yolu 9/31 = %29, ama flat satılabilir nesne değil)
```
**Önerilen hali:**
```
1. **DAMAR-YETENEK = %95.2 garment · %81.0 flat · %11.9 surfacepattern** (21 kalemlik
   ANAYASA paydası, F0-A §3.1 yöntemi, F11-C'de yeniden ölçüldü — sapma 0) ·
   **DAMAR-SEVKİYAT = %9.5** (aynı payda, VARSAYILAN GarmentSpec: 21 kalemin 19'u
   opt-in None, `engine/src/measurements.hpp:248-320`). Eski tek "%0" sayısı
   surfacepattern hattında ölçülmüştü; HAT VARSAYIMI sevk edilen hattı `garment`
   ilan ettiği için o sayı artık sevkiyatın değil, SEVK EDİLMEYEN kütüphanenin ölçüsü.
```

**Gerekçe (neden bu satır ve neden bu hali):**
- §0.1 "iki doğru bırakılmaz" — ama burada iki doğru DEĞİL, **iki ayrı soru**
  vardı. Çözüm sayılardan birini silmek değil, ikisine AD vermek; adsız kaldıkları
  sürece her faz hangisini devraldığını bilmiyor (F0-A ile F0.md'nin çarpışma
  sebebi tam bu).
- `%0` tek başına devredilirse bir sonraki faz "motor hiçbir damar detayı
  çizemiyor" sanır; bu **ölçümle yanlış** (26 adlı kapı yeşil).
  `%95.2` tek başına devredilirse "damar sorunu çözülmüş" sanır; bu da yanlış
  (varsayılan nesnede 21'in 19'u kapalı, `h10_gate_check` kırmızı).
  İkisi yan yana durmadan hiçbiri doğru okunmuyor.
- `%0` → `%9.5` yükseltmesi bir **gevşetme DEĞİL**: payda aynı 21, kriter
  sertleşti (artık varsayılan yapılandırmadan ölçülüyor, hat varsayımına uygun
  hatta). Yükselen sayı, ölçünün doğru hatta taşınmasından geliyor ve
  taşındığı hat KOSU.md'nin kendi ilan ettiği hat.

**İkinci (isteğe bağlı, aynı satır değil):** `GECE/KOSU.md:41` "gerçek `absent`
sayısı **4**" → **5** (`zipperPiece` eklenir). Ölçüm §2.5'te.
