# F6 — KOL (22 Ağu, v3)

Faz öncesi commit: `962407d` (`GECE/log/F6.before`).
Üç kart yazıldı, ikisi koştu, biri araç izni olmadığı için koşamadı.
Kart seti 1 = PARALEL (F6-A, F6-B; kapalı liste) · kart 2 = SIRALI (F6-C).

---

## 1. NE KURULDU — `sleeve_armhole_agree_check`

Yeni kapı: `engine/tests/sleeve_armhole_agree_check.cpp`
(ctest kaydı `engine/CMakeLists.txt`, `sleeve_check` ile `cap_sleeve_check`
arasına). **96 hücre**: EU34..EU48 × {Dart, Princess} × {Woven, Knit} ×
{Plain, Gathered, Puffed}. Her hücrenin sayısı `printf` ile basılıyor
(RULES 6 — sayı test çıktısında yaşar).

Kapı ÇİZİLMİŞ poligondan ölçer, sayaç değişkeninden değil. Üç hüküm:

| hüküm | ne ölçer | limit | bugünkü en kötü |
|---|---|---|---|
| **H1** | emit edilen gövde parçalarının oyuk kenarı toplamı ↔ `pattern.sleeveArmholeLenMM` | 0.79375 mm | **0.015479 mm** (EU48 Princess Woven) |
| **H2** | Plain: çizilen kapak/oyuk − `capEaseFor(fabric)` | 0.0015 | **0.001297** (EU36 Princess Woven) |
| **H3** | Gathered/Puffed: beyan `capSpreadFrac` ↔ çizilen yay fazlası | 0.02 | **0.000000** (düzeltmeden SONRA) |

Ham çıktılar: `GECE/log/F6C.gate.before.txt` · `GECE/log/F6C.gate.after.txt`.

---

## 2. NE YAKALANDI VE KAPATILDI — H3, 64/64 hücrede kırmızıydı

`GECE/log/F6C.gate.before.txt`, düzeltmeden önce:
- **Gathered**: beyan `0.200000`, çizilen yay fazlası **0.110078 … 0.133395**
  (beyanın %31-45'i kumaşa hiç girmiyordu).
- **Puffed**: beyan `0.450000`, çizilen **0.716370 … 0.790968**
  (beyanın 1.6-1.8 katı).
- En kötü sapma **0.340968** — EU46 Dart Woven Puffed. Exit 1, 64 failure.

**Kök sebep** (`engine/src/sleeve.cpp`, eski 104-107 bloğu): `capSpreadFrac`
bir **GENİŞLİK** kesri olarak uygulanıyordu; oyuğa büzülen fazlalık ise **YAY**.
Puff'ta ayrıca `capHeight += spread` ikinci eksende de büyütüyordu, fazlalığın
beyanın 1.6-1.8 katına çıkmasının sebebi buydu.

**Düzeltme:** `spread` artık Adım 1'in ikili aramasının aynısıyla ÇÖZÜLÜYOR:
`capCurveLength(width+s, capHeight + (puff ? s : 0)) == capCurveLength(width, capHeight) * (1 + capSpreadFrac)`.
Puff'un doğrulanmış "yükselme == yayılma" değişmezi korundu. Hiçbir sabit ya da
tolerans oynatılmadı; `engine/tests/` altındaki mevcut hiçbir dosyaya
dokunulmadı (K6). Sonrası: H3 en kötü **0.000000**, exit 0. H1/H2 sayıları
BİREBİR kımıldamadı (0.015479 / 0.001297) — Plain hattı ve oyuk hattı sabit.

**ctest:** önce 7 kırmızı / 96 (`GECE/log/F6C.ctest.before.txt`), sonra
**aynı 7 ad** / 97 (`GECE/log/F6C.ctest.after.txt`). §0.6 kümesi büyümedi.
Golden: `ctest -R "golden_check|sleeve_check|cap_sleeve_check|recipe_golden_check|recipe_dress_golden_check"`
→ 5/5 Passed. (`engine/golden-diff.py` tek başına koşmuyor, iki CSV argümanı
istiyor; ctest'teki hakem `engine/tests/golden_check.sh`.)

---

## 3. MUTASYON — koşuldu, kapı gerçekten öldü

`GECE/mutasyon.tsv` F6 satırı (4 alan, TAB ayrık, `awk -F'\t'` ile doğrulandı):

```
F6	kol-kapak-arti-5mm	sleeve_armhole_agree_check	sed -i '' 's|...targetCapLength...|... + 5.0;|' engine/src/sleeve.cpp
```

Şef bu komutu **koşturdu**: `engine/src/sleeve.cpp:55` mutasyonlandı →
`ctest -R '^sleeve_armhole_agree_check$'` **exit 8, FAILED**
(`GECE/log/F6.mutasyon-kanit.txt`). Geri alındıktan sonra kapı yine yeşil.
Mutasyon sahte knob değil: 5 mm, ~430 mm'lik oyukta 0.0116'lık ease kayması
demek, H2'nin 0.0015 limitinin 7.7 katı.

---

## 4. ÖLÇÜLEN AMA KAPATILMAYAN — `sleeve_check.cpp:89` TOTOLOJİK, 18/18

`GECE/F6-B.md` (alet: `engine/tools/f6-armhole-cap.cpp`, ham veri
`GECE/log/F6.armhole-cap.json`, 96 + 18 hücre):

`sleeve_check.cpp:89` cap ease'i `bod.armholeLength`e bölüp "1-9% bandında"
diyor. Ama `SleeveBlock::draft` kapağı zaten `armholeLength*(1+capEase)`e ikili
aramayla oturtuyor. Ölçüldü: **testin koştuğu 18 hücrenin 18'inde de** çözücü
hedefine ±0.5 mm'de ulaşıyor (en kötü ıska −0.4567 mm); ölçülen ease bandı
Woven 0.0386-0.0409 / Knit 0.0186-0.0207 — yani **genişliği 0.004** olan bir
bant, testin 0.08 genişliğindeki penceresini asla ısıramaz.

Kartın hipotezi ölçümle DARALDI: biceps tabanı 12/18'de tetikleniyor, ama
tetiklendiğinde de aynı hedefi yükseklik ekseninde çözüyor
(`engine/src/sleeve.cpp:86-94`) — taban totolojiyi kırmıyor. Tek kaçış deliği
`chLo = 20.0` kelepçesi, ölçülen 114 hücrenin hiçbirinde ateşlemiyor.

**Kapatılmadı, çünkü K6 mevcut teste dokunmayı kırmızı sayıyor.** Yeni kapı
onun YANINA kuruldu. Karar satırı DAMLA-KUYRUK'ta.

---

## 5. EK ÖLÇÜM — üst kol çevresi motorda YOK

`grep -rn "armGirth|upperArm|bicepCM|bicepsCM|topArm|arm_circ" engine/src/ contract/`
→ **0 eşleşme**. `BodyMeasurementsSnapshot`'ta uzunluk var (`armLengthCM`),
çevre yok. Yerine `kBicepsBustRatio = 0.30` (`engine/src/constants.gen.hpp:21`),
çekirdekte **5 okuma yeri**: `sleeve.hpp:9` · `bodice.hpp:78` · `sleeve.cpp:53` ·
`bodice.cpp:627` · `validator.cpp:322` ve `:353`.
KOSU.md'nin "gerçek kol ölçüsü (`m.armMM`) hiç kullanılmıyor" satırı bugün
düzeltildi: **`m.armMM` diye bir alan yok**; eksik olan ölçünün kendisi.

---

## 6. HARNESS KUSURU — K2 YAPISAL OLARAK ÖLÜ (bu gecenin en sert bulgusu)

`GECE/kapi.sh:149-168` (K2) fazın eklediği her yeni testin, faz-öncesi
commit'te kırmızı düşmüş olmasını ister:
`grep -qx "$T" GECE/log/$F.red.before`.

Ama `red.before`, `kapi.sh:120-128`'de **`git worktree add --detach $ONCE`**
ile açılan TEMİZ bir ağaçta ölçülüyor. Worktree yalnız TAKİPLİ dosyaları alır.
Fazın yeni test dosyası o ağaçta **yoktur**, dolayısıyla ctest çıktısında adı
hiç geçmez, dolayısıyla `red.before`'da olamaz.

**Şef bunu koşturarak kanıtladı** (22 Ağu):
```
$ touch engine/tests/__f6_probe_check.cpp
$ git ls-files --others --exclude-standard -- 'engine/tests/*'
engine/tests/__f6_probe_check.cpp          <- calisma agacinda VAR
$ ls /tmp/f6-k2-proof/engine/tests/__f6_probe_check.cpp
No such file or directory                   <- before-worktree'de YOK
```

Sonuç: **yeni test dosyası ekleyen hiçbir faz K2'yi geçemez.** K6 mevcut testi
değiştirmeyi de kırmızı saydığı için, hiçbir faz yeni bir kapı KURAMAZ.
Bu F6'yı değil, F2–F10'un hepsini bloke eder.

`GECE/kapi.sh` §K5 gereği dokunulmaz ve mühürlü — şef DEĞİŞTİRMEDİ.
Ölçülmüş çözüm adayı (§0.4): worktree açıldıktan hemen sonra, `kur_ve_kos`
çağrılmadan ÖNCE, çalışma ağacındaki takipsiz `engine/tests/*` dosyaları ve
`engine/CMakeLists.txt` before-ağacına KOPYALANIR. O zaman yeni kapı ESKİ
motora karşı koşar ve K2 tam amaçladığı şeyi ölçer. Bu bir kapı commit'idir,
gerekçesiyle atılır; şef sessizce yapmadı (§0.12).

Not: bu gece F6'nın kapısı bu düzeltme olsaydı K2'yi **geçerdi** — H3 eski
motorda 64/64 kırmızıydı (`GECE/log/F6C.gate.before.txt`).

---

## 7. KOŞMAYAN KART — F6-A (araştırma)

`GECE/KART/F6-A-arastirma.md` yazıldı ve `isci-arastirma` salındı, ama
oturumda **`WebSearch` ve `WebFetch` izin reddiyle döndü** (5 deneme).
Sıfır URL çekildi. İşçi kaynaksız eşik yazmayı reddetti (§0.4 doğru davranış)
ve `knowledge/kol-kapak-yedirme-2026-08-22.md`'yi **toptan DOĞRULANMADI**
etiketiyle, "eşik önerilmiyor" diyerek bıraktı.

Bunun kapıya etkisi ÖLÇÜLDÜ ve sınırlı: H2'nin 0.0015 eşiği literatürden
DEĞİL, bu gecenin kendi ölçümünden geliyor (F6-B'nin en kötüsü 0.00130) ve bu
testin başına yorum olarak yazıldı. H3'ün 0.02'si de bir doktrin değil, bir
TUTARLILIK toleransı — "beyan == çizim". Yani F6'nın hiçbir hükmü uydurulmuş
bir sektör sayısına dayanmıyor.

**Ama §4'ün araştırma kuralı bu gece yerine getirilemedi.** Açık kalan gerçek
soru: `gatheredSpreadFrac = 0.20` ve `puffedSpreadFrac = 0.45` sabitlerinin
KENDİSİ hâlâ ölçülmemiş. F6 "beyan neyse çizim o olsun"u kurdu; "beyanın
doğru sayı olduğunu" kurmadı. Bu ayrım DAMLA-KUYRUK'ta.

---

## 8. AÇIK BIRAKILAN

- `engine/tools/f6-armhole-cap.cpp`'nin Adım 1/2 replikası artık motorun
  gathered/puff yolunu taklit etmiyor (`predSpread = cv.spread * sol.finalWidth`).
  Alet yeniden koşulursa 64 hücrede `replica_bad` yanar. Alet ctest'te DEĞİL,
  kapı kırmızıya düşmüyor; F6-B'nin tutanaklı çıktısını bozmamak için
  dokunulmadı. Sonraki faz ya aleti günceller ya siler.
- Puf kolun ÇİZİMİ görünür şekilde değişti (yay fazlası %79 → %45, yani puf
  daha küçük). Kapı sadece "beyan == çizim" diyor; **görsel doğrulanmadı**,
  PNG üretilmedi (RULES invariant 3 gereği "çizim düzeldi" DENMEZ).
- `sleeve_check.cpp:89` totolojik satırı yerinde duruyor.

---

## 9. FAZ SONU DOĞRULAMASI (şef, 16:2x)

`GECE/log/F6.ctest.final.txt` — çalışma ağacının kendisinde, tam ctest:

```
93% tests passed, 7 tests failed out of 97
Total Test time (real) = 223.02 sec
	  5 - style_check          12 - sizechart_source_check
	 72 - bugra_bridge_check   79 - contract_check
	 83 - preview_truth_check  84 - figure_check
	 90 - h10_gate_check
```

Devralınan küme (`GECE/log/F0v3.red.now`) ile **birebir aynı 7 ad**. §0.6 sağlandı:
kırmızı küme büyümedi, test sayısı 96 → 97 (yeni kapı yeşil eklendi).

⚠ `F6.ctest.final.txt`'in sonundaki `=== kirmizi adlar ===` başlığı BOŞ:
altındaki ikinci ctest turu (yalnız ad süzmek için) şef tarafından
durduruldu, çünkü adlar zaten birinci turun özetinde basılı. Boş başlık
"sıfır kırmızı" DEMEK DEĞİLDİR — kırmızılar yukarıdaki 7 satırdır.
