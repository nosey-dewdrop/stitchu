# V9-B3 — D2'NİN VARLIK SORUSU DİSKTEN GİT'E TAŞINDI (tutanak)

Koşu: 2026-08-25 · kapı `engine/tests/docs_truth_check.mjs` · taban
`engine/tests/docs-truth-baseline.json` · ctest adı `docs_truth_check`.
Commit'ler: `c29701c` (kök onarım) · `128324c` (dizin kuralı probu) · bu tutanak.

**`docs/`, `README.md`, `web/`, `contract/`, `engine/src/`, `engine/tools/`
DEĞİŞTİRİLMEDİ.** Mutasyon temiz worktree'de yapıldı ve geri alındı; kanıt
`GECE/log/V9-B3.kanit.txt` §c3 → `git status --porcelain -- docs/ README.md`
**0 satır**.

---

## 1. KUSUR YENİDEN ÜRETİLDİ VE ÖLÇÜLDÜ

Ham çıktı: `GECE/log/V9-B3.clean-checkout.txt`

```
git worktree add /tmp/v9b3 HEAD          # HEAD = 5885b80
cd /tmp/v9b3 && node engine/tests/docs_truth_check.mjs   → EXIT 1
  yoklanan hedef 568 · VAR 491 · GERÇEK ölü 42 · YENİ 42
```

Aynı commit, çalışma dizininde `VAR 540 · GERÇEK ölü 0 · exit 0`. Fark tek bir
satırdan geliyordu: D2 hedefi `fs.existsSync` ile yokluyordu. Yani mühürlenen
"D2 0" **dokümanın değil TEK BİR ÇALIŞMA DİZİNİNİN özelliğiydi**; temiz
klonda / CI'da kapı YENİ BİR KIRMIZI AD olurdu = RULES md.9 ihlali.

### 42 hedefin sınıflaması (tekil 19) — `git check-ignore -v`, tahmin yok

| sınıf | tekil | kanıt |
|---|---|---|
| gitignore'lı ÜRETİLEN ARTEFAKT | **17** | `engine/.gitignore:1:build/` · `engine/.gitignore:5:dist/` · `.gitignore:94:Logs/` · `.gitignore:3:CLAUDE.md` |
| GERÇEKTEN ÖLÜ | **2** | ne izlenen ağaçta ne gitignore'da |

Ölü ikisi de `docs/H1.0-KAPI.md:54`'ün aynı tablo satırında:
`patterns_real/geometry/geometry-full.json` ve `seamgraph.json`. İkisi de
**diskte var, repoda yok** — `2f748db` ("move stitchu reports into a single
corpus dump, drop the duplicates from the repo") onları repodan çıkardı,
`.gitignore`'a ise hiç girmediler (`git check-ignore` eşleşme vermiyor).
Doküman onları "Birincil kaynak" diye sunuyor; temiz klonda o kaynak YOK.

---

## 2. KÖK ÇÖZÜM (yama değil)

`git ls-files -z` ∪ `git ls-tree -r HEAD --name-only -z` ile **izlenen yol
kümesi** kuruluyor (bugün 2000 yol) ve hedef ONA yoklanıyor. Disk yoklaması
(`fs.existsSync`, `fs.readdirSync`, tüm-ağaç `walk` basename haritası) D2'den
**tamamen kalktı** — glob çözümü ve basename çözümü de izlenen ağaçta koşuyor.
`git ls-files` koşmazsa kapı **exit 3** (kendi arızası), geçiş DEĞİL.

Çözülemeyen hedef üç sınıfa ayrılıyor:

1. **ÜRETİLEN ARTEFAKT** — `git check-ignore` üç çapada (repo kökü, dokümanın
   dizini, `engine/`) eşleşiyorsa. Ölü DEĞİL, kapıyı kırmaz, **sayısı ve tam
   listesi her koşuda basılır**.
   ⚠ Dizin kuralları (`dist/`) yalnız GERÇEK dizinlere uyar ve git dizin
   olup olmadığını DİSKTEN öğrenir — bu, kusurun ikinci yarısıydı: `engine/dist/`
   çalışma dizininde eşleşip temiz checkout'ta eşleşmiyordu (artefakt 24 vs 21,
   ölü 1 vs 4; ölçüldü). Çözüm: hedefin ALTINDAKİ varsayımsal bir yol da
   soruluyor (`<hedef>/.stitchu-ignore-probe`) — bir dizin ignore'luysa
   içindeki her yol ignore'ludur ve bu cevap dosya sisteminden bağımsızdır.
2. **ÇIPASIZ AD** — `/` içermeyen jeton. `print-info.pdf`, `body.yaml`,
   `stitchu_specification.json` bir REPO YOLU İDDİASI değil, üretilen bir
   paketin dosya adı. Eski kapı bunları BÜTÜN DİSKTE arıyordu (V9-A §8'in
   kabul ettiği "VAR sayısını yukarı yanlı yapar" zaafı). Kapsam dışı, ama
   sayısı ve tam listesi basılır.
3. **ÖLÜ** — kalan her şey. KIRMIZI düşer.

**Kapı GEVŞETİLMEDİ, SIKILAŞTI:** disk yoklaması kalktığı için `VAR` çalışma
dizininde **540 → 491**. 49 hedef "diskte bulundu" diye VAR sayılıyordu; artık
adı konmuş sınıflara düşüyorlar.

---

## 3. TABAN — D2 BORCU 0 → 1 (BİLİNÇLİ, GİZLENMEDİ)

`docs/H1.0-KAPI.md → patterns_real/geometry/geometry-full.json` gerçekten ölü
ve bu kart `docs/`'a dokunamıyor. Üç yol vardı:

- docs'u onarmak → **kart YASAKLIYOR**;
- `patterns_real/geometry/`'yi `.gitignore`'a eklemek → kapıyı gevşetmek için
  repo politikasını değiştirmek olurdu, üstelik `patterns_real/` altında 41
  dosya İZLENİYOR (CLAUDE.md'nin kendi "GİZLİLİK ÇELİŞKİSİ" notu). Damla kararı.
- kırmızı bırakmak → `docs_truth_check` yeni bir kırmızı AD olurdu, RULES md.9.

Seçilen: kapının KENDİ mekanizması — `bilinenAcik` kayıtlı borç listesi (Vale
`accept.txt` / woke `.wokeignore` emsali). Borç **adıyla, dosyasıyla ve
hedefiyle** versiyonlanmış dosyada duruyor, gerekçe `_note` alanında.
`seamgraph.json` borç değil, ÇIPASIZ AD sınıfına düştü (§4 md.2'de zaaf olarak
kayıtlı).

**D3 tabanı 0'da KALDI. D1 borcu 0'da KALDI.** Yükselen tek sayı D2 borcu (0→1)
ve gerekçesi yukarıda. Bu, kartın "tabanı yukarı kesme" şartıyla çelişiyor —
çelişki gizlenmiyor, kartın "42'nin hepsi gitignore'lı" varsayımı ÖLÇÜMLE
yanlış çıktığı için ortaya çıktı.

---

## 4. KANIT — ÜÇ ÖLÇÜM

Tam çıktı: `GECE/log/V9-B3.kanit.txt`

| # | ölçüm | komut | sonuç |
|---|---|---|---|
| a | çalışma dizini | `node engine/tests/docs_truth_check.mjs` | **exit 0** · VAR 491 · artefakt 24 · çıpasız 17 · ölü 1 · YENİ 0 |
| b | temiz worktree | `git worktree add /tmp/v9b3 HEAD` + aynı komut | **exit 0** · VAR 491 · artefakt 24 · çıpasız 17 · ölü 1 · YENİ 0 |
| c1 | mutasyon: olmayan iki yol | `docs/ARCHITECTURE.md`'ye backtick referansı | **exit 1** · `FAIL YENI … -> docs/BOYLE-BIR-DOSYA-ASLA-OLMADI.md` + `… -> engine/src/hayalet-modul.cpp` |
| c2 | mutasyon: gitignore'lı yol | `` `Logs/hicbir-zaman-uretilmedi/rapor.txt` `` | **exit 0** · artefakt 24 → **25**, listede adıyla basıldı |
| c3 | geri alma | `git checkout -- docs/ARCHITECTURE.md` | **exit 0** · `git status -- docs/ README.md` 0 satır |
| d | ctest | `ctest --test-dir engine/build -R docs_truth_check` | `1/1 Passed 0.17 sec` |

(a) ile (b) **bit bit aynı D2 sayılarını** veriyor — portatiflik kusurunun
kapandığının ölçüsü budur. Worktree koşu sonunda `git worktree remove` ile
silindi (`git worktree list` doğrulandı).

---

## 5. BİLİNEN ZAAF — HAKEMİN BİLDİRDİĞİ ÜÇ KUSUR (onarılmadı, kayda geçti)

1. **Kaçış grameri TEK JETONLUK.** Bir tırnak, bir "tarih + bağlam", bir
   `does not exist`, ya da rastgele bir `GECE/…` yolu kapıyı yeşil bırakıyor.
   Kaçışın SAYIYLA alakası aranmıyor: D3'ün sağlayıcısı satırda geçen herhangi
   bir `GECE/…` dizesi olabilir, o dizenin o sayıyı üretip üretmediği
   sorulmuyor. D1'in `EXCEPTION_CONTEXT` + `DATE` şartı da aynı: yasaklı iddia
   ile aynı satıra bir tarih ve `ölçüldü` kelimesi koymak muafiyet için yeter.
2. **Kapı kendi fazını denetlemiyor.** Kapsam `docs/**` + `README.md`.
   `GECE/`, `CLAUDE.md`, `HEDEF.md`, `knowledge/`, `contract/`, `engine/*.md`
   KAPSAM DIŞI — yani bu tutanağın kendisi hiçbir kapıdan geçmiyor.
3. **D3'ün "sağlayıcı"sı semantik değil.** Hiçbir şeyi koşturmayan bir dize de
   sağlayıcı sayılıyor. 139 sayısal satırın kaçının sağlayıcısı gerçekten o
   sayıyı BASIYOR **ÖLÇÜLMEDİ**. "D3 0" = "her sayının yanında bir alet adı
   var", "her sayı doğru" DEĞİL.

### Bu koşunun EKLEDİĞİ zaaflar (dürüstlük payı)

4. **ARTEFAKT sınıfı bir kaçış kanalıdır — ölçüldü (§4 c2).** Gitignore'lı bir
   dizinin altına uydurma bir yol yazmak (`Logs/hicbir-zaman-uretilmedi/…`)
   kapıyı yeşil bırakıyor. Bedeli bilerek ödendi: alternatifi, üretilen her
   artefakt referansının temiz checkout'ta kırmızı düşmesiydi. Sayısı ve tam
   listesi her koşuda basıldığı için SAKLI değil, ama DENETLENMİYOR.
5. **ÇIPASIZ AD sınıfı da kaçış kanalı.** Dizinsiz uydurma bir dosya adı
   (`hayalet.cpp`) artık kırmızı düşmüyor; eskiden düşerdi. Bu bir GEVŞETMEDİR,
   17 kalemle birlikte listeleniyor. Kapatmanın yolu `pathlike()`'ın uzantı
   tabanlı yakalamasını daraltmak — bu kartta yapılmadı.
6. **`git ls-files` İNDEKSİ okur, HEAD'i değil.** Sahnelenmiş (staged) ama
   commit'lenmemiş bir dosya kirli çalışma dizininde VAR sayılır. `ls-tree HEAD`
   birleşimi tersini (commit'li ama silinmiş) kapatır, bunu kapatmaz.
7. V9-B/V9-B2'nin diğer zaafları (D2 parmak izinin satır no taşımaması,
   `engine/`-göreli çözümün yanlış pozitifi, `GIT_HISTORY_CITE`'ın hash'i
   doğrulamaması, Cog sınıfı POZİTİF tarafın yokluğu) **AYNEN DURUYOR**.

---

## 6. YAPILAMAYAN

- `docs/H1.0-KAPI.md:54`'ün ölü referansı ONARILMADI (docs'a dokunmak yasak).
  Kalıcı çözüm: ya cümleyi yeniden yazmak, ya `patterns_real/geometry/`'nin
  repo statüsünü Damla'nın kararına bağlamak (izlenecek mi, gitignore mu).
- `seamgraph.json` aynı satırda ve aynı durumda, ama ÇIPASIZ AD olduğu için
  borca yazılmadı — §5 md.5'in canlı örneği.
- `KOSU.md`, `GECE/arsiv/`, diğer kartlar AÇILMADI.
