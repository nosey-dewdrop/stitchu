# V1 — PİN VE KAYNAK TEMİZLİĞİ · TUTANAK (2026-08-24)

Faz amacı: çekirdeğe SIFIR taşıma-kırmızısıyla girmek.
Sonuç: **kırmızı 6 → 4, yeni kırmızı ad 0.** Kalan dördünün hiçbiri mühendislik
kusuru değil — hakem üçünü **kaynak/karar eksiği**, birini **ilan edilmiş bedel**
diye sınıfladı. Çekirdek sıfır kırmızıyla açılmıyor; kalanların dördü de
DAMLA-KUYRUK'ta karar satırı olarak duruyor ve varsayılanlarıyla yürüyor (§3.1).

Kartlar: `GECE/KART/V1-R-arastirma.md` · `V1-B-recete.md` · `V1-D-figure.md` ·
`V1-E-stylepin.md` · `V1-A-golden-mühür.md`
(V1-C kesilmedi: sizechart'ın tek işi R-kartının çıktısıydı, R "dört kolon da
BAĞLANAMAZ" basınca yazacak kod kalmadı — bulgu kuyruğa K-V1C olarak düştü.)

---

## ÖLÇÜLEN (sayı + komut + hash)

### Makine kapısı (§4.1)
| | önce | sonra |
|---|---|---|
| ctest | `94% tests passed, 6 tests failed out of 105` | `96% tests passed, 4 tests failed out of 105` |
| log | `GECE/log/V1.ctest.before.txt` | `GECE/log/V1-A.ctest.after.txt` |

Komut: `ctest --test-dir engine/build --output-on-failure`.
Şef bağımsız yeniden koştu (`/tmp/v1-verify.txt`): aynı 4 ad, exit 8.
105 koşan test, 106'ncı `h10_gate_check` iki koşuda da **Disabled**.
`CMAKE_BUILD_TYPE:STRING=Release` doğrulandı.

### Reçete hattı — `recipe_dress_check` (#71)
`./engine/build/recipe_dress_check recipes/shift-dress-square-spaghetti.json`
- HEAD (faz öncesi): **PASS 115 / FAIL 10**
- sabit/formül senkronu: **116 / 9** (V0-0F'in ölçtüğü adayla birebir aynı)
- `scye` opu: **PASS 125 / FAIL 0, exit 0**

Kök teşhis dört sürükleme, hepsi ölçüldü:
1. `52ae85c` scye derinliği formülü (`bodice.hpp:113-114`).
2. `0cb5d23` omuz dikişini de grade'ledi (`shoulderSeamTargetMM 126` →
   `bustMM*0.0625 + 67.5`, `bodice.hpp:148-149`) — kartın "1.39–3.72 mm omuz ucu
   farkı" gözleminin kaynağı bu.
3. Kalan 9 FAIL'in tamamı TEK komut: `outline[4]` oyuk kübiği. Uç noktalar
   `d=0.0000`, sapmanın tamamı kontrol noktalarında (EU38 ön `cp d=47.1653`).
   Bias binding (61.3 mm) ve `cutLine` nokta sayısı farkı (43↔48) bunun türevi.
4. Sebep: oyuk artık kapalı form değil — üç iç kelepçeli (Aldrich genişlik
   çizgisi, katlanmama, 25° kink) bisection çözücü. JSON DSL'e çözücü yazılamaz.

**Çözüm düzeltme katsayısı DEĞİL, fonksiyon paylaşımı** (`e4516cf`):
`recipe.cpp:975-983` yeni `scye` opu çerçeveyi (from/to/neck/half/innerX/maxInset)
İSİMLENDİRİYOR, eğriyi `BodiceBlock::scyeCurve` → `bodice.cpp:820-821`
`armholeCurveFor` çiziyor — motorun `makePiece`/`makePrincessPieces` yollarının
(`bodice.cpp:508`, `:624`) çağırdığı fonksiyonun ta kendisi. Reçeteden **10
el-yazması kontrol-noktası skaleri silindi** (`cp1x/cp1y/cp2x/cp2y/hollow/stx/
sty/tanReach/slen/stxRaw`).

### Golden mührü — `golden_check` (#3)
`scripts/repin-golden.sh "<52ae85c + Aldrich p.11 etiketi>"`
→ `re-pinned: 23406 -> 23406 lines, md5 d5b5f28b2ef41a776b14699e9220982a`
(eski `fcaa935448b58ef38d108ffeda49e2df`). Satır sayısı eşit = parça eklenmedi.

İçerik diff (eski pin vs taze `golden_dump`; ekleme/silme 0):
**9651 satır (%41.23) YERİNDE değişti, max 62.7764 mm, medyan 5.6000 mm.**

| parça | n | max mm | medyan mm |
|---|---|---|---|
| Bodice Front | 1020 | 62.7764 | 6.1286 |
| Top Front | 615 | 62.7764 | 6.1286 |
| Balloon Sleeve | 2520 | 49.7051 | 23.7478 |
| Sleeve | 1995 | 49.7051 | 23.2728 |
| Bodice Back | 2120 | 47.4355 | 0.0022 |
| Top Back | 840 | 47.4355 | 3.2499 |
| fabric (metraj) | 41 | 0.1000 | 0.1000 |
| Skirt Front/Back/Panel | 500 | **0.0001** | 0.0001 |

**Etek kımıldamadı.** Sapma gövde + kol hattında.
Hakem bu tablonun **on satırının hepsini bağımsız yeniden hesapladı**, birebir tuttu;
ayrıca `golden_dump` çıktısının pinle **bayt-özdeş** olduğunu `cmp` ile doğruladı
(elle dokunulmamış). `contract/generated-paths.sha256` `d28297e4…` tuttu.

### Bağımsız tanık — ÖNCE UYDURULDU, SONRA ÖLÇÜLDÜ
Defterin ilk hâli ve `e8b7f19` gövdesi *"sloper_check 52ae85c'den ÖNCE
KIRMIZIYDI, 189.0 → 210.0"* diyordu. **UYDURMA.** Hakem çürüttü, işçi bağımsız
doğruladı (`GECE/log/V1-F.sloper-tanik.txt`), `05156a1`'de düzeltildi:

| ağaç | çıktı | hüküm |
|---|---|---|
| `c3d4359` (=`52ae85c^`) | `scye depth below nape  engine 204.4  aldrich 215.0  err -10.6 mm` | `[PASS]`, exit 0 |
| HEAD | `engine 210.0  aldrich 215.0  err -5.0 mm` | `[PASS]`, exit 0 |

Hareket kırmızıdan yeşile DEĞİL, **yeşilden daha-yeşile**. `189.0` iki sondanın
hiçbirinde yok. Tanık yeni pinin **YÖNÜNÜ** destekler; "kırmızıyı yeşile çevirdi"
sınıfında kanıt DEĞİLDİR ve defter artık bunu böyle yazıyor. Çürüyen cümle
sessizce silinmedi, çürüdüğü tarihiyle görünür bırakıldı.

Aynı sondada defterin hiç anmadığı ikinci hareket: `shoulder seam (drawn)`
err +3.5 → **+0.0 mm**, `shoulder tip drop` +0.7 → −0.6 mm. Kalan 18 kalem
iki ağaçta birebir aynı.

### `style_check` (#8) — kapı gerçek kapı oldu
Kartın varsayılan işi ("0 pinde FAIL yaz") HEAD'de zaten yapılmıştı
(`style_check.mjs:30-42`). **Asıl delik başkaydı:** hüküm listesi pin
DİZİNİNDEN okunuyordu — yani kapı "elinde ne varsa onu" doğruluyordu.
31 stilin 1'i pinlense kapı YEŞİL basar, kalan 30 korumasız kalırdı.
`repin-style.sh --status` bunu ilan ediyordu, kodda karşılığı yoktu
(RULES §1: kodda zorlanmayan garanti yoktur).

`513b175`: hüküm listesi artık `engine/flat-engine/styles.json` — beklenen pin
kümesi sözlüğün HER anahtarı. Eksik pin · sahipsiz pin · sözlüğün
okunamaması · byte-diff = FAIL. "0 pin" özel bir dal değil, 31 eksik pinin
sonucu. Onay mekanizmasına (`repin-style.sh:125-136` tty şartı + elle yazılan
cümle) DOKUNULMADI.

**Mutasyon kanıtı (§4.5)** — `GECE/log/V1-E.mutasyon.txt`, dokuz koşu:

| # | mutasyon | çıktı | exit |
|---|---|---|---|
| M0 | pin dizini yok (gerçek hâl) | `kapsam: 0/31` · `FAIL` | 1 |
| M1 | dizin + 1 dosya | `kapsam: 1/31` · `KISMİ PİN YEŞİL SAYILMAZ` | 1 |
| M2 | 31/31 sahte pin | 31 × `PASS byte-identical` | 0 |
| M3 | 1 pine 1 bayt | `FAIL: wrap_dress differs` (13939/13940) | 1 |
| M3b | bayt geri alındı | `PASS byte-identical` | 0 |
| M4 | 1 pin silindi | `kapsam: 30/31` · `FAIL` | 1 |
| M5 | sahipsiz `.svg` | `FAIL: 1 sahipsiz pin` | 1 |
| M6/M7 | dizin boşaltıldı / geri alındı | M0 ile birebir | 1 |
| **M8** | **eski test, M1'in aynı dizininde** | **`PASS` → exit 0 = YEŞİL** | **0** |

M8 = boş test kapısının (§4.2) karşılığı: aynı dizinde ESKİ kapı yeşil, YENİ kapı
kırmızı. Hakem M8'i **sıfırdan yeniden üretti ve doğruladı**. Sahte pinler
commit'lenmedi; `git status --porcelain engine/STYLE-PIN` = 0 satır.

### `figure_check` (#90) — "kardeş yok" İDDİA DEĞİL, KANIT
Pinin ölçtüğü büyüklüğün tanımı kaynaktan çıkarıldı (`figure-lint.mjs:54-69`):
`waist` = f∈[0.10,0.82]'deki en dar yarı genişlik, `bust` = f∈[0.09,waist.f)'deki
EN BÜYÜK yarı genişlik. **Payda sabit anatomik nokta değil:** 15 `top:shoulder`
stilin 15'inde payda f=0.09 / y=64.9 = **OMUZ**; iki `top:band` stilde gerçek
**BÜST** hattında. Yani metrik omuzluda bel/omuz, straplezde bel/büst ölçüyor.

| ölçüt | eşleşen pinli stil |
|---|---|
| aynı gövde (`top:band`) | yalnız `drawstring_babydoll` |
| aynı etek (daire) | 4 stil, **dördü de** `top:shoulder` |
| **ikisi birden** | **0 — kesişim BOŞ** |

Tek aday babydoll: pin 0.856, bandeau 0.872, fark **0.016 = drift_tolerans
0.02'nin %80'i**. Geçerdi ama bütçenin %80'i doğduğu gün yanardı; dosyanın kendi
emsali (F-N devri) toleransın çeyreğinde. Bileşenler ayrı ayrı ~%5 ayrı
(büst %5.9, bel %4.3) — oranların yakınlığı ortak sebepten değil, iki bağımsız
%5 farkın bölmede götürmesinden.
**HÜKÜM: kardeş YOK. Pin yazılmadı, 0.872 pinlenmedi, bant genişletilmedi.**

### Araştırma (R-kartı, §5.1) — `GECE/V1-R.md`
İki birincil tablo diske çekilip `pdftotext -layout` ile ölçüldü: Burda *Damen
Maßtabellen* (117 satır, 3 seri) ve Aldrich 6. baskı s.11. Aldrich büst serisi
80..122 çizelgenin `bustCM`'iyle **10/10 birebir** — kolon eşlemesi tahmin değil,
kilitli. **Dört kolon da BAĞLANAMAZ** (sayılar K-V1C'de).
Mühür taşıma pratiği: 8 şart damıtıldı (Bache · Jest snapshot docs · Chromium
`web_test_expectations.md`); **stitchu 8'in 6'sını sağlıyor**, eksik ikisi
Ş6 (izleme kaydı/bug-id alanı yok) ve Ş7 (`repin-golden.sh:18-28` mührü hep
TÜMDEN basar, tek-giysi yenileme yolu yok). Ş5'te stitchu Chromium'dan **daha sert**.

---

## KAPANAN / AÇILAN KIRMIZI (ad ad)

**KAPANDI (2):** `golden_check` · `recipe_dress_check`
**AÇILAN YENİ AD: 0.** `diff` yalnız iki silme, sıfır ekleme (hakem doğruladı).

**AÇIK KALAN (4)** — hakemin sınıf hükmü (`GECE/V1-SINIF.md`, `527d41b`):
altı kırmızının **sıfırı GERİLEME**; ikisi bilinçli bayat pin (bu gece kapandı),
dördü kaynak/karar eksiği.

| kırmızı | sınıf | 4.7: kök + ölçülmüş aday | hangi faz |
|---|---|---|---|
| `style_check` | karar eksiği | pin ÖLÇÜM değil KARAR; işçi üretirse regen-vs-regen. Aday koşulabilir: `repin-style.sh <style> "<beyan>"`. Maliyet ölçüldü: render 0.05 sn, darboğaz **31 kez GÖZ**. Kapsam 0/31 | **TEK KAPI** (K-V0B) |
| `sizechart_source_check` | kaynak eksiği | 4 kolon 40 sayı, dört ada da BAĞLANAMAZ (ölçüldü); 5 yayın paywall/403. Aday: kolonları ÇİZELGEDEN AT — motor `backLengthCM`'i `52ae85c`'de zaten terk etti | **V5** (K-V1C) |
| `contract_check` | ilan edilmiş bedel | `git ls-files patterns_real` = 41 takipli telifli dosya. Aday V0'da ÖLÇÜLDÜ: `git rm -r --cached` → `contract validation GREEN, exit=0`. Geçmiş izi ayrı iş (filter-repo) | **Damla** (K-V0A) |
| `figure_check` | kaynak eksiği | Eksik pin değil, **sınıfı olmayan stil**. İki ölçülmüş aday: (A) `top:band` için 4. bant `[0.84,0.90]` · (B) siluet düzeltmesi → oran 0.820, bandın İÇİNE düşer, pin bile gerekmez | **V4** (K-V1B) |

**Geçici doğan iki kırmızı, aynı turda kapatıldı** (test dosyasına dokunulmadan):
`generated_ratchet_check` (manifest sha'sı kaymadı → testin kendi ilan ettiği
`--accept` yolu, `contract/generated-paths.sha256` 57 yol) ve `bundle_fresh_check`
(`built` alanını `git log -1 -- <artifact>`'tan okuyor, commit'le kapandı).

---

## KAPI HÜKÜMLERİ (§4, altı alt kapı) — `GECE/KAPI.md`

| alt kapı | hüküm | dayanak |
|---|---|---|
| 4.1 makine | GEÇTİ | 6→4, yeni ad 0; şef bağımsız yeniden koştu |
| 4.2 boş test | GEÇTİ | M8: aynı dizinde eski kapı yeşil, yeni kapı kırmızı — hakem sıfırdan üretti |
| 4.3 kanıt | GEÇTİ | 8 dosya yolunun 8'i `test -f` ile doğrulandı; ctest bağımsız yeniden koşuldu |
| 4.4 hakem | önce **KALDI**, sonra GEÇTİ | uydurma tanık cümlesi `05156a1`'de düzeltilince |
| 4.5 mutasyon | GEÇTİ | dokuz koşu, `GECE/log/V1-E.mutasyon.txt` |
| 4.6 tolerans | GEÇTİ | hiçbir eşik değişmedi; kımıldayan tek şey mührün kendisi |
| kâtip | GEÇTİ | `8bcd674` — 9 bayat/çürük docs cümlesi düzeltildi |

**Mühür usulü satırı KALDI ve öyle kalıyor:** `repin-golden.sh:33-38` şart 2
(Damla'nın açık onayı) sağlanmadı. Script kendi cümlesiyle *"the pin is not valid
without these"* diyor. Gizlenmedi — defterde iki yerde ilan edili, kuyrukta K-V1A.
§4 son paragrafı gereği koşu varsayılanla devam eder.

★ **Hakemin sorulmamış ama önemli bulgusu:** `scye` opunun getirdiği 8 yeni JSON
katsayısının **sekizi de** `recipe_dress_check.cpp:124-154` K0 sabit-parite
mandalında YOK; dosyanın kendi başlığındaki *"every recipe const == its motor
counterpart"* (`:3`) bugün YANLIŞ. Ayrıca 6 sabit hâlâ latch'leniyor ama reçetede
ölü, ve oyuk eğrisi için "canlı ikinci-yol kanıtı" artık totolojik (iki taraf aynı
fonksiyonu çağırıyor). Mevcut testi değiştirme yasağı yüzünden dokunulmadı →
**V2/V3'e kart.**

---

## SONRAKİ FAZA DEVİR (üç sayı)

1. **KIRMIZI = 4** — `style_check` · `sizechart_source_check` · `contract_check` ·
   `figure_check`. V0'ın 6'sından 2 düştü, yeni ad **0**. Dördü de sınıf (c)
   kaynak/karar eksiği; **sıfır gerileme**, sıfır mühendislik kusuru.
   Log: `GECE/log/V1.ctest.before.txt` → `GECE/log/V1-A.ctest.after.txt`
2. **GOLDEN MÜHRÜ TAŞINDI = 9651 satır / %41.23 / max 62.7764 mm** — gövde+kol
   hattında, etek 0.0001 mm. Bundan sonraki her faz bu YENİ mühre karşı ölçülür;
   `engine/golden-reference.csv` md5 `d5b5f28b2ef41a776b14699e9220982a`.
   Onay K-V1A'da bekliyor, varsayılan (A) yürüdü.
3. **STYLE PİN KAPSAMI = 0/31** — kapı artık kısmi pinlemeyi yeşil saymıyor
   (`513b175`). Kapatan tek yol Damla'nın gözü; ölçülen maliyet render 0.05 sn ×
   31, darboğaz makine değil GÖZ.

## TABAN BANTLARI — DEĞİŞMEDİ (§4.1)
V0'ın ölçtüğü bantlara bu faz dokunmadı: `draftJSON` medyan 1.031 ms ·
`gradeJSON` 8.800 ms · native↔wasm en kötü 1e-4 mm · çağrı yolu main thread.
Alet: `engine/tools/wasm-baseline.mjs`. Wasm bu gece yeniden derlendi
(`build-wasm.sh`, exit 0, 1218186 bayt) ve **iki parite testi de yeşil**
(`recipe_wasm_parity_dress`, `dxf_wasm_parity_dress`).

## ÖLÇÜLEMEYEN / DOĞRULANMADI
- `52ae85c` gövdesinin ilan ettiği iki kırmızı hâlâ açık ama **ctest'te
  GÖRÜNMÜYOR**: `garment_armhole_check` 12/24 FAIL iddiası ve 82980 draftın
  30'unda `sideseam` tam tavanda 3.0 mm (hepsi `pear`). `engine_check` bu koşuda
  Passed. Ya sınırda geçiyorlar ya kapı onları yargılamıyor — **DOĞRULANMADI.**
- `h10_gate_check` (#96) iki koşuda da **Disabled**, hiç çalışmadı.
- `web/recipes/index.json` ve `skirt-aline-dart.json` aynası kontrol edilmedi;
  bayt-aynası kuralı yalnız `shift-dress-square-spaghetti.json` için doğrulandı.
- `styles.json`'ın kaybolma/boşalma dalı (`style_check.mjs:70-88`) mutasyonla
  kanıtlanmadı — paylaşılan dosyayı taşımak paralel işçiyi kırabilirdi.
- 62.7764 mm'lik en büyük hareketin hangi GİYSİye düştüğü: tablo parça bazında
  doğrulandı, giysi bazında değil.
- `web/vendor/stitchu-engine.js` + worker bundle'larının bu ağaçtan derlendiği
  yeniden derlemeyle doğrulanmadı; yalnız `bundle_fresh_check` yeşil görüldü.
- `docs/ARCHITECTURE.md:39-41` ve `README.md:15,17,18,42` hâlâ duran-iddia
  taşıyor (*"8 ctest suites"*, *"worst pair 0.00 mm"*, *"0 failures"*) — bu
  turun çürüttüğü şeyler olmadığı için artımlı kâtip kapsamı dışında bırakıldı.
  **V9'un işi.**
- Çalışma ağacında `patterns_real/BUGRA-DEFTER.md`, `patterns_real/geometry/`,
  `patterns_real/tools/bugra-geometry-2026-07-23.json` **takipsiz** duruyor.
  Biri `git add .` yaparsa telifli materyal stage'e girer. Dokunulmadı.
