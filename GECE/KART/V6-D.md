# KART V6-D — ÇIPA SÖZLÜĞÜ **ÜRETECİ** (elle liste YASAK)

ETİKET: **PARALEL** (V6-E ile) · SÜRE TAVANI: **60 dk**

## NE
Konumlu edit için semantik çıpa sözlüğünü **ÜRET**. Elle yazılan çıpa listesi
menüdür ve yasaktır. Sözlük bileşen topolojisinden + motorun GERÇEKTEN bastığı
panel adlarından türetilir; her çıpanın yanında onu doğuran KAYNAK durur.
Bir de **bekçi testi** kur: liste elle edit edilirse test KIRILIR.

## GİRDİ DOSYALARI (isim isim)
- `ENV.md` · `RULES.md`
- `GECE/V6-C.md` ← ÖNCEKİ ÖLÇÜM. Bunu GİRDİ olarak oku; sayılarını devralma,
  kaynak yollarını kullan.
- `contract/composition.json` · `contract/primitives-v1.json`
  · `contract/garment-spec-v2.json` · `contract/edit-locality-v1.json`
- `engine/tools/spec-diff.mjs` (SADECE OKU — V6-E bu dosyaya yazıyor, DOKUNMA)
- `engine/tools/gen-vocab.mjs` (üreteç+bekçi EMSALİ — desenini kopyala)
- `engine/CMakeLists.txt` (yalnız yeni `add_test` satırı için)

## YAPILACAKLAR
1. **`engine/tools/gen-anchors.mjs`** yaz (YENİ, üreteç):
   - Girdi: `composition.json` bileşenleri + `primitives-v1.json` + motorun
     bastığı panel adları (spec taraması, V6-C'nin yöntemi).
   - Çıktı: `contract/anchors-v1.json`, ilk satırında
     `GENERATED by engine/tools/gen-anchors.mjs — DO NOT EDIT` damgası.
   - Her çıpa kaydı: `ad` · `kaynak` (dosya:anahtar) · `bilesen` ·
     `panelDeseni` (hangi panellere iner) · `kenarOrani` desteği (0..1, opsiyonel).
   - `--check` bayrağı: yeniden üretip diskteki dosyayla karşılaştırır,
     fark varsa **exit 1** (gen-vocab.mjs emsali).
   - ★ KROSS-ÇARPIM YASAK: V6-C "taraf × jeton" naif üretiminin 54 adayın en az
     8'ini kendiyle çelişik yaptığını ölçtü. Çıpa ancak bir bileşenin İLAN
     ETTİĞİ konumdan doğar; doğmayan ad ÜRETİLMEZ.
2. **`engine/tests/anchor_source_check.mjs`** yaz (YENİ, bekçi):
   - `gen-anchors.mjs --check` koşar (elle edit = kırmızı).
   - Her çıpanın `kaynak` alanı gerçekten o dosyada o anahtarla var mı — DOĞRULA.
   - Kaynaksız / kendiyle çelişik ad (`frontCenterback` sınıfı) varsa **KIRMIZI**.
   - `engine/CMakeLists.txt`'e `add_test(NAME anchor_source_check ...)` ekle.
3. **MUTASYON KANITI (§4.5)**, en az 3, her biri GERİ ALINARAK:
   M1 `contract/anchors-v1.json`'a elle bir çıpa ekle → `--check` KIRMALI.
   M2 bir çıpanın `kaynak` alanını olmayan bir anahtara çevir → KIRMALI.
   M3 üretece kross-çarpım ekle (çelişik ad doğsun) → KIRMALI.
   Her mutasyondan sonra geri al + testin yeşile döndüğünü göster.
4. **ÖLÇ**: kaç çıpa üretildi · kaçı kenar-oranı taşıyabiliyor · V6-C'nin
   saydığı **26 serbest terimin kaçının konum ibaresini karşılıyor** (kendi
   komutunla, `vision/eval/live-2026-08-22.json` outOfVocab kanalından).

## ÇIKTI
`engine/tools/gen-anchors.mjs` · `contract/anchors-v1.json` ·
`engine/tests/anchor_source_check.mjs` · `engine/CMakeLists.txt` (tek satır) ·
`GECE/V6-D.md` · `GECE/log/V6-D.mutasyon.txt`

## YASAKLAR
- `engine/tools/spec-diff.mjs` · `engine/tests/edit_locality_check.mjs` ·
  `engine/tools/foto-spec-olcum.mjs` DEĞİŞTİRİLMEZ (başka işçilerin dosyaları).
- `engine/src/`, `backend/`, `web/` DEĞİŞTİRİLMEZ. `patterns_real/` dokunma.
- Model ağırlığı / GPU / API anahtarı / bulut servis = kalıcı veto.
- Çıpa adını ELLE yazma. Kaynağı olmayan ad üretme. Kross-çarpım yasak.
- Yeni kırmızı AD doğurma (RULES 9): koşumun sonunda
  `ctest --test-dir engine/build -R "edit_locality|anchor|vocab" --output-on-failure`
  koş ve çıktısını logla.

## COMMIT
`git commit -- engine/tools/gen-anchors.mjs contract/anchors-v1.json engine/tests/anchor_source_check.mjs engine/CMakeLists.txt GECE/V6-D.md GECE/log/V6-D.mutasyon.txt`
`git add -A` KULLANMA (paralel işçi var).
