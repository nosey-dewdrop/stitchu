# KART V5-B2 — V5-B'NİN KANIT KAPISI (4.3) + EKSİK RAPOR

## NE
`engine/tools/bugra/overlay-png.mjs` kesilen bir oturumda yazıldı ve çıktısı
diskte duruyor ama RAPORU (`GECE/V5-B.md`) hiç yazılmadı. Aleti YENİDEN
KOŞTUR, diskteki sayıların bugün de aynı çıktığını KANITLA, raporu yaz.

## ETİKET
PARALEL (V5-R2 ile). SÜRE TAVANI: 40 dk. Bu bir KAPI KARTI DEĞİL.

## GİRDİ DOSYALARI (isim isim, başka dosya açma)
- ENV.md · RULES.md
- engine/tools/bugra/overlay-png.mjs
- GECE/log/V5-B.run-locket-36.txt   (kesilen oturumun bastığı sayılar)
- GECE/log/V5-B.overlay/            (6 PNG + 6 SVG)
- engine/tools/bugra-dump.cpp · engine/CMakeLists.txt (SADECE OKU)
- patterns_real/geometry/geometry-full.json  (SADECE JSON — PDF'e DOKUNMA)

## YAPILACAK
1. `GECE/log/V5-B.overlay/` altındaki 12 dosyanın hepsini `test -f` ile
   doğrula, çıktıyı raporuna yapıştır (v6 §4.3).
2. `engine/tools/bugra/overlay-png.mjs`'i BUGÜN yeniden koştur (gerekirse
   `cmake --build engine/build -j8 --target bugra-dump` ile hedefi kur;
   `-DCMAKE_BUILD_TYPE=Release` şart). Çıktıyı
   `GECE/log/V5-B2.rerun.txt`'ye yaz.
3. Bugünkü sayı tablosunu `GECE/log/V5-B.run-locket-36.txt`'nin tablosuyla
   SATIR SATIR karşılaştır. Fark varsa farkı ADIYLA yaz; yoksa "birebir aynı"
   diye yaz ve karşılaştırma komutunu (`diff`) çıktısıyla göster.
4. Aletin ne yaptığını KAYNAKTAN oku ve tek paragrafta yaz: hizalama usulü
   nedir, ölçek nasıl kuruluyor, sapma sütunu neyin dik mesafesi.
   İDDİA ETME — satır numarası ver.
5. `corset_bustier` modu koşuluyor mu? Koşulmadıysa dene; koşmuyorsa SEBEBİNİ
   komut çıktısıyla yaz (kartın "yapılamayan" satırı).

## YASAKLAR
- Bu bir KAPI DEĞİL. Test yazma, `engine/CMakeLists.txt`'e dokunma,
  `add_test` ekleme.
- `engine/tools/bugra/overlay-png.mjs`'i "iyileştirme". Yalnız koşmasını
  engelleyen bir HATA varsa düzelt ve düzeltmeyi raporda ADIYLA yaz.
- Hiçbir kapı Buğra'ya benzerlikle kurulamaz (v6 §7.3). Fark tablosu
  BİLGİDİR, eşik değildir; "sapma şu kadar, demek ki kalıp yanlış" YAZMA.
- `patterns_real/` altındaki PDF'lere dokunma (kalıcı veto).
- `engine/src/` altında kaynak DEĞİŞTİRME. Yeni bağımlılık kurma.

## ÇIKTI
- `GECE/log/V5-B2.rerun.txt`
- `GECE/V5-B.md` — yapılan (dosya yolu + commit hash) · ölçülen (sayı + onu
  basan komut) · yapılamayan (sebep) · kart dışı fark edilen (dokunma, yaz).
"Baktım / doğru görünüyor" YASAK (RULES 3): PNG yolu ya da komut çıktısı.
Bitince commit at (lowercase english), hash'i rapora yaz.
